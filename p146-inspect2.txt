### function refreshPubFilters count=1
a uma foto ou cole um print.');return}\n  const projeto=$('#gProjeto').value==='__outro'?$('#gProjetoOutro').value.trim():$('#gProjeto').value;\n  const btn=$('#savePhotoBtn');btn.disabled=true;let ok=0;\n  try{\n    for(let i=0;i<imgs.length;i++){\n      btn.textContent=imgs.length>1?`Enviando ${i+1}/${imgs.length}…`:'Enviando…';\n      const {url,path}=await fbUploadImage('galeria',imgs[i]);\n      const photoRef=await FB().addDoc(FB().collection(window.db,'rede_photos'),{url,storagePath:path,legenda:$('#gCap').value.trim(),projeto,acao:$('#gAcao').value.trim(),dataRealizacao:$('#gData').value,tipoImagem:$('#gTipoImagem').value,autor:$('#gAutor').value.trim(),autorId:myId||null,authorUid:window.auth.currentUser?.uid||'',ts:FB().serverTimestamp()});\n      await awardPoints(myId,'photo',photoRef.id,$('#gAcao').value.trim()||projeto||'Foto com metadados');\n      ok++;\n    }\n    closeOverlay('photoOverlay');toast(ok===1?'Imagem publicada.':`${ok} imagens publicadas.`);\n  }catch(e){console.error(e);toast(ok?`${ok} enviadas, depois falhou: ${e.message||'erro'}`:'Erro ao enviar a imagem.')}\n  finally{btn.disabled=false;btn.textContent='Publicar'}\n}\n\n/* ============ PUBLICAÇÕES ============ */\nfunction refreshPubFilters(){\n  const cc=$('#pubCategoria'), curC=cc.value;\n  cc.innerHTML='<option value="">Todas as categorias</option>'+\n    Object.entries(CATEGORIAS).map(([k,v])=>`<option value="${k}">${v.label}</option>`).join('');\n  cc.value=curC;\n  const cp=$('#pubPessoa'), curP=cp.value;\n  const ordered=[...state.members].sort((a,b)=>a.nome.localeCompare(b.nome));\n  cp.innerHTML='<option value="">Todas as pessoas</option>'+\n    ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');\n  cp.value=curP;\n}\nfunction setRepoViewMode(mode){\n  repoViewMode=['general','mine','shared'].includes(mode)?mode:'general';\n  document.querySelectorAll('[data-repo-mode]').forEach(b=>b.classList.toggle('on',b.dataset.repoMode===repoViewMode));\n  renderPubs();\n}\nfunction repoRootCollaborators(){return (state.personalRepoRoot?.collaboratorMemberIds||[]).filter(id=>id&&id!==myId)}\nfunction personalPackagesOwned(){return (state.repositoryPackages||[]).filter(p=>p.ownerMemberId===myId)}\nfunction folderPurposeIcon(p){const k=p?.purpose||'';return ({artigos:'📝',resumos:'📑',instrumentos:'🧰',dados:'📊',ferramentas:'🛠️',scripts:'💻',aula:'🎓',referencias:'📚',projeto:'🔬',revisao:'✍️',documentos:'🗂️',bolsa:'🎓',processo:'📋',prestacao:'🧾'})[k]||'📁'}\nfunction repoMiniAvatar(m,name=''){const n=m?.nome||name||'?';const nv=NIVEIS[m?.nivel]||{color:'#385366'};return m?.foto?`<img class="avatar" src="${esc(m.foto)}" alt="" style="object-fit:cover">`:`<div class="avatar" style="background:${nv.color}">${initials(n)}</div>`}\nfunction renderRepoFolderCard(p,shared=false){\n  const a=repoAccessLabel(p),owner=memberById(p.ownerMemberId),manage=canManageRepoPackage(p),rootShared=(p.rootSharedMemberIds||[]).length;\n  return `<div class="repo-folder"><div class="repo-folder-icon">${folderPurposeIcon(p)}</div><div class="repo-folder-main"><div class="repo-folder-title">${esc(p.title||'Pasta')}</div><div class="repo-folder-meta"><span>${esc(repoPurposeLabel(p))}</span><span>· ${Number(p.fileCount||0)} arquivo${Number(p.fileCount||0)===1?'':'s'}</span>${shared?`<span>· ${esc(p.ownerName||owner?.nome||'')}</span>`:''}<span class="repo-access-chip ${a.cls}">${esc(a.text)}</span>${rootShared?`<span>🔗 repo compartilhado</span>`:''}</div></div><div class="repo-folder-actions"><button class="btn ghost" type="button" onclick="openRepositoryPackage('${p.id}')">📂 Abrir</button>${manage?`<button class="btn" type="button" onclick="addFilesToRepositoryPackage('${p.id}')">＋ Arquivos</button><button class="btn" type="button" onclick="openRepoFolderAccess('${p.id}')">👥 Acesso</button><button class="btn" type="button" onclick="deleteRepositoryPackage('${p.id}')">🗑</button>`:''}</div></div>`;\n}\nfunction renderMyRepository(){\n  const me=memberById(myId),folders=personalPackagesOwned().sort((a,b)=>((b.updatedAt?.toMillis?.()||b.ts?.toMillis?.()||0)-(a.updatedAt?.toMillis?.()||a.ts?.toMillis?.()||0))),collabs=repoRootCollaborators();\n  if($('#repoPersonalName'))$('#repoPersonalName').textContent='Repositório de '+(me?.nome?.split(' ')[0]||'você');\n  if($('#repoMyFolderCount'))$('#repoMyFolderCount').textContent=folders.length;\n  if($('#repoMyFileCount'))$('#repoMyFileCount').textContent=folders.reduce((n,p)=>n+Number(p.fileCount||0),0);\n  if($('#repoMyCollabCount'))$('#repoMyCollabCount').textContent=collabs.length;\n  const strip=$('#repoRootCollaborators');if(strip)strip.innerHTML=collabs.length?collabs.map(id=>`<span class="repo-collab-chip">👤 ${esc(memberById(id)?.nome||id)}</spa
---
### function renderPubs count=1
turn;const removable=repoExactDuplicateGroups().reduce((n,g)=>n+g.slice(1).filter(canDeleteRepoDoc).length,0);b.classList.toggle('has-dupes',removable>0);b.classList.toggle('clean',!removable);b.textContent=removable?`🧹 ${removable} duplicado${removable===1?'':'s'}`:'🧹 Sem duplicados';b.disabled=!removable}\nasync function cleanupExactRepoDuplicates(){\n  const groups=repoExactDuplicateGroups(),toDelete=groups.flatMap(g=>g.slice(1).filter(canDeleteRepoDoc));\n  if(!toDelete.length)return toast('Nenhum duplicado exato removível encontrado.');\n  if(!confirm(`Remover ${toDelete.length} duplicação(ões) EXATA(S)?\n\nO Carbonautas só removerá entradas com o mesmo arquivo/URL e mesma identidade de versão. Versões diferentes de uma correção serão preservadas.`))return;\n  try{const f=FB();for(let i=0;i<toDelete.length;i+=400){const batch=f.writeBatch(window.db);toDelete.slice(i,i+400).forEach(p=>batch.delete(f.doc(window.db,'rede_publicacoes',p.id)));await batch.commit()}toast(`${toDelete.length} duplicação(ões) removida(s).`)}catch(e){console.error(e);toast(e?.code==='permission-denied'?'Você só pode limpar duplicados dos seus próprios arquivos.':'Não foi possível limpar os duplicados.')}\n}\n\nfunction renderPubs(){\n  const general=$('#repoGeneralPanel'),minePanel=$('#repoPersonalPanel'),sharedPanel=$('#repoSharedPanel');\n  if(general)general.style.display=repoViewMode==='general'?'block':'none';\n  if(minePanel)minePanel.style.display=repoViewMode==='mine'?'block':'none';\n  if(sharedPanel)sharedPanel.style.display=repoViewMode==='shared'?'block':'none';\n  document.querySelectorAll('[data-repo-mode]').forEach(b=>b.classList.toggle('on',b.dataset.repoMode===repoViewMode));\n  const newOffice=$('#newOfficeBtn'),packageBtn=$('#addRepoPackageBtn'),pubBtn=$('#addPubBtn'),dedupeBtn=$('#repoDedupeBtn');\n  if(newOffice)newOffice.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(packageBtn)packageBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(pubBtn)pubBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(dedupeBtn)dedupeBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(repoViewMode==='mine'){renderMyRepository();return}\n  if(repoViewMode==='shared'){renderSharedRepository();return}\n  refreshPubFilters();\n  const wrap=$('#pubList');\n  const cat=$('#pubCategoria').value, pessoa=$('#pubPessoa').value, mine=$('#pubMine').checked;\n  const packageCount=renderRepoPackages(cat,pessoa,mine,'general');\n  let list=state.publicacoes;\n  if(cat) list=list.filter(p=>p.categoria===cat);\n  if(pessoa) list=list.filter(p=>p.memberId===pessoa);\n  if(mine){\n    if(!myId){ toast('Identifique-se primeiro para ver só as suas.'); $('#pubMine').checked=false; }\n    else list=list.filter(p=>p.memberId===myId);\n  }\n  list=compactPublicationsForDisplay(list);updateRepoDedupeButton();\n  if(!list.length){ wrap.innerHTML=packageCount?'':'<div class="crono-empty">Nenhuma publicação encontrada com esses filtros.</div>'; return; }\n  wrap.innerHTML=list.map(p=>{\n    const cfg=CATEGORIAS[p.categoria]||CATEGORIAS.outro;\n    const catLabel = p.categoria==='outro' && p.categoriaOutro ? p.categoriaOutro : cfg.label;\n    const m=memberById(p.memberId);\n    const canDel = isAdmin || (p.reviewFlow ? p.ownerUid===(window.auth.currentUser?.uid||'') : canEditMember(p.memberId));\n    const dt = p.ts?.toMillis?new Date(p.ts.toMillis()):null;\n    const dateLabel = dt ? dt.toLocaleDateString('pt-BR',{day:'2-digit',month:'short',year:'numeric'}) : '';\n    return `<div class="pub-row"><div class="pub-cat" style="background:${cfg.color}"></div><div class="pub-body"><div class="pub-t">${esc(p.titulo)}</div><div class="pub-meta"><span class="pub-cat-tag" style="background:${cfg.color}">${esc(catLabel)}</span><span class="pub-author"><span class="d" style="background:${m?NIVEIS[m.nivel]?.color:'var(--ink-3)'}"></span>${esc(p.memberNome||(m?m.nome:'—'))}</span>${dateLabel?`<span class="pub-date">${dateLabel}</span>`:''}</div>${!p.reviewFlow&&Number(p.onlineEditVersion||1)>1?`<span class="office-version">✏️ edição v${Number(p.onlineEditVersion||1)}</span>`:''}${!p.reviewFlow?repositorySequenceHTML(p):''}${p.reviewFlow?reviewBadgeHTML(p):''}${p.reviewNote?`<div class="review-note">💬 ${esc(p.reviewNote)}</div>`:''}${scientificContextHTML(p)}${officeFileChip(p)}${publicationCollaboratorIds(p).length?`<span class="repo-collab-chip">👥 ${publicationCollaboratorIds(p).length} colaborador${publicationCollaboratorIds(p).length===1?'':'es'}</span>`:''}</div><div class="pub-act">${p.labRun?`<button type="button" class="btn primary" onclick="openLabRun('${p.labRunId}')">📈 Resultados</button>`:p.tipo==='arquivo'?`<button type="butto
---
### pubCategoriaSel count=4
laceholder="Diga de forma objetiva o que você precisa, quando e como o grupo pode ajudar."></textarea></div>\n    <div class="fg"><label>Link (opcional)</label><input id="postUrl" placeholder="https://..."></div>\n    <div class="fg"><label>Anexo (foto, PDF, Word, planilha, apresentação etc.; até 15 MB)</label><button class="btn" id="postFilePick" style="justify-content:center">Escolher arquivo</button><input type="file" id="postFile" style="display:none"><span class="hint" id="postFileName">Nenhum arquivo escolhido.</span></div>\n  </div>\n  <div class="modal-f"><button class="btn" data-close="postOverlay">Cancelar</button><button class="btn primary" id="savePostBtn">Publicar</button></div>\n</div></div>\n\n<!-- PUBLICATION MODAL -->\n<div class="overlay" id="pubOverlay">\n  <div class="modal" style="max-width:480px">\n    <div class="modal-h"><h2 id="pubModalTitle">Adicionar documento ao repositório</h2><div class="spacer"></div><button class="btn ghost" data-close="pubOverlay">Fechar</button></div>\n    <div class="modal-b">\n      <div class="fg" id="pubAutorRow"><label>Autor(a)</label><select id="pubAutor"></select></div>\n      <div class="fg"><label>Categoria</label>\n        <select id="pubCategoriaSel">\n          <option value="nota_tecnica">Nota técnica</option>\n          <option value="artigo">Artigo científico</option>\n          <option value="resumo_expandido">Resumo expandido</option>\n          <option value="resumo_simples">Resumo simples</option>\n          <option value="visibilidade">Plano de visibilidade</option>\n          <option value="jogo">Jogo</option>\n          <option value="plataforma">Plataforma</option>\n          <option value="projeto">Projeto</option>\n          <option value="apresentacao">Apresentação</option>\n          <option value="cronograma">Cronograma</option>\n          <option value="relatorio">Relatório</option>\n          <option value="dados">Dados / planilha</option>\n          <option value="outro">Outro</option>\n        </select>\n      </div>\n      <div class="fg" id="pubOutroRow" style="display:none"><label>Especifique</label><input id="pubOutroTxt" placeholder="Qual tipo de produto?"></div>\n      <div class="fg"><label>Título</label><input id="pubTitulo" placeholder="Título do trabalho"></div>\n      <div class="fg" id="pubFinalidadeRow"><label>Finalidade</label>\n        <select id="pubFinalidade">\n          <option value="guardar">📚 Guardar no repositório</option>\n          <option value="correcao">✍️ Enviar para correção</option>\n        </select>\n      </div>\n      <div id="pubReviewFields" style="display:none">\n        <div class="review-callout" id="pubReviewInfo"><b>✍️ Fluxo de correção</b><div class="hint">O arquivo original fica preservado. A pessoa escolhida recebe uma notificação e poderá devolver uma nova versão.</div></div>\n        <div class="fg" id="pubReviewerRow"><label>Quem deve corrigir / ler?</label><select id="pubReviewer"></select></div>\n        <div class="fg"><label id="pubReviewNoteLabel">Orientação para a correção (opcional)</label><textarea id="pubReviewNote" style="min-height:90px" placeholder="Ex.: revisar introdução, conferir referências e devolver com comentários."></textarea></div>\n      </div>\n      <div class="fg">\n        <label>Conteúdo</label>\n        <div style="display:flex;gap:14px;margin-bottom:8px">\n          <label class="toggle" style="padding:0"><input type="radio" name="pubTipo" value="link" checked> Link</label>\n          <label class="toggle" style="padding:0"><input type="radio" name="pubTipo" value="arquivo"> Arquivo (PDF/Word/Excel/PPT etc.)</label>\n        </div>\n        <input id="pubUrl" placeholder="https://...">\n        <div id="pubFileArea" style="display:none;margin-top:8px">\n          <button class="btn" id="pubFilePick" style="width:100%;justify-content:center">Escolher arquivo</button>\n          <input type="file" id="pubFile" style="display:none">\n          <p class="hint" id="pubFileName" style="margin:8px 0 0">Nenhum arquivo escolhido. Máximo 15 MB.</p>\n        </div>\n      </div>\n    </div>\n    <div class="modal-f">\n      <button class="btn" data-close="pubOverlay">Cancelar</button>\n      <button class="btn primary" id="savePubBtn">Salvar</button>\n    </div>\n  </div>\n</div>\n\n<!-- P20 · CONVERSA PRIVADA DA CORREÇÃO -->\n<div class="overlay" id="reviewConversationOverlay">\n  <div class="modal" style="max-width:980px">\n    <div class="modal-h"><div><h2 id="reviewConversationTitle">💬 Conversa da correção</h2><div class="hint" id="reviewConversationSubtitle">Somente autor/remetente e revisor desta correção.</div></div><div class="spacer"></div><button class="btn ghost" type="button" id="reviewConversationCloseBtn">Fechar</butto
---
e){}\n      resolved=resolveCurrentMember();\n    }\n\n    const sel=$('#pubAutor');\n    if(!sel) throw new Error('Formulário do repositório não foi carregado.');\n\n    if(isAdmin){\n      $('#pubAutorRow').style.display='block';\n      const ordered=[...state.members].sort((a,b)=>a.nome.localeCompare(b.nome));\n      const defaultId=presetMemberId||(resolved?.id)||myId||ordered[0]?.id||'';\n      sel.innerHTML=ordered.map(m=>`<option value="${m.id}" ${defaultId===m.id?'selected':''}>${esc(m.nome)}</option>`).join('');\n      if(defaultId) sel.value=defaultId;\n    }else{\n      resolved=resolved||memberById(myId);\n      if(!resolved){\n        toast('Seu login ainda não está vinculado ao seu perfil. Clique no seu nome e use “Corrigir vínculo”.');\n        openIdentity();\n        return;\n      }\n      // Mantém o id efetivo sincronizado no navegador mesmo quando o Firestore tinha um vínculo antigo.\n      myId=resolved.id;\n      $('#pubAutorRow').style.display='none';\n      sel.innerHTML=`<option value="${resolved.id}">${esc(resolved.nome)}</option>`;\n      sel.value=resolved.id;\n    }\n\n    pubReviewContext=reviewContext||null;\n    if(reviewContext)$('#pubAutorRow').style.display='none';\n    $('#pubCategoriaSel').value=reviewContext?.categoria||'artigo';\n    $('#pubOutroRow').style.display=reviewContext?.categoria==='outro'?'block':'none';\n    $('#pubOutroTxt').value=reviewContext?.categoriaOutro||'';\n    $('#pubTitulo').value=reviewContext?.reviewBaseTitle||reviewContext?.titulo||'';\n    $('#pubUrl').value='';\n    $('#pubFinalidade').value=reviewContext?'correcao':'guardar';\n    $('#pubFinalidadeRow').style.display=reviewContext?'none':'block';\n    $('#pubReviewerRow').style.display=reviewContext?'none':'block';\n    $('#pubReviewNote').value='';\n    $('#pubReviewNoteLabel').textContent=reviewContext?(myId===reviewContext.reviewReviewerId?'Resposta / observações da correção':'O que mudou nesta nova versão?'):'Orientação para a correção (opcional)';\n    $('#pubReviewNote').placeholder=reviewContext?(myId===reviewContext.reviewReviewerId?'Ex.: corrigi comentários no texto e deixei observações na versão devolvida.':'Ex.: incorporei as correções e ajustei a discussão.'):'Ex.: revisar introdução, conferir referências e devolver com comentários.';\n    $('#pubReviewInfo').innerHTML=reviewContext?`<b>✍️ ${myId===reviewContext.reviewReviewerId?'Devolver correção':'Enviar nova versão'}</b><div class="hint">Versão ${reviewNextVersion(reviewContext)} do fluxo. O arquivo anterior continuará disponível.</div>`:'<b>✍️ Fluxo de correção</b><div class="hint">O arquivo original fica preservado. A pessoa escolhida recebe uma notificação e poderá devolver uma nova versão.</div>';\n    populatePubReviewer(reviewContext?.reviewReviewerId||'');\n    pubTipo=reviewContext?'arquivo':'link'; pubFileData=''; pubFileName=''; pubFileHash=''; pubFileHashPromise=null;\n    $$('input[name="pubTipo"]').forEach(r=>{r.checked=r.value===pubTipo;r.disabled=!!reviewContext});\n    $('#pubUrl').style.display=pubTipo==='link'?'block':'none';\n    $('#pubFileArea').style.display=pubTipo==='arquivo'?'block':'none';\n    $('#pubFileName').textContent='Nenhum arquivo escolhido. Máximo 15 MB.';\n    $('#pubModalTitle').textContent=reviewContext?(myId===reviewContext.reviewReviewerId?'Devolver arquivo corrigido':'Enviar nova versão'):'Adicionar documento ao repositório';\n    $('#savePubBtn').textContent=reviewContext?(myId===reviewContext.reviewReviewerId?'Devolver correção':'Enviar nova versão'):'Salvar';\n    syncPubFinalidade();\n    openOverlay('pubOverlay');\n    setTimeout(()=>reviewContext?$('#pubFilePick')?.focus():$('#pubTitulo')?.focus(),50);\n  }catch(e){\n    console.error('Erro ao abrir o repositório',e);\n    toast('Não consegui abrir o formulário do repositório. Recarregue a página e tente novamente.');\n  }\n}\nfunction handlePubFile(file){\n  if(!file)return;\n  if(file.size > 15*1024*1024){ toast('Arquivo maior que 15 MB. Escolha um arquivo menor.'); return; }\n  pubFileHash='';pubFileHashPromise=sha256File(file).then(h=>pubFileHash=h).catch(()=>{pubFileHash='';return''});\n  const reader=new FileReader();\n  reader.onload=e=>{ pubFileData=e.target.result; pubFileName=file.name; $('#pubFileName').textContent=file.name+' · verificando duplicação…'; Promise.resolve(pubFileHashPromise).finally(()=>{$('#pubFileName').textContent=file.name;}); };\n  reader.readAsDataURL(file);\n}\nasync function savePublicacao(){\n  const saveBtn=$('#savePubBtn');if(saveBtn?.disabled)return;\n  const resolved=resolveCurrentMember();\n  const autorId = pubReviewContext?.memberId || (isAdmin? $('#pubAutor').value : (resolved?.id||myId));\n  const categoria=$('#pubCategoriaSel').value;\n  const categoriaOutr
---
evolver correção':'Enviar nova versão'):'Salvar';\n    syncPubFinalidade();\n    openOverlay('pubOverlay');\n    setTimeout(()=>reviewContext?$('#pubFilePick')?.focus():$('#pubTitulo')?.focus(),50);\n  }catch(e){\n    console.error('Erro ao abrir o repositório',e);\n    toast('Não consegui abrir o formulário do repositório. Recarregue a página e tente novamente.');\n  }\n}\nfunction handlePubFile(file){\n  if(!file)return;\n  if(file.size > 15*1024*1024){ toast('Arquivo maior que 15 MB. Escolha um arquivo menor.'); return; }\n  pubFileHash='';pubFileHashPromise=sha256File(file).then(h=>pubFileHash=h).catch(()=>{pubFileHash='';return''});\n  const reader=new FileReader();\n  reader.onload=e=>{ pubFileData=e.target.result; pubFileName=file.name; $('#pubFileName').textContent=file.name+' · verificando duplicação…'; Promise.resolve(pubFileHashPromise).finally(()=>{$('#pubFileName').textContent=file.name;}); };\n  reader.readAsDataURL(file);\n}\nasync function savePublicacao(){\n  const saveBtn=$('#savePubBtn');if(saveBtn?.disabled)return;\n  const resolved=resolveCurrentMember();\n  const autorId = pubReviewContext?.memberId || (isAdmin? $('#pubAutor').value : (resolved?.id||myId));\n  const categoria=$('#pubCategoriaSel').value;\n  const categoriaOutro=$('#pubOutroTxt').value.trim();\n  const titulo=$('#pubTitulo').value.trim();\n  const finalidade=pubReviewContext?'correcao':$('#pubFinalidade').value;\n  const reviewNote=$('#pubReviewNote').value.trim();\n  if(!autorId){toast('Selecione o autor.');return}\n  if(!titulo){toast('Escreva o título.');return}\n  if(categoria==='outro' && !categoriaOutro){toast('Especifique a categoria.');return}\n  if(finalidade==='correcao'&&!pubReviewContext&&!$('#pubReviewer').value){toast('Selecione quem deve receber o arquivo para correção.');return}\n  if(finalidade==='correcao')pubTipo='arquivo';\n\n  let url='', tipoFinal=pubTipo,contentHash='';\n  try{\n    if(saveBtn)saveBtn.disabled=true;\n    if(pubTipo==='arquivo'){\n      if(!pubFileData){toast('Escolha o arquivo que será enviado.');return}\n      contentHash=pubFileHash||await Promise.resolve(pubFileHashPromise||'').catch(()=> '');\n      if(finalidade==='correcao'){\n        if(pubReviewContext){const dup=exactHashDuplicate(contentHash,{memberId:autorId,title:pubReviewContext.reviewBaseTitle||pubReviewContext.titulo||titulo,reviewThreadId:pubReviewContext.reviewThreadId||pubReviewContext.id});if(dup){toast(`Esse arquivo é idêntico à versão ${dup.reviewVersion||1}. Nenhuma duplicação foi criada.`);return}}\n        else{const reviewer=$('#pubReviewer').value,dup=exactHashDuplicate(contentHash,{memberId:autorId,title:titulo,reviewerId:reviewer});if(dup?.reviewFlow){toast('Esse mesmo arquivo já foi enviado para essa correção. Abra o fluxo existente em vez de enviar novamente.');return}}\n      }else{const dup=exactHashDuplicate(contentHash,{memberId:autorId,title:titulo});if(dup&&!dup.reviewFlow){toast('Esse arquivo já está no Repositório com o mesmo título. Não criei outra cópia.');return}}\n      toast('Enviando arquivo…');\n      const {url:u}=await fbUploadFile('pub_'+autorId,pubFileData,pubFileName);\n      url=u;\n    }else{\n      url=$('#pubUrl').value.trim();\n      if(!url){toast('Cole o link.');return}\n    }\n    const m=memberById(autorId),actor=currentActor(),uid=window.auth.currentUser?.uid||'';\n    if(finalidade==='correcao'){\n      const f=FB();let ref=f.doc(f.collection(window.db,'rede_publicacoes'));\n      let reviewThreadId=ref.id,reviewVersion=1,reviewReviewerId=$('#pubReviewer').value,reviewReturnToId=actor?.id||myId||'',reviewNextRecipientId=reviewReviewerId,reviewStatus='aguardando_correcao',baseTitle=titulo,parentId='';\n      if(pubReviewContext){\n        reviewThreadId=pubReviewContext.reviewThreadId||pubReviewContext.id;\n        reviewVersion=reviewNextVersion(pubReviewContext);\n        reviewReviewerId=pubReviewContext.reviewReviewerId;\n        reviewReturnToId=pubReviewContext.reviewReturnToId;\n        parentId=pubReviewContext.id;\n        const actingAsReviewer=(myId===reviewReviewerId);\n        reviewNextRecipientId=actingAsReviewer?reviewReturnToId:reviewReviewerId;\n        reviewStatus=actingAsReviewer?'correcao_devolvida':'nova_versao';\n        baseTitle=pubReviewContext.reviewBaseTitle||pubReviewContext.titulo||titulo;\n        ref=f.doc(window.db,'rede_publicacoes',reviewVersionDocId(reviewThreadId,reviewVersion));\n      }\n      await f.setDoc(ref,{memberId:autorId,ownerUid:uid,createdByUid:uid,memberNome:m?m.nome:'',categoria,categoriaOutro,titulo:baseTitle,tipo:'arquivo',url,fileName:pubFileName,contentHash,reviewFlow:true,reviewThreadId,reviewVersion,reviewStatus,reviewReviewerId,reviewReturnToId,reviewNextRecipientId,reviewParentId:p
---
olderAccessId);if(p)renderFolderAccessPeople(p)};\nif($('#repoFolderAccessSaveBtn'))$('#repoFolderAccessSaveBtn').onclick=saveRepoFolderAccess;\nif($('#repoPackageAddFilesInput'))$('#repoPackageAddFilesInput').onchange=e=>uploadAdditionalRepoFiles(e.target.files);\nif($('#repoPackageFilePick'))$('#repoPackageFilePick').onclick=()=>$('#repoPackageFiles').click();\nif($('#repoPackageFiles'))$('#repoPackageFiles').onchange=e=>handleRepoPackageFiles(e.target.files);\nif($('#repoPackagePurpose'))$('#repoPackagePurpose').onchange=e=>{$('#repoPackagePurposeOtherRow').style.display=e.target.value==='outro'?'block':'none'};\nif($('#repoPackageOwner'))$('#repoPackageOwner').onchange=()=>{if(document.querySelector('input[name="repoPackageAccess"]:checked')?.value==='selected')renderRepoPackagePeople()};\nif($('#repoPackagePeopleSearch'))$('#repoPackagePeopleSearch').oninput=renderRepoPackagePeople;\ndocument.querySelectorAll('input[name="repoPackageAccess"]').forEach(r=>r.onchange=updateRepoPackageAccessUI);\nif($('#repoPackageSaveBtn'))$('#repoPackageSaveBtn').onclick=saveRepoPackage;\n\n$('#pubCategoria').onchange=renderPubs; $('#pubPessoa').onchange=renderPubs; $('#pubMine').onchange=renderPubs;\n$('#pubCategoriaSel').onchange=e=>{$('#pubOutroRow').style.display=e.target.value==='outro'?'block':'none'};\n$$('input[name="pubTipo"]').forEach(r=>r.onchange=e=>{\n  pubTipo=e.target.value;\n  $('#pubUrl').style.display=pubTipo==='link'?'block':'none';\n  $('#pubFileArea').style.display=pubTipo==='arquivo'?'block':'none';\n});\n$('#pubFilePick').onclick=()=>$('#pubFile').click();\n$('#pubFile').onchange=e=>handlePubFile(e.target.files[0]);\n$('#savePubBtn').onclick=savePublicacao;\nif($('#pubFinalidade'))$('#pubFinalidade').onchange=syncPubFinalidade;\n$$('[data-close="pubOverlay"]').forEach(b=>b.addEventListener('click',()=>{pubReviewContext=null;$$('input[name="pubTipo"]').forEach(r=>r.disabled=false)}));\n$('#repoAddBtn').onclick=async()=>{const id=repoMemberId;if(!(isAdmin||isMe(id))){toast('Sem permissão para adicionar documentos para esta pessoa.');return}closeOverlay('repoOverlay');await openPubModal(id)};\n\nif($('#fileCollaboratorsSearch'))$('#fileCollaboratorsSearch').oninput=renderFileCollaborators;\nif($('#fileCollaboratorsSaveBtn'))$('#fileCollaboratorsSaveBtn').onclick=saveFileCollaborators;\nif($('#privateNewBtn'))$('#privateNewBtn').onclick=openPrivateNew;\nif($('#privateThreadSearch'))$('#privateThreadSearch').oninput=renderPrivateThreads;\nif($('#privatePersonSearch'))$('#privatePersonSearch').oninput=renderPrivatePeople;\nif($('#privateBackBtn'))$('#privateBackBtn').onclick=()=>{$('#privateShell')?.classList.remove('chat-open')};\nif($('#privateSendBtn'))$('#privateSendBtn').onclick=sendPrivateMessage;\nif($('#privateInput')){$('#privateInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendPrivateMessage()}});$('#privateInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(130,e.target.scrollHeight)+'px'})}\nif($('#privateEmojiToggle'))$('#privateEmojiToggle').onclick=()=>$('#privateEmojiBar')?.classList.toggle('show');\n$$('#privateEmojiBar button').forEach(b=>b.onclick=()=>{const i=$('#privateInput');if(!i)return;i.value+=(i.value?' ':'')+b.textContent;i.focus()});\nif($('#privateReplyCancel'))$('#privateReplyCancel').onclick=()=>{privateReplyTarget=null;renderPrivateReply()};\n$$('[data-close]').forEach(b=>b.onclick=()=>closeOverlay(b.dataset.close));\n$$('.overlay').forEach(o=>o.addEventListener('click',e=>{if(e.target===o)o.classList.remove('open')}));\nwindow.addEventListener('keydown',e=>{if(e.key==='Escape'){$$('.overlay.open').forEach(o=>o.classList.remove('open'));closeDetail();closeLightbox()}});\nwindow.addEventListener('resize',()=>{if(simulation){const[W,H]=graphSize();simulation.force('center')&&simulation.force('center').x(W/2).y(H/2);simulation.alpha(.3).restart()}});\n\n// expose for inline handlers\nwindow.selectNode=selectNode; window.closeDetail=closeDetail; window.openMember=openMember;\nwindow.filterLinha=filterLinha; window.recadoTo=recadoTo;\nwindow.toggleCronoItem=toggleCronoItem; window.deleteCronoItem=deleteCronoItem; window.unlinkAccess=unlinkAccess;\nwindow.deleteChatMsg=deleteChatMsg; window.deletePublicacao=deletePublicacao; window.openMuralAction=openMuralAction; window.openProjectProfile=openProjectProfile; window.editActivity=openActivity; window.toggleReaction=toggleReaction; window.toggleComments=toggleComments; window.addSocialComment=addSocialComment; window.deleteFeedPost=deleteFeedPost;\n\n\nfunction sparkleLogo(mode='wow'){\n  const marks=$$('.logo-mark');\n  marks.forEach(m=>{\n    m.classList.remove('is-awakening','is-wow');\n    voi
---
### CATEGORIAS[ count=2
;\n  let label='Bolsa em andamento', remLabel=human(remaining)+' restantes';\n  if(now<start){label='Bolsa ainda não iniciada';remLabel=human(elapsed)+' para começar'}\n  if(now>end){label='Bolsa encerrada';remLabel=human(remaining)+' desde o término'}\n  return {progress,label,remaining:remLabel,start,end,daysRemaining:remaining};\n}\nfunction scholarshipHTML(m,compact=false){\n  const si=scholarshipInfo(m); if(!si)return '';\n  const modal=m.bolsaModalidade?esc(m.bolsaModalidade):'Bolsa';\n  return `<div class="scholar-box"><div class="scholar-head"><b>🎓 ${modal}</b><span>${esc(si.label)}</span><span class="remain">${esc(si.remaining)}</span></div><div class="scholar-bar"><span style="width:${si.progress}%"></span></div><div class="scholar-foot"><span>${m.bolsaInicio?'Início '+fmtDate(m.bolsaInicio):'Início não informado'}</span><span>${si.progress}% do período</span><span>${m.bolsaFim?'Fim '+fmtDate(m.bolsaFim):'Fim não informado'}</span></div></div>`;\n}\nfunction memberRepositoryItems(memberId){\n  const out=[];\n  compactPublicationsForDisplay(state.publicacoes.filter(p=>p.memberId===memberId)).forEach(p=>out.push({source:'publicacao',id:p.id,title:p.titulo||p.fileName||'Documento',kind:(CATEGORIAS[p.categoria]?.label||p.categoriaOutro||p.categoria||'Publicação'),url:p.url||'',fileName:p.fileName||'',isFile:p.tipo==='arquivo',ts:p.ts,reviewFlow:!!p.reviewFlow,reviewVersion:p.reviewVersion||0,reviewStatus:p.reviewStatus||'',reviewReviewerId:p.reviewReviewerId||'',reviewReturnToId:p.reviewReturnToId||'',reviewNextRecipientId:p.reviewNextRecipientId||'',reviewNote:p.reviewNote||'',onlineEditVersion:p.onlineEditVersion||1,raw:p}));\n  state.repositoryPackages.filter(p=>p.ownerMemberId===memberId).forEach(p=>out.push({source:'package',id:p.id,title:p.title||'Conjunto de arquivos',kind:'Dossiê · '+repoPurposeLabel(p),url:'',fileName:'',isFile:false,ts:p.ts,package:p}));\n  state.activities.filter(a=>a.ownerId===memberId && (a.fileUrl||a.externalUrl)).forEach(a=>out.push({source:'atividade',id:a.id,title:a.title||'Registro de acompanhamento',kind:ACTIVITY_TYPES[a.type]||a.type||'Acompanhamento',url:a.fileUrl||a.externalUrl||'',fileName:a.fileName||'',isFile:!!a.fileUrl,ts:a.updatedAt||a.createdAt}));\n  const m=memberById(memberId); (m?.resultados||[]).forEach((r,i)=>{if(r.url)out.push({source:'resultado',id:'r'+i,title:r.titulo||'Resultado / produto',kind:'Resultado / produto',url:r.url,fileName:'',isFile:false,ts:null})});\n  return out.sort((a,b)=>{const ta=a.ts?.toMillis?a.ts.toMillis():0,tb=b.ts?.toMillis?b.ts.toMillis():0;return tb-ta});\n}\nlet repoMemberId=null;\nfunction openMemberRepository(memberId){repoMemberId=memberId;renderMemberRepository();openOverlay('repoOverlay')}\nfunction renderMemberRepository(){\n  const m=memberById(repoMemberId); if(!m||!$('#repoList'))return;\n  $('#repoTitle').textContent='Repositório · '+m.nome;\n  $('#repoScholar').innerHTML=scholarshipHTML(m);\n  const list=memberRepositoryItems(m.id); $('#repoCount').textContent=list.length;\n  const canAdd=isAdmin||isMe(m.id); $('#repoAddBtn').style.display=canAdd?'inline-flex':'none';\n  $('#repoList').innerHTML=list.length?list.map(it=>{\n    const icon=it.reviewFlow?'✍️':(it.isFile?'📄':'🔗');\n    const view=it.source==='package'?`<button class="btn ghost" type="button" onclick="openRepositoryPackage('${it.id}')">📦 Abrir dossiê</button>`:it.raw?.labRun?`<button class="btn primary" type="button" onclick="openLabRun('${it.raw.labRunId}')">📈 Resultados</button>`:it.isFile?`<button class="btn ghost" type="button" onclick="openFilePreview(decodeURIComponent('${encodeURIComponent(it.url)}'),decodeURIComponent('${encodeURIComponent(it.fileName||it.title||'Arquivo')}'),{source:'publication',id:'${it.id}'})">Visualizar</button><button class="btn review-download" type="button" onclick="downloadRepoFile(decodeURIComponent('${encodeURIComponent(it.url)}'),decodeURIComponent('${encodeURIComponent(it.fileName||it.title||'Arquivo')}'))">Baixar</button>`:`<a class="btn ghost" href="${esc(it.url)}" target="_blank" rel="noopener">Abrir</a>`;\n    const rev=it.reviewFlow?reviewBadgeHTML(it.raw):'';\n    const online=it.source==='publicacao'?officeActionButtons(it.raw):'';\n    const chat=it.source==='publicacao'&&it.isFile?repositoryCommunicationButtons(it.raw):'';\n    const respond=it.reviewFlow&&canActOnReview(it.raw)?`<button class="btn review-action" type="button" onclick="openReviewResponse('${it.id}')">${reviewActionLabel(it.raw)}</button>`:'';\n    const ver=!it.reviewFlow&&it.onlineEditVersion>1?`<span class="office-version">✏️ edição v${it.onlineEditVersion}</span>`:'';\n    return `<div class="repo-item"><div class="repo-icon">${icon}</div><div class="repo-main"><div clas
---
play=repoViewMode==='general'?'inline-flex':'none';\n  if(packageBtn)packageBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(pubBtn)pubBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(dedupeBtn)dedupeBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(repoViewMode==='mine'){renderMyRepository();return}\n  if(repoViewMode==='shared'){renderSharedRepository();return}\n  refreshPubFilters();\n  const wrap=$('#pubList');\n  const cat=$('#pubCategoria').value, pessoa=$('#pubPessoa').value, mine=$('#pubMine').checked;\n  const packageCount=renderRepoPackages(cat,pessoa,mine,'general');\n  let list=state.publicacoes;\n  if(cat) list=list.filter(p=>p.categoria===cat);\n  if(pessoa) list=list.filter(p=>p.memberId===pessoa);\n  if(mine){\n    if(!myId){ toast('Identifique-se primeiro para ver só as suas.'); $('#pubMine').checked=false; }\n    else list=list.filter(p=>p.memberId===myId);\n  }\n  list=compactPublicationsForDisplay(list);updateRepoDedupeButton();\n  if(!list.length){ wrap.innerHTML=packageCount?'':'<div class="crono-empty">Nenhuma publicação encontrada com esses filtros.</div>'; return; }\n  wrap.innerHTML=list.map(p=>{\n    const cfg=CATEGORIAS[p.categoria]||CATEGORIAS.outro;\n    const catLabel = p.categoria==='outro' && p.categoriaOutro ? p.categoriaOutro : cfg.label;\n    const m=memberById(p.memberId);\n    const canDel = isAdmin || (p.reviewFlow ? p.ownerUid===(window.auth.currentUser?.uid||'') : canEditMember(p.memberId));\n    const dt = p.ts?.toMillis?new Date(p.ts.toMillis()):null;\n    const dateLabel = dt ? dt.toLocaleDateString('pt-BR',{day:'2-digit',month:'short',year:'numeric'}) : '';\n    return `<div class="pub-row"><div class="pub-cat" style="background:${cfg.color}"></div><div class="pub-body"><div class="pub-t">${esc(p.titulo)}</div><div class="pub-meta"><span class="pub-cat-tag" style="background:${cfg.color}">${esc(catLabel)}</span><span class="pub-author"><span class="d" style="background:${m?NIVEIS[m.nivel]?.color:'var(--ink-3)'}"></span>${esc(p.memberNome||(m?m.nome:'—'))}</span>${dateLabel?`<span class="pub-date">${dateLabel}</span>`:''}</div>${!p.reviewFlow&&Number(p.onlineEditVersion||1)>1?`<span class="office-version">✏️ edição v${Number(p.onlineEditVersion||1)}</span>`:''}${!p.reviewFlow?repositorySequenceHTML(p):''}${p.reviewFlow?reviewBadgeHTML(p):''}${p.reviewNote?`<div class="review-note">💬 ${esc(p.reviewNote)}</div>`:''}${scientificContextHTML(p)}${officeFileChip(p)}${publicationCollaboratorIds(p).length?`<span class="repo-collab-chip">👥 ${publicationCollaboratorIds(p).length} colaborador${publicationCollaboratorIds(p).length===1?'':'es'}</span>`:''}</div><div class="pub-act">${p.labRun?`<button type="button" class="btn primary" onclick="openLabRun('${p.labRunId}')">📈 Resultados</button>`:p.tipo==='arquivo'?`<button type="button" class="btn ghost" onclick="openFilePreview(decodeURIComponent('${encodeURIComponent(p.url)}'),decodeURIComponent('${encodeURIComponent(p.fileName||p.titulo||'Arquivo')}'),{source:'publication',id:'${p.id}'})" title="Visualizar" style="padding:6px 9px">👁️</button><button type="button" class="btn review-download" onclick="downloadRepoFile(decodeURIComponent('${encodeURIComponent(p.url)}'),decodeURIComponent('${encodeURIComponent(p.fileName||p.titulo||'Arquivo')}'))" title="Baixar" style="padding:6px 9px">⬇</button>`:`<a class="btn ghost" href="${esc(p.url)}" target="_blank" rel="noopener" title="Abrir link" style="padding:6px 9px">↗</a>`}${labRepoButton(p)}${officeActionButtons(p,'compact')}${p.tipo==='arquivo'?repositoryCommunicationButtons(p):''}${p.reviewFlow&&canActOnReview(p)?`<button type="button" class="btn review-action" onclick="openReviewResponse('${p.id}')">${reviewActionLabel(p)}</button>`:''}${canDel?`<button class="mini-x" onclick="deletePublicacao('${p.id}')" title="Excluir">×</button>`:''}</div></div>`;\n  }).join('');\n}\n\n\n/* ============ P13 · DOSSIÊS / MÚLTIPLOS ARQUIVOS COM ACL ============ */\nlet repoPackageSelectedFiles=[],activeRepoPackageId='';\nconst REPO_PURPOSES={artigos:'Artigos / manuscritos',resumos:'Resumos / eventos',instrumentos:'Instrumentos de coleta',dados:'Bases de dados',ferramentas:'Ferramentas / materiais',scripts:'Scripts / códigos',aula:'Material de aula',referencias:'Referências / leituras',bolsa:'Inscrição / bolsa',processo:'Processo seletivo / inscrição',prestacao:'Prestação de contas',documentos:'Documentos pessoais/acadêmicos',projeto:'Projeto / pesquisa',revisao:'Material para revisão',outro:'Outro'};\nfunction repoPurposeLabel(p){return p?.purpose==='outro'?(p.purposeOther||'Outro'):(REPO_PURPOSES[p?.purpose]||'Conjunto de arquivos')}\nfunction repoAccessLabel(p){\n  const
---
### CATEGORIAS. count=1
ral'?'inline-flex':'none';\n  if(packageBtn)packageBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(pubBtn)pubBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(dedupeBtn)dedupeBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(repoViewMode==='mine'){renderMyRepository();return}\n  if(repoViewMode==='shared'){renderSharedRepository();return}\n  refreshPubFilters();\n  const wrap=$('#pubList');\n  const cat=$('#pubCategoria').value, pessoa=$('#pubPessoa').value, mine=$('#pubMine').checked;\n  const packageCount=renderRepoPackages(cat,pessoa,mine,'general');\n  let list=state.publicacoes;\n  if(cat) list=list.filter(p=>p.categoria===cat);\n  if(pessoa) list=list.filter(p=>p.memberId===pessoa);\n  if(mine){\n    if(!myId){ toast('Identifique-se primeiro para ver só as suas.'); $('#pubMine').checked=false; }\n    else list=list.filter(p=>p.memberId===myId);\n  }\n  list=compactPublicationsForDisplay(list);updateRepoDedupeButton();\n  if(!list.length){ wrap.innerHTML=packageCount?'':'<div class="crono-empty">Nenhuma publicação encontrada com esses filtros.</div>'; return; }\n  wrap.innerHTML=list.map(p=>{\n    const cfg=CATEGORIAS[p.categoria]||CATEGORIAS.outro;\n    const catLabel = p.categoria==='outro' && p.categoriaOutro ? p.categoriaOutro : cfg.label;\n    const m=memberById(p.memberId);\n    const canDel = isAdmin || (p.reviewFlow ? p.ownerUid===(window.auth.currentUser?.uid||'') : canEditMember(p.memberId));\n    const dt = p.ts?.toMillis?new Date(p.ts.toMillis()):null;\n    const dateLabel = dt ? dt.toLocaleDateString('pt-BR',{day:'2-digit',month:'short',year:'numeric'}) : '';\n    return `<div class="pub-row"><div class="pub-cat" style="background:${cfg.color}"></div><div class="pub-body"><div class="pub-t">${esc(p.titulo)}</div><div class="pub-meta"><span class="pub-cat-tag" style="background:${cfg.color}">${esc(catLabel)}</span><span class="pub-author"><span class="d" style="background:${m?NIVEIS[m.nivel]?.color:'var(--ink-3)'}"></span>${esc(p.memberNome||(m?m.nome:'—'))}</span>${dateLabel?`<span class="pub-date">${dateLabel}</span>`:''}</div>${!p.reviewFlow&&Number(p.onlineEditVersion||1)>1?`<span class="office-version">✏️ edição v${Number(p.onlineEditVersion||1)}</span>`:''}${!p.reviewFlow?repositorySequenceHTML(p):''}${p.reviewFlow?reviewBadgeHTML(p):''}${p.reviewNote?`<div class="review-note">💬 ${esc(p.reviewNote)}</div>`:''}${scientificContextHTML(p)}${officeFileChip(p)}${publicationCollaboratorIds(p).length?`<span class="repo-collab-chip">👥 ${publicationCollaboratorIds(p).length} colaborador${publicationCollaboratorIds(p).length===1?'':'es'}</span>`:''}</div><div class="pub-act">${p.labRun?`<button type="button" class="btn primary" onclick="openLabRun('${p.labRunId}')">📈 Resultados</button>`:p.tipo==='arquivo'?`<button type="button" class="btn ghost" onclick="openFilePreview(decodeURIComponent('${encodeURIComponent(p.url)}'),decodeURIComponent('${encodeURIComponent(p.fileName||p.titulo||'Arquivo')}'),{source:'publication',id:'${p.id}'})" title="Visualizar" style="padding:6px 9px">👁️</button><button type="button" class="btn review-download" onclick="downloadRepoFile(decodeURIComponent('${encodeURIComponent(p.url)}'),decodeURIComponent('${encodeURIComponent(p.fileName||p.titulo||'Arquivo')}'))" title="Baixar" style="padding:6px 9px">⬇</button>`:`<a class="btn ghost" href="${esc(p.url)}" target="_blank" rel="noopener" title="Abrir link" style="padding:6px 9px">↗</a>`}${labRepoButton(p)}${officeActionButtons(p,'compact')}${p.tipo==='arquivo'?repositoryCommunicationButtons(p):''}${p.reviewFlow&&canActOnReview(p)?`<button type="button" class="btn review-action" onclick="openReviewResponse('${p.id}')">${reviewActionLabel(p)}</button>`:''}${canDel?`<button class="mini-x" onclick="deletePublicacao('${p.id}')" title="Excluir">×</button>`:''}</div></div>`;\n  }).join('');\n}\n\n\n/* ============ P13 · DOSSIÊS / MÚLTIPLOS ARQUIVOS COM ACL ============ */\nlet repoPackageSelectedFiles=[],activeRepoPackageId='';\nconst REPO_PURPOSES={artigos:'Artigos / manuscritos',resumos:'Resumos / eventos',instrumentos:'Instrumentos de coleta',dados:'Bases de dados',ferramentas:'Ferramentas / materiais',scripts:'Scripts / códigos',aula:'Material de aula',referencias:'Referências / leituras',bolsa:'Inscrição / bolsa',processo:'Processo seletivo / inscrição',prestacao:'Prestação de contas',documentos:'Documentos pessoais/acadêmicos',projeto:'Projeto / pesquisa',revisao:'Material para revisão',outro:'Outro'};\nfunction repoPurposeLabel(p){return p?.purpose==='outro'?(p.purposeOther||'Outro'):(REPO_PURPOSES[p?.purpose]||'Conjunto de arquivos')}\nfunction repoAccessLabel(p){\n  const mode=p?.accessMode||'pri
---
### categoria|| count=4
emaining)+' restantes';\n  if(now<start){label='Bolsa ainda não iniciada';remLabel=human(elapsed)+' para começar'}\n  if(now>end){label='Bolsa encerrada';remLabel=human(remaining)+' desde o término'}\n  return {progress,label,remaining:remLabel,start,end,daysRemaining:remaining};\n}\nfunction scholarshipHTML(m,compact=false){\n  const si=scholarshipInfo(m); if(!si)return '';\n  const modal=m.bolsaModalidade?esc(m.bolsaModalidade):'Bolsa';\n  return `<div class="scholar-box"><div class="scholar-head"><b>🎓 ${modal}</b><span>${esc(si.label)}</span><span class="remain">${esc(si.remaining)}</span></div><div class="scholar-bar"><span style="width:${si.progress}%"></span></div><div class="scholar-foot"><span>${m.bolsaInicio?'Início '+fmtDate(m.bolsaInicio):'Início não informado'}</span><span>${si.progress}% do período</span><span>${m.bolsaFim?'Fim '+fmtDate(m.bolsaFim):'Fim não informado'}</span></div></div>`;\n}\nfunction memberRepositoryItems(memberId){\n  const out=[];\n  compactPublicationsForDisplay(state.publicacoes.filter(p=>p.memberId===memberId)).forEach(p=>out.push({source:'publicacao',id:p.id,title:p.titulo||p.fileName||'Documento',kind:(CATEGORIAS[p.categoria]?.label||p.categoriaOutro||p.categoria||'Publicação'),url:p.url||'',fileName:p.fileName||'',isFile:p.tipo==='arquivo',ts:p.ts,reviewFlow:!!p.reviewFlow,reviewVersion:p.reviewVersion||0,reviewStatus:p.reviewStatus||'',reviewReviewerId:p.reviewReviewerId||'',reviewReturnToId:p.reviewReturnToId||'',reviewNextRecipientId:p.reviewNextRecipientId||'',reviewNote:p.reviewNote||'',onlineEditVersion:p.onlineEditVersion||1,raw:p}));\n  state.repositoryPackages.filter(p=>p.ownerMemberId===memberId).forEach(p=>out.push({source:'package',id:p.id,title:p.title||'Conjunto de arquivos',kind:'Dossiê · '+repoPurposeLabel(p),url:'',fileName:'',isFile:false,ts:p.ts,package:p}));\n  state.activities.filter(a=>a.ownerId===memberId && (a.fileUrl||a.externalUrl)).forEach(a=>out.push({source:'atividade',id:a.id,title:a.title||'Registro de acompanhamento',kind:ACTIVITY_TYPES[a.type]||a.type||'Acompanhamento',url:a.fileUrl||a.externalUrl||'',fileName:a.fileName||'',isFile:!!a.fileUrl,ts:a.updatedAt||a.createdAt}));\n  const m=memberById(memberId); (m?.resultados||[]).forEach((r,i)=>{if(r.url)out.push({source:'resultado',id:'r'+i,title:r.titulo||'Resultado / produto',kind:'Resultado / produto',url:r.url,fileName:'',isFile:false,ts:null})});\n  return out.sort((a,b)=>{const ta=a.ts?.toMillis?a.ts.toMillis():0,tb=b.ts?.toMillis?b.ts.toMillis():0;return tb-ta});\n}\nlet repoMemberId=null;\nfunction openMemberRepository(memberId){repoMemberId=memberId;renderMemberRepository();openOverlay('repoOverlay')}\nfunction renderMemberRepository(){\n  const m=memberById(repoMemberId); if(!m||!$('#repoList'))return;\n  $('#repoTitle').textContent='Repositório · '+m.nome;\n  $('#repoScholar').innerHTML=scholarshipHTML(m);\n  const list=memberRepositoryItems(m.id); $('#repoCount').textContent=list.length;\n  const canAdd=isAdmin||isMe(m.id); $('#repoAddBtn').style.display=canAdd?'inline-flex':'none';\n  $('#repoList').innerHTML=list.length?list.map(it=>{\n    const icon=it.reviewFlow?'✍️':(it.isFile?'📄':'🔗');\n    const view=it.source==='package'?`<button class="btn ghost" type="button" onclick="openRepositoryPackage('${it.id}')">📦 Abrir dossiê</button>`:it.raw?.labRun?`<button class="btn primary" type="button" onclick="openLabRun('${it.raw.labRunId}')">📈 Resultados</button>`:it.isFile?`<button class="btn ghost" type="button" onclick="openFilePreview(decodeURIComponent('${encodeURIComponent(it.url)}'),decodeURIComponent('${encodeURIComponent(it.fileName||it.title||'Arquivo')}'),{source:'publication',id:'${it.id}'})">Visualizar</button><button class="btn review-download" type="button" onclick="downloadRepoFile(decodeURIComponent('${encodeURIComponent(it.url)}'),decodeURIComponent('${encodeURIComponent(it.fileName||it.title||'Arquivo')}'))">Baixar</button>`:`<a class="btn ghost" href="${esc(it.url)}" target="_blank" rel="noopener">Abrir</a>`;\n    const rev=it.reviewFlow?reviewBadgeHTML(it.raw):'';\n    const online=it.source==='publicacao'?officeActionButtons(it.raw):'';\n    const chat=it.source==='publicacao'&&it.isFile?repositoryCommunicationButtons(it.raw):'';\n    const respond=it.reviewFlow&&canActOnReview(it.raw)?`<button class="btn review-action" type="button" onclick="openReviewResponse('${it.id}')">${reviewActionLabel(it.raw)}</button>`:'';\n    const ver=!it.reviewFlow&&it.onlineEditVersion>1?`<span class="office-version">✏️ edição v${it.onlineEditVersion}</span>`:'';\n    return `<div class="repo-item"><div class="repo-icon">${icon}</div><div class="repo-main"><div class="repo-title">${esc(it.title)}</div><div class="rep
---
Meu Repositório · v${nextV}.`);\n  }else if(ooSession.purpose==='review'){\n    const tid=p.reviewThreadId||p.id,same=contentHash?reviewThreadItems(tid).find(x=>x.contentHash===contentHash):null;if(same){setOoStatus(`✅ O arquivo final é idêntico à versão ${same.reviewVersion||1}. Nenhuma duplicação foi criada.`);toast('Nenhuma nova versão: o conteúdo já estava salvo.');ooSession.saved=true;ooSession.changed=false;ooSession.syncPending=false;return}\n    const nextVersion=reviewNextVersion(p),baseName=splitOfficeName(p.fileName||p.titulo||'documento',ext);const fileName=`${baseName}_v${nextVersion}.${ext}`;\n    const {url}=await fbUploadFile('onlyoffice_'+p.memberId,dataUrl,fileName);const ref=f.doc(window.db,'rede_publicacoes',reviewVersionDocId(tid,nextVersion));\n    const reviewerId=p.reviewReviewerId,returnToId=p.reviewReturnToId,actingAsReviewer=(myId===reviewerId);\n    const nextRecipientId=actingAsReviewer?returnToId:reviewerId,status=actingAsReviewer?'correcao_devolvida':'nova_versao',baseTitle=p.reviewBaseTitle||p.titulo||'Documento';\n    await f.setDoc(ref,{memberId:p.memberId,ownerUid:uid,createdByUid:uid,memberNome:p.memberNome||memberById(p.memberId)?.nome||'',categoria:p.categoria||'outro',categoriaOutro:p.categoriaOutro||'',titulo:baseTitle,tipo:'arquivo',url,fileName,contentHash,reviewFlow:true,reviewThreadId:tid,reviewVersion:nextVersion,reviewStatus:status,reviewReviewerId:reviewerId,reviewReturnToId:returnToId,reviewNextRecipientId:nextRecipientId,reviewParentId:p.id,reviewSenderId:actor?.id||myId||'',reviewSenderName:actor?.name||'',reviewBaseTitle:baseTitle,reviewNote:actingAsReviewer?(ext==='docx'?'Correção realizada online com controle de alterações.':'Arquivo revisado online.'):'Nova versão revisada online.',editedOnline:true,ts:f.serverTimestamp()});\n    await createRepoNotification(nextRecipientId,{sourceId:ref.id,ownerId:p.memberId,title:actingAsReviewer?'✅ Correção online devolvida':'⬆️ Nova versão para revisar',message:`${baseTitle} · versão ${nextVersion} · editada no ONLYOFFICE`});\n    setOoStatus('✅ Nova versão salva e destinatário notificado.');toast('Nova versão salva e notificação enviada.');\n  }else{\n    const currentV=Number(p.onlineEditVersion||1),nextV=currentV+1,baseName=splitOfficeName(p.fileName||p.titulo||'arquivo',ext),fileName=`${baseName}_v${nextV}.${ext}`;\n    const {url}=await fbUploadFile('onlyoffice_workspace_'+p.memberId,dataUrl,fileName);const editorMemberId=actor?.id||myId||'',editorName=actor?.name||myName||'Carbonauta';\n    const ownerMemberId=p.memberId||'',owner=memberById(ownerMemberId),ownerName=p.memberNome||owner?.nome||'proprietário';const editingSomeoneElsesFile=!!(ownerMemberId&&editorMemberId&&ownerMemberId!==editorMemberId);\n    const history={version:currentV,url:p.url||'',fileName:p.fileName||`${baseName}.${ext}`,savedAt:new Date().toISOString(),byUid:uid,byMemberId:editorMemberId,byName:editorName};\n    await f.updateDoc(f.doc(window.db,'rede_publicacoes',p.id),{url,fileName,onlineEditVersion:nextV,onlineEditHistory:f.arrayUnion(history),editedOnline:true,editedAt:f.serverTimestamp(),editedByUid:uid,editedByMemberId:editorMemberId,editedByName:editorName,repositoryTurnMemberId:editingSomeoneElsesFile?ownerMemberId:'',repositoryTurnFromId:editingSomeoneElsesFile?editorMemberId:'',repositoryTurnFromName:editingSomeoneElsesFile?editorName:'',repositoryTurnAt:f.serverTimestamp()});\n    if(editingSomeoneElsesFile){await createRepoNotification(ownerMemberId,{kind:'repositorio_retorno',sourceId:p.id,ownerId:ownerMemberId,title:`✨ Agora é sua vez, ${ownerName}`,message:`${editorName} editou “${p.titulo||p.fileName||'seu arquivo'}”. Dê uma olhada no que foi ajustado e continue a partir daqui.`});setOoStatus(`✅ Edição v${nextV} salva. ${ownerName} foi notificado(a) para continuar.`);toast(`Edição v${nextV} salva. ${ownerName} foi notificado(a).`);}else{setOoStatus(`✅ Edição v${nextV} salva. A versão anterior foi preservada no histórico.`);toast(`Edição v${nextV} salva no Repositório.`);}\n  }\n  ooSession.saved=true;ooSession.changed=false;ooSession.syncPending=false;if(btn){btn.disabled=true;btn.textContent='✅ Salvo'};\n}\nasync function saveOnlineDocument(){\n  if(!ooSession||ooSession.saved||ooSession.closingForSave)return;\n  const btn=$('#ooSaveBtn'),discardBtn=$('#ooCloseBtn'),p=state.publicacoes.find(x=>x.id===ooSession.pubId)||ooSession.p;\n  if(!ooSession.changed){\n    if(confirm('Nenhuma alteração nova foi detectada. Deseja apenas sair do editor?'))await discardOnlineDocument({confirmDiscard:false});\n    return;\n  }\n  try{\n    ooSession.closingForSave=true;\n    if(btn){btn.disabled=true;btn.textContent='⏳ Finalizando…'}if(discardBtn)discardBtn.disabled=tru
---
r();\n    }\n\n    const sel=$('#pubAutor');\n    if(!sel) throw new Error('Formulário do repositório não foi carregado.');\n\n    if(isAdmin){\n      $('#pubAutorRow').style.display='block';\n      const ordered=[...state.members].sort((a,b)=>a.nome.localeCompare(b.nome));\n      const defaultId=presetMemberId||(resolved?.id)||myId||ordered[0]?.id||'';\n      sel.innerHTML=ordered.map(m=>`<option value="${m.id}" ${defaultId===m.id?'selected':''}>${esc(m.nome)}</option>`).join('');\n      if(defaultId) sel.value=defaultId;\n    }else{\n      resolved=resolved||memberById(myId);\n      if(!resolved){\n        toast('Seu login ainda não está vinculado ao seu perfil. Clique no seu nome e use “Corrigir vínculo”.');\n        openIdentity();\n        return;\n      }\n      // Mantém o id efetivo sincronizado no navegador mesmo quando o Firestore tinha um vínculo antigo.\n      myId=resolved.id;\n      $('#pubAutorRow').style.display='none';\n      sel.innerHTML=`<option value="${resolved.id}">${esc(resolved.nome)}</option>`;\n      sel.value=resolved.id;\n    }\n\n    pubReviewContext=reviewContext||null;\n    if(reviewContext)$('#pubAutorRow').style.display='none';\n    $('#pubCategoriaSel').value=reviewContext?.categoria||'artigo';\n    $('#pubOutroRow').style.display=reviewContext?.categoria==='outro'?'block':'none';\n    $('#pubOutroTxt').value=reviewContext?.categoriaOutro||'';\n    $('#pubTitulo').value=reviewContext?.reviewBaseTitle||reviewContext?.titulo||'';\n    $('#pubUrl').value='';\n    $('#pubFinalidade').value=reviewContext?'correcao':'guardar';\n    $('#pubFinalidadeRow').style.display=reviewContext?'none':'block';\n    $('#pubReviewerRow').style.display=reviewContext?'none':'block';\n    $('#pubReviewNote').value='';\n    $('#pubReviewNoteLabel').textContent=reviewContext?(myId===reviewContext.reviewReviewerId?'Resposta / observações da correção':'O que mudou nesta nova versão?'):'Orientação para a correção (opcional)';\n    $('#pubReviewNote').placeholder=reviewContext?(myId===reviewContext.reviewReviewerId?'Ex.: corrigi comentários no texto e deixei observações na versão devolvida.':'Ex.: incorporei as correções e ajustei a discussão.'):'Ex.: revisar introdução, conferir referências e devolver com comentários.';\n    $('#pubReviewInfo').innerHTML=reviewContext?`<b>✍️ ${myId===reviewContext.reviewReviewerId?'Devolver correção':'Enviar nova versão'}</b><div class="hint">Versão ${reviewNextVersion(reviewContext)} do fluxo. O arquivo anterior continuará disponível.</div>`:'<b>✍️ Fluxo de correção</b><div class="hint">O arquivo original fica preservado. A pessoa escolhida recebe uma notificação e poderá devolver uma nova versão.</div>';\n    populatePubReviewer(reviewContext?.reviewReviewerId||'');\n    pubTipo=reviewContext?'arquivo':'link'; pubFileData=''; pubFileName=''; pubFileHash=''; pubFileHashPromise=null;\n    $$('input[name="pubTipo"]').forEach(r=>{r.checked=r.value===pubTipo;r.disabled=!!reviewContext});\n    $('#pubUrl').style.display=pubTipo==='link'?'block':'none';\n    $('#pubFileArea').style.display=pubTipo==='arquivo'?'block':'none';\n    $('#pubFileName').textContent='Nenhum arquivo escolhido. Máximo 15 MB.';\n    $('#pubModalTitle').textContent=reviewContext?(myId===reviewContext.reviewReviewerId?'Devolver arquivo corrigido':'Enviar nova versão'):'Adicionar documento ao repositório';\n    $('#savePubBtn').textContent=reviewContext?(myId===reviewContext.reviewReviewerId?'Devolver correção':'Enviar nova versão'):'Salvar';\n    syncPubFinalidade();\n    openOverlay('pubOverlay');\n    setTimeout(()=>reviewContext?$('#pubFilePick')?.focus():$('#pubTitulo')?.focus(),50);\n  }catch(e){\n    console.error('Erro ao abrir o repositório',e);\n    toast('Não consegui abrir o formulário do repositório. Recarregue a página e tente novamente.');\n  }\n}\nfunction handlePubFile(file){\n  if(!file)return;\n  if(file.size > 15*1024*1024){ toast('Arquivo maior que 15 MB. Escolha um arquivo menor.'); return; }\n  pubFileHash='';pubFileHashPromise=sha256File(file).then(h=>pubFileHash=h).catch(()=>{pubFileHash='';return''});\n  const reader=new FileReader();\n  reader.onload=e=>{ pubFileData=e.target.result; pubFileName=file.name; $('#pubFileName').textContent=file.name+' · verificando duplicação…'; Promise.resolve(pubFileHashPromise).finally(()=>{$('#pubFileName').textContent=file.name;}); };\n  reader.readAsDataURL(file);\n}\nasync function savePublicacao(){\n  const saveBtn=$('#savePubBtn');if(saveBtn?.disabled)return;\n  const resolved=resolveCurrentMember();\n  const autorId = pubReviewContext?.memberId || (isAdmin? $('#pubAutor').value : (resolved?.id||myId));\n  const categoria=$('#pubCategoriaSel').value;\n  const categoriaOutro=$('#pubOutroTxt').value.trim();\n  con
---
der-radius:10px}</style></head><body>${dossierHTML(m)}</body></html>`);w.document.close();setTimeout(()=>w.print(),350)}\nfunction exportGeneralBackup(){if(isAdmin&&!window.XLSX){ensureLib('xlsx').then(exportGeneralBackup).catch(e=>toast(e.message));return}if(!isAdmin||!window.XLSX){toast('Backup geral disponível somente para o coordenador.');return}const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(state.members.map(m=>({id:m.id,nome:m.nome,nivel:m.nivel,programa:m.programa||'',status:m.status||'',bolsista:m.bolsista?'Sim':'Não',pergunta:m.perguntaCientifica||'',objetivo:m.objetivoProjeto||'',publico:m.grupoAtendido||'',visibilidade:m.ideiaVisibilidade||''}))), 'Pessoas');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(state.activities.map(a=>({id:a.id,ownerId:a.ownerId,ownerName:a.ownerName||'',tipo:a.type,status:a.status,titulo:a.title||'',descricao:a.description||'',inicio:a.startDate||'',prazo:a.dueDate||'',mes:a.checkinMonth||'',produto:a.productType||''}))), 'Acompanhamento');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(state.publicacoes.map(x=>({id:x.id,ownerUid:x.ownerUid||'',titulo:x.titulo||x.title||'',categoria:x.categoria||'',url:x.fileUrl||x.url||x.externalUrl||''}))), 'Repositorio');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(buildCronoItems().map(x=>({tipo:x.kind,titulo:x.titulo,data:x.data,owner:x.ownerName,concluido:x.feito?'Sim':'Não'}))), 'Cronograma');XLSX.writeFile(wb,`Carbonautas_backup_${new Date().toISOString().slice(0,10)}.xlsx`);toast('Backup Excel gerado.')}\n\n/* ============ FEED SOCIAL ============ */\nlet postFileData='',postFileName='';\nfunction renderFeedSide(){\n  if(!$('#feedStats'))return; const active=state.members.filter(m=>m.status==='ativo').length,eg=state.members.filter(m=>m.status==='egresso').length,done=state.activities.filter(a=>a.status==='concluido').length;\n  $('#feedStats').innerHTML=`<div class="side-stat"><span>Carbonautas ativos</span><b>${active}</b></div><div class="side-stat"><span>Egressos</span><b>${eg}</b></div><div class="side-stat"><span>Registros acompanhados</span><b>${state.activities.length}</b></div><div class="side-stat"><span>Entregas concluídas</span><b>${done}</b></div>`;\n  const upcoming=buildCronoItems().filter(x=>!x.feito&&x.data).sort((a,b)=>a.data.localeCompare(b.data)).slice(0,5); $('#feedDeadlines').innerHTML=upcoming.length?upcoming.map(x=>`<div class="side-stat"><span>${esc(x.titulo)}</span><b>${fmtDate(x.data)}</b></div>`).join(''):'<div class="hint">Nenhuma entrega próxima.</div>';\n}\nfunction renderComments(targetType,targetId){\n  const s=socialFor(targetType,targetId);\n  return `<div class="comments" id="comments_${targetType}_${targetId}" style="display:none">${s.cs.slice(-20).map(c=>{const m=memberForAuthor(c.authorId,c.authorName);const pic=m?.foto||'';return `<div class="comment"><div class="comment-av" style="background:${m?NIVEIS[m.nivel]?.color:(c.authorColor||'#7c8b90')}">${pic?`<img src="${esc(pic)}" alt="" style="width:100%;height:100%;object-fit:cover;border-radius:8px">`:initials(c.authorName||'?')}</div><div class="comment-b"><div class="comment-name">${esc(c.authorName||m?.nome||'')}</div><div class="comment-text">${esc(c.text||'')}</div></div></div>`}).join('')}<div class="comment-form"><input id="comment_${targetType}_${targetId}" placeholder="fala ae xomano(a)…" onkeydown="if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();addSocialComment('${targetType}','${targetId}','comment_${targetType}_${targetId}')}"><button class="btn primary" onclick="addSocialComment('${targetType}','${targetId}','comment_${targetType}_${targetId}')">Enviar</button></div></div>`\n}\nfunction toggleComments(targetType,targetId){\n  const el=$('#comments_'+targetType+'_'+targetId);if(!el)return;\n  const open=el.style.display!=='none';el.style.display=open?'none':'block';\n  if(!open)setTimeout(()=>$('#comment_'+targetType+'_'+targetId)?.focus(),30);\n}\nfunction socialBar(targetType,targetId){\n  const s=socialFor(targetType,targetId),mine=myReaction(targetType,targetId),total=s.likes+s.hearts+s.parties+s.dislikes;\n  const left=[s.likes?`👍 ${s.likes}`:'',s.hearts?`💙 ${s.hearts}`:'',s.parties?`🎉 ${s.parties}`:'',s.dislikes?`🤨 ${s.dislikes}`:''].filter(Boolean).join(' · ');\n  const summary=(total||s.cs.length)?`<div class="social-summary"><span class="sum-left">${left||'Sem reações ainda'}</span><span class="sum-right">${s.cs.length?`${s.cs.length} comentário${s.cs.length===1?'':'s'}`:''}</span></div>`:'';\n  return `<div class="social-wrap">${summary}<div class="social-bar"><button type="button" class="react-btn ${mine?.type==='like'?'on':''}" onclick="toggleReaction('${t
---
### categoria === count=0
### pubCategoria count=10
Crie, edite e analise sem sair do Carbonautas.</div></div>\n            <div class="spacer"></div>\n            <span class="engine-pill" id="scienceEngineStatus"><span class="engine-dot"></span><span>checando motor…</span></span>\n          </div>\n          <div class="science-dock-grid">\n            <button class="science-action" type="button" onclick="openNewOfficeType('docx')"><span class="sa-icon">📄</span><b>Novo Word</b><small>Artigo, relatório, tese ou capítulo.</small></button>\n            <button class="science-action" type="button" onclick="openNewOfficeType('xlsx')"><span class="sa-icon">📊</span><b>Novo Excel</b><small>Planilha editável no navegador.</small></button>\n            <button class="science-action" type="button" onclick="openNewOfficeType('pptx')"><span class="sa-icon">📽️</span><b>Novo PowerPoint</b><small>Apresentação criada no Repositório.</small></button>\n            <button class="science-action lab" type="button" onclick="switchView('lab')"><span class="sa-icon">🧪</span><b>Laboratório</b><small>Python científico · figuras publicáveis.</small></button>\n          </div>\n        </section>\n\n        <div class="crono-filters">\n          <select class="mini" id="pubCategoria" style="width:auto"><option value="">Todas as categorias</option></select>\n          <select class="mini" id="pubPessoa" style="width:auto"><option value="">Todas as pessoas</option></select>\n          <label class="toggle"><input type="checkbox" id="pubMine"> Só as minhas</label>\n        </div>\n        <div id="repoPackageList" class="repo-package-zone"></div>\n        <div id="pubList"></div>\n        </section>\n\n        <section id="repoPersonalPanel" style="display:none">\n          <div class="repo-personal-hero">\n            <div class="repo-personal-kicker">🔐 Seu espaço científico</div>\n            <h3 id="repoPersonalName">Meu Repositório</h3>\n            <p>Arquivos e pastas daqui começam privados. Você decide o que continua só seu, o que compartilha com pessoas específicas e o que publica no Repositório Geral.</p>\n            <div class="repo-personal-actions">\n              <button class="btn primary" type="button" id="repoPersonalNewFolderBtn" onclick="openRepoPackageUpload({personal:true});return false">＋ Nova pasta + arquivos</button>\n              <button class="btn" type="button" id="repoRootShareBtn">👥 Compartilhar meu repositório</button>\n              <button class="btn" type="button" id="repoPersonalRefreshBtn">↻ Atualizar</button>\n            </div>\n            <div class="repo-collab-strip" id="repoRootCollaborators"></div>\n            <div class="repo-personal-stats">\n              <div class="repo-personal-stat"><b id="repoMyFolderCount">0</b><span>pastas</span></div>\n              <div class="repo-personal-stat"><b id="repoMyFileCount">0</b><span>arquivos</span></div>\n              <div class="repo-personal-stat"><b id="repoMyCollabCount">0</b><span>colaboradores</span></div>\n            </div>\n          </div>\n          <div class="repo-section-head"><div><h3>📁 Minhas pastas</h3><small>Instrumentos, artigos, dados, scripts, resumos, aulas e o que você quiser guardar.</small></div><div class="spacer"></div><button class="btn primary" type="button" id="repoPersonalNewFolderBtn2" onclick="openRepoPackageUpload({personal:true});return false">＋ Criar pasta</button></div>\n          <div class="repo-folder-grid" id="repoPersonalFolders"></div>\n        </section>\n\n        <section id="repoSharedPanel" style="display:none">\n          <div class="repo-section-head"><div><h3>👥 Compartilhado comigo</h3><small>Somente espaços que deram acesso explícito à sua conta.</small></div></div>\n          <div id="repoSharedOwners"></div>\n        </section>\n      </div>\n    </div>\n  </div>\n</div>\n\n\n<!-- PEOPLE MANAGER -->\n<div class="overlay" id="peopleOverlay">\n  <div class="modal" style="max-width:820px">\n    <div class="modal-h"><h2>Gerenciar alunos da rede</h2><div class="spacer"></div><button class="btn" id="peopleDedupeBtn">Verificar duplicados</button><button class="btn primary" id="peopleAddBtn">+ Adicionar aluno</button><button class="btn ghost" data-close="peopleOverlay">Fechar</button></div>\n    <div class="modal-b">\n      <div class="fg"><label>Buscar</label><input id="peopleSearch" placeholder="Nome, programa ou situação"></div>\n      <div class="people-list" id="peopleList"></div>\n    </div>\n  </div>\n</div>\n\n<!-- FILE PREVIEW -->\n<div class="overlay" id="filePreviewOverlay">\n  <div class="modal file-preview-modal">\n    <div class="modal-h"><h2 id="filePreviewTitle">Visualizar arquivo</h2><div class="spacer"></div><div class="preview-actions"><a class="btn" id="filePreviewOpen" target="_blank" rel="noopener">Abr
---
laceholder="Diga de forma objetiva o que você precisa, quando e como o grupo pode ajudar."></textarea></div>\n    <div class="fg"><label>Link (opcional)</label><input id="postUrl" placeholder="https://..."></div>\n    <div class="fg"><label>Anexo (foto, PDF, Word, planilha, apresentação etc.; até 15 MB)</label><button class="btn" id="postFilePick" style="justify-content:center">Escolher arquivo</button><input type="file" id="postFile" style="display:none"><span class="hint" id="postFileName">Nenhum arquivo escolhido.</span></div>\n  </div>\n  <div class="modal-f"><button class="btn" data-close="postOverlay">Cancelar</button><button class="btn primary" id="savePostBtn">Publicar</button></div>\n</div></div>\n\n<!-- PUBLICATION MODAL -->\n<div class="overlay" id="pubOverlay">\n  <div class="modal" style="max-width:480px">\n    <div class="modal-h"><h2 id="pubModalTitle">Adicionar documento ao repositório</h2><div class="spacer"></div><button class="btn ghost" data-close="pubOverlay">Fechar</button></div>\n    <div class="modal-b">\n      <div class="fg" id="pubAutorRow"><label>Autor(a)</label><select id="pubAutor"></select></div>\n      <div class="fg"><label>Categoria</label>\n        <select id="pubCategoriaSel">\n          <option value="nota_tecnica">Nota técnica</option>\n          <option value="artigo">Artigo científico</option>\n          <option value="resumo_expandido">Resumo expandido</option>\n          <option value="resumo_simples">Resumo simples</option>\n          <option value="visibilidade">Plano de visibilidade</option>\n          <option value="jogo">Jogo</option>\n          <option value="plataforma">Plataforma</option>\n          <option value="projeto">Projeto</option>\n          <option value="apresentacao">Apresentação</option>\n          <option value="cronograma">Cronograma</option>\n          <option value="relatorio">Relatório</option>\n          <option value="dados">Dados / planilha</option>\n          <option value="outro">Outro</option>\n        </select>\n      </div>\n      <div class="fg" id="pubOutroRow" style="display:none"><label>Especifique</label><input id="pubOutroTxt" placeholder="Qual tipo de produto?"></div>\n      <div class="fg"><label>Título</label><input id="pubTitulo" placeholder="Título do trabalho"></div>\n      <div class="fg" id="pubFinalidadeRow"><label>Finalidade</label>\n        <select id="pubFinalidade">\n          <option value="guardar">📚 Guardar no repositório</option>\n          <option value="correcao">✍️ Enviar para correção</option>\n        </select>\n      </div>\n      <div id="pubReviewFields" style="display:none">\n        <div class="review-callout" id="pubReviewInfo"><b>✍️ Fluxo de correção</b><div class="hint">O arquivo original fica preservado. A pessoa escolhida recebe uma notificação e poderá devolver uma nova versão.</div></div>\n        <div class="fg" id="pubReviewerRow"><label>Quem deve corrigir / ler?</label><select id="pubReviewer"></select></div>\n        <div class="fg"><label id="pubReviewNoteLabel">Orientação para a correção (opcional)</label><textarea id="pubReviewNote" style="min-height:90px" placeholder="Ex.: revisar introdução, conferir referências e devolver com comentários."></textarea></div>\n      </div>\n      <div class="fg">\n        <label>Conteúdo</label>\n        <div style="display:flex;gap:14px;margin-bottom:8px">\n          <label class="toggle" style="padding:0"><input type="radio" name="pubTipo" value="link" checked> Link</label>\n          <label class="toggle" style="padding:0"><input type="radio" name="pubTipo" value="arquivo"> Arquivo (PDF/Word/Excel/PPT etc.)</label>\n        </div>\n        <input id="pubUrl" placeholder="https://...">\n        <div id="pubFileArea" style="display:none;margin-top:8px">\n          <button class="btn" id="pubFilePick" style="width:100%;justify-content:center">Escolher arquivo</button>\n          <input type="file" id="pubFile" style="display:none">\n          <p class="hint" id="pubFileName" style="margin:8px 0 0">Nenhum arquivo escolhido. Máximo 15 MB.</p>\n        </div>\n      </div>\n    </div>\n    <div class="modal-f">\n      <button class="btn" data-close="pubOverlay">Cancelar</button>\n      <button class="btn primary" id="savePubBtn">Salvar</button>\n    </div>\n  </div>\n</div>\n\n<!-- P20 · CONVERSA PRIVADA DA CORREÇÃO -->\n<div class="overlay" id="reviewConversationOverlay">\n  <div class="modal" style="max-width:980px">\n    <div class="modal-h"><div><h2 id="reviewConversationTitle">💬 Conversa da correção</h2><div class="hint" id="reviewConversationSubtitle">Somente autor/remetente e revisor desta correção.</div></div><div class="spacer"></div><button class="btn ghost" type="button" id="reviewConversationCloseBtn">Fechar</butto
---
t projeto=$('#gProjeto').value==='__outro'?$('#gProjetoOutro').value.trim():$('#gProjeto').value;\n  const btn=$('#savePhotoBtn');btn.disabled=true;let ok=0;\n  try{\n    for(let i=0;i<imgs.length;i++){\n      btn.textContent=imgs.length>1?`Enviando ${i+1}/${imgs.length}…`:'Enviando…';\n      const {url,path}=await fbUploadImage('galeria',imgs[i]);\n      const photoRef=await FB().addDoc(FB().collection(window.db,'rede_photos'),{url,storagePath:path,legenda:$('#gCap').value.trim(),projeto,acao:$('#gAcao').value.trim(),dataRealizacao:$('#gData').value,tipoImagem:$('#gTipoImagem').value,autor:$('#gAutor').value.trim(),autorId:myId||null,authorUid:window.auth.currentUser?.uid||'',ts:FB().serverTimestamp()});\n      await awardPoints(myId,'photo',photoRef.id,$('#gAcao').value.trim()||projeto||'Foto com metadados');\n      ok++;\n    }\n    closeOverlay('photoOverlay');toast(ok===1?'Imagem publicada.':`${ok} imagens publicadas.`);\n  }catch(e){console.error(e);toast(ok?`${ok} enviadas, depois falhou: ${e.message||'erro'}`:'Erro ao enviar a imagem.')}\n  finally{btn.disabled=false;btn.textContent='Publicar'}\n}\n\n/* ============ PUBLICAÇÕES ============ */\nfunction refreshPubFilters(){\n  const cc=$('#pubCategoria'), curC=cc.value;\n  cc.innerHTML='<option value="">Todas as categorias</option>'+\n    Object.entries(CATEGORIAS).map(([k,v])=>`<option value="${k}">${v.label}</option>`).join('');\n  cc.value=curC;\n  const cp=$('#pubPessoa'), curP=cp.value;\n  const ordered=[...state.members].sort((a,b)=>a.nome.localeCompare(b.nome));\n  cp.innerHTML='<option value="">Todas as pessoas</option>'+\n    ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');\n  cp.value=curP;\n}\nfunction setRepoViewMode(mode){\n  repoViewMode=['general','mine','shared'].includes(mode)?mode:'general';\n  document.querySelectorAll('[data-repo-mode]').forEach(b=>b.classList.toggle('on',b.dataset.repoMode===repoViewMode));\n  renderPubs();\n}\nfunction repoRootCollaborators(){return (state.personalRepoRoot?.collaboratorMemberIds||[]).filter(id=>id&&id!==myId)}\nfunction personalPackagesOwned(){return (state.repositoryPackages||[]).filter(p=>p.ownerMemberId===myId)}\nfunction folderPurposeIcon(p){const k=p?.purpose||'';return ({artigos:'📝',resumos:'📑',instrumentos:'🧰',dados:'📊',ferramentas:'🛠️',scripts:'💻',aula:'🎓',referencias:'📚',projeto:'🔬',revisao:'✍️',documentos:'🗂️',bolsa:'🎓',processo:'📋',prestacao:'🧾'})[k]||'📁'}\nfunction repoMiniAvatar(m,name=''){const n=m?.nome||name||'?';const nv=NIVEIS[m?.nivel]||{color:'#385366'};return m?.foto?`<img class="avatar" src="${esc(m.foto)}" alt="" style="object-fit:cover">`:`<div class="avatar" style="background:${nv.color}">${initials(n)}</div>`}\nfunction renderRepoFolderCard(p,shared=false){\n  const a=repoAccessLabel(p),owner=memberById(p.ownerMemberId),manage=canManageRepoPackage(p),rootShared=(p.rootSharedMemberIds||[]).length;\n  return `<div class="repo-folder"><div class="repo-folder-icon">${folderPurposeIcon(p)}</div><div class="repo-folder-main"><div class="repo-folder-title">${esc(p.title||'Pasta')}</div><div class="repo-folder-meta"><span>${esc(repoPurposeLabel(p))}</span><span>· ${Number(p.fileCount||0)} arquivo${Number(p.fileCount||0)===1?'':'s'}</span>${shared?`<span>· ${esc(p.ownerName||owner?.nome||'')}</span>`:''}<span class="repo-access-chip ${a.cls}">${esc(a.text)}</span>${rootShared?`<span>🔗 repo compartilhado</span>`:''}</div></div><div class="repo-folder-actions"><button class="btn ghost" type="button" onclick="openRepositoryPackage('${p.id}')">📂 Abrir</button>${manage?`<button class="btn" type="button" onclick="addFilesToRepositoryPackage('${p.id}')">＋ Arquivos</button><button class="btn" type="button" onclick="openRepoFolderAccess('${p.id}')">👥 Acesso</button><button class="btn" type="button" onclick="deleteRepositoryPackage('${p.id}')">🗑</button>`:''}</div></div>`;\n}\nfunction renderMyRepository(){\n  const me=memberById(myId),folders=personalPackagesOwned().sort((a,b)=>((b.updatedAt?.toMillis?.()||b.ts?.toMillis?.()||0)-(a.updatedAt?.toMillis?.()||a.ts?.toMillis?.()||0))),collabs=repoRootCollaborators();\n  if($('#repoPersonalName'))$('#repoPersonalName').textContent='Repositório de '+(me?.nome?.split(' ')[0]||'você');\n  if($('#repoMyFolderCount'))$('#repoMyFolderCount').textContent=folders.length;\n  if($('#repoMyFileCount'))$('#repoMyFileCount').textContent=folders.reduce((n,p)=>n+Number(p.fileCount||0),0);\n  if($('#repoMyCollabCount'))$('#repoMyCollabCount').textContent=collabs.length;\n  const strip=$('#repoRootCollaborators');if(strip)strip.innerHTML=collabs.length?collabs.map(id=>`<span class="repo-collab-chip">👤 ${esc(memberById(id)?.nome||id)}</span>`).join(''):'<span class="repo-collab-chip 
---
os arquivos.':'Não foi possível limpar os duplicados.')}\n}\n\nfunction renderPubs(){\n  const general=$('#repoGeneralPanel'),minePanel=$('#repoPersonalPanel'),sharedPanel=$('#repoSharedPanel');\n  if(general)general.style.display=repoViewMode==='general'?'block':'none';\n  if(minePanel)minePanel.style.display=repoViewMode==='mine'?'block':'none';\n  if(sharedPanel)sharedPanel.style.display=repoViewMode==='shared'?'block':'none';\n  document.querySelectorAll('[data-repo-mode]').forEach(b=>b.classList.toggle('on',b.dataset.repoMode===repoViewMode));\n  const newOffice=$('#newOfficeBtn'),packageBtn=$('#addRepoPackageBtn'),pubBtn=$('#addPubBtn'),dedupeBtn=$('#repoDedupeBtn');\n  if(newOffice)newOffice.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(packageBtn)packageBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(pubBtn)pubBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(dedupeBtn)dedupeBtn.style.display=repoViewMode==='general'?'inline-flex':'none';\n  if(repoViewMode==='mine'){renderMyRepository();return}\n  if(repoViewMode==='shared'){renderSharedRepository();return}\n  refreshPubFilters();\n  const wrap=$('#pubList');\n  const cat=$('#pubCategoria').value, pessoa=$('#pubPessoa').value, mine=$('#pubMine').checked;\n  const packageCount=renderRepoPackages(cat,pessoa,mine,'general');\n  let list=state.publicacoes;\n  if(cat) list=list.filter(p=>p.categoria===cat);\n  if(pessoa) list=list.filter(p=>p.memberId===pessoa);\n  if(mine){\n    if(!myId){ toast('Identifique-se primeiro para ver só as suas.'); $('#pubMine').checked=false; }\n    else list=list.filter(p=>p.memberId===myId);\n  }\n  list=compactPublicationsForDisplay(list);updateRepoDedupeButton();\n  if(!list.length){ wrap.innerHTML=packageCount?'':'<div class="crono-empty">Nenhuma publicação encontrada com esses filtros.</div>'; return; }\n  wrap.innerHTML=list.map(p=>{\n    const cfg=CATEGORIAS[p.categoria]||CATEGORIAS.outro;\n    const catLabel = p.categoria==='outro' && p.categoriaOutro ? p.categoriaOutro : cfg.label;\n    const m=memberById(p.memberId);\n    const canDel = isAdmin || (p.reviewFlow ? p.ownerUid===(window.auth.currentUser?.uid||'') : canEditMember(p.memberId));\n    const dt = p.ts?.toMillis?new Date(p.ts.toMillis()):null;\n    const dateLabel = dt ? dt.toLocaleDateString('pt-BR',{day:'2-digit',month:'short',year:'numeric'}) : '';\n    return `<div class="pub-row"><div class="pub-cat" style="background:${cfg.color}"></div><div class="pub-body"><div class="pub-t">${esc(p.titulo)}</div><div class="pub-meta"><span class="pub-cat-tag" style="background:${cfg.color}">${esc(catLabel)}</span><span class="pub-author"><span class="d" style="background:${m?NIVEIS[m.nivel]?.color:'var(--ink-3)'}"></span>${esc(p.memberNome||(m?m.nome:'—'))}</span>${dateLabel?`<span class="pub-date">${dateLabel}</span>`:''}</div>${!p.reviewFlow&&Number(p.onlineEditVersion||1)>1?`<span class="office-version">✏️ edição v${Number(p.onlineEditVersion||1)}</span>`:''}${!p.reviewFlow?repositorySequenceHTML(p):''}${p.reviewFlow?reviewBadgeHTML(p):''}${p.reviewNote?`<div class="review-note">💬 ${esc(p.reviewNote)}</div>`:''}${scientificContextHTML(p)}${officeFileChip(p)}${publicationCollaboratorIds(p).length?`<span class="repo-collab-chip">👥 ${publicationCollaboratorIds(p).length} colaborador${publicationCollaboratorIds(p).length===1?'':'es'}</span>`:''}</div><div class="pub-act">${p.labRun?`<button type="button" class="btn primary" onclick="openLabRun('${p.labRunId}')">📈 Resultados</button>`:p.tipo==='arquivo'?`<button type="button" class="btn ghost" onclick="openFilePreview(decodeURIComponent('${encodeURIComponent(p.url)}'),decodeURIComponent('${encodeURIComponent(p.fileName||p.titulo||'Arquivo')}'),{source:'publication',id:'${p.id}'})" title="Visualizar" style="padding:6px 9px">👁️</button><button type="button" class="btn review-download" onclick="downloadRepoFile(decodeURIComponent('${encodeURIComponent(p.url)}'),decodeURIComponent('${encodeURIComponent(p.fileName||p.titulo||'Arquivo')}'))" title="Baixar" style="padding:6px 9px">⬇</button>`:`<a class="btn ghost" href="${esc(p.url)}" target="_blank" rel="noopener" title="Abrir link" style="padding:6px 9px">↗</a>`}${labRepoButton(p)}${officeActionButtons(p,'compact')}${p.tipo==='arquivo'?repositoryCommunicationButtons(p):''}${p.reviewFlow&&canActOnReview(p)?`<button type="button" class="btn review-action" onclick="openReviewResponse('${p.id}')">${reviewActionLabel(p)}</button>`:''}${canDel?`<button class="mini-x" onclick="deletePublicacao('${p.id}')" title="Excluir">×</button>`:''}</div></div>`;\n  }).join('');\n}\n\n\n/* ============ P13 · DOSSIÊS / MÚLTIPLOS ARQUIVOS COM ACL ============ */\nlet repoPa
---
e){}\n      resolved=resolveCurrentMember();\n    }\n\n    const sel=$('#pubAutor');\n    if(!sel) throw new Error('Formulário do repositório não foi carregado.');\n\n    if(isAdmin){\n      $('#pubAutorRow').style.display='block';\n      const ordered=[...state.members].sort((a,b)=>a.nome.localeCompare(b.nome));\n      const defaultId=presetMemberId||(resolved?.id)||myId||ordered[0]?.id||'';\n      sel.innerHTML=ordered.map(m=>`<option value="${m.id}" ${defaultId===m.id?'selected':''}>${esc(m.nome)}</option>`).join('');\n      if(defaultId) sel.value=defaultId;\n    }else{\n      resolved=resolved||memberById(myId);\n      if(!resolved){\n        toast('Seu login ainda não está vinculado ao seu perfil. Clique no seu nome e use “Corrigir vínculo”.');\n        openIdentity();\n        return;\n      }\n      // Mantém o id efetivo sincronizado no navegador mesmo quando o Firestore tinha um vínculo antigo.\n      myId=resolved.id;\n      $('#pubAutorRow').style.display='none';\n      sel.innerHTML=`<option value="${resolved.id}">${esc(resolved.nome)}</option>`;\n      sel.value=resolved.id;\n    }\n\n    pubReviewContext=reviewContext||null;\n    if(reviewContext)$('#pubAutorRow').style.display='none';\n    $('#pubCategoriaSel').value=reviewContext?.categoria||'artigo';\n    $('#pubOutroRow').style.display=reviewContext?.categoria==='outro'?'block':'none';\n    $('#pubOutroTxt').value=reviewContext?.categoriaOutro||'';\n    $('#pubTitulo').value=reviewContext?.reviewBaseTitle||reviewContext?.titulo||'';\n    $('#pubUrl').value='';\n    $('#pubFinalidade').value=reviewContext?'correcao':'guardar';\n    $('#pubFinalidadeRow').style.display=reviewContext?'none':'block';\n    $('#pubReviewerRow').style.display=reviewContext?'none':'block';\n    $('#pubReviewNote').value='';\n    $('#pubReviewNoteLabel').textContent=reviewContext?(myId===reviewContext.reviewReviewerId?'Resposta / observações da correção':'O que mudou nesta nova versão?'):'Orientação para a correção (opcional)';\n    $('#pubReviewNote').placeholder=reviewContext?(myId===reviewContext.reviewReviewerId?'Ex.: corrigi comentários no texto e deixei observações na versão devolvida.':'Ex.: incorporei as correções e ajustei a discussão.'):'Ex.: revisar introdução, conferir referências e devolver com comentários.';\n    $('#pubReviewInfo').innerHTML=reviewContext?`<b>✍️ ${myId===reviewContext.reviewReviewerId?'Devolver correção':'Enviar nova versão'}</b><div class="hint">Versão ${reviewNextVersion(reviewContext)} do fluxo. O arquivo anterior continuará disponível.</div>`:'<b>✍️ Fluxo de correção</b><div class="hint">O arquivo original fica preservado. A pessoa escolhida recebe uma notificação e poderá devolver uma nova versão.</div>';\n    populatePubReviewer(reviewContext?.reviewReviewerId||'');\n    pubTipo=reviewContext?'arquivo':'link'; pubFileData=''; pubFileName=''; pubFileHash=''; pubFileHashPromise=null;\n    $$('input[name="pubTipo"]').forEach(r=>{r.checked=r.value===pubTipo;r.disabled=!!reviewContext});\n    $('#pubUrl').style.display=pubTipo==='link'?'block':'none';\n    $('#pubFileArea').style.display=pubTipo==='arquivo'?'block':'none';\n    $('#pubFileName').textContent='Nenhum arquivo escolhido. Máximo 15 MB.';\n    $('#pubModalTitle').textContent=reviewContext?(myId===reviewContext.reviewReviewerId?'Devolver arquivo corrigido':'Enviar nova versão'):'Adicionar documento ao repositório';\n    $('#savePubBtn').textContent=reviewContext?(myId===reviewContext.reviewReviewerId?'Devolver correção':'Enviar nova versão'):'Salvar';\n    syncPubFinalidade();\n    openOverlay('pubOverlay');\n    setTimeout(()=>reviewContext?$('#pubFilePick')?.focus():$('#pubTitulo')?.focus(),50);\n  }catch(e){\n    console.error('Erro ao abrir o repositório',e);\n    toast('Não consegui abrir o formulário do repositório. Recarregue a página e tente novamente.');\n  }\n}\nfunction handlePubFile(file){\n  if(!file)return;\n  if(file.size > 15*1024*1024){ toast('Arquivo maior que 15 MB. Escolha um arquivo menor.'); return; }\n  pubFileHash='';pubFileHashPromise=sha256File(file).then(h=>pubFileHash=h).catch(()=>{pubFileHash='';return''});\n  const reader=new FileReader();\n  reader.onload=e=>{ pubFileData=e.target.result; pubFileName=file.name; $('#pubFileName').textContent=file.name+' · verificando duplicação…'; Promise.resolve(pubFileHashPromise).finally(()=>{$('#pubFileName').textContent=file.name;}); };\n  reader.readAsDataURL(file);\n}\nasync function savePublicacao(){\n  const saveBtn=$('#savePubBtn');if(saveBtn?.disabled)return;\n  const resolved=resolveCurrentMember();\n  const autorId = pubReviewContext?.memberId || (isAdmin? $('#pubAutor').value : (resolved?.id||myId));\n  const categoria=$('#pubCategoriaSel').value;\n  const categoriaOutr
---
evolver correção':'Enviar nova versão'):'Salvar';\n    syncPubFinalidade();\n    openOverlay('pubOverlay');\n    setTimeout(()=>reviewContext?$('#pubFilePick')?.focus():$('#pubTitulo')?.focus(),50);\n  }catch(e){\n    console.error('Erro ao abrir o repositório',e);\n    toast('Não consegui abrir o formulário do repositório. Recarregue a página e tente novamente.');\n  }\n}\nfunction handlePubFile(file){\n  if(!file)return;\n  if(file.size > 15*1024*1024){ toast('Arquivo maior que 15 MB. Escolha um arquivo menor.'); return; }\n  pubFileHash='';pubFileHashPromise=sha256File(file).then(h=>pubFileHash=h).catch(()=>{pubFileHash='';return''});\n  const reader=new FileReader();\n  reader.onload=e=>{ pubFileData=e.target.result; pubFileName=file.name; $('#pubFileName').textContent=file.name+' · verificando duplicação…'; Promise.resolve(pubFileHashPromise).finally(()=>{$('#pubFileName').textContent=file.name;}); };\n  reader.readAsDataURL(file);\n}\nasync function savePublicacao(){\n  const saveBtn=$('#savePubBtn');if(saveBtn?.disabled)return;\n  const resolved=resolveCurrentMember();\n  const autorId = pubReviewContext?.memberId || (isAdmin? $('#pubAutor').value : (resolved?.id||myId));\n  const categoria=$('#pubCategoriaSel').value;\n  const categoriaOutro=$('#pubOutroTxt').value.trim();\n  const titulo=$('#pubTitulo').value.trim();\n  const finalidade=pubReviewContext?'correcao':$('#pubFinalidade').value;\n  const reviewNote=$('#pubReviewNote').value.trim();\n  if(!autorId){toast('Selecione o autor.');return}\n  if(!titulo){toast('Escreva o título.');return}\n  if(categoria==='outro' && !categoriaOutro){toast('Especifique a categoria.');return}\n  if(finalidade==='correcao'&&!pubReviewContext&&!$('#pubReviewer').value){toast('Selecione quem deve receber o arquivo para correção.');return}\n  if(finalidade==='correcao')pubTipo='arquivo';\n\n  let url='', tipoFinal=pubTipo,contentHash='';\n  try{\n    if(saveBtn)saveBtn.disabled=true;\n    if(pubTipo==='arquivo'){\n      if(!pubFileData){toast('Escolha o arquivo que será enviado.');return}\n      contentHash=pubFileHash||await Promise.resolve(pubFileHashPromise||'').catch(()=> '');\n      if(finalidade==='correcao'){\n        if(pubReviewContext){const dup=exactHashDuplicate(contentHash,{memberId:autorId,title:pubReviewContext.reviewBaseTitle||pubReviewContext.titulo||titulo,reviewThreadId:pubReviewContext.reviewThreadId||pubReviewContext.id});if(dup){toast(`Esse arquivo é idêntico à versão ${dup.reviewVersion||1}. Nenhuma duplicação foi criada.`);return}}\n        else{const reviewer=$('#pubReviewer').value,dup=exactHashDuplicate(contentHash,{memberId:autorId,title:titulo,reviewerId:reviewer});if(dup?.reviewFlow){toast('Esse mesmo arquivo já foi enviado para essa correção. Abra o fluxo existente em vez de enviar novamente.');return}}\n      }else{const dup=exactHashDuplicate(contentHash,{memberId:autorId,title:titulo});if(dup&&!dup.reviewFlow){toast('Esse arquivo já está no Repositório com o mesmo título. Não criei outra cópia.');return}}\n      toast('Enviando arquivo…');\n      const {url:u}=await fbUploadFile('pub_'+autorId,pubFileData,pubFileName);\n      url=u;\n    }else{\n      url=$('#pubUrl').value.trim();\n      if(!url){toast('Cole o link.');return}\n    }\n    const m=memberById(autorId),actor=currentActor(),uid=window.auth.currentUser?.uid||'';\n    if(finalidade==='correcao'){\n      const f=FB();let ref=f.doc(f.collection(window.db,'rede_publicacoes'));\n      let reviewThreadId=ref.id,reviewVersion=1,reviewReviewerId=$('#pubReviewer').value,reviewReturnToId=actor?.id||myId||'',reviewNextRecipientId=reviewReviewerId,reviewStatus='aguardando_correcao',baseTitle=titulo,parentId='';\n      if(pubReviewContext){\n        reviewThreadId=pubReviewContext.reviewThreadId||pubReviewContext.id;\n        reviewVersion=reviewNextVersion(pubReviewContext);\n        reviewReviewerId=pubReviewContext.reviewReviewerId;\n        reviewReturnToId=pubReviewContext.reviewReturnToId;\n        parentId=pubReviewContext.id;\n        const actingAsReviewer=(myId===reviewReviewerId);\n        reviewNextRecipientId=actingAsReviewer?reviewReturnToId:reviewReviewerId;\n        reviewStatus=actingAsReviewer?'correcao_devolvida':'nova_versao';\n        baseTitle=pubReviewContext.reviewBaseTitle||pubReviewContext.titulo||titulo;\n        ref=f.doc(window.db,'rede_publicacoes',reviewVersionDocId(reviewThreadId,reviewVersion));\n      }\n      await f.setDoc(ref,{memberId:autorId,ownerUid:uid,createdByUid:uid,memberNome:m?m.nome:'',categoria,categoriaOutro,titulo:baseTitle,tipo:'arquivo',url,fileName:pubFileName,contentHash,reviewFlow:true,reviewThreadId,reviewVersion,reviewStatus,reviewReviewerId,reviewReturnToId,reviewNextRecipientId,reviewParentId:p
---
ssSearch'))$('#repoFolderAccessSearch').oninput=()=>{const p=state.repositoryPackages.find(x=>x.id===activeRepoFolderAccessId);if(p)renderFolderAccessPeople(p)};\nif($('#repoFolderAccessSaveBtn'))$('#repoFolderAccessSaveBtn').onclick=saveRepoFolderAccess;\nif($('#repoPackageAddFilesInput'))$('#repoPackageAddFilesInput').onchange=e=>uploadAdditionalRepoFiles(e.target.files);\nif($('#repoPackageFilePick'))$('#repoPackageFilePick').onclick=()=>$('#repoPackageFiles').click();\nif($('#repoPackageFiles'))$('#repoPackageFiles').onchange=e=>handleRepoPackageFiles(e.target.files);\nif($('#repoPackagePurpose'))$('#repoPackagePurpose').onchange=e=>{$('#repoPackagePurposeOtherRow').style.display=e.target.value==='outro'?'block':'none'};\nif($('#repoPackageOwner'))$('#repoPackageOwner').onchange=()=>{if(document.querySelector('input[name="repoPackageAccess"]:checked')?.value==='selected')renderRepoPackagePeople()};\nif($('#repoPackagePeopleSearch'))$('#repoPackagePeopleSearch').oninput=renderRepoPackagePeople;\ndocument.querySelectorAll('input[name="repoPackageAccess"]').forEach(r=>r.onchange=updateRepoPackageAccessUI);\nif($('#repoPackageSaveBtn'))$('#repoPackageSaveBtn').onclick=saveRepoPackage;\n\n$('#pubCategoria').onchange=renderPubs; $('#pubPessoa').onchange=renderPubs; $('#pubMine').onchange=renderPubs;\n$('#pubCategoriaSel').onchange=e=>{$('#pubOutroRow').style.display=e.target.value==='outro'?'block':'none'};\n$$('input[name="pubTipo"]').forEach(r=>r.onchange=e=>{\n  pubTipo=e.target.value;\n  $('#pubUrl').style.display=pubTipo==='link'?'block':'none';\n  $('#pubFileArea').style.display=pubTipo==='arquivo'?'block':'none';\n});\n$('#pubFilePick').onclick=()=>$('#pubFile').click();\n$('#pubFile').onchange=e=>handlePubFile(e.target.files[0]);\n$('#savePubBtn').onclick=savePublicacao;\nif($('#pubFinalidade'))$('#pubFinalidade').onchange=syncPubFinalidade;\n$$('[data-close="pubOverlay"]').forEach(b=>b.addEventListener('click',()=>{pubReviewContext=null;$$('input[name="pubTipo"]').forEach(r=>r.disabled=false)}));\n$('#repoAddBtn').onclick=async()=>{const id=repoMemberId;if(!(isAdmin||isMe(id))){toast('Sem permissão para adicionar documentos para esta pessoa.');return}closeOverlay('repoOverlay');await openPubModal(id)};\n\nif($('#fileCollaboratorsSearch'))$('#fileCollaboratorsSearch').oninput=renderFileCollaborators;\nif($('#fileCollaboratorsSaveBtn'))$('#fileCollaboratorsSaveBtn').onclick=saveFileCollaborators;\nif($('#privateNewBtn'))$('#privateNewBtn').onclick=openPrivateNew;\nif($('#privateThreadSearch'))$('#privateThreadSearch').oninput=renderPrivateThreads;\nif($('#privatePersonSearch'))$('#privatePersonSearch').oninput=renderPrivatePeople;\nif($('#privateBackBtn'))$('#privateBackBtn').onclick=()=>{$('#privateShell')?.classList.remove('chat-open')};\nif($('#privateSendBtn'))$('#privateSendBtn').onclick=sendPrivateMessage;\nif($('#privateInput')){$('#privateInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendPrivateMessage()}});$('#privateInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(130,e.target.scrollHeight)+'px'})}\nif($('#privateEmojiToggle'))$('#privateEmojiToggle').onclick=()=>$('#privateEmojiBar')?.classList.toggle('show');\n$$('#privateEmojiBar button').forEach(b=>b.onclick=()=>{const i=$('#privateInput');if(!i)return;i.value+=(i.value?' ':'')+b.textContent;i.focus()});\nif($('#privateReplyCancel'))$('#privateReplyCancel').onclick=()=>{privateReplyTarget=null;renderPrivateReply()};\n$$('[data-close]').forEach(b=>b.onclick=()=>closeOverlay(b.dataset.close));\n$$('.overlay').forEach(o=>o.addEventListener('click',e=>{if(e.target===o)o.classList.remove('open')}));\nwindow.addEventListener('keydown',e=>{if(e.key==='Escape'){$$('.overlay.open').forEach(o=>o.classList.remove('open'));closeDetail();closeLightbox()}});\nwindow.addEventListener('resize',()=>{if(simulation){const[W,H]=graphSize();simulation.force('center')&&simulation.force('center').x(W/2).y(H/2);simulation.alpha(.3).restart()}});\n\n// expose for inline handlers\nwindow.selectNode=selectNode; window.closeDetail=closeDetail; window.openMember=openMember;\nwindow.filterLinha=filterLinha; window.recadoTo=recadoTo;\nwindow.toggleCronoItem=toggleCronoItem; window.deleteCronoItem=deleteCronoItem; window.unlinkAccess=unlinkAccess;\nwindow.deleteChatMsg=deleteChatMsg; window.deletePublicacao=deletePublicacao; window.openMuralAction=openMuralAction; window.openProjectProfile=openProjectProfile; window.editActivity=openActivity; window.toggleReaction=toggleReaction; window.toggleComments=toggleComments; window.addSocialComment=addSocialComment; window.deleteFeedPost=deleteFeedPost;\n\n\nfunction sparkleLogo(mode='wow'
---
olderAccessId);if(p)renderFolderAccessPeople(p)};\nif($('#repoFolderAccessSaveBtn'))$('#repoFolderAccessSaveBtn').onclick=saveRepoFolderAccess;\nif($('#repoPackageAddFilesInput'))$('#repoPackageAddFilesInput').onchange=e=>uploadAdditionalRepoFiles(e.target.files);\nif($('#repoPackageFilePick'))$('#repoPackageFilePick').onclick=()=>$('#repoPackageFiles').click();\nif($('#repoPackageFiles'))$('#repoPackageFiles').onchange=e=>handleRepoPackageFiles(e.target.files);\nif($('#repoPackagePurpose'))$('#repoPackagePurpose').onchange=e=>{$('#repoPackagePurposeOtherRow').style.display=e.target.value==='outro'?'block':'none'};\nif($('#repoPackageOwner'))$('#repoPackageOwner').onchange=()=>{if(document.querySelector('input[name="repoPackageAccess"]:checked')?.value==='selected')renderRepoPackagePeople()};\nif($('#repoPackagePeopleSearch'))$('#repoPackagePeopleSearch').oninput=renderRepoPackagePeople;\ndocument.querySelectorAll('input[name="repoPackageAccess"]').forEach(r=>r.onchange=updateRepoPackageAccessUI);\nif($('#repoPackageSaveBtn'))$('#repoPackageSaveBtn').onclick=saveRepoPackage;\n\n$('#pubCategoria').onchange=renderPubs; $('#pubPessoa').onchange=renderPubs; $('#pubMine').onchange=renderPubs;\n$('#pubCategoriaSel').onchange=e=>{$('#pubOutroRow').style.display=e.target.value==='outro'?'block':'none'};\n$$('input[name="pubTipo"]').forEach(r=>r.onchange=e=>{\n  pubTipo=e.target.value;\n  $('#pubUrl').style.display=pubTipo==='link'?'block':'none';\n  $('#pubFileArea').style.display=pubTipo==='arquivo'?'block':'none';\n});\n$('#pubFilePick').onclick=()=>$('#pubFile').click();\n$('#pubFile').onchange=e=>handlePubFile(e.target.files[0]);\n$('#savePubBtn').onclick=savePublicacao;\nif($('#pubFinalidade'))$('#pubFinalidade').onchange=syncPubFinalidade;\n$$('[data-close="pubOverlay"]').forEach(b=>b.addEventListener('click',()=>{pubReviewContext=null;$$('input[name="pubTipo"]').forEach(r=>r.disabled=false)}));\n$('#repoAddBtn').onclick=async()=>{const id=repoMemberId;if(!(isAdmin||isMe(id))){toast('Sem permissão para adicionar documentos para esta pessoa.');return}closeOverlay('repoOverlay');await openPubModal(id)};\n\nif($('#fileCollaboratorsSearch'))$('#fileCollaboratorsSearch').oninput=renderFileCollaborators;\nif($('#fileCollaboratorsSaveBtn'))$('#fileCollaboratorsSaveBtn').onclick=saveFileCollaborators;\nif($('#privateNewBtn'))$('#privateNewBtn').onclick=openPrivateNew;\nif($('#privateThreadSearch'))$('#privateThreadSearch').oninput=renderPrivateThreads;\nif($('#privatePersonSearch'))$('#privatePersonSearch').oninput=renderPrivatePeople;\nif($('#privateBackBtn'))$('#privateBackBtn').onclick=()=>{$('#privateShell')?.classList.remove('chat-open')};\nif($('#privateSendBtn'))$('#privateSendBtn').onclick=sendPrivateMessage;\nif($('#privateInput')){$('#privateInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendPrivateMessage()}});$('#privateInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(130,e.target.scrollHeight)+'px'})}\nif($('#privateEmojiToggle'))$('#privateEmojiToggle').onclick=()=>$('#privateEmojiBar')?.classList.toggle('show');\n$$('#privateEmojiBar button').forEach(b=>b.onclick=()=>{const i=$('#privateInput');if(!i)return;i.value+=(i.value?' ':'')+b.textContent;i.focus()});\nif($('#privateReplyCancel'))$('#privateReplyCancel').onclick=()=>{privateReplyTarget=null;renderPrivateReply()};\n$$('[data-close]').forEach(b=>b.onclick=()=>closeOverlay(b.dataset.close));\n$$('.overlay').forEach(o=>o.addEventListener('click',e=>{if(e.target===o)o.classList.remove('open')}));\nwindow.addEventListener('keydown',e=>{if(e.key==='Escape'){$$('.overlay.open').forEach(o=>o.classList.remove('open'));closeDetail();closeLightbox()}});\nwindow.addEventListener('resize',()=>{if(simulation){const[W,H]=graphSize();simulation.force('center')&&simulation.force('center').x(W/2).y(H/2);simulation.alpha(.3).restart()}});\n\n// expose for inline handlers\nwindow.selectNode=selectNode; window.closeDetail=closeDetail; window.openMember=openMember;\nwindow.filterLinha=filterLinha; window.recadoTo=recadoTo;\nwindow.toggleCronoItem=toggleCronoItem; window.deleteCronoItem=deleteCronoItem; window.unlinkAccess=unlinkAccess;\nwindow.deleteChatMsg=deleteChatMsg; window.deletePublicacao=deletePublicacao; window.openMuralAction=openMuralAction; window.openProjectProfile=openProjectProfile; window.editActivity=openActivity; window.toggleReaction=toggleReaction; window.toggleComments=toggleComments; window.addSocialComment=addSocialComment; window.deleteFeedPost=deleteFeedPost;\n\n\nfunction sparkleLogo(mode='wow'){\n  const marks=$$('.logo-mark');\n  marks.forEach(m=>{\n    m.classList.remove('is-awakening','is-wow');\n    voi
---
ap=new Map();\n  items.forEach(item=>{\n    let g;\n    if(mode==='az')g={key:'person:'+norm(item.person),label:item.person,sort:norm(item.person),sub:'Arquivo pessoal',letter:(item.person.trim()[0]||'#').toUpperCase()};\n    else if(mode==='date')g=dateFolder(item.stamp);\n    else g={key:'pending:'+item.pending.key,label:item.pending.label,sort:item.pending.key==='acao'?4:item.pending.key==='correcao'?3:item.pending.key==='aguardando'?2:1,sub:item.pending.key==='ok'?'Itens sem ação pendente':'Itens que merecem atenção'};\n    if(!map.has(g.key))map.set(g.key,{...g,items:[]});map.get(g.key).items.push(item);\n  });\n  let groups=[...map.values()];\n  if(mode==='az')groups.sort((a,b)=>String(a.sort).localeCompare(String(b.sort),'pt-BR'));\n  else groups.sort((a,b)=>Number(b.sort)-Number(a.sort));\n  return groups;\n}\nfunction visibleItems(){\n  const nq=norm(query);let items=collectItems();\n  if(nq)items=items.filter(x=>norm([x.title,x.person,x.category,x.pending.label].join(' ')).includes(nq));\n  return items;\n}\nfunction currentGroup(){return groupsFor(visibleItems()).find(g=>g.key===selectedFolder)||null}\nfunction sig(items){return JSON.stringify([mode,selectedFolder,selectedLetter,query,$('#pubCategoria')?.value||'',$('#pubPessoa')?.value||'',$('#pubMine')?.checked||false,items.map(x=>[x.id,x.title,x.person,x.category,x.stamp,x.pending.key])])}\nfunction folderCard(g){\n  const pending=g.items.filter(x=>x.pending.key!=='ok').length;\n  const last=Math.max(0,...g.items.map(x=>x.stamp||0));\n  const lastLabel=last?new Date(last).toLocaleDateString('pt-BR',{day:'2-digit',month:'short'}):'—';\n  return `<button type="button" class="p88-folder" data-p88-folder="${esc(g.key)}">\n    <span class="p88-folder-tab"></span><span class="p88-folder-icon">📁</span>\n    <span class="p88-folder-main"><b>${esc(g.label)}</b><small>${esc(g.sub||'')} · ${g.items.length} item${g.items.length===1?'':'s'}</small></span>\n    <span class="p88-folder-meta">${pending?`<em>${pending} pend.</em>`:'<em class="ok">em ordem</em>'}<small>${esc(lastLabel)}</small></span>\n  </button>`;\n}\nfunction itemCard(item){\n  const d=item.stamp?new Date(item.stamp).toLocaleDateString('pt-BR',{day:'2-digit',month:'short',year:'numeric'}):(item.dateText||'Sem data');\n  return `<button type="button" class="p88-file" data-p88-item="${esc(item.id)}">\n    <span class="p88-file-icon">${item.icon}</span>\n    <span class="p88-file-main"><b>${esc(item.title)}</b><small>${esc(item.category)} · ${esc(item.person)} · ${esc(d)}</small></span>\n    <span class="p88-status ${esc(item.pending.cls)}">${esc(item.pending.label)}</span><span class="p88-open">Abrir ›</span>\n  </button>`;\n}\nfunction alphaHtml(groups){\n  if(mode!=='az'||selectedFolder)return '';\n  const letters=[...new Set(groups.map(g=>g.letter).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR'));\n  return `<div class="p88-alpha"><button data-p88-letter="*" class="${selectedLetter==='*'?'on':''}">Todos</button>${letters.map(l=>`<button data-p88-letter="${esc(l)}" class="${selectedLetter===l?'on':''}">${esc(l)}</button>`).join('')}</div>`;\n}\nfunction render(force=false){\n  const root=$('#p88Archive');if(!root)return;\n  const items=visibleItems();const signature=sig(items);if(!force&&signature===lastSig)return;lastSig=signature;\n  const groups=groupsFor(items);const group=currentGroup();\n  const alpha=$('#p88Alpha',root),body=$('#p88Body',root),crumb=$('#p88Crumb',root);\n  alpha.innerHTML=alphaHtml(groups);\n  $$('.p88-mode',root).forEach(b=>b.classList.toggle('on',b.dataset.p88Mode===mode));\n  if(group){\n    crumb.innerHTML=`<button type="button" data-p88-back>← Voltar às pastas</button><div><b>${esc(group.label)}</b><small>${group.items.length} item${group.items.length===1?'':'s'}</small></div>`;\n    body.className='p88-files';body.innerHTML=group.items.sort((a,b)=>(b.stamp||0)-(a.stamp||0)).map(itemCard).join('')||'<div class="p88-empty">Nada nesta pasta.</div>';\n  }else{\n    crumb.innerHTML='<div><b>Folhear o arquivo</b><small>Escolha uma pasta para abrir</small></div>';\n    let show=groups;if(mode==='az'&&selectedLetter!=='*')show=groups.filter(g=>g.letter===selectedLetter);\n    body.className='p88-folders';body.innerHTML=show.map(folderCard).join('')||'<div class="p88-empty">Nenhum item encontrado com esses filtros.</div>';\n  }\n}\nfunction buildUI(){\n  const panel=$('#repoGeneralPanel');if(!panel||$('#p88Archive'))return;\n  panel.classList.add('p88-active');\n  const filters=$('.crono-filters',panel);\n  const root=document.createElement('section');root.id='p88Archive';root.innerHTML=`\n    <div class="p88-head"><div><span class="p88-kicker">🗂 ARQUIVO VIVO</span><h3>Folheie o Repositório</h3><p>A mesma produção da Rede, organiz
---
tton><button class="p88-mode" data-p88-mode="pending">Pendências</button></div>\n    <div id="p88Alpha"></div><div class="p88-crumb" id="p88Crumb"></div><div id="p88Body"></div>`;\n  if(filters)filters.insertAdjacentElement('afterend',root);else panel.prepend(root);\n  root.addEventListener('click',e=>{\n    const m=e.target.closest('[data-p88-mode]');if(m){mode=m.dataset.p88Mode;localStorage.setItem('carbonautas_repo_archive_mode',mode);selectedFolder='';selectedLetter='*';lastSig='';render(true);return}\n    const l=e.target.closest('[data-p88-letter]');if(l){selectedLetter=l.dataset.p88Letter;lastSig='';render(true);return}\n    if(e.target.closest('[data-p88-back]')){selectedFolder='';lastSig='';render(true);return}\n    const f=e.target.closest('[data-p88-folder]');if(f){selectedFolder=f.dataset.p88Folder;lastSig='';render(true);requestAnimationFrame(()=>root.scrollIntoView({block:'start',behavior:'smooth'}));return}\n    const b=e.target.closest('[data-p88-item]');if(b){const item=visibleItems().find(x=>x.id===b.dataset.p88Item);if(item)openFloat(item)}\n  });\n  $('#p88Search',root).addEventListener('input',e=>{query=e.target.value||'';selectedFolder='';lastSig='';render(true)});\n  ['pubCategoria','pubPessoa','pubMine'].forEach(id=>$('#'+id)?.addEventListener('change',()=>setTimeout(()=>{selectedFolder='';lastSig='';render(true)},80)));\n  ensureOverlay();render(true);\n}\nfunction ensureOverlay(){\n  if($('#p88Float'))return;\n  const ov=document.createElement('div');ov.id='p88Float';ov.hidden=true;ov.innerHTML=`<div class="p88-float-backdrop" data-p88-close></div><section class="p88-float-card"><header><button type="button" data-p88-close>← Voltar</button><div><span>ARQUIVO DO REPOSITÓRIO</span><b id="p88FloatTitle">Documento</b></div><button type="button" class="p88-x" data-p88-close>×</button></header><div id="p88FloatStage"></div></section>`;\n  document.body.appendChild(ov);ov.addEventListener('click',e=>{if(e.target.closest('[data-p88-close]'))closeFloat()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!ov.hidden)closeFloat()});\n}\nfunction openFloat(item){\n  ensureOverlay();if(openCtx)closeFloat();\n  const card=item.card;if(!card?.isConnected)return;\n  const placeholder=document.createComment('p88-return');const parent=card.parentNode;parent.insertBefore(placeholder,card);\n  openCtx={card,parent,placeholder,item};\n  $('#p88FloatTitle').textContent=item.title;$('#p88FloatStage').appendChild(card);card.classList.add('p88-live-card');\n  $('#p88Float').hidden=false;document.documentElement.classList.add('p88-modal-open');\n}\nfunction closeFloat(){\n  const ov=$('#p88Float');if(!ov||ov.hidden)return;\n  if(openCtx){const {card,parent,placeholder}=openCtx;card.classList.remove('p88-live-card');if(placeholder?.parentNode)placeholder.replaceWith(card);else if(parent?.isConnected)parent.appendChild(card);openCtx=null}\n  ov.hidden=true;document.documentElement.classList.remove('p88-modal-open');lastSig='';setTimeout(()=>render(true),30);\n}\nfunction injectCss(){if($('#p88Style'))return;const st=document.createElement('style');st.id='p88Style';st.textContent=`\n#repoGeneralPanel.p88-active>#pubList,#repoGeneralPanel.p88-active>#repoPackageList{display:none!important}\n#p88Archive{margin:14px 0 24px;border:1px solid #cfe0e5;border-radius:24px;background:linear-gradient(180deg,#fbfeff,#f3f8f9);box-shadow:0 18px 50px rgba(12,39,51,.08);overflow:hidden}\n.p88-head{display:flex;gap:18px;align-items:center;justify-content:space-between;padding:20px 22px 16px;background:linear-gradient(135deg,#faffff,#f2f7ff)}.p88-head h3{font-family:'Fraunces',serif;font-size:25px;margin:3px 0 2px}.p88-head p{margin:0;color:#6b7f87;font-size:12px}.p88-kicker{font-size:10px;font-weight:900;letter-spacing:.12em;color:#0e7681}.p88-search{width:min(420px,45%);display:flex;align-items:center;gap:9px;border:1px solid #bed3d9;border-radius:14px;background:#fff;padding:10px 13px;font-size:20px;color:#6a8189}.p88-search input{width:100%;border:0!important;box-shadow:none!important;outline:0;background:transparent;font-size:13px;color:#18343e}\n.p88-modes{display:flex;gap:8px;padding:12px 22px 0}.p88-mode{border:1px solid #ccdadf;background:#fff;color:#35515c;border-radius:999px;padding:9px 14px;font-weight:850;font-size:12px}.p88-mode.on{background:#0b7883;border-color:#0b7883;color:#fff;box-shadow:0 5px 18px rgba(11,120,131,.18)}\n.p88-alpha{display:flex;gap:5px;overflow-x:auto;padding:11px 22px 2px}.p88-alpha button{min-width:34px;height:32px;border:1px solid #d3e0e4;background:#fff;border-radius:10px;font-weight:800;color:#50666e}.p88-alpha button.on{background:#172f3a;color:#fff;border-color:#172f3a}.p88-crumb{display:flex;align-items:center;gap:
---