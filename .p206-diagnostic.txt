

===== openActivity =====

--- occurrence 1 at char 573182 ---
onst periods=acts.filter(a=>TRACK_PERIOD_TYPES.has(a.type));if(m.bolsista)periods.push({startDate:m.bolsaInicio,dueDate:m.bolsaFim});return periods.some(a=>{const pi=trackPeriodInfo(a.startDate,a.dueDate);return pi.days!==null&&pi.days>=0&&pi.days<=90})}return true})
  }
  const grid=$('#trackGrid');$('#trackEmpty').style.display=members.length?'none':'block';
  grid.innerHTML=members.map(m=>{
    const acts=all.filter(a=>a.ownerId===m.id).sort((a,b)=>_activityTime(b)-_activityTime(a));const can=isAdmin||isMe(m.id);
    const periods=acts.filter(a=>TRACK_PERIOD_TYPES.has(a.type));const follows=acts.filter(a=>!TRACK_PERIOD_TYPES.has(a.type)&&!TRACK_LEGACY_TYPES.has(a.type)).sort((a,b)=>{const ad=_activityDate(a),bd=_activityDate(b);return (bd||'').localeCompare(ad||'')||_activityTime(b)-_activityTime(a)});
    const pending=follows.filter(a=>_isPending(a)||_isOverdue(a));const lastOrient=follows.filter(a=>a.type==='orientacao').sort((a,b)=>(_activityDate(b)||'').localeCompare(_activityDate(a)||''))[0];const nextDue=follows.filter(a=>!_activityDone(a)&&a.dueDate&&!dateInfo(a.dueDate)?.over).sort((a,b)=>a.dueDate.localeCompare(b.dueDate))[0];const corrections=follows.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
    const nv=NIVEIS[m.nivel]||{short:m.nivel||'',color:'#607d8b'};
    const periodsHTML=_scholarPeriodHTML(m)+periods.map(a=>_periodHTML(a.type,a.title,a.startDate,a.dueDate,can,a.id,a.progress)).join('');
    const listHTML=follows.slice(0,8).map(a=>_followItemHTML(a,can)).join('');
    return `<section class="track-person"><div class="track-person-head"><div class="track-person-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="track-person-title">${esc(m.nome)}</div><div class="track-person-sub">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){

--- occurrence 2 at char 573699 ---
id);
    const periods=acts.filter(a=>TRACK_PERIOD_TYPES.has(a.type));const follows=acts.filter(a=>!TRACK_PERIOD_TYPES.has(a.type)&&!TRACK_LEGACY_TYPES.has(a.type)).sort((a,b)=>{const ad=_activityDate(a),bd=_activityDate(b);return (bd||'').localeCompare(ad||'')||_activityTime(b)-_activityTime(a)});
    const pending=follows.filter(a=>_isPending(a)||_isOverdue(a));const lastOrient=follows.filter(a=>a.type==='orientacao').sort((a,b)=>(_activityDate(b)||'').localeCompare(_activityDate(a)||''))[0];const nextDue=follows.filter(a=>!_activityDone(a)&&a.dueDate&&!dateInfo(a.dueDate)?.over).sort((a,b)=>a.dueDate.localeCompare(b.dueDate))[0];const corrections=follows.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
    const nv=NIVEIS[m.nivel]||{short:m.nivel||'',color:'#607d8b'};
    const periodsHTML=_scholarPeriodHTML(m)+periods.map(a=>_periodHTML(a.type,a.title,a.startDate,a.dueDate,can,a.id,a.progress)).join('');
    const listHTML=follows.slice(0,8).map(a=>_followItemHTML(a,can)).join('');
    return `<section class="track-person"><div class="track-person-head"><div class="track-person-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="track-person-title">${esc(m.nome)}</div><div class="track-person-sub">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openAct

--- occurrence 3 at char 573792 ---
er(a=>!TRACK_PERIOD_TYPES.has(a.type)&&!TRACK_LEGACY_TYPES.has(a.type)).sort((a,b)=>{const ad=_activityDate(a),bd=_activityDate(b);return (bd||'').localeCompare(ad||'')||_activityTime(b)-_activityTime(a)});
    const pending=follows.filter(a=>_isPending(a)||_isOverdue(a));const lastOrient=follows.filter(a=>a.type==='orientacao').sort((a,b)=>(_activityDate(b)||'').localeCompare(_activityDate(a)||''))[0];const nextDue=follows.filter(a=>!_activityDone(a)&&a.dueDate&&!dateInfo(a.dueDate)?.over).sort((a,b)=>a.dueDate.localeCompare(b.dueDate))[0];const corrections=follows.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
    const nv=NIVEIS[m.nivel]||{short:m.nivel||'',color:'#607d8b'};
    const periodsHTML=_scholarPeriodHTML(m)+periods.map(a=>_periodHTML(a.type,a.title,a.startDate,a.dueDate,can,a.id,a.progress)).join('');
    const listHTML=follows.slice(0,8).map(a=>_followItemHTML(a,can)).join('');
    return `<section class="track-person"><div class="track-person-head"><div class="track-person-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="track-person-title">${esc(m.nome)}</div><div class="track-person-sub">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openActivity(null,ownerPreset);setTimeout(()=>{if($('#activityType'))$('#activityType').value=type;i

--- occurrence 4 at char 575015 ---
m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openActivity(null,ownerPreset);setTimeout(()=>{if($('#activityType'))$('#activityType').value=type;if(type==='pendencia'){$('#activityStatus').value='afazer';$('#activityName').placeholder='Ex.: Enviar capítulo revisado';}if(type==='correcao'){$('#activityStatus').value='recebido';$('#activityName').placeholder='Ex.: Correção do capítulo 2';}},20)}
async function notifyAcompanhamentoOwner(ownerId,meta={}){if(!isAdmin||!ownerId||ownerId===myId)return;const me=currentActor(),f=FB();try{await f.addDoc(f.collection(window.db,'rede_notifications'),{recipientId:ownerId,senderId:me?.id||myId||'',senderUid:window.auth.currentUser?.uid||'',senderName:me?.name||'Coordenação',kind:meta.kind||'acompanhamento',title:meta.title||'Novo item de acompanhamento',message:(meta.message||'').slice(0,240),sourceType:'acompanhamento',sourceId:meta.sourceId||'',ownerId,read:false,ts:f.serverTimestamp()})}catch(e){console.warn('Não foi possível notificar o acompanhamento',e)}}
async function saveActivity(){
  if(activitySaving)return false;
  const ownerId=isAdmin?$('#activityOwnerSel').value:myId,type=$('#activityType').value;
  const title=$('#activityName').value.trim()||ACTIVITY_TYPES[type]||'Acompanhamento';
  if(!ownerId){toast('Selecione o Carbonauta.');return false}
  const startDate=$('#activityStart').value,dueDate=$

--- occurrence 5 at char 577125 ---
rrectionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openActivity(null,ownerPreset);setTimeout(()=>{if($('#activityType'))$('#activityType').value=type;if(type==='pendencia'){$('#activityStatus').value='afazer';$('#activityName').placeholder='Ex.: Enviar capítulo revisado';}if(type==='correcao'){$('#activityStatus').value='recebido';$('#activityName').placeholder='Ex.: Correção do capítulo 2';}},20)}
async function notifyAcompanhamentoOwner(ownerId,meta={}){if(!isAdmin||!ownerId||ownerId===myId)return;const me=currentActor(),f=FB();try{await f.addDoc(f.collection(window.db,'rede_notifications'),{recipientId:ownerId,senderId:me?.id||myId||'',senderUid:window.auth.currentUser?.uid||'',senderName:me?.name||'Coordenação',kind:meta.kind||'acompanhamento',title:meta.title||'Novo item de acompanhamento',message:(meta.message||'').slice(0,240),sourceType:'acompanhamento',sourceId:meta.sourceId||'',ownerId,read:false,ts:f.serverTimestamp()})}catch(e){console.warn('Não foi possível notificar o acompanhamento',e)}}
async function saveActivity(){
  if(activitySaving)return false;
  const ownerId=isAdmin?$('#activityOwnerSel').value:myId,type=$('#activityType').value;
  const title=$('#activityName').value.trim()||ACTIVITY_TYPES[type]||'Acompanhamento';
  if(!ownerId){toast('Selecione o Carbonauta.');return false}
  const startDate=$('#activityStart').value,dueDate=$('#activityDue').value;
  if(startDate&&dueDate&&startDate>dueDate){toast('A data final não pode ser anterior ao início.');return false}
  const m=memberById(ownerId),status=$('#activityStatus').value;
  if(!isAdmin&&['aprovado','reprovado'].includes(status)){toast('A aprovação fica com o orientador.');return false}
  const f=FB(),actorUid=window.auth.currentUser?.uid||'',wasNew=!editingActivityId;
  const ref=editingActivityId?f.doc(window.db,'rede_activities',editingActivityId):f.doc(f.collection(window.db,'rede_activities'));
  const data={ownerId,ownerName:m?.nome||'',type,status,title,description:$('#activityDesc').value.trim(),startDate,dueDate,
    progress:Math.max(0,Math.min(100,+$('#activityProgress').value||0)),updatedAt:f.serverTimestamp()};
  const button=$('#saveActivityBtn');activitySaving=true;button.disabled=true;button.textContent='Salvando…';
  try{
    await f.runTransaction(window.db,async tx=>{
      const snap=await tx.get(ref);
      if(!wasNew&&!snap.exists())throw new Error('Este registro foi excluído em outra tela. Reabra o acompanhamento.');
      if(snap.exists()){
        const previous=snap.data();
        if((previous.updatedAt?.toMillis?.()??null)!==activityLoadedRevision)throw new Error('Esse registro mudou em outra tela. Reabra antes de salvar para não perder nenhuma alteração.');
      }else{data.createdAt=f.serverTimestamp();data.ownerUid=(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?actorUid:'')}
      tx.set(ref,data,{merge:true});
    });
    activitySaving=false;closeOverlay('activityOverlay');toast('Acompanhamento salvo.');
    // Rewards/notifications must not turn a successful save into a false error.
    try{
      if(wasNew)await awardPoints(ownerId,'activity_created',ref.id,title);
      if(['concluido','aprovado','feito'].includes(status)||data.progress>=100)await awardPoints(ownerId,'activity_completed',ref.id,title);
      if(isAdmin&&ownerId!==myId)await notifyAcompanhamentoOwner(ownerId,{kind:type,title:'📌 Acompanhamento atualizado',message:`${title}${dueDate?' · prazo '+fmtDate(dueDate):''}`,source

--- occurrence 6 at char 577152 ---
)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openActivity(null,ownerPreset);setTimeout(()=>{if($('#activityType'))$('#activityType').value=type;if(type==='pendencia'){$('#activityStatus').value='afazer';$('#activityName').placeholder='Ex.: Enviar capítulo revisado';}if(type==='correcao'){$('#activityStatus').value='recebido';$('#activityName').placeholder='Ex.: Correção do capítulo 2';}},20)}
async function notifyAcompanhamentoOwner(ownerId,meta={}){if(!isAdmin||!ownerId||ownerId===myId)return;const me=currentActor(),f=FB();try{await f.addDoc(f.collection(window.db,'rede_notifications'),{recipientId:ownerId,senderId:me?.id||myId||'',senderUid:window.auth.currentUser?.uid||'',senderName:me?.name||'Coordenação',kind:meta.kind||'acompanhamento',title:meta.title||'Novo item de acompanhamento',message:(meta.message||'').slice(0,240),sourceType:'acompanhamento',sourceId:meta.sourceId||'',ownerId,read:false,ts:f.serverTimestamp()})}catch(e){console.warn('Não foi possível notificar o acompanhamento',e)}}
async function saveActivity(){
  if(activitySaving)return false;
  const ownerId=isAdmin?$('#activityOwnerSel').value:myId,type=$('#activityType').value;
  const title=$('#activityName').value.trim()||ACTIVITY_TYPES[type]||'Acompanhamento';
  if(!ownerId){toast('Selecione o Carbonauta.');return false}
  const startDate=$('#activityStart').value,dueDate=$('#activityDue').value;
  if(startDate&&dueDate&&startDate>dueDate){toast('A data final não pode ser anterior ao início.');return false}
  const m=memberById(ownerId),status=$('#activityStatus').value;
  if(!isAdmin&&['aprovado','reprovado'].includes(status)){toast('A aprovação fica com o orientador.');return false}
  const f=FB(),actorUid=window.auth.currentUser?.uid||'',wasNew=!editingActivityId;
  const ref=editingActivityId?f.doc(window.db,'rede_activities',editingActivityId):f.doc(f.collection(window.db,'rede_activities'));
  const data={ownerId,ownerName:m?.nome||'',type,status,title,description:$('#activityDesc').value.trim(),startDate,dueDate,
    progress:Math.max(0,Math.min(100,+$('#activityProgress').value||0)),updatedAt:f.serverTimestamp()};
  const button=$('#saveActivityBtn');activitySaving=true;button.disabled=true;button.textContent='Salvando…';
  try{
    await f.runTransaction(window.db,async tx=>{
      const snap=await tx.get(ref);
      if(!wasNew&&!snap.exists())throw new Error('Este registro foi excluído em outra tela. Reabra o acompanhamento.');
      if(snap.exists()){
        const previous=snap.data();
        if((previous.updatedAt?.toMillis?.()??null)!==activityLoadedRevision)throw new Error('Esse registro mudou em outra tela. Reabra antes de salvar para não perder nenhuma alteração.');
      }else{data.createdAt=f.serverTimestamp();data.ownerUid=(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?actorUid:'')}
      tx.set(ref,data,{merge:true});
    });
    activitySaving=false;closeOverlay('activityOverlay');toast('Acompanhamento salvo.');
    // Rewards/notifications must not turn a successful save into a false error.
    try{
      if(wasNew)await awardPoints(ownerId,'activity_created',ref.id,title);
      if(['concluido','aprovado','feito'].includes(status)||data.progress>=100)await awardPoints(ownerId,'activity_completed',ref.id,title);
      if(isAdmin&&ownerId!==myId)await notifyAcompanhamentoOwner(ownerId,{kind:type,title:'📌 Acompanhamento atualizado',message:`${title}${dueDate?' · prazo '+fmtDate(dueDate):''}`,sourceId:ref.id});
    }catch(e){

--- occurrence 7 at char 577192 ---
lock':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openActivity(null,ownerPreset);setTimeout(()=>{if($('#activityType'))$('#activityType').value=type;if(type==='pendencia'){$('#activityStatus').value='afazer';$('#activityName').placeholder='Ex.: Enviar capítulo revisado';}if(type==='correcao'){$('#activityStatus').value='recebido';$('#activityName').placeholder='Ex.: Correção do capítulo 2';}},20)}
async function notifyAcompanhamentoOwner(ownerId,meta={}){if(!isAdmin||!ownerId||ownerId===myId)return;const me=currentActor(),f=FB();try{await f.addDoc(f.collection(window.db,'rede_notifications'),{recipientId:ownerId,senderId:me?.id||myId||'',senderUid:window.auth.currentUser?.uid||'',senderName:me?.name||'Coordenação',kind:meta.kind||'acompanhamento',title:meta.title||'Novo item de acompanhamento',message:(meta.message||'').slice(0,240),sourceType:'acompanhamento',sourceId:meta.sourceId||'',ownerId,read:false,ts:f.serverTimestamp()})}catch(e){console.warn('Não foi possível notificar o acompanhamento',e)}}
async function saveActivity(){
  if(activitySaving)return false;
  const ownerId=isAdmin?$('#activityOwnerSel').value:myId,type=$('#activityType').value;
  const title=$('#activityName').value.trim()||ACTIVITY_TYPES[type]||'Acompanhamento';
  if(!ownerId){toast('Selecione o Carbonauta.');return false}
  const startDate=$('#activityStart').value,dueDate=$('#activityDue').value;
  if(startDate&&dueDate&&startDate>dueDate){toast('A data final não pode ser anterior ao início.');return false}
  const m=memberById(ownerId),status=$('#activityStatus').value;
  if(!isAdmin&&['aprovado','reprovado'].includes(status)){toast('A aprovação fica com o orientador.');return false}
  const f=FB(),actorUid=window.auth.currentUser?.uid||'',wasNew=!editingActivityId;
  const ref=editingActivityId?f.doc(window.db,'rede_activities',editingActivityId):f.doc(f.collection(window.db,'rede_activities'));
  const data={ownerId,ownerName:m?.nome||'',type,status,title,description:$('#activityDesc').value.trim(),startDate,dueDate,
    progress:Math.max(0,Math.min(100,+$('#activityProgress').value||0)),updatedAt:f.serverTimestamp()};
  const button=$('#saveActivityBtn');activitySaving=true;button.disabled=true;button.textContent='Salvando…';
  try{
    await f.runTransaction(window.db,async tx=>{
      const snap=await tx.get(ref);
      if(!wasNew&&!snap.exists())throw new Error('Este registro foi excluído em outra tela. Reabra o acompanhamento.');
      if(snap.exists()){
        const previous=snap.data();
        if((previous.updatedAt?.toMillis?.()??null)!==activityLoadedRevision)throw new Error('Esse registro mudou em outra tela. Reabra antes de salvar para não perder nenhuma alteração.');
      }else{data.createdAt=f.serverTimestamp();data.ownerUid=(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?actorUid:'')}
      tx.set(ref,data,{merge:true});
    });
    activitySaving=false;closeOverlay('activityOverlay');toast('Acompanhamento salvo.');
    // Rewards/notifications must not turn a successful save into a false error.
    try{
      if(wasNew)await awardPoints(ownerId,'activity_created',ref.id,title);
      if(['concluido','aprovado','feito'].includes(status)||data.progress>=100)await awardPoints(ownerId,'activity_completed',ref.id,title);
      if(isAdmin&&ownerId!==myId)await notifyAcompanhamentoOwner(ownerId,{kind:type,title:'📌 Acompanhamento atualizado',message:`${title}${dueDate?' · prazo '+fmtDate(dueDate):''}`,sourceId:ref.id});
    }catch(e){console.warn('Registro salvo; aviso/pont

--- occurrence 8 at char 686336 ---
Btn').onclick=pullGoogleToCarbonautas;
if($('#medalPrev'))$('#medalPrev').onclick=()=>shiftMedalMonth(-1);
if($('#medalNext'))$('#medalNext').onclick=()=>shiftMedalMonth(1);
if($('#medalToday'))$('#medalToday').onclick=()=>{medalCursor=new Date(new Date().getFullYear(),new Date().getMonth(),1);medalLoadedKey='';renderMedals()};
if($('#reloadGameBtn'))$('#reloadGameBtn').onclick=()=>{const f=$('#carbonautaGameFrame');if(f)f.src='./game.html?v=P28&t='+Date.now()};
window.addEventListener('message',e=>{const frame=$('#carbonautaGameFrame');if(!frame||e.source!==frame.contentWindow)return;if(location.origin!=='null'&&e.origin!==location.origin)return;const d=e.data||{};if(d.type==='carbonautas-game-result'&&d.completed===true)reportGameReward(d.score)});

$('#chatInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(120,e.target.scrollHeight)+'px'});
$('#chatInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat()}});
$('#sendBtn').onclick=sendChat;
if($('#mOnlyMine')) $('#mOnlyMine').onchange=renderRecados;
if($('#muralRecadoBtn')) $('#muralRecadoBtn').onclick=openRecado;
const muralCommandEl=$('#muralCommand'), muralCommandToggle=$('#muralCommandToggle');
function syncMuralCommandToggle(){ if(!muralCommandEl||!muralCommandToggle) return; muralCommandToggle.textContent=muralCommandEl.classList.contains('collapsed') ? 'Mostrar atalhos' : 'Ocultar atalhos'; }
if(muralCommandToggle){ muralCommandToggle.onclick=()=>{ muralCommandEl.classList.toggle('collapsed'); syncMuralCommandToggle(); }; syncMuralCommandToggle(); }
if($('#goMuralBtn')) $('#goMuralBtn').onclick=()=>switchView('mural');
if($('#muralNewBtn')) $('#muralNewBtn').onclick=()=>openPostModal('atualizacao');
if($('#muralDirectTopBtn')) $('#muralDirectTopBtn').onclick=openRecado;
if($('#muralDirectBtn')) $('#muralDirectBtn').onclick=openRecado;

$('#dashCheckinBtn').onclick=()=>openCheckin();
$('#dashProductBtn').onclick=()=>openProduct();
$('#dashMedalsBtn').onclick=()=>switchView('medalhas');
$('#dashGameBtn').onclick=()=>switchView('jogo');
$('#dashDossierBtn').onclick=()=>openDossier();
$('#dashNewTaskBtn').onclick=()=>openActivityPreset('pendencia');
$('#dashProductsAllBtn').onclick=()=>switchView('track');
$('#dashBackupBtn').onclick=exportGeneralBackup;
$('#trackCheckinBtn').onclick=()=>openCheckin();
$('#trackProductBtn').onclick=()=>openProduct();
$('#trackDossierBtn').onclick=()=>openDossier();
$('#checkinOwner').onchange=loadCheckinForSelection; $('#checkinMonth').onchange=loadCheckinForSelection; $('#saveCheckinBtn').onclick=saveCheckin;
$('#saveProductBtn').onclick=saveProduct;
$('#dossierPerson').onchange=renderDossier; $('#dossierExcelBtn').onclick=exportDossierExcel; $('#dossierCsvBtn').onclick=exportDossierCsv; $('#dossierWordBtn').onclick=exportDossierWord; $('#dossierPrintBtn').onclick=printDossier;
$('#addActivityBtn').onclick=()=>openActivity(null);
$('#trackOwner').onchange=renderTrack; $('#trackStatus').onchange=renderTrack; $('#trackMine').onchange=renderTrack;
$('#activityProgress').oninput=e=>$('#activityProgressLabel').textContent=e.target.value+'%'; $('#activityType').onchange=syncActivityTypeUi;
$('#saveActivityBtn').onclick=saveActivity; $('#deleteActivityBtn').onclick=deleteActivity;
$('#saveProjectProfileBtn').onclick=saveProjectProfile;
if($('#newPostBtn')) $('#newPostBtn').onclick=()=>openPostModal('atualizacao'); if($('#muralPostBtn')) $('#muralPostBtn').onclick=()=>openPostModal('atualizacao'); $$('[data-mural-kind]').forEach(b=>b.onclick=()=>openMuralAction(b.dataset.muralKind)); $$('[data-mf]').forEach(b=>b.onclick=()=>{muralFilter=b.dataset.mf;$$('[data-mf]').forEach(x=>x.classList.toggle('on',x===b));renderMural();if($('#muralMsgs'))$('#muralMsgs').scrollTop=0;}); $('#postFilePick').onclick=()=>$('#postFile').click(); $('#postFile').onchange=e=>handlePostFile(e.target.files[0]); $('#savePostBtn').onclick=saveFeedPost;

$('#addPhotoBtn').onclick=openPhotoModal;
$('#gPickBtn').onclick=()=>$('#gFile').click();
$('#gFile').onchange=e=>{handleGalleryFiles(e.target.files);e.target.value=''};
$('#gProjeto').onchange=e=>{$('#gProjetoOutroRow').style.display=e.target.value==='__outro'?'block':'none'};
const gPasteZone=$('#gPasteZone');
if(gPasteZone){
  gPasteZone.addEventListener('paste',e=>{const item=[...(e.clipboardData?.items||[])].find(x=>x.type?.startsWith('image/'));if(item){e.preventDefault();handleGalleryFile(item.getAsFile(),true)}});
  gPasteZone.addEventListener('dragover',e=>{e.preventDefault();gPasteZone.classList.add('drag')});
  gPasteZone.addEventListener('dragleave',()=>gPasteZone.classList.remove('drag'));
  gPasteZone.addEventListener('drop',e=>{e.preventDefault();gPasteZone.classList.remove('drag');handleGalleryFiles([...(e.dataTransfer?.files||[])],true)});
}
$('#savePhotoBtn').onclick=savePhoto;
['fProjeto','fPessoa','fAno','fTipo'].forEach(id=>{const el=$('#'+id);if(el)el.onchange=renderFotos});
if($('#fBusca'))$('#fBusca').oninput=()=>{clearTimeout(window.__fBuscaT);window.__fBuscaT=setTimeout(renderFotos,180)};
if($('#fLimpar'))$('#fLimpar').onclick=()=>{['fProjeto','fPessoa','fAno','fTipo','fBusca'].forEach(id=>{const el=$('#'+id);if(el)el.value=''});renderFotos()};
if($('#fotosSelectBtn'))$('#fotosSelectBtn').onclick=toggleFotoSelecting;
if($('#fMais'))$('#fMais').onclick=()=>{const w=$('#fMais').closest('.mural-filters');w.classList.toggle('open');$('#fMais').textContent=w.classList.contains('open')?'Menos':'Filtros'};
if($('#muralSelAll'))$('#muralSelAll').onclick=()=>{fotosFiltradas().forEach(p=>fotoSelected.add(p.id));renderFotos()};
if($('#muralSelNone'))$('#muralSelNone').onclick=()=>{fotoS


===== openProjectProfile =====

--- occurrence 1 at char 567148 ---
ate(start):'Início não informado'}</span><b>${pct}% do tempo</b><span>${end?'Fim '+fmtDate(end):'Fim não informado'}</span></div></div>`;
}
function _scholarPeriodHTML(m){
  if(!m.bolsista)return '';
  const pi=trackPeriodInfo(m.bolsaInicio,m.bolsaFim),title=m.bolsaModalidade||'Bolsa';
  return `<div class="period-card"><div class="period-top"><span class="period-type">🎓 Bolsa</span><span class="period-title">${esc(title)}</span><span class="period-remain">${esc(pi.remain)}</span></div><div class="period-bar"><span style="width:${pi.pct}%"></span></div><div class="period-foot"><span>${m.bolsaInicio?'Início '+fmtDate(m.bolsaInicio):'Início não informado'}</span><b>${pi.pct}% do tempo</b><span>${m.bolsaFim?'Fim '+fmtDate(m.bolsaFim):'Fim não informado'}</span></div></div>`;
}
function _followItemHTML(a,can){
  const over=_isOverdue(a), d=a.dueDate||a.startDate||'', status=ACTIVITY_STATUS[a.status]||a.status||'';
  return `<div class="follow-item"><div class="follow-icon">${TRACK_ICONS[a.type]||'•'}</div><div class="follow-main"><div class="follow-name">${esc(ACTIVITY_TYPES[a.type]||a.type||'Acompanhamento')} · ${esc(a.title||status)}</div>${a.description?`<div class="follow-desc">${esc(a.description)}</div>`:''}<div style="margin-top:4px"><span class="track-alert ${_activityDone(a)?'track-ok':over?'':'track-warn'}">${_activityDone(a)?esc(status||'Concluído'):over?'Atrasado':esc(status)}</span></div></div><div style="display:flex;gap:4px;align-items:start"><span class="follow-date ${over?'over':''}">${d?fmtDate(d):''}</span>${can?`<button class="follow-edit" onclick="editActivity('${a.id}')" title="Editar">✎</button>`:''}</div></div>`;
}
let projectProfileMemberId='';
function projectIdentityHTML(m,can){
  const q=(m.perguntaCientifica||'').trim(),obj=(m.objetivoProjeto||'').trim(),aud=(m.grupoAtendido||'').trim(),vis=(m.ideiaVisibilidade||'').trim(),oth=(m.projetoOutros||'').trim();
  const empty='<span class="project-empty">Ainda não preenchido.</span>';
  return `<section class="project-identity"><div class="project-identity-head"><div class="project-identity-title">🧭 Identidade do projeto</div>${can?`<button type="button" class="project-identity-edit" onclick="openProjectProfile('${m.id}')">✎ Editar projeto</button>`:''}</div><div class="science-question"><div class="sq-label">★ Pergunta científica</div><div class="sq-text">${q?esc(q):empty}</div></div><div class="project-identity-grid"><div class="project-identity-item"><div class="pi-label">Objetivo central</div><div class="pi-text">${obj?esc(obj):empty}</div></div><div class="project-identity-item"><div class="pi-label">Grupo / público atendido</div><div class="pi-text">${aud?esc(aud):empty}</div></div><div class="project-identity-item full"><div class="pi-label">Ideia de visibilidade</div><div class="pi-text">${vis?esc(vis):empty}</div></div>${oth?`<div class="project-identity-item full"><div class="pi-label">Outros aspectos</div><div class="pi-text">${esc(oth)}</div></div>`:''}</div></section>`;
}
function openProjectProfile(id){
  const m=memberById(id);if(!m)return;if(!(isAdmin||isMe(id))){toast('Somente o próprio Carbonauta ou o coordenador pode editar este projeto.');return}
  projectProfileMemberId=id;$('#projectQuestion').value=m.perguntaCientifica||'';$('#projectObjective').value=m.objetivoProjeto||'';$('#projectAudience').value=m.grupoAtendido||'';$('#projectVisibility').value=m.ideiaVisibilidade||'';$('#projectOther').value=m.projetoOutros||'';openOverlay('projectProfileOverlay');
}
async function saveProjectProfile(){
  const id=projectProfileMemberId,m=memberById(id);if(!id||!m||!(isAdmin||isMe(id))){toast('Sem permissão para editar este projeto.');return}
  const data={perguntaCientifica:$('#projectQuestion').value.trim(),objetivoProjeto:$('#projectObjective').value.trim(),grupoAtendido:$('#projectAudience').value.trim(),ideiaVisibilidade:$('#projectVisibility').value.trim(),projetoOutros:$('#projectOther').value.trim(),projectProfileUpdatedAt:FB().serverTimestamp()};
  try{await fbSetMember(id,data);closeOverlay('projectProfileOverlay');toast('Identidade do projeto salva.');}catch(e){console.error(e);toast(e?.code==='permission-denied'?'O Firestore bloqueou a edição do projeto.':'Erro ao salvar a identidade do projeto.')}
}

function renderTrack(){
  refreshTrackOwners();
  let members=[...state.members].sort((a,b)=>(a.status==='egresso')-(b.status==='egresso')||a.nome.localeCompare(b.nome));
  const owner=$('#trackOwner').value,mode=$('#trackStatus').value;
  if(owner)members=members.filter(m=>m.id===owner);
  if($('#trackMine').checked){if(!myId){$('#trackMine').checked=false;toast('Seu perfil precisa estar vinculado ao login.')}else members=members.filter(m=>m.id===myId)}
  const now=new Date();now.setHours(0,0,0,0);
  const all=state.activities;
  const pendingCorrections=all.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
  const overdue=all.filter(_isOverdue).length;
  const next30=all.filter(a=>!_activityDone(a)&&a.dueDate).filter(a=>{const di=dateInfo(a.dueDate);return di&&di.diff>=0&&di.diff<=30}).length;
  const activeScholar=state.members.filter(m=>{if(!m.bolsista)return false;const pi=trackPeriodInfo(m.bolsaInicio,m.bolsaFim);return pi.days!==null&&pi.days>=0}).length;
  $('#trackKpis').innerHTML=`<div class="track-kpi"><div class="n">${activeScholar}</div><div class="l">bolsas em acompanhamento</div></div><div class="track-kpi"><div class="n">${next30}</div><div class="l">prazos em até 30 dias</div></div><div class="track-kpi"><div class="n">${pendingCorrections}</div><div class="l">correções pendentes</div></div><div class="track-kpi"><div class="n">${overdue}</div><div class="l">itens atrasados</div>

--- occurrence 2 at char 567965 ---

  const over=_isOverdue(a), d=a.dueDate||a.startDate||'', status=ACTIVITY_STATUS[a.status]||a.status||'';
  return `<div class="follow-item"><div class="follow-icon">${TRACK_ICONS[a.type]||'•'}</div><div class="follow-main"><div class="follow-name">${esc(ACTIVITY_TYPES[a.type]||a.type||'Acompanhamento')} · ${esc(a.title||status)}</div>${a.description?`<div class="follow-desc">${esc(a.description)}</div>`:''}<div style="margin-top:4px"><span class="track-alert ${_activityDone(a)?'track-ok':over?'':'track-warn'}">${_activityDone(a)?esc(status||'Concluído'):over?'Atrasado':esc(status)}</span></div></div><div style="display:flex;gap:4px;align-items:start"><span class="follow-date ${over?'over':''}">${d?fmtDate(d):''}</span>${can?`<button class="follow-edit" onclick="editActivity('${a.id}')" title="Editar">✎</button>`:''}</div></div>`;
}
let projectProfileMemberId='';
function projectIdentityHTML(m,can){
  const q=(m.perguntaCientifica||'').trim(),obj=(m.objetivoProjeto||'').trim(),aud=(m.grupoAtendido||'').trim(),vis=(m.ideiaVisibilidade||'').trim(),oth=(m.projetoOutros||'').trim();
  const empty='<span class="project-empty">Ainda não preenchido.</span>';
  return `<section class="project-identity"><div class="project-identity-head"><div class="project-identity-title">🧭 Identidade do projeto</div>${can?`<button type="button" class="project-identity-edit" onclick="openProjectProfile('${m.id}')">✎ Editar projeto</button>`:''}</div><div class="science-question"><div class="sq-label">★ Pergunta científica</div><div class="sq-text">${q?esc(q):empty}</div></div><div class="project-identity-grid"><div class="project-identity-item"><div class="pi-label">Objetivo central</div><div class="pi-text">${obj?esc(obj):empty}</div></div><div class="project-identity-item"><div class="pi-label">Grupo / público atendido</div><div class="pi-text">${aud?esc(aud):empty}</div></div><div class="project-identity-item full"><div class="pi-label">Ideia de visibilidade</div><div class="pi-text">${vis?esc(vis):empty}</div></div>${oth?`<div class="project-identity-item full"><div class="pi-label">Outros aspectos</div><div class="pi-text">${esc(oth)}</div></div>`:''}</div></section>`;
}
function openProjectProfile(id){
  const m=memberById(id);if(!m)return;if(!(isAdmin||isMe(id))){toast('Somente o próprio Carbonauta ou o coordenador pode editar este projeto.');return}
  projectProfileMemberId=id;$('#projectQuestion').value=m.perguntaCientifica||'';$('#projectObjective').value=m.objetivoProjeto||'';$('#projectAudience').value=m.grupoAtendido||'';$('#projectVisibility').value=m.ideiaVisibilidade||'';$('#projectOther').value=m.projetoOutros||'';openOverlay('projectProfileOverlay');
}
async function saveProjectProfile(){
  const id=projectProfileMemberId,m=memberById(id);if(!id||!m||!(isAdmin||isMe(id))){toast('Sem permissão para editar este projeto.');return}
  const data={perguntaCientifica:$('#projectQuestion').value.trim(),objetivoProjeto:$('#projectObjective').value.trim(),grupoAtendido:$('#projectAudience').value.trim(),ideiaVisibilidade:$('#projectVisibility').value.trim(),projetoOutros:$('#projectOther').value.trim(),projectProfileUpdatedAt:FB().serverTimestamp()};
  try{await fbSetMember(id,data);closeOverlay('projectProfileOverlay');toast('Identidade do projeto salva.');}catch(e){console.error(e);toast(e?.code==='permission-denied'?'O Firestore bloqueou a edição do projeto.':'Erro ao salvar a identidade do projeto.')}
}

function renderTrack(){
  refreshTrackOwners();
  let members=[...state.members].sort((a,b)=>(a.status==='egresso')-(b.status==='egresso')||a.nome.localeCompare(b.nome));
  const owner=$('#trackOwner').value,mode=$('#trackStatus').value;
  if(owner)members=members.filter(m=>m.id===owner);
  if($('#trackMine').checked){if(!myId){$('#trackMine').checked=false;toast('Seu perfil precisa estar vinculado ao login.')}else members=members.filter(m=>m.id===myId)}
  const now=new Date();now.setHours(0,0,0,0);
  const all=state.activities;
  const pendingCorrections=all.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
  const overdue=all.filter(_isOverdue).length;
  const next30=all.filter(a=>!_activityDone(a)&&a.dueDate).filter(a=>{const di=dateInfo(a.dueDate);return di&&di.diff>=0&&di.diff<=30}).length;
  const activeScholar=state.members.filter(m=>{if(!m.bolsista)return false;const pi=trackPeriodInfo(m.bolsaInicio,m.bolsaFim);return pi.days!==null&&pi.days>=0}).length;
  $('#trackKpis').innerHTML=`<div class="track-kpi"><div class="n">${activeScholar}</div><div class="l">bolsas em acompanhamento</div></div><div class="track-kpi"><div class="n">${next30}</div><div class="l">prazos em até 30 dias</div></div><div class="track-kpi"><div class="n">${pendingCorrections}</div><div class="l">correções pendentes</div></div><div class="track-kpi"><div class="n">${overdue}</div><div class="l">itens atrasados</div></div>`;
  if(mode){
    members=members.filter(m=>{const acts=all.filter(a=>a.ownerId===m.id);if(mode==='pendentes')return acts.some(a=>_isPending(a)||_isOverdue(a));if(mode==='proximos')return acts.some(a=>{const di=a.dueDate?dateInfo(a.dueDate):null;return !_activityDone(a)&&di&&di.diff>=0&&di.diff<=30});if(mode==='encerrando'){const periods=acts.filter(a=>TRACK_PERIOD_TYPES.has(a.type));if(m.bolsista)periods.push({startDate:m.bolsaInicio,dueDate:m.bolsaFim});return periods.some(a=>{const pi=trackPeriodInfo(a.startDate,a.dueDate);return pi.days!==null&&pi.days>=0&&pi.days<=90})}return true})
  }
  const grid=$('#trackGrid');$('#trackEmpty').style.display=members.length?'none':'block';
  grid.innerHTML=members.map(m=>{
    const acts=all.filter(a=>a.ownerId===m.id).sort((a,b)=>_activityTime(b)-_activityT

--- occurrence 3 at char 696566 ---
arch'))$('#privatePersonSearch').oninput=renderPrivatePeople;
if($('#privateBackBtn'))$('#privateBackBtn').onclick=e=>{e.preventDefault();e.stopPropagation();closePrivateThread()};
if($('#privateSendBtn'))$('#privateSendBtn').onclick=sendPrivateMessage;
if($('#privateInput')){$('#privateInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendPrivateMessage()}});$('#privateInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(130,e.target.scrollHeight)+'px'})}
if($('#privateEmojiToggle'))$('#privateEmojiToggle').onclick=()=>$('#privateEmojiBar')?.classList.toggle('show');
$$('#privateEmojiBar button').forEach(b=>b.onclick=()=>{const i=$('#privateInput');if(!i)return;i.value+=(i.value?' ':'')+b.textContent;i.focus()});
if($('#privateReplyCancel'))$('#privateReplyCancel').onclick=()=>{privateReplyTarget=null;renderPrivateReply()};
$$('[data-close]').forEach(b=>b.onclick=()=>closeOverlay(b.dataset.close));
$$('.overlay').forEach(o=>o.addEventListener('click',e=>{if(e.target===o)closeOverlay(o.id)}));
window.addEventListener('keydown',e=>{if(e.key==='Escape'){$$('.overlay.open').forEach(o=>closeOverlay(o.id));closeDetail();closeLightbox()}});
window.addEventListener('resize',()=>{if(simulation){const[W,H]=graphSize();simulation.force('center')&&simulation.force('center').x(W/2).y(H/2);simulation.alpha(.3).restart()}});

// expose for inline handlers
window.selectNode=selectNode; window.closeDetail=closeDetail; window.openMember=openMember;
/* P203H1: pontes públicas usadas pelos patches consolidados fora do módulo. */
window.switchView=switchView; window.renderSubNav=renderSubNav; window.renderMensagens=renderMensagens; window.renderPrivateThreads=renderPrivateThreads; window.openPrivateThread=openPrivateThread; window.openPrivateNew=openPrivateNew; window.closePrivateThread=closePrivateThread;
window.filterLinha=filterLinha; window.recadoTo=recadoTo;
window.toggleCronoItem=toggleCronoItem; window.deleteCronoItem=deleteCronoItem; window.unlinkAccess=unlinkAccess;
window.deleteChatMsg=deleteChatMsg; window.deletePublicacao=deletePublicacao; window.openMuralAction=openMuralAction; window.openProjectProfile=openProjectProfile; window.editActivity=openActivity; window.toggleReaction=toggleReaction; window.toggleComments=toggleComments; window.addSocialComment=addSocialComment; window.deleteFeedPost=deleteFeedPost;


function sparkleLogo(mode='wow'){
  const marks=$$('.logo-mark');
  marks.forEach(m=>{
    m.classList.remove('is-awakening','is-wow');
    void m.offsetWidth;
    m.classList.add(mode==='wake'?'is-awakening':'is-wow');
    setTimeout(()=>m.classList.remove('is-awakening','is-wow'),1700);
  });
}
setTimeout(()=>sparkleLogo('wake'),650);
setInterval(()=>{if(document.visibilityState==='visible') sparkleLogo('wow')},28000);
document.addEventListener('click',e=>{
  if(e.target.closest('.tab,.btn.primary,#sendBtn,#muralRecadoBtn,#addPubBtn,#addPhotoBtn,#newEventBtn,#myPrazoBtn,#privatePrazoBtn,#addActivityBtn')){
    sparkleLogo('wow');
  }
});
window.sparkleLogo=sparkleLogo;

let deferredInstallPrompt=null;
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredInstallPrompt=e;});
const installPwaBtn=$('#installPwaBtn');
if(installPwaBtn){
  const standalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true;
  if(standalone) installPwaBtn.style.display='none';
  installPwaBtn.onclick=async()=>{
    if(deferredInstallPrompt){deferredInstallPrompt.prompt();try{await deferredInstallPrompt.userChoice}catch(_e){}deferredInstallPrompt=null;return}
    if(/iphone|ipad|ipod/i.test(navigator.userAgent)) alert('No iPhone/iPad: toque em Compartilhar e escolha “Adicionar à Tela de Início”.');
    else alert('No celular ou PC: abra o menu do navegador e escolha “Instalar app” ou “Adicionar à tela inicial”.');
  };
  window.addEventListener('appinstalled',()=>{installPwaBtn.style.display='none'});
}
window.CARBONAUTAS_BUILD='P205-20260921';
if('serviceWorker' in navigator){
  window.addEventListener('load',async()=>{
    try{
      const reg=await navigator.serviceWorker.register('./sw.js?v=P205-20260921',{updateViaCache:'none'});
      await reg.update();
    }catch(e){console.warn('SW update',e)}
  });
}
refreshIdentityUI();
waitFirebase().then(startAuth);
</script>

<script id="carbonautas-dynamic-favicon">
(()=>{
  const link=document.getElementById('dynamicFavicon'); if(!link)return;
  const img=new Image(); img.src='./icon-192.png?v=P28';
  img.onload=()=>{
    const c=document.createElement('canvas'),ctx=c.getContext('2d'); c.width=c.height=64; let phase=0,last=0;
    const draw=(ts)=>{
      const cadence=document.hidden?1800:420; if(ts-last<cadence){requestAnimationFrame(draw);return} last=ts; phase=(phase+1)%240;
      const a=Math.sin(phase/18)*0.055,scale=0.88+0.05*(1+Math.sin(phase/14))/2;
      ctx.clearRect(0,0,64,64);ctx.save();ctx.translate(32,32);ctx.rotate(a);ctx.scale(scale,scale);ctx.shadowColor='rgba(40,225,255,.85)';ctx.shadowBlur=5+3*(1+Math.sin(phase/11))/2;ctx.drawImage(img,-32,-32,64,64);ctx.restore();
      try{link.href=c.toDataURL('image/png')}catch{} requestAnimationFrame(draw);
    }; requestAnimationFrame(draw);
  };
})();
</script>

<!-- ===== Carbonautas P128: camadas P44 a P129 consolidadas neste arquivo (antes injetadas pelo sw.js) ===== -->
<style data-src="agenda-soft-p108.css">
/* Carbonautas P108 · Agenda leve, clara e sem alterar a lógica */
#viewCrono .crono-scroll{
  background:
    radial-gradient(700px 320px at 12% 0%, rgba(135,214,198,.13), transparent 60%),
    radial-gradient(620px 300px at 92% 10%, rgba(160,183,242,

--- occurrence 4 at char 696585 ---
ersonSearch').oninput=renderPrivatePeople;
if($('#privateBackBtn'))$('#privateBackBtn').onclick=e=>{e.preventDefault();e.stopPropagation();closePrivateThread()};
if($('#privateSendBtn'))$('#privateSendBtn').onclick=sendPrivateMessage;
if($('#privateInput')){$('#privateInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendPrivateMessage()}});$('#privateInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(130,e.target.scrollHeight)+'px'})}
if($('#privateEmojiToggle'))$('#privateEmojiToggle').onclick=()=>$('#privateEmojiBar')?.classList.toggle('show');
$$('#privateEmojiBar button').forEach(b=>b.onclick=()=>{const i=$('#privateInput');if(!i)return;i.value+=(i.value?' ':'')+b.textContent;i.focus()});
if($('#privateReplyCancel'))$('#privateReplyCancel').onclick=()=>{privateReplyTarget=null;renderPrivateReply()};
$$('[data-close]').forEach(b=>b.onclick=()=>closeOverlay(b.dataset.close));
$$('.overlay').forEach(o=>o.addEventListener('click',e=>{if(e.target===o)closeOverlay(o.id)}));
window.addEventListener('keydown',e=>{if(e.key==='Escape'){$$('.overlay.open').forEach(o=>closeOverlay(o.id));closeDetail();closeLightbox()}});
window.addEventListener('resize',()=>{if(simulation){const[W,H]=graphSize();simulation.force('center')&&simulation.force('center').x(W/2).y(H/2);simulation.alpha(.3).restart()}});

// expose for inline handlers
window.selectNode=selectNode; window.closeDetail=closeDetail; window.openMember=openMember;
/* P203H1: pontes públicas usadas pelos patches consolidados fora do módulo. */
window.switchView=switchView; window.renderSubNav=renderSubNav; window.renderMensagens=renderMensagens; window.renderPrivateThreads=renderPrivateThreads; window.openPrivateThread=openPrivateThread; window.openPrivateNew=openPrivateNew; window.closePrivateThread=closePrivateThread;
window.filterLinha=filterLinha; window.recadoTo=recadoTo;
window.toggleCronoItem=toggleCronoItem; window.deleteCronoItem=deleteCronoItem; window.unlinkAccess=unlinkAccess;
window.deleteChatMsg=deleteChatMsg; window.deletePublicacao=deletePublicacao; window.openMuralAction=openMuralAction; window.openProjectProfile=openProjectProfile; window.editActivity=openActivity; window.toggleReaction=toggleReaction; window.toggleComments=toggleComments; window.addSocialComment=addSocialComment; window.deleteFeedPost=deleteFeedPost;


function sparkleLogo(mode='wow'){
  const marks=$$('.logo-mark');
  marks.forEach(m=>{
    m.classList.remove('is-awakening','is-wow');
    void m.offsetWidth;
    m.classList.add(mode==='wake'?'is-awakening':'is-wow');
    setTimeout(()=>m.classList.remove('is-awakening','is-wow'),1700);
  });
}
setTimeout(()=>sparkleLogo('wake'),650);
setInterval(()=>{if(document.visibilityState==='visible') sparkleLogo('wow')},28000);
document.addEventListener('click',e=>{
  if(e.target.closest('.tab,.btn.primary,#sendBtn,#muralRecadoBtn,#addPubBtn,#addPhotoBtn,#newEventBtn,#myPrazoBtn,#privatePrazoBtn,#addActivityBtn')){
    sparkleLogo('wow');
  }
});
window.sparkleLogo=sparkleLogo;

let deferredInstallPrompt=null;
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredInstallPrompt=e;});
const installPwaBtn=$('#installPwaBtn');
if(installPwaBtn){
  const standalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true;
  if(standalone) installPwaBtn.style.display='none';
  installPwaBtn.onclick=async()=>{
    if(deferredInstallPrompt){deferredInstallPrompt.prompt();try{await deferredInstallPrompt.userChoice}catch(_e){}deferredInstallPrompt=null;return}
    if(/iphone|ipad|ipod/i.test(navigator.userAgent)) alert('No iPhone/iPad: toque em Compartilhar e escolha “Adicionar à Tela de Início”.');
    else alert('No celular ou PC: abra o menu do navegador e escolha “Instalar app” ou “Adicionar à tela inicial”.');
  };
  window.addEventListener('appinstalled',()=>{installPwaBtn.style.display='none'});
}
window.CARBONAUTAS_BUILD='P205-20260921';
if('serviceWorker' in navigator){
  window.addEventListener('load',async()=>{
    try{
      const reg=await navigator.serviceWorker.register('./sw.js?v=P205-20260921',{updateViaCache:'none'});
      await reg.update();
    }catch(e){console.warn('SW update',e)}
  });
}
refreshIdentityUI();
waitFirebase().then(startAuth);
</script>

<script id="carbonautas-dynamic-favicon">
(()=>{
  const link=document.getElementById('dynamicFavicon'); if(!link)return;
  const img=new Image(); img.src='./icon-192.png?v=P28';
  img.onload=()=>{
    const c=document.createElement('canvas'),ctx=c.getContext('2d'); c.width=c.height=64; let phase=0,last=0;
    const draw=(ts)=>{
      const cadence=document.hidden?1800:420; if(ts-last<cadence){requestAnimationFrame(draw);return} last=ts; phase=(phase+1)%240;
      const a=Math.sin(phase/18)*0.055,scale=0.88+0.05*(1+Math.sin(phase/14))/2;
      ctx.clearRect(0,0,64,64);ctx.save();ctx.translate(32,32);ctx.rotate(a);ctx.scale(scale,scale);ctx.shadowColor='rgba(40,225,255,.85)';ctx.shadowBlur=5+3*(1+Math.sin(phase/11))/2;ctx.drawImage(img,-32,-32,64,64);ctx.restore();
      try{link.href=c.toDataURL('image/png')}catch{} requestAnimationFrame(draw);
    }; requestAnimationFrame(draw);
  };
})();
</script>

<!-- ===== Carbonautas P128: camadas P44 a P129 consolidadas neste arquivo (antes injetadas pelo sw.js) ===== -->
<style data-src="agenda-soft-p108.css">
/* Carbonautas P108 · Agenda leve, clara e sem alterar a lógica */
#viewCrono .crono-scroll{
  background:
    radial-gradient(700px 320px at 12% 0%, rgba(135,214,198,.13), transparent 60%),
    radial-gradient(620px 300px at 92% 10%, rgba(160,183,242,.10), transparent 6

--- occurrence 5 at char 1306636 ---
n</button><button type="button" data-p97-action="product" data-mid="${esc97(m.id)}">🎯 Produto</button><button type="button" data-p97-action="preset" data-kind="pendencia" data-mid="${esc97(m.id)}">⚠️ Pendência</button><button type="button" data-p97-action="preset" data-kind="correcao" data-mid="${esc97(m.id)}">✍️ Correção</button></div>`:''}`};
}
function buildDeckCards(m){const can=memberCanEdit(m.id);return [overviewCard(m,can),academicCard(m,can),projectCard(m,can),timelineCard(m,can),historyCard(m,can),pendingCard(m,can)]}

function ensureDeck(){
  let ov=$('#p97Deck');if(ov)return ov;
  ov=document.createElement('div');ov.id='p97Deck';ov.hidden=true;ov.innerHTML=`
    <div class="p97-backdrop" data-p97-close></div>
    <section class="p97-table" role="dialog" aria-modal="true" aria-label="Acompanhamento em cartas">
      <header class="p97-deck-head"><button type="button" data-p97-close>← Pastas</button><div class="p97-deck-title"><span id="p97DeckKicker">ACOMPANHAMENTO</span><b id="p97DeckName">Carbonauta</b></div><button type="button" class="x" data-p97-close>×</button></header>
      <div class="p97-stage-wrap"><div class="p97-stage" id="p97Stage"></div></div>
      <footer class="p97-deck-nav"><button type="button" class="prev">← Anterior</button><div class="p97-deck-count"><small id="p97CardTitle">CARTA</small><b id="p97CardCount">1 de 6</b></div><button type="button" class="next">Próxima →</button></footer>
    </section>`;
  document.body.appendChild(ov);
  ov.addEventListener('click',e=>{
    if(e.target.closest('[data-p97-close]')){closeDeck();return}
    const a=e.target.closest('[data-p97-action]');if(!a)return;
    e.preventDefault();e.stopPropagation();
    const mid=a.dataset.mid||deckMemberId,action=a.dataset.p97Action;
    if(action==='activity')call('openActivity',null,mid);
    else if(action==='dossier')call('openDossier',mid);
    else if(action==='repo')call('openMemberRepository',mid);
    else if(action==='academic-edit')call('openAcademicCardEditor',mid);
    else if(action==='academic-download')call('downloadAcademicCard',mid);
    else if(action==='academic-share')call('shareAcademicCard',mid);
    else if(action==='project')call('openProjectProfile',mid);
    else if(action==='checkin')call('openCheckin',mid);
    else if(action==='product')call('openProduct',mid);
    else if(action==='preset')call('openActivityPreset',a.dataset.kind||'pendencia',mid);
    else if(action==='edit')call('editActivity',a.dataset.id);
  });
  $('.p97-deck-nav .prev',ov).addEventListener('click',()=>navigateDeck(-1));
  $('.p97-deck-nav .next',ov).addEventListener('click',()=>navigateDeck(1));
  const stage=$('#p97Stage',ov);
  stage.addEventListener('touchstart',e=>{if(e.touches.length!==1)return;deckTouch={x:e.touches[0].clientX,y:e.touches[0].clientY,t:Date.now()}},{passive:true});
  stage.addEventListener('touchend',e=>{if(!deckTouch)return;const p=e.changedTouches[0],dx=p.clientX-deckTouch.x,dy=p.clientY-deckTouch.y,dt=Date.now()-deckTouch.t;deckTouch=null;if(dt<750&&Math.abs(dx)>52&&Math.abs(dx)>Math.abs(dy)*1.2)navigateDeck(dx<0?1:-1)},{passive:true});
  return ov;
}
function openDeck(memberId){
  const s=appState(),m=(s?.members||[]).find(x=>x.id===memberId);if(!m)return;
  const ov=ensureDeck();deckMemberId=memberId;deckCardIndex=0;deckCards=buildDeckCards(m);
  const [soft,border,accent]=colorsFor(m.nome),table=$('.p97-table',ov);table.style.setProperty('--soft',soft);table.style.setProperty('--border',border);table.style.setProperty('--accent',accent);
  $('#p97DeckName').textContent=m.nome;ov.hidden=false;document.documentElement.style.overflow='hidden';renderDeckCard(0,true);
}
function closeDeck(){const ov=$('#p97Deck');if(!ov||ov.hidden)return;ov.hidden=true;$('#p97Stage').innerHTML='';deckMemberId='';deckCards=[];deckCardIndex=0;deckAnimating=false;document.documentElement.style.overflow=''}
function renderDeckCard(dir=0,first=false){
  if(!deckCards.length)return;const stage=$('#p97Stage'),old=$('.p97-playing-card',stage),card=deckCards[deckCardIndex];
  const put=()=>{stage.innerHTML='';const el=document.createElement('article');el.className='p97-playing-card';el.innerHTML=card.html;stage.appendChild(el);$('#p97DeckKicker').textContent=card.kicker||'ACOMPANHAMENTO';$('#p97CardTitle').textContent=card.title;$('#p97CardCount').textContent=`${deckCardIndex+1} de ${deckCards.length}`;requestAnimationFrame(()=>el.classList.add('drop'));el.scrollTop=0;deckAnimating=false};
  if(!old||first){put();return}deckAnimating=true;old.classList.remove('drop');old.classList.add(dir>=0?'out-left':'out-right');setTimeout(put,175);
}
function navigateDeck(delta){if(deckAnimating||!deckCards.length)return;deckCardIndex=(deckCardIndex+delta+deckCards.length)%deckCards.length;renderDeckCard(delta,false)}
function refreshOpenDeck(){
  if(!deckMemberId)return;const s=appState(),m=(s?.members||[]).find(x=>x.id===deckMemberId);if(!m){closeDeck();return}
  const oldTitle=deckCards[deckCardIndex]?.title;deckCards=buildDeckCards(m);const i=deckCards.findIndex(c=>c.title===oldTitle);if(i>=0)deckCardIndex=i;renderDeckCard(0,true);
}
window.p202RefreshAcademicDeck=refreshOpenDeck;

function renderAfterTrack(){renderFolders();refreshOpenDeck()}
function installTrackWrapper(){
  if(trackWrapped)return;
  let original=null;try{original=window.renderTrack||renderTrack}catch(_e){}
  if(typeof original!=='function'){setTimeout(installTrackWrapper,120);return}
  if(original.__p97Wrapped){trackWrapped=true;return}
  const wrapped=function(){const r=original.apply(this,arguments);requestAnimationFrame(renderAfterTrack);return r};
  wrapped.__p97Wrapped=true;wrapped.__p97Original=original;
  try{renderTrac


===== openCheckin =====

--- occurrence 1 at char 573556 ---
rHTML=members.map(m=>{
    const acts=all.filter(a=>a.ownerId===m.id).sort((a,b)=>_activityTime(b)-_activityTime(a));const can=isAdmin||isMe(m.id);
    const periods=acts.filter(a=>TRACK_PERIOD_TYPES.has(a.type));const follows=acts.filter(a=>!TRACK_PERIOD_TYPES.has(a.type)&&!TRACK_LEGACY_TYPES.has(a.type)).sort((a,b)=>{const ad=_activityDate(a),bd=_activityDate(b);return (bd||'').localeCompare(ad||'')||_activityTime(b)-_activityTime(a)});
    const pending=follows.filter(a=>_isPending(a)||_isOverdue(a));const lastOrient=follows.filter(a=>a.type==='orientacao').sort((a,b)=>(_activityDate(b)||'').localeCompare(_activityDate(a)||''))[0];const nextDue=follows.filter(a=>!_activityDone(a)&&a.dueDate&&!dateInfo(a.dueDate)?.over).sort((a,b)=>a.dueDate.localeCompare(b.dueDate))[0];const corrections=follows.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
    const nv=NIVEIS[m.nivel]||{short:m.nivel||'',color:'#607d8b'};
    const periodsHTML=_scholarPeriodHTML(m)+periods.map(a=>_periodHTML(a.type,a.title,a.startDate,a.dueDate,can,a.id,a.progress)).join('');
    const listHTML=follows.slice(0,8).map(a=>_followItemHTML(a,can)).join('');
    return `<section class="track-person"><div class="track-person-head"><div class="track-person-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="track-person-title">${esc(m.nome)}</div><div class="track-person-sub">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode edita

--- occurrence 2 at char 588694 ---
"><div class="k-n">${critical}</div><div class="k-l">situações críticas</div></div><div class="dash-kpi ${overdue?'bad':'good'}"><div class="k-n">${overdue}</div><div class="k-l">itens atrasados</div></div><div class="dash-kpi ${corr?'warn':'good'}"><div class="k-n">${corr}</div><div class="k-l">correções abertas</div></div><div class="dash-kpi ${unread?'warn':'good'}"><div class="k-n">${unread}</div><div class="k-l">notificações não lidas</div></div><div class="dash-kpi"><div class="k-n">${products}</div><div class="k-l">produtos registrados</div></div>`;
  $('#dashAttention').innerHTML=att.length?att.map(x=>`<div class="attention-item ${x.sev==='critical'?'critical':''}" onclick="focusMemberTrack('${x.memberId}')"><span class="attention-dot"></span><div><div class="attention-who">${esc(x.who)}</div><div class="attention-why">${esc(x.why)}</div></div><div class="attention-date">${x.date?fmtDate(x.date):''}</div></div>`).join(''):'<div class="dash-empty">✅ Nada crítico agora. Os projetos estão em dia.</div>';
  $('#dashHealth').innerHTML=scope.length?scope.map(m=>{const h=memberHealth(m);return `<div class="health-row" onclick="focusMemberTrack('${m.id}')" style="cursor:pointer"><span class="health-dot ${h.level}"></span><div class="health-person"><b>${esc(m.nome)}</b><span>${esc(h.reasons.slice(0,2).join(' · '))}</span></div><span class="health-score">${h.level==='green'?'Em dia':h.level==='yellow'?'Atenção':'Intervir'}</span></div>`}).join(''):'<div class="dash-empty">Sem pessoas neste recorte.</div>';
  const mk=monthKey(),target=isAdmin?(memberById(myId)||scope[0]):memberById(myId),myCheck=target&&state.activities.find(a=>a.ownerId===target.id&&a.type==='checkin'&&a.checkinMonth===mk);
  $('#dashCheckin').innerHTML=target?`<div class="checkin-spot"><div class="checkin-month">${esc(monthLabel(mk))}</div><div class="checkin-title">${myCheck?'✅ Check-in preenchido':'Seu check-in está esperando'}</div><div class="checkin-note">${myCheck?esc((myCheck.checkinNext||myCheck.description||'Registrado.').slice(0,180)):'Registre o que fez, o que entregou, a principal dificuldade e o próximo passo.'}</div><div class="checkin-actions"><button class="btn primary" onclick="openCheckin('${target.id}')">${myCheck?'Ver / atualizar':'Preencher agora'}</button></div></div>`:'<div class="dash-empty">Vincule seu perfil para usar o check-in.</div>';
  const prods=state.activities.filter(a=>a.type==='produto'&&scope.some(m=>m.id===a.ownerId)).sort((a,b)=>_activityTime(b)-_activityTime(a)).slice(0,6);$('#dashProducts').innerHTML=prods.length?prods.map(a=>`<div class="product-mini"><span class="product-type">${({artigo:'📄',resumo:'🧾',apresentacao:'🖥️',relatorio:'📋',base:'🗃️',plataforma:'💻',material:'📚',evento:'🎪',tcc:'🎓',outro:'🎯'})[a.productType]||'🎯'}</span><div class="product-main"><b>${esc(a.title||'Produto')}</b><span>${esc(a.ownerName||'')} ${a.dueDate?'· '+fmtDate(a.dueDate):''}</span></div><span class="product-status">${esc(ACTIVITY_STATUS[a.status]||a.status||'')}</span></div>`).join(''):'<div class="dash-empty">Nenhum produto registrado ainda.</div>';
  $('#dashBackupBtn')?.style.setProperty('display',isAdmin?'inline-flex':'none');updateAttentionBadge();
}
function _fillOwnerSelect(selId,ownerPreset=''){const sel=$(selId),ordered=[...state.members].filter(m=>m.status!=='egresso').sort((a,b)=>a.nome.localeCompare(b.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');sel.value=ownerPreset||myId||ordered[0]?.id||''}
function openCheckin(ownerPreset=''){if(!myId&&!isAdmin){toast('Vincule seu perfil ao login.');return}_fillOwnerSelect('#checkinOwner',ownerPreset);$('#checkinOwnerRow').style.display=isAdmin?'block':'none';const owner=isAdmin?$('#checkinOwner').value:myId,mk=monthKey();$('#checkinMonth').value=mk;const a=state.activities.find(x=>x.ownerId===owner&&x.type==='checkin'&&x.checkinMonth===mk);$('#checkinDid').value=a?.checkinDid||'';$('#checkinDelivered').value=a?.checkinDelivered||'';$('#checkinDifficulty').value=a?.checkinDifficulty||'';$('#checkinNext').value=a?.checkinNext||'';openOverlay('checkinOverlay')}
function loadCheckinForSelection(){const owner=isAdmin?$('#checkinOwner').value:myId,mk=$('#checkinMonth').value||monthKey(),a=state.activities.find(x=>x.ownerId===owner&&x.type==='checkin'&&x.checkinMonth===mk);$('#checkinDid').value=a?.checkinDid||'';$('#checkinDelivered').value=a?.checkinDelivered||'';$('#checkinDifficulty').value=a?.checkinDifficulty||'';$('#checkinNext').value=a?.checkinNext||''}
async function saveCheckin(){const ownerId=isAdmin?$('#checkinOwner').value:myId,mk=$('#checkinMonth').value||monthKey();if(!ownerId){toast('Selecione o Carbonauta.');return}const m=memberById(ownerId),did=$('#checkinDid').value.trim(),del=$('#checkinDelivered').value.trim(),dif=$('#checkinDifficulty').value.trim(),next=$('#checkinNext').value.trim();if(!did&&!del&&!dif&&!next){toast('Preencha ao menos uma informação do check-in.');return}const data={ownerId,ownerUid:(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?(window.auth.currentUser?.uid||''):''),ownerName:m?.nome||'',type:'checkin',status:'concluido',title:`Check-in mensal · ${monthLabel(mk)}`,description:next||did,checkinMonth:mk,checkinDid:did,checkinDelivered:del,checkinDifficulty:dif,checkinNext:next,startDate:mk+'-01',progress:100,updatedAt:FB().serverTimestamp()};const existing=state.activities.find(x=>x.ownerId===ownerId&&x.type==='checkin'&&x.checkinMonth===mk);try{if(existing)await FB().setDoc(FB().doc(window.db,'rede_activities',existing.id),data,{merge:true});else await FB().addDoc(FB().collection(window.db,'rede_activities'),{...data,createdAt:FB().serverTimestamp

--- occurrence 3 at char 590011 ---
0,2).join(' · '))}</span></div><span class="health-score">${h.level==='green'?'Em dia':h.level==='yellow'?'Atenção':'Intervir'}</span></div>`}).join(''):'<div class="dash-empty">Sem pessoas neste recorte.</div>';
  const mk=monthKey(),target=isAdmin?(memberById(myId)||scope[0]):memberById(myId),myCheck=target&&state.activities.find(a=>a.ownerId===target.id&&a.type==='checkin'&&a.checkinMonth===mk);
  $('#dashCheckin').innerHTML=target?`<div class="checkin-spot"><div class="checkin-month">${esc(monthLabel(mk))}</div><div class="checkin-title">${myCheck?'✅ Check-in preenchido':'Seu check-in está esperando'}</div><div class="checkin-note">${myCheck?esc((myCheck.checkinNext||myCheck.description||'Registrado.').slice(0,180)):'Registre o que fez, o que entregou, a principal dificuldade e o próximo passo.'}</div><div class="checkin-actions"><button class="btn primary" onclick="openCheckin('${target.id}')">${myCheck?'Ver / atualizar':'Preencher agora'}</button></div></div>`:'<div class="dash-empty">Vincule seu perfil para usar o check-in.</div>';
  const prods=state.activities.filter(a=>a.type==='produto'&&scope.some(m=>m.id===a.ownerId)).sort((a,b)=>_activityTime(b)-_activityTime(a)).slice(0,6);$('#dashProducts').innerHTML=prods.length?prods.map(a=>`<div class="product-mini"><span class="product-type">${({artigo:'📄',resumo:'🧾',apresentacao:'🖥️',relatorio:'📋',base:'🗃️',plataforma:'💻',material:'📚',evento:'🎪',tcc:'🎓',outro:'🎯'})[a.productType]||'🎯'}</span><div class="product-main"><b>${esc(a.title||'Produto')}</b><span>${esc(a.ownerName||'')} ${a.dueDate?'· '+fmtDate(a.dueDate):''}</span></div><span class="product-status">${esc(ACTIVITY_STATUS[a.status]||a.status||'')}</span></div>`).join(''):'<div class="dash-empty">Nenhum produto registrado ainda.</div>';
  $('#dashBackupBtn')?.style.setProperty('display',isAdmin?'inline-flex':'none');updateAttentionBadge();
}
function _fillOwnerSelect(selId,ownerPreset=''){const sel=$(selId),ordered=[...state.members].filter(m=>m.status!=='egresso').sort((a,b)=>a.nome.localeCompare(b.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');sel.value=ownerPreset||myId||ordered[0]?.id||''}
function openCheckin(ownerPreset=''){if(!myId&&!isAdmin){toast('Vincule seu perfil ao login.');return}_fillOwnerSelect('#checkinOwner',ownerPreset);$('#checkinOwnerRow').style.display=isAdmin?'block':'none';const owner=isAdmin?$('#checkinOwner').value:myId,mk=monthKey();$('#checkinMonth').value=mk;const a=state.activities.find(x=>x.ownerId===owner&&x.type==='checkin'&&x.checkinMonth===mk);$('#checkinDid').value=a?.checkinDid||'';$('#checkinDelivered').value=a?.checkinDelivered||'';$('#checkinDifficulty').value=a?.checkinDifficulty||'';$('#checkinNext').value=a?.checkinNext||'';openOverlay('checkinOverlay')}
function loadCheckinForSelection(){const owner=isAdmin?$('#checkinOwner').value:myId,mk=$('#checkinMonth').value||monthKey(),a=state.activities.find(x=>x.ownerId===owner&&x.type==='checkin'&&x.checkinMonth===mk);$('#checkinDid').value=a?.checkinDid||'';$('#checkinDelivered').value=a?.checkinDelivered||'';$('#checkinDifficulty').value=a?.checkinDifficulty||'';$('#checkinNext').value=a?.checkinNext||''}
async function saveCheckin(){const ownerId=isAdmin?$('#checkinOwner').value:myId,mk=$('#checkinMonth').value||monthKey();if(!ownerId){toast('Selecione o Carbonauta.');return}const m=memberById(ownerId),did=$('#checkinDid').value.trim(),del=$('#checkinDelivered').value.trim(),dif=$('#checkinDifficulty').value.trim(),next=$('#checkinNext').value.trim();if(!did&&!del&&!dif&&!next){toast('Preencha ao menos uma informação do check-in.');return}const data={ownerId,ownerUid:(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?(window.auth.currentUser?.uid||''):''),ownerName:m?.nome||'',type:'checkin',status:'concluido',title:`Check-in mensal · ${monthLabel(mk)}`,description:next||did,checkinMonth:mk,checkinDid:did,checkinDelivered:del,checkinDifficulty:dif,checkinNext:next,startDate:mk+'-01',progress:100,updatedAt:FB().serverTimestamp()};const existing=state.activities.find(x=>x.ownerId===ownerId&&x.type==='checkin'&&x.checkinMonth===mk);try{if(existing)await FB().setDoc(FB().doc(window.db,'rede_activities',existing.id),data,{merge:true});else await FB().addDoc(FB().collection(window.db,'rede_activities'),{...data,createdAt:FB().serverTimestamp()});await awardPoints(ownerId,'checkin',`${ownerId}_${mk}`,`Check-in · ${monthLabel(mk)}`);closeOverlay('checkinOverlay');toast('Check-in salvo.')}catch(e){console.error(e);toast(e?.code==='permission-denied'?'O Firestore bloqueou o check-in.':'Erro ao salvar check-in.') }}
function openProduct(ownerPreset=''){if(!myId&&!isAdmin){toast('Vincule seu perfil ao login.');return}_fillOwnerSelect('#productOwner',ownerPreset);$('#productOwnerRow').style.display=isAdmin?'block':'none';$('#productType').value='artigo';$('#productTitle').value='';$('#productStatus').value='planejado';$('#productDue').value='';$('#productNotes').value='';openOverlay('productOverlay')}
async function saveProduct(){const ownerId=isAdmin?$('#productOwner').value:myId,title=$('#productTitle').value.trim();if(!ownerId||!title){toast('Informe o Carbonauta e o produto.');return}const m=memberById(ownerId),ptype=$('#productType').value,data={ownerId,ownerUid:(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?(window.auth.currentUser?.uid||''):''),ownerName:m?.nome||'',type:'produto',productType:ptype,status:$('#productStatus').value,title,description:$('#productNotes').value.trim(),dueDate:$('#productDue').value,startDate:new Date().toISOString().slice(0,10),progress:$('#productStatus').value==='concluido'?100:$

--- occurrence 4 at char 686086 ---
t('Google Agenda conectado nesta sessão.')}catch(e){console.error(e);toast(e.message||'Não consegui conectar ao Google Agenda.')}};
if($('#syncAllGoogleBtn'))$('#syncAllGoogleBtn').onclick=syncAllPrivateToGoogle;
if($('#pullGoogleBtn'))$('#pullGoogleBtn').onclick=pullGoogleToCarbonautas;
if($('#medalPrev'))$('#medalPrev').onclick=()=>shiftMedalMonth(-1);
if($('#medalNext'))$('#medalNext').onclick=()=>shiftMedalMonth(1);
if($('#medalToday'))$('#medalToday').onclick=()=>{medalCursor=new Date(new Date().getFullYear(),new Date().getMonth(),1);medalLoadedKey='';renderMedals()};
if($('#reloadGameBtn'))$('#reloadGameBtn').onclick=()=>{const f=$('#carbonautaGameFrame');if(f)f.src='./game.html?v=P28&t='+Date.now()};
window.addEventListener('message',e=>{const frame=$('#carbonautaGameFrame');if(!frame||e.source!==frame.contentWindow)return;if(location.origin!=='null'&&e.origin!==location.origin)return;const d=e.data||{};if(d.type==='carbonautas-game-result'&&d.completed===true)reportGameReward(d.score)});

$('#chatInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(120,e.target.scrollHeight)+'px'});
$('#chatInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat()}});
$('#sendBtn').onclick=sendChat;
if($('#mOnlyMine')) $('#mOnlyMine').onchange=renderRecados;
if($('#muralRecadoBtn')) $('#muralRecadoBtn').onclick=openRecado;
const muralCommandEl=$('#muralCommand'), muralCommandToggle=$('#muralCommandToggle');
function syncMuralCommandToggle(){ if(!muralCommandEl||!muralCommandToggle) return; muralCommandToggle.textContent=muralCommandEl.classList.contains('collapsed') ? 'Mostrar atalhos' : 'Ocultar atalhos'; }
if(muralCommandToggle){ muralCommandToggle.onclick=()=>{ muralCommandEl.classList.toggle('collapsed'); syncMuralCommandToggle(); }; syncMuralCommandToggle(); }
if($('#goMuralBtn')) $('#goMuralBtn').onclick=()=>switchView('mural');
if($('#muralNewBtn')) $('#muralNewBtn').onclick=()=>openPostModal('atualizacao');
if($('#muralDirectTopBtn')) $('#muralDirectTopBtn').onclick=openRecado;
if($('#muralDirectBtn')) $('#muralDirectBtn').onclick=openRecado;

$('#dashCheckinBtn').onclick=()=>openCheckin();
$('#dashProductBtn').onclick=()=>openProduct();
$('#dashMedalsBtn').onclick=()=>switchView('medalhas');
$('#dashGameBtn').onclick=()=>switchView('jogo');
$('#dashDossierBtn').onclick=()=>openDossier();
$('#dashNewTaskBtn').onclick=()=>openActivityPreset('pendencia');
$('#dashProductsAllBtn').onclick=()=>switchView('track');
$('#dashBackupBtn').onclick=exportGeneralBackup;
$('#trackCheckinBtn').onclick=()=>openCheckin();
$('#trackProductBtn').onclick=()=>openProduct();
$('#trackDossierBtn').onclick=()=>openDossier();
$('#checkinOwner').onchange=loadCheckinForSelection; $('#checkinMonth').onchange=loadCheckinForSelection; $('#saveCheckinBtn').onclick=saveCheckin;
$('#saveProductBtn').onclick=saveProduct;
$('#dossierPerson').onchange=renderDossier; $('#dossierExcelBtn').onclick=exportDossierExcel; $('#dossierCsvBtn').onclick=exportDossierCsv; $('#dossierWordBtn').onclick=exportDossierWord; $('#dossierPrintBtn').onclick=printDossier;
$('#addActivityBtn').onclick=()=>openActivity(null);
$('#trackOwner').onchange=renderTrack; $('#trackStatus').onchange=renderTrack; $('#trackMine').onchange=renderTrack;
$('#activityProgress').oninput=e=>$('#activityProgressLabel').textContent=e.target.value+'%'; $('#activityType').onchange=syncActivityTypeUi;
$('#saveActivityBtn').onclick=saveActivity; $('#deleteActivityBtn').onclick=deleteActivity;
$('#saveProjectProfileBtn').onclick=saveProjectProfile;
if($('#newPostBtn')) $('#newPostBtn').onclick=()=>openPostModal('atualizacao'); if($('#muralPostBtn')) $('#muralPostBtn').onclick=()=>openPostModal('atualizacao'); $$('[data-mural-kind]').forEach(b=>b.onclick=()=>openMuralAction(b.dataset.muralKind)); $$('[data-mf]').forEach(b=>b.onclick=()=>{muralFilter=b.dataset.mf;$$('[data-mf]').forEach(x=>x.classList.toggle('on',x===b));renderMural();if($('#muralMsgs'))$('#muralMsgs').scrollTop=0;}); $('#postFilePick').onclick=()=>$('#postFile').click(); $('#postFile').onchange=e=>handlePostFile(e.target.files[0]); $('#savePostBtn').onclick=saveFeedPost;

$('#addPhotoBtn').onclick=openPhotoModal;
$('#gPickBtn').onclick=()=>$('#gFile').click();
$('#gFile').onchange=e=>{handleGalleryFiles(e.target.files);e.target.value=''};
$('#gProjeto').onchange=e=>{$('#gProjetoOutroRow').style.display=e.target.value==='__outro'?'block':'none'};
const gPasteZone=$('#gPasteZone');
if(gPasteZone){
  gPasteZone.addEventListener('paste',e=>{const item=[...(e.clipboardData?.items||[])].find(x=>x.type?.startsWith('image/'));if(item){e.preventDefault();handleGalleryFile(item.getAsFile(),true)}});
  gPasteZone.addEventListener('dragover',e=>{e.preventDefault();gPasteZone.classList.add('drag')});
  gPasteZone.addEventListener('dragleave',()=>gPasteZone.classList.remove('drag'));
  gPasteZone.addEventListener('drop',e=>{e.preventDefault();gPasteZone.classList.remove('drag');handleGalleryFiles([...(e.dataTransfer?.files||[])],true)});
}
$('#savePhotoBtn').onclick=savePhoto;
['fProjeto','fPessoa','fAno','fTipo'].forEach(id=>{const el=$('#'+id);if(el)el.onchange=renderFotos});
if($('#fBusca'))$('#fBusca').oninput=()=>{clearTimeout(window.__fBuscaT);window.__fBuscaT=setTimeout(renderFotos,180)};
if($('#fLimpar'))$('#fLimpar').onclick=()=>{['fProjeto','fPessoa','fAno','fTipo','fBusca'].forEach(id=>{const el=$('#'+id);if(el)el.value=''});renderFotos()};
if($('#fotosSelectBtn'))$('#fotosSelectBtn').onclick=toggleFotoSelecting;
if($('#fMais'))$('#fMais').onclick=()=>{const w=$('#fMais').closest('.mural-filters');w.classList.toggle('open');$(

--- occurrence 5 at char 686510 ---
if($('#medalToday'))$('#medalToday').onclick=()=>{medalCursor=new Date(new Date().getFullYear(),new Date().getMonth(),1);medalLoadedKey='';renderMedals()};
if($('#reloadGameBtn'))$('#reloadGameBtn').onclick=()=>{const f=$('#carbonautaGameFrame');if(f)f.src='./game.html?v=P28&t='+Date.now()};
window.addEventListener('message',e=>{const frame=$('#carbonautaGameFrame');if(!frame||e.source!==frame.contentWindow)return;if(location.origin!=='null'&&e.origin!==location.origin)return;const d=e.data||{};if(d.type==='carbonautas-game-result'&&d.completed===true)reportGameReward(d.score)});

$('#chatInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(120,e.target.scrollHeight)+'px'});
$('#chatInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat()}});
$('#sendBtn').onclick=sendChat;
if($('#mOnlyMine')) $('#mOnlyMine').onchange=renderRecados;
if($('#muralRecadoBtn')) $('#muralRecadoBtn').onclick=openRecado;
const muralCommandEl=$('#muralCommand'), muralCommandToggle=$('#muralCommandToggle');
function syncMuralCommandToggle(){ if(!muralCommandEl||!muralCommandToggle) return; muralCommandToggle.textContent=muralCommandEl.classList.contains('collapsed') ? 'Mostrar atalhos' : 'Ocultar atalhos'; }
if(muralCommandToggle){ muralCommandToggle.onclick=()=>{ muralCommandEl.classList.toggle('collapsed'); syncMuralCommandToggle(); }; syncMuralCommandToggle(); }
if($('#goMuralBtn')) $('#goMuralBtn').onclick=()=>switchView('mural');
if($('#muralNewBtn')) $('#muralNewBtn').onclick=()=>openPostModal('atualizacao');
if($('#muralDirectTopBtn')) $('#muralDirectTopBtn').onclick=openRecado;
if($('#muralDirectBtn')) $('#muralDirectBtn').onclick=openRecado;

$('#dashCheckinBtn').onclick=()=>openCheckin();
$('#dashProductBtn').onclick=()=>openProduct();
$('#dashMedalsBtn').onclick=()=>switchView('medalhas');
$('#dashGameBtn').onclick=()=>switchView('jogo');
$('#dashDossierBtn').onclick=()=>openDossier();
$('#dashNewTaskBtn').onclick=()=>openActivityPreset('pendencia');
$('#dashProductsAllBtn').onclick=()=>switchView('track');
$('#dashBackupBtn').onclick=exportGeneralBackup;
$('#trackCheckinBtn').onclick=()=>openCheckin();
$('#trackProductBtn').onclick=()=>openProduct();
$('#trackDossierBtn').onclick=()=>openDossier();
$('#checkinOwner').onchange=loadCheckinForSelection; $('#checkinMonth').onchange=loadCheckinForSelection; $('#saveCheckinBtn').onclick=saveCheckin;
$('#saveProductBtn').onclick=saveProduct;
$('#dossierPerson').onchange=renderDossier; $('#dossierExcelBtn').onclick=exportDossierExcel; $('#dossierCsvBtn').onclick=exportDossierCsv; $('#dossierWordBtn').onclick=exportDossierWord; $('#dossierPrintBtn').onclick=printDossier;
$('#addActivityBtn').onclick=()=>openActivity(null);
$('#trackOwner').onchange=renderTrack; $('#trackStatus').onchange=renderTrack; $('#trackMine').onchange=renderTrack;
$('#activityProgress').oninput=e=>$('#activityProgressLabel').textContent=e.target.value+'%'; $('#activityType').onchange=syncActivityTypeUi;
$('#saveActivityBtn').onclick=saveActivity; $('#deleteActivityBtn').onclick=deleteActivity;
$('#saveProjectProfileBtn').onclick=saveProjectProfile;
if($('#newPostBtn')) $('#newPostBtn').onclick=()=>openPostModal('atualizacao'); if($('#muralPostBtn')) $('#muralPostBtn').onclick=()=>openPostModal('atualizacao'); $$('[data-mural-kind]').forEach(b=>b.onclick=()=>openMuralAction(b.dataset.muralKind)); $$('[data-mf]').forEach(b=>b.onclick=()=>{muralFilter=b.dataset.mf;$$('[data-mf]').forEach(x=>x.classList.toggle('on',x===b));renderMural();if($('#muralMsgs'))$('#muralMsgs').scrollTop=0;}); $('#postFilePick').onclick=()=>$('#postFile').click(); $('#postFile').onchange=e=>handlePostFile(e.target.files[0]); $('#savePostBtn').onclick=saveFeedPost;

$('#addPhotoBtn').onclick=openPhotoModal;
$('#gPickBtn').onclick=()=>$('#gFile').click();
$('#gFile').onchange=e=>{handleGalleryFiles(e.target.files);e.target.value=''};
$('#gProjeto').onchange=e=>{$('#gProjetoOutroRow').style.display=e.target.value==='__outro'?'block':'none'};
const gPasteZone=$('#gPasteZone');
if(gPasteZone){
  gPasteZone.addEventListener('paste',e=>{const item=[...(e.clipboardData?.items||[])].find(x=>x.type?.startsWith('image/'));if(item){e.preventDefault();handleGalleryFile(item.getAsFile(),true)}});
  gPasteZone.addEventListener('dragover',e=>{e.preventDefault();gPasteZone.classList.add('drag')});
  gPasteZone.addEventListener('dragleave',()=>gPasteZone.classList.remove('drag'));
  gPasteZone.addEventListener('drop',e=>{e.preventDefault();gPasteZone.classList.remove('drag');handleGalleryFiles([...(e.dataTransfer?.files||[])],true)});
}
$('#savePhotoBtn').onclick=savePhoto;
['fProjeto','fPessoa','fAno','fTipo'].forEach(id=>{const el=$('#'+id);if(el)el.onchange=renderFotos});
if($('#fBusca'))$('#fBusca').oninput=()=>{clearTimeout(window.__fBuscaT);window.__fBuscaT=setTimeout(renderFotos,180)};
if($('#fLimpar'))$('#fLimpar').onclick=()=>{['fProjeto','fPessoa','fAno','fTipo','fBusca'].forEach(id=>{const el=$('#'+id);if(el)el.value=''});renderFotos()};
if($('#fotosSelectBtn'))$('#fotosSelectBtn').onclick=toggleFotoSelecting;
if($('#fMais'))$('#fMais').onclick=()=>{const w=$('#fMais').closest('.mural-filters');w.classList.toggle('open');$('#fMais').textContent=w.classList.contains('open')?'Menos':'Filtros'};
if($('#muralSelAll'))$('#muralSelAll').onclick=()=>{fotosFiltradas().forEach(p=>fotoSelected.add(p.id));renderFotos()};
if($('#muralSelNone'))$('#muralSelNone').onclick=()=>{fotoSelected.clear();renderFotos()};
if($('#muralZipBtn'))$('#muralZipBtn').onclick=downloadSelectedPhotos;
$('#lbClose').onclick=closeLightbox;
$('#lbDel').onclick=deleteLightbox

--- occurrence 6 at char 901240 ---
olsaInicio,m.bolsaFim);
      if(pi.days!==null&&pi.days>=0&&pi.days<=45){
        const key=`bolsa:${m.id}:${m.bolsaFim}`;
        if(!p56IsAck(key))open.push({key,kind:'BOLSA · ATENÇÃO',who:m.nome,what:m.bolsaModalidade||'Bolsa em encerramento',detail:`Termina em ${_daysText(pi.days)} · ${fmtDate(m.bolsaFim)}.`,date:m.bolsaFim,sev:pi.days<=15?'critical':'warn',sort:2.5,action:'member',memberId:m.id,canAck:true});
      }
    }
    const acts=(state.activities||[]).filter(a=>a.ownerId===m.id);
    const check=acts.find(a=>a.type==='checkin'&&a.checkinMonth===mk);
    if(m.status!=='egresso'&&!check){
      open.push({key:`checkin:${m.id}:${mk}`,kind:'CHECK-IN · MÊS',who:m.nome,what:`Check-in de ${monthLabel(mk)}`,detail:'Registrar o que foi feito, dificuldades e próximo passo.',date:'',sev:'warn',sort:4,action:'checkin',memberId:m.id,canAck:false});
    } else if(check && (check.ownerId===myId || isAdmin)){
      const ms=p56Millis(check.updatedAt)||p56Millis(check.createdAt);
      if(p56MonthFromMillis(ms)===mk)done.push({key:`donecheckin:${check.id}`,kind:'✓ CHECK-IN',who:m.nome,what:`Check-in de ${monthLabel(mk)}`,doneAt:ms,backendDone:true});
    }
  });

  // Itens marcados manualmente como tratados continuam visíveis em Feitos.
  Object.values(acks).forEach(a=>{
    if(!a||!a.key)return;
    done.push({key:a.key,kind:`✓ ${a.kind||'TRATADO'}`,who:a.who||'—',what:a.what||'Item tratado',doneAt:+a.doneAt||0,localAck:true});
  });

  // evita duplicar o mesmo concluído quando houver cache local + conclusão real
  const seen=new Set();
  const cleanDone=done.sort((a,b)=>(b.doneAt||0)-(a.doneAt||0)).filter(x=>{const k=`${x.kind}|${x.who}|${x.what}|${x.sourceId||x.key}`;if(seen.has(k))return false;seen.add(k);return true}).slice(0,30);
  open.sort((a,b)=>a.sort-b.sort||(a.date||'9999').localeCompare(b.date||'9999'));
  return {open,done:cleanDone};
}

function p56OpenItem(key){
  const {open}=p56BuildItems(),it=open.find(x=>x.key===key);if(!it)return;
  try{
    if(it.action==='activity'){editActivity(it.sourceId);return}
    if(it.action==='review'){switchView('pubs');setTimeout(()=>openReviewConversation(it.sourceId),120);return}
    if(it.action==='checkin'){openCheckin(it.memberId);return}
    focusMemberTrack(it.memberId);
  }catch(e){console.warn('P56 abrir item',e)}
}
async function p56CompleteActivity(id){
  const a=(state.activities||[]).find(x=>x.id===id);if(!a)return toast('Item não encontrado.');
  if(_activityDone(a))return toast('Este item já está concluído.');
  if(!(isAdmin||isMe(a.ownerId)))return toast('Você não pode concluir este item.');
  if(!confirm(`Marcar como concluído?\n\n${p56ActivityLabel(a)}`))return;
  try{
    const f=FB();
    await f.updateDoc(f.doc(window.db,'rede_activities',id),{status:'concluido',updatedAt:f.serverTimestamp()});
    try{await awardPoints(a.ownerId,'activity_completed',id,p56ActivityLabel(a));if(a.dueDate)await awardPoints(a.ownerId,'deadline_done',id,p56ActivityLabel(a))}catch(_e){}
    toast('✓ Concluído. O item foi para Feitos.');
    setTimeout(()=>renderPainel(),120);
  }catch(e){console.error(e);toast('Não foi possível concluir este item.')}
}
function p56AckItem(key){
  const {open}=p56BuildItems(),it=open.find(x=>x.key===key);if(!it)return;
  p56SetAck(key,{kind:it.kind,who:it.who,what:it.what,memberId:it.memberId||'',sourceId:it.sourceId||''});
  renderPainel();toast('✓ Marcado como tratado.');
}
function p56UndoAck(key){p56UnsetAck(key);renderPainel();toast('Item voltou para a lista de atenção.')}

function p56ItemHTML(it){
  const tag=it.kind.split('·')[0].trim();
  return `<div class="p56-task ${it.sev==='critical'?'critical':'warn'}">
    <div class="p56-task-main">
      <div class="p56-task-top"><span class="p56-source">${esc(tag)}</span>${it.date?`<span class="p56-date">${esc(fmtDate(it.date))}</span>`:''}</div>
      <div class="p56-who">${esc(it.who)}</div>
      <div class="p56-what">${esc(it.what)}</div>
      <div class="p56-detail">${esc(it.detail||'')}</div>
    </div>
    <div class="p56-task-actions">
      <button type="button" class="btn" onclick="p56OpenItem('${esc(it.key)}')">Abrir</button>
      ${it.canComplete?`<button type="button" class="btn p56-done-btn" onclick="p56CompleteActivity('${esc(it.sourceId)}')">✓ Concluir</button>`:''}
      ${it.canAck?`<button type="button" class="btn p56-done-btn" onclick="p56AckItem('${esc(it.key)}')">✓ Já tratei</button>`:''}
    </div>
  </div>`;
}
function p56DoneHTML(it){
  return `<div class="p56-done-row"><span class="p56-check">✓</span><div class="p56-done-main"><b>${esc(it.who)}</b><span>${esc(it.what)}</span><small>${it.doneAt?p56DateTime(it.doneAt):'Concluído'}</small></div>${it.localAck?`<button type="button" class="p56-undo" onclick="p56UndoAck('${esc(it.key)}')">desfazer check</button>`:''}</div>`;
}
function p56RenderPanel(){
  if(!$('#dashKpis'))return;
  const {open,done}=p56BuildItems();
  const now=open.filter(x=>x.sev==='critical');
  const week=open.filter(x=>x.sev!=='critical');
  const overdue=open.filter(x=>x.action==='activity'&&x.date&&dateInfo(x.date)?.over).length;
  const redPeople=p56Scope().filter(m=>memberHealth(m).level==='red').length;
  $('#dashGreeting').textContent='Aqui ficam somente coisas que pedem uma ação. Abra o item certo e marque ✓ quando resolver.';
  $('#dashKpis').innerHTML=`<div class="dash-kpi ${open.length?'warn':'good'}"><div class="k-n">${open.length}</div><div class="k-l">precisam de ação</div></div><div class="dash-kpi ${overdue?'bad':'good'}"><div class="k-n">${overdue}</div><div class="k-l">vencidos</div></div><div class="dash-kpi ${week.length?'warn':'good'}"><div class="k-n">${week.length}</div><div class="k-l">esta s

--- occurrence 7 at char 907024 ---
v class="k-l">feitos / tratados</div></div><div class="dash-kpi ${redPeople?'bad':'good'}"><div class="k-n">${redPeople}</div><div class="k-l">pessoas em intervenção</div></div>`;
  $('#dashAttention').innerHTML=`
    <div class="p56-panel-section"><div class="p56-section-head"><b>🔥 Precisa de você</b><span>${now.length}</span></div>${now.length?now.map(p56ItemHTML).join(''):'<div class="dash-empty">Nada urgente agora.</div>'}</div>
    <div class="p56-panel-section"><div class="p56-section-head"><b>🗓 Esta semana</b><span>${week.length}</span></div>${week.length?week.map(p56ItemHTML).join(''):'<div class="dash-empty">Nenhuma ação próxima.</div>'}</div>
    <details class="p56-done-box" ${done.length<=6?'open':''}><summary>✓ Feitos / tratados <span>${done.length}</span></summary><div class="p56-done-list">${done.length?done.map(p56DoneHTML).join(''):'<div class="dash-empty">Nenhum item concluído ainda.</div>'}</div></details>`;

  // Mantém as partes úteis do painel antigo, mas com nomes mais claros.
  const scope=p56Scope();
  $('#dashHealth').innerHTML=scope.length?scope.map(m=>{const h=memberHealth(m);return `<div class="health-row" onclick="focusMemberTrack('${m.id}')" style="cursor:pointer"><span class="health-dot ${h.level}"></span><div class="health-person"><b>${esc(m.nome)}</b><span>${esc(h.reasons.slice(0,2).join(' · '))}</span></div><span class="health-score">${h.level==='green'?'Em dia':h.level==='yellow'?'Atenção':'Intervir'}</span></div>`}).join(''):'<div class="dash-empty">Sem pessoas neste recorte.</div>';
  const mk=monthKey(),target=isAdmin?(memberById(myId)||scope[0]):memberById(myId),myCheck=target&&(state.activities||[]).find(a=>a.ownerId===target.id&&a.type==='checkin'&&a.checkinMonth===mk);
  $('#dashCheckin').innerHTML=target?`<div class="checkin-spot"><div class="checkin-month">${esc(monthLabel(mk))}</div><div class="checkin-title">${myCheck?'✅ Check-in preenchido':'Check-in ainda pendente'}</div><div class="checkin-note">${myCheck?esc((myCheck.checkinNext||myCheck.description||'Registrado.').slice(0,180)):'Ao preencher, o Painel reconhece automaticamente como feito.'}</div><div class="checkin-actions"><button class="btn primary" onclick="openCheckin('${target.id}')">${myCheck?'Ver / atualizar':'Preencher agora'}</button></div></div>`:'<div class="dash-empty">Vincule seu perfil para usar o check-in.</div>';
  const prods=(state.activities||[]).filter(a=>a.type==='produto'&&scope.some(m=>m.id===a.ownerId)).sort((a,b)=>_activityTime(b)-_activityTime(a)).slice(0,5);
  $('#dashProducts').innerHTML=prods.length?prods.map(a=>`<div class="product-mini"><span class="product-type">🎯</span><div class="product-main"><b>${esc(a.title||'Produto')}</b><span>${esc(a.ownerName||p56MemberName(a.ownerId))}${a.dueDate?' · '+fmtDate(a.dueDate):''}</span></div><span class="product-status">${esc(ACTIVITY_STATUS[a.status]||a.status||'')}</span></div>`).join(''):'<div class="dash-empty">Nenhum produto registrado ainda.</div>';
  $('#dashBackupBtn')?.style.setProperty('display',isAdmin?'inline-flex':'none');
  p56UpdateAttentionBadge();
}
function p56UpdateAttentionBadge(){const b=$('#attentionBadge');if(!b)return;const n=p56BuildItems().open.filter(x=>x.sev==='critical').length;b.textContent=n>9?'9+':String(n);b.style.display=n?'grid':'none'}

// ================= MEDALHAS: MÉTRICA 0–100 =================
function p56UniqueCount(points,action){return new Set((points||[]).filter(p=>p.action===action).map(p=>String(p.sourceId||p.id||Math.random()))).size}
function p56CorrectionDoneCount(points,memberId){
  const ids=new Set((points||[]).filter(p=>p.memberId===memberId&&p.action==='activity_completed').map(p=>String(p.sourceId||'')));
  let n=0;ids.forEach(id=>{const a=(state.activities||[]).find(x=>String(x.id)===id);if(a?.type==='correcao')n++});return n;
}
function p56MetricFor(member,points,mk){
  const mine=(points||[]).filter(p=>p.memberId===member.id);
  const completed=p56UniqueCount(mine,'activity_completed');
  const deadline=p56UniqueCount(mine,'deadline_done');
  const pubs=p56UniqueCount(mine,'publication');
  const products=p56UniqueCount(mine,'product');
  const help=p56UniqueCount(mine,'help_post');
  const check=p56UniqueCount(mine,'checkin');
  const packages=p56UniqueCount(mine,'repo_package');
  const corrections=p56CorrectionDoneCount(mine,member.id);
  const delivery=Math.min(40,Math.min(24,completed*8)+Math.min(16,deadline*8));
  const production=Math.min(30,Math.min(20,pubs*10)+Math.min(10,products*5));
  const collaboration=Math.min(20,Math.min(16,help*8)+Math.min(8,corrections*4));
  const organization=Math.min(10,Math.min(5,check*5)+Math.min(2,packages*2)+Math.min(3,deadline));
  const score=delivery+production+collaboration+organization;
  const current=mk===monthKey();
  const critical=current?(state.activities||[]).filter(a=>a.ownerId===member.id&&_isOverdue(a)).length:0;
  let medal='Em progresso',icon='🌱',next=50;
  if(score>=95&&critical===0){medal='Carbonauta Topizera';icon='💎';next=100}
  else if(score>=85){medal='Ouro';icon='🥇';next=critical&&score>=95?95:95}
  else if(score>=70){medal='Prata';icon='🥈';next=85}
  else if(score>=50){medal='Bronze';icon='🥉';next=70}
  return {memberId:member.id,name:member.nome||'Carbonauta',score,delivery,production,collaboration,organization,critical,medal,icon,next,events:mine.length};
}
function p56ScoreRules(){
  const host=$('#scoreRules');if(!host)return;
  host.innerHTML=`
    <div class="p56-rule"><span>🎯 Entrega</span><b>40 pts</b><small>Tarefas concluídas e prazos cumpridos</small></div>
    <div class="p56-rule"><span>🔬 Produção</span><b>30 pts</b><small>Publicações/arquivos científicos e produtos</small></div>
    <di

--- occurrence 8 at char 1012693 ---
fe('Não consegui abrir o item relacionado.')}};
  const complete=foot.querySelector('.p66-complete'),chat=[...foot.querySelectorAll('button')].find(x=>x.textContent.includes('Conversar'));
  if(complete)foot.insertBefore(b,complete);else if(chat)foot.insertBefore(b,chat);else foot.insertBefore(b,foot.lastElementChild||null);
  if(chat){chat.classList.remove('p66-primary');chat.classList.add('p67-chat-secondary')}
}
async function enrichActivityDetail(id){
  const a=activityById(id);if(!a)return;tuneExistingButtons();ensureCss();addSearching();
  const target=await resolveTarget(a);removeSearching();
  if(!document.getElementById('p66ActionOverlay')?.classList.contains('open'))return;
  if(target){addLinkedCard(target);insertTargetButton(target);return}
  const body=document.getElementById('p66Body');if(body&&!body.querySelector('.p67-linked')){const el=document.createElement('div');el.className='p67-linked';el.innerHTML='<div class="p67-linked-ico">💬</div><div class="p67-linked-main"><b>Nenhum documento vinculado automaticamente</b><span>Use a conversa para tratar a demanda ou “Ajustar registro” para corrigir o cadastro.</span></div>';const progress=body.querySelector('.p66-progress');if(progress)body.insertBefore(el,progress);else body.appendChild(el)}
}
function openActivity(id){
  if(typeof window.p66OpenActivityDetail!=='function')return toastSafe('O detalhe do Painel ainda não terminou de carregar.');
  window.p66OpenActivityDetail(id);setTimeout(()=>enrichActivityDetail(id),0);
}
function latestReviewForThread(tid){
  try{return (state.publicacoes||[]).filter(p=>p.reviewFlow&&(p.reviewThreadId===tid||p.id===tid)).sort((a,b)=>Number(b.reviewVersion||1)-Number(a.reviewVersion||1))[0]||null}catch(_e){return null}
}
function openNonActivity(key){
  const k=String(key||'');
  try{
    if(k.startsWith('review:')){const p=latestReviewForThread(k.slice(7));if(p){if(typeof switchView==='function')switchView('pubs');setTimeout(()=>{if(typeof window.openReviewConversation==='function')window.openReviewConversation(p.id)},120)}return}
    if(k.startsWith('checkin:')){const rest=k.slice(8),pos=rest.lastIndexOf(':');const memberId=pos>0?rest.slice(0,pos):rest;if(typeof openCheckin==='function')openCheckin(memberId);return}
    if(k.startsWith('bolsa:')){const rest=k.slice(6),pos=rest.lastIndexOf(':');const memberId=pos>0?rest.slice(0,pos):rest;if(typeof focusMemberTrack==='function')focusMemberTrack(memberId);return}
  }catch(e){console.warn('P67 ação auxiliar',e)}
}
function p67OpenItem(key){const k=String(key||'');if(k.startsWith('activity:')){openActivity(k.slice(9));return}openNonActivity(k)}
function install(){ensureCss();window.p67OpenActivity=openActivity;window.p56OpenItem=p67OpenItem}
function boot(){install();setTimeout(install,1000);setTimeout(install,1800);setTimeout(install,3000);console.info('Carbonautas',VERSION,BUILD,'ações do Painel ligadas ao destino real')}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();

</script>
<script data-src="dashboard-clarity-p68.js">
/* Carbonautas P68 · Painel claro: ação sua x só acompanhar + status do arquivo */
(function(){
'use strict';
const VERSION='P68';
const BUILD='20260917';
let busy=false,scheduled=false,baseRender=null;

const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function norm(v=''){return String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/\.(docx?|xlsx?|pptx?|pdf|odt|rtf|csv)$/,'').replace(/[^a-z0-9]+/g,' ').trim().replace(/\s+/g,' ')}
function tokens(v=''){return new Set(norm(v).split(' ').filter(x=>x.length>2))}
function scoreTitle(a,b){const A=norm(a),B=norm(b);if(!A||!B)return 0;if(A===B)return 140;if(A.includes(B)||B.includes(A))return 105;const ta=tokens(A),tb=tokens(B);let hit=0;ta.forEach(x=>{if(tb.has(x))hit++});return ta.size&&tb.size?Math.round(hit/Math.max(ta.size,tb.size)*90):0}
function first(n=''){return String(n||'').trim().split(/\s+/)[0]||'Carbonauta'}
function me(){try{return typeof myId!=='undefined'?myId:''}catch(_e){return''}}
function admin(){try{return !!isAdmin}catch(_e){return false}}
function memberName(id){try{return memberById(id)?.nome||'Carbonauta'}catch(_e){return'Carbonauta'}}
function activity(id){try{return (state.activities||[]).find(a=>a.id===id)||null}catch(_e){return null}}
function reviewThreadId(p){try{return typeof repoReviewThreadId==='function'?repoReviewThreadId(p):(p.reviewThreadId||p.id)}catch(_e){return p?.reviewThreadId||p?.id||''}}
function latestReview(tid){try{return (state.publicacoes||[]).filter(p=>p.reviewFlow&&reviewThreadId(p)===tid).sort((a,b)=>Number(b.reviewVersion||1)-Number(a.reviewVersion||1))[0]||null}catch(_e){return null}}
function fmt(v){try{return typeof fmtDate==='function'?fmtDate(v):String(v||'')}catch(_e){return String(v||'')}}
function isMineActivity(a){if(!a)return false;const id=me();return a.ownerId===id||a.actionForMemberId===id||a.assignedToMemberId===id||a.responsibleMemberId===id}
function needsFile(a){return ['produto','entrega','correcao'].includes(String(a?.type||'').toLowerCase())}
function visibleFileForActivity(a){
  if(!a)return null;
  try{
    const pubs=(state.publicacoes||[]).filter(p=>p.tipo==='arquivo'&&p.url);
    const explicitId=a.linkedPublicationId||a.publicationId||a.repoPublicationId||a.sourcePublicationId||a.relatedPublicationId||'';
    if(explicitId){const ex=pubs.find(p=>p.id===explicitId);if(ex)return ex}
    let best=null;
    pubs.forEach(p=>{
      let s=Math.max(scoreTitle(a.title,p.titulo),scoreTitle(a.title,p.fileName),scoreTitle(a.title,p.reviewBaseTitle));
  


===== openProduct =====

--- occurrence 1 at char 573628 ---
rt((a,b)=>_activityTime(b)-_activityTime(a));const can=isAdmin||isMe(m.id);
    const periods=acts.filter(a=>TRACK_PERIOD_TYPES.has(a.type));const follows=acts.filter(a=>!TRACK_PERIOD_TYPES.has(a.type)&&!TRACK_LEGACY_TYPES.has(a.type)).sort((a,b)=>{const ad=_activityDate(a),bd=_activityDate(b);return (bd||'').localeCompare(ad||'')||_activityTime(b)-_activityTime(a)});
    const pending=follows.filter(a=>_isPending(a)||_isOverdue(a));const lastOrient=follows.filter(a=>a.type==='orientacao').sort((a,b)=>(_activityDate(b)||'').localeCompare(_activityDate(a)||''))[0];const nextDue=follows.filter(a=>!_activityDone(a)&&a.dueDate&&!dateInfo(a.dueDate)?.over).sort((a,b)=>a.dueDate.localeCompare(b.dueDate))[0];const corrections=follows.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
    const nv=NIVEIS[m.nivel]||{short:m.nivel||'',color:'#607d8b'};
    const periodsHTML=_scholarPeriodHTML(m)+periods.map(a=>_periodHTML(a.type,a.title,a.startDate,a.dueDate,can,a.id,a.progress)).join('');
    const listHTML=follows.slice(0,8).map(a=>_followItemHTML(a,can)).join('');
    return `<section class="track-person"><div class="track-person-head"><div class="track-person-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="track-person-title">${esc(m.nome)}</div><div class="track-person-sub">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){ope

--- occurrence 2 at char 592479 ---
eckinMonth').value=mk;const a=state.activities.find(x=>x.ownerId===owner&&x.type==='checkin'&&x.checkinMonth===mk);$('#checkinDid').value=a?.checkinDid||'';$('#checkinDelivered').value=a?.checkinDelivered||'';$('#checkinDifficulty').value=a?.checkinDifficulty||'';$('#checkinNext').value=a?.checkinNext||'';openOverlay('checkinOverlay')}
function loadCheckinForSelection(){const owner=isAdmin?$('#checkinOwner').value:myId,mk=$('#checkinMonth').value||monthKey(),a=state.activities.find(x=>x.ownerId===owner&&x.type==='checkin'&&x.checkinMonth===mk);$('#checkinDid').value=a?.checkinDid||'';$('#checkinDelivered').value=a?.checkinDelivered||'';$('#checkinDifficulty').value=a?.checkinDifficulty||'';$('#checkinNext').value=a?.checkinNext||''}
async function saveCheckin(){const ownerId=isAdmin?$('#checkinOwner').value:myId,mk=$('#checkinMonth').value||monthKey();if(!ownerId){toast('Selecione o Carbonauta.');return}const m=memberById(ownerId),did=$('#checkinDid').value.trim(),del=$('#checkinDelivered').value.trim(),dif=$('#checkinDifficulty').value.trim(),next=$('#checkinNext').value.trim();if(!did&&!del&&!dif&&!next){toast('Preencha ao menos uma informação do check-in.');return}const data={ownerId,ownerUid:(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?(window.auth.currentUser?.uid||''):''),ownerName:m?.nome||'',type:'checkin',status:'concluido',title:`Check-in mensal · ${monthLabel(mk)}`,description:next||did,checkinMonth:mk,checkinDid:did,checkinDelivered:del,checkinDifficulty:dif,checkinNext:next,startDate:mk+'-01',progress:100,updatedAt:FB().serverTimestamp()};const existing=state.activities.find(x=>x.ownerId===ownerId&&x.type==='checkin'&&x.checkinMonth===mk);try{if(existing)await FB().setDoc(FB().doc(window.db,'rede_activities',existing.id),data,{merge:true});else await FB().addDoc(FB().collection(window.db,'rede_activities'),{...data,createdAt:FB().serverTimestamp()});await awardPoints(ownerId,'checkin',`${ownerId}_${mk}`,`Check-in · ${monthLabel(mk)}`);closeOverlay('checkinOverlay');toast('Check-in salvo.')}catch(e){console.error(e);toast(e?.code==='permission-denied'?'O Firestore bloqueou o check-in.':'Erro ao salvar check-in.') }}
function openProduct(ownerPreset=''){if(!myId&&!isAdmin){toast('Vincule seu perfil ao login.');return}_fillOwnerSelect('#productOwner',ownerPreset);$('#productOwnerRow').style.display=isAdmin?'block':'none';$('#productType').value='artigo';$('#productTitle').value='';$('#productStatus').value='planejado';$('#productDue').value='';$('#productNotes').value='';openOverlay('productOverlay')}
async function saveProduct(){const ownerId=isAdmin?$('#productOwner').value:myId,title=$('#productTitle').value.trim();if(!ownerId||!title){toast('Informe o Carbonauta e o produto.');return}const m=memberById(ownerId),ptype=$('#productType').value,data={ownerId,ownerUid:(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?(window.auth.currentUser?.uid||''):''),ownerName:m?.nome||'',type:'produto',productType:ptype,status:$('#productStatus').value,title,description:$('#productNotes').value.trim(),dueDate:$('#productDue').value,startDate:new Date().toISOString().slice(0,10),progress:$('#productStatus').value==='concluido'?100:$('#productStatus').value==='revisao'?80:$('#productStatus').value==='andamento'?45:5,updatedAt:FB().serverTimestamp()};try{const ref=await FB().addDoc(FB().collection(window.db,'rede_activities'),{...data,createdAt:FB().serverTimestamp()});await awardPoints(ownerId,'product',ref.id,title);closeOverlay('productOverlay');toast('Produto registrado.')}catch(e){console.error(e);toast(e?.code==='permission-denied'?'O Firestore bloqueou o produto.':'Erro ao registrar produto.')}}
function dossierRowsFor(m){const acts=state.activities.filter(a=>a.ownerId===m.id).sort((a,b)=>_activityTime(b)-_activityTime(a)),repo=memberRepositoryItems(m.id),cron=buildCronoItems().filter(x=>x.ownerId===m.id);return {acts,repo,cron,checkins:acts.filter(a=>a.type==='checkin'),products:acts.filter(a=>a.type==='produto')}}
function dossierHTML(m){const {acts,repo,cron,checkins,products}=dossierRowsFor(m),nv=NIVEIS[m.nivel]||{color:'#607d8b',short:m.nivel||''},h=memberHealth(m);const table=acts.length?`<table class="dossier-table"><thead><tr><th>Tipo</th><th>Registro</th><th>Status</th><th>Data/prazo</th></tr></thead><tbody>${acts.map(a=>`<tr><td>${esc(ACTIVITY_TYPES[a.type]||a.type)}</td><td><b>${esc(a.title||'')}</b>${a.description?'<br>'+esc(a.description):''}</td><td>${esc(ACTIVITY_STATUS[a.status]||a.status||'')}</td><td>${esc(a.dueDate?fmtDate(a.dueDate):a.startDate?fmtDate(a.startDate):'')}</td></tr>`).join('')}</tbody></table>`:'<div class="dash-empty">Sem registros.</div>';return `<article class="dossier-sheet"><div class="dossier-head"><div class="dossier-avatar" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="dossier-name">${esc(m.nome)}</div><div class="dossier-meta">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.bolsista?' · Bolsista':''} · Saúde: ${h.level==='green'?'em dia':h.level==='yellow'?'atenção':'intervenção'}</div></div></div><section class="dossier-section"><h3>★ Pergunta científica</h3><div class="dossier-question">${esc(m.perguntaCientifica||'Ainda não preenchida.')}</div></section><section class="dossier-section"><h3>Identidade do projeto</h3><table class="dossier-table"><tr><th>Objetivo</th><td>${esc(m.objetivoProjeto||'—')}</td></tr><tr><th>Público atendido</th><td>${esc(m.grupoAtendido||'—')}</td></tr><tr><th>Visibilidade</th><td>${esc(m.ideiaVisibilidade||'—')}</td></tr><tr><th>Outros</th><td>${esc(m.projetoOutros||'—')}</td></tr></table></section

--- occurrence 3 at char 686134 ---
(e){console.error(e);toast(e.message||'Não consegui conectar ao Google Agenda.')}};
if($('#syncAllGoogleBtn'))$('#syncAllGoogleBtn').onclick=syncAllPrivateToGoogle;
if($('#pullGoogleBtn'))$('#pullGoogleBtn').onclick=pullGoogleToCarbonautas;
if($('#medalPrev'))$('#medalPrev').onclick=()=>shiftMedalMonth(-1);
if($('#medalNext'))$('#medalNext').onclick=()=>shiftMedalMonth(1);
if($('#medalToday'))$('#medalToday').onclick=()=>{medalCursor=new Date(new Date().getFullYear(),new Date().getMonth(),1);medalLoadedKey='';renderMedals()};
if($('#reloadGameBtn'))$('#reloadGameBtn').onclick=()=>{const f=$('#carbonautaGameFrame');if(f)f.src='./game.html?v=P28&t='+Date.now()};
window.addEventListener('message',e=>{const frame=$('#carbonautaGameFrame');if(!frame||e.source!==frame.contentWindow)return;if(location.origin!=='null'&&e.origin!==location.origin)return;const d=e.data||{};if(d.type==='carbonautas-game-result'&&d.completed===true)reportGameReward(d.score)});

$('#chatInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(120,e.target.scrollHeight)+'px'});
$('#chatInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat()}});
$('#sendBtn').onclick=sendChat;
if($('#mOnlyMine')) $('#mOnlyMine').onchange=renderRecados;
if($('#muralRecadoBtn')) $('#muralRecadoBtn').onclick=openRecado;
const muralCommandEl=$('#muralCommand'), muralCommandToggle=$('#muralCommandToggle');
function syncMuralCommandToggle(){ if(!muralCommandEl||!muralCommandToggle) return; muralCommandToggle.textContent=muralCommandEl.classList.contains('collapsed') ? 'Mostrar atalhos' : 'Ocultar atalhos'; }
if(muralCommandToggle){ muralCommandToggle.onclick=()=>{ muralCommandEl.classList.toggle('collapsed'); syncMuralCommandToggle(); }; syncMuralCommandToggle(); }
if($('#goMuralBtn')) $('#goMuralBtn').onclick=()=>switchView('mural');
if($('#muralNewBtn')) $('#muralNewBtn').onclick=()=>openPostModal('atualizacao');
if($('#muralDirectTopBtn')) $('#muralDirectTopBtn').onclick=openRecado;
if($('#muralDirectBtn')) $('#muralDirectBtn').onclick=openRecado;

$('#dashCheckinBtn').onclick=()=>openCheckin();
$('#dashProductBtn').onclick=()=>openProduct();
$('#dashMedalsBtn').onclick=()=>switchView('medalhas');
$('#dashGameBtn').onclick=()=>switchView('jogo');
$('#dashDossierBtn').onclick=()=>openDossier();
$('#dashNewTaskBtn').onclick=()=>openActivityPreset('pendencia');
$('#dashProductsAllBtn').onclick=()=>switchView('track');
$('#dashBackupBtn').onclick=exportGeneralBackup;
$('#trackCheckinBtn').onclick=()=>openCheckin();
$('#trackProductBtn').onclick=()=>openProduct();
$('#trackDossierBtn').onclick=()=>openDossier();
$('#checkinOwner').onchange=loadCheckinForSelection; $('#checkinMonth').onchange=loadCheckinForSelection; $('#saveCheckinBtn').onclick=saveCheckin;
$('#saveProductBtn').onclick=saveProduct;
$('#dossierPerson').onchange=renderDossier; $('#dossierExcelBtn').onclick=exportDossierExcel; $('#dossierCsvBtn').onclick=exportDossierCsv; $('#dossierWordBtn').onclick=exportDossierWord; $('#dossierPrintBtn').onclick=printDossier;
$('#addActivityBtn').onclick=()=>openActivity(null);
$('#trackOwner').onchange=renderTrack; $('#trackStatus').onchange=renderTrack; $('#trackMine').onchange=renderTrack;
$('#activityProgress').oninput=e=>$('#activityProgressLabel').textContent=e.target.value+'%'; $('#activityType').onchange=syncActivityTypeUi;
$('#saveActivityBtn').onclick=saveActivity; $('#deleteActivityBtn').onclick=deleteActivity;
$('#saveProjectProfileBtn').onclick=saveProjectProfile;
if($('#newPostBtn')) $('#newPostBtn').onclick=()=>openPostModal('atualizacao'); if($('#muralPostBtn')) $('#muralPostBtn').onclick=()=>openPostModal('atualizacao'); $$('[data-mural-kind]').forEach(b=>b.onclick=()=>openMuralAction(b.dataset.muralKind)); $$('[data-mf]').forEach(b=>b.onclick=()=>{muralFilter=b.dataset.mf;$$('[data-mf]').forEach(x=>x.classList.toggle('on',x===b));renderMural();if($('#muralMsgs'))$('#muralMsgs').scrollTop=0;}); $('#postFilePick').onclick=()=>$('#postFile').click(); $('#postFile').onchange=e=>handlePostFile(e.target.files[0]); $('#savePostBtn').onclick=saveFeedPost;

$('#addPhotoBtn').onclick=openPhotoModal;
$('#gPickBtn').onclick=()=>$('#gFile').click();
$('#gFile').onchange=e=>{handleGalleryFiles(e.target.files);e.target.value=''};
$('#gProjeto').onchange=e=>{$('#gProjetoOutroRow').style.display=e.target.value==='__outro'?'block':'none'};
const gPasteZone=$('#gPasteZone');
if(gPasteZone){
  gPasteZone.addEventListener('paste',e=>{const item=[...(e.clipboardData?.items||[])].find(x=>x.type?.startsWith('image/'));if(item){e.preventDefault();handleGalleryFile(item.getAsFile(),true)}});
  gPasteZone.addEventListener('dragover',e=>{e.preventDefault();gPasteZone.classList.add('drag')});
  gPasteZone.addEventListener('dragleave',()=>gPasteZone.classList.remove('drag'));
  gPasteZone.addEventListener('drop',e=>{e.preventDefault();gPasteZone.classList.remove('drag');handleGalleryFiles([...(e.dataTransfer?.files||[])],true)});
}
$('#savePhotoBtn').onclick=savePhoto;
['fProjeto','fPessoa','fAno','fTipo'].forEach(id=>{const el=$('#'+id);if(el)el.onchange=renderFotos});
if($('#fBusca'))$('#fBusca').oninput=()=>{clearTimeout(window.__fBuscaT);window.__fBuscaT=setTimeout(renderFotos,180)};
if($('#fLimpar'))$('#fLimpar').onclick=()=>{['fProjeto','fPessoa','fAno','fTipo','fBusca'].forEach(id=>{const el=$('#'+id);if(el)el.value=''});renderFotos()};
if($('#fotosSelectBtn'))$('#fotosSelectBtn').onclick=toggleFotoSelecting;
if($('#fMais'))$('#fMais').onclick=()=>{const w=$('#fMais').closest('.mural-filters');w.classList.toggle('open');$('#fMais').textContent=w.classList.contains('open

--- occurrence 4 at char 686559 ---
{medalCursor=new Date(new Date().getFullYear(),new Date().getMonth(),1);medalLoadedKey='';renderMedals()};
if($('#reloadGameBtn'))$('#reloadGameBtn').onclick=()=>{const f=$('#carbonautaGameFrame');if(f)f.src='./game.html?v=P28&t='+Date.now()};
window.addEventListener('message',e=>{const frame=$('#carbonautaGameFrame');if(!frame||e.source!==frame.contentWindow)return;if(location.origin!=='null'&&e.origin!==location.origin)return;const d=e.data||{};if(d.type==='carbonautas-game-result'&&d.completed===true)reportGameReward(d.score)});

$('#chatInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(120,e.target.scrollHeight)+'px'});
$('#chatInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat()}});
$('#sendBtn').onclick=sendChat;
if($('#mOnlyMine')) $('#mOnlyMine').onchange=renderRecados;
if($('#muralRecadoBtn')) $('#muralRecadoBtn').onclick=openRecado;
const muralCommandEl=$('#muralCommand'), muralCommandToggle=$('#muralCommandToggle');
function syncMuralCommandToggle(){ if(!muralCommandEl||!muralCommandToggle) return; muralCommandToggle.textContent=muralCommandEl.classList.contains('collapsed') ? 'Mostrar atalhos' : 'Ocultar atalhos'; }
if(muralCommandToggle){ muralCommandToggle.onclick=()=>{ muralCommandEl.classList.toggle('collapsed'); syncMuralCommandToggle(); }; syncMuralCommandToggle(); }
if($('#goMuralBtn')) $('#goMuralBtn').onclick=()=>switchView('mural');
if($('#muralNewBtn')) $('#muralNewBtn').onclick=()=>openPostModal('atualizacao');
if($('#muralDirectTopBtn')) $('#muralDirectTopBtn').onclick=openRecado;
if($('#muralDirectBtn')) $('#muralDirectBtn').onclick=openRecado;

$('#dashCheckinBtn').onclick=()=>openCheckin();
$('#dashProductBtn').onclick=()=>openProduct();
$('#dashMedalsBtn').onclick=()=>switchView('medalhas');
$('#dashGameBtn').onclick=()=>switchView('jogo');
$('#dashDossierBtn').onclick=()=>openDossier();
$('#dashNewTaskBtn').onclick=()=>openActivityPreset('pendencia');
$('#dashProductsAllBtn').onclick=()=>switchView('track');
$('#dashBackupBtn').onclick=exportGeneralBackup;
$('#trackCheckinBtn').onclick=()=>openCheckin();
$('#trackProductBtn').onclick=()=>openProduct();
$('#trackDossierBtn').onclick=()=>openDossier();
$('#checkinOwner').onchange=loadCheckinForSelection; $('#checkinMonth').onchange=loadCheckinForSelection; $('#saveCheckinBtn').onclick=saveCheckin;
$('#saveProductBtn').onclick=saveProduct;
$('#dossierPerson').onchange=renderDossier; $('#dossierExcelBtn').onclick=exportDossierExcel; $('#dossierCsvBtn').onclick=exportDossierCsv; $('#dossierWordBtn').onclick=exportDossierWord; $('#dossierPrintBtn').onclick=printDossier;
$('#addActivityBtn').onclick=()=>openActivity(null);
$('#trackOwner').onchange=renderTrack; $('#trackStatus').onchange=renderTrack; $('#trackMine').onchange=renderTrack;
$('#activityProgress').oninput=e=>$('#activityProgressLabel').textContent=e.target.value+'%'; $('#activityType').onchange=syncActivityTypeUi;
$('#saveActivityBtn').onclick=saveActivity; $('#deleteActivityBtn').onclick=deleteActivity;
$('#saveProjectProfileBtn').onclick=saveProjectProfile;
if($('#newPostBtn')) $('#newPostBtn').onclick=()=>openPostModal('atualizacao'); if($('#muralPostBtn')) $('#muralPostBtn').onclick=()=>openPostModal('atualizacao'); $$('[data-mural-kind]').forEach(b=>b.onclick=()=>openMuralAction(b.dataset.muralKind)); $$('[data-mf]').forEach(b=>b.onclick=()=>{muralFilter=b.dataset.mf;$$('[data-mf]').forEach(x=>x.classList.toggle('on',x===b));renderMural();if($('#muralMsgs'))$('#muralMsgs').scrollTop=0;}); $('#postFilePick').onclick=()=>$('#postFile').click(); $('#postFile').onchange=e=>handlePostFile(e.target.files[0]); $('#savePostBtn').onclick=saveFeedPost;

$('#addPhotoBtn').onclick=openPhotoModal;
$('#gPickBtn').onclick=()=>$('#gFile').click();
$('#gFile').onchange=e=>{handleGalleryFiles(e.target.files);e.target.value=''};
$('#gProjeto').onchange=e=>{$('#gProjetoOutroRow').style.display=e.target.value==='__outro'?'block':'none'};
const gPasteZone=$('#gPasteZone');
if(gPasteZone){
  gPasteZone.addEventListener('paste',e=>{const item=[...(e.clipboardData?.items||[])].find(x=>x.type?.startsWith('image/'));if(item){e.preventDefault();handleGalleryFile(item.getAsFile(),true)}});
  gPasteZone.addEventListener('dragover',e=>{e.preventDefault();gPasteZone.classList.add('drag')});
  gPasteZone.addEventListener('dragleave',()=>gPasteZone.classList.remove('drag'));
  gPasteZone.addEventListener('drop',e=>{e.preventDefault();gPasteZone.classList.remove('drag');handleGalleryFiles([...(e.dataTransfer?.files||[])],true)});
}
$('#savePhotoBtn').onclick=savePhoto;
['fProjeto','fPessoa','fAno','fTipo'].forEach(id=>{const el=$('#'+id);if(el)el.onchange=renderFotos});
if($('#fBusca'))$('#fBusca').oninput=()=>{clearTimeout(window.__fBuscaT);window.__fBuscaT=setTimeout(renderFotos,180)};
if($('#fLimpar'))$('#fLimpar').onclick=()=>{['fProjeto','fPessoa','fAno','fTipo','fBusca'].forEach(id=>{const el=$('#'+id);if(el)el.value=''});renderFotos()};
if($('#fotosSelectBtn'))$('#fotosSelectBtn').onclick=toggleFotoSelecting;
if($('#fMais'))$('#fMais').onclick=()=>{const w=$('#fMais').closest('.mural-filters');w.classList.toggle('open');$('#fMais').textContent=w.classList.contains('open')?'Menos':'Filtros'};
if($('#muralSelAll'))$('#muralSelAll').onclick=()=>{fotosFiltradas().forEach(p=>fotoSelected.add(p.id));renderFotos()};
if($('#muralSelNone'))$('#muralSelNone').onclick=()=>{fotoSelected.clear();renderFotos()};
if($('#muralZipBtn'))$('#muralZipBtn').onclick=downloadSelectedPhotos;
$('#lbClose').onclick=closeLightbox;
$('#lbDel').onclick=deleteLightboxPhoto;
$('#lightbox').addEventListener('click',e=

--- occurrence 5 at char 1306755 ---
ton" data-p97-action="preset" data-kind="pendencia" data-mid="${esc97(m.id)}">⚠️ Pendência</button><button type="button" data-p97-action="preset" data-kind="correcao" data-mid="${esc97(m.id)}">✍️ Correção</button></div>`:''}`};
}
function buildDeckCards(m){const can=memberCanEdit(m.id);return [overviewCard(m,can),academicCard(m,can),projectCard(m,can),timelineCard(m,can),historyCard(m,can),pendingCard(m,can)]}

function ensureDeck(){
  let ov=$('#p97Deck');if(ov)return ov;
  ov=document.createElement('div');ov.id='p97Deck';ov.hidden=true;ov.innerHTML=`
    <div class="p97-backdrop" data-p97-close></div>
    <section class="p97-table" role="dialog" aria-modal="true" aria-label="Acompanhamento em cartas">
      <header class="p97-deck-head"><button type="button" data-p97-close>← Pastas</button><div class="p97-deck-title"><span id="p97DeckKicker">ACOMPANHAMENTO</span><b id="p97DeckName">Carbonauta</b></div><button type="button" class="x" data-p97-close>×</button></header>
      <div class="p97-stage-wrap"><div class="p97-stage" id="p97Stage"></div></div>
      <footer class="p97-deck-nav"><button type="button" class="prev">← Anterior</button><div class="p97-deck-count"><small id="p97CardTitle">CARTA</small><b id="p97CardCount">1 de 6</b></div><button type="button" class="next">Próxima →</button></footer>
    </section>`;
  document.body.appendChild(ov);
  ov.addEventListener('click',e=>{
    if(e.target.closest('[data-p97-close]')){closeDeck();return}
    const a=e.target.closest('[data-p97-action]');if(!a)return;
    e.preventDefault();e.stopPropagation();
    const mid=a.dataset.mid||deckMemberId,action=a.dataset.p97Action;
    if(action==='activity')call('openActivity',null,mid);
    else if(action==='dossier')call('openDossier',mid);
    else if(action==='repo')call('openMemberRepository',mid);
    else if(action==='academic-edit')call('openAcademicCardEditor',mid);
    else if(action==='academic-download')call('downloadAcademicCard',mid);
    else if(action==='academic-share')call('shareAcademicCard',mid);
    else if(action==='project')call('openProjectProfile',mid);
    else if(action==='checkin')call('openCheckin',mid);
    else if(action==='product')call('openProduct',mid);
    else if(action==='preset')call('openActivityPreset',a.dataset.kind||'pendencia',mid);
    else if(action==='edit')call('editActivity',a.dataset.id);
  });
  $('.p97-deck-nav .prev',ov).addEventListener('click',()=>navigateDeck(-1));
  $('.p97-deck-nav .next',ov).addEventListener('click',()=>navigateDeck(1));
  const stage=$('#p97Stage',ov);
  stage.addEventListener('touchstart',e=>{if(e.touches.length!==1)return;deckTouch={x:e.touches[0].clientX,y:e.touches[0].clientY,t:Date.now()}},{passive:true});
  stage.addEventListener('touchend',e=>{if(!deckTouch)return;const p=e.changedTouches[0],dx=p.clientX-deckTouch.x,dy=p.clientY-deckTouch.y,dt=Date.now()-deckTouch.t;deckTouch=null;if(dt<750&&Math.abs(dx)>52&&Math.abs(dx)>Math.abs(dy)*1.2)navigateDeck(dx<0?1:-1)},{passive:true});
  return ov;
}
function openDeck(memberId){
  const s=appState(),m=(s?.members||[]).find(x=>x.id===memberId);if(!m)return;
  const ov=ensureDeck();deckMemberId=memberId;deckCardIndex=0;deckCards=buildDeckCards(m);
  const [soft,border,accent]=colorsFor(m.nome),table=$('.p97-table',ov);table.style.setProperty('--soft',soft);table.style.setProperty('--border',border);table.style.setProperty('--accent',accent);
  $('#p97DeckName').textContent=m.nome;ov.hidden=false;document.documentElement.style.overflow='hidden';renderDeckCard(0,true);
}
function closeDeck(){const ov=$('#p97Deck');if(!ov||ov.hidden)return;ov.hidden=true;$('#p97Stage').innerHTML='';deckMemberId='';deckCards=[];deckCardIndex=0;deckAnimating=false;document.documentElement.style.overflow=''}
function renderDeckCard(dir=0,first=false){
  if(!deckCards.length)return;const stage=$('#p97Stage'),old=$('.p97-playing-card',stage),card=deckCards[deckCardIndex];
  const put=()=>{stage.innerHTML='';const el=document.createElement('article');el.className='p97-playing-card';el.innerHTML=card.html;stage.appendChild(el);$('#p97DeckKicker').textContent=card.kicker||'ACOMPANHAMENTO';$('#p97CardTitle').textContent=card.title;$('#p97CardCount').textContent=`${deckCardIndex+1} de ${deckCards.length}`;requestAnimationFrame(()=>el.classList.add('drop'));el.scrollTop=0;deckAnimating=false};
  if(!old||first){put();return}deckAnimating=true;old.classList.remove('drop');old.classList.add(dir>=0?'out-left':'out-right');setTimeout(put,175);
}
function navigateDeck(delta){if(deckAnimating||!deckCards.length)return;deckCardIndex=(deckCardIndex+delta+deckCards.length)%deckCards.length;renderDeckCard(delta,false)}
function refreshOpenDeck(){
  if(!deckMemberId)return;const s=appState(),m=(s?.members||[]).find(x=>x.id===deckMemberId);if(!m){closeDeck();return}
  const oldTitle=deckCards[deckCardIndex]?.title;deckCards=buildDeckCards(m);const i=deckCards.findIndex(c=>c.title===oldTitle);if(i>=0)deckCardIndex=i;renderDeckCard(0,true);
}
window.p202RefreshAcademicDeck=refreshOpenDeck;

function renderAfterTrack(){renderFolders();refreshOpenDeck()}
function installTrackWrapper(){
  if(trackWrapped)return;
  let original=null;try{original=window.renderTrack||renderTrack}catch(_e){}
  if(typeof original!=='function'){setTimeout(installTrackWrapper,120);return}
  if(original.__p97Wrapped){trackWrapped=true;return}
  const wrapped=function(){const r=original.apply(this,arguments);requestAnimationFrame(renderAfterTrack);return r};
  wrapped.__p97Wrapped=true;wrapped.__p97Original=original;
  try{renderTrack=wrapped}catch(_e){}window.renderTrack=wrapped;trackWrapped=true;
  try{if(document.body.dataset.view==='track'||$('#v


===== openActivityPreset =====

--- occurrence 1 at char 573699 ---
id);
    const periods=acts.filter(a=>TRACK_PERIOD_TYPES.has(a.type));const follows=acts.filter(a=>!TRACK_PERIOD_TYPES.has(a.type)&&!TRACK_LEGACY_TYPES.has(a.type)).sort((a,b)=>{const ad=_activityDate(a),bd=_activityDate(b);return (bd||'').localeCompare(ad||'')||_activityTime(b)-_activityTime(a)});
    const pending=follows.filter(a=>_isPending(a)||_isOverdue(a));const lastOrient=follows.filter(a=>a.type==='orientacao').sort((a,b)=>(_activityDate(b)||'').localeCompare(_activityDate(a)||''))[0];const nextDue=follows.filter(a=>!_activityDone(a)&&a.dueDate&&!dateInfo(a.dueDate)?.over).sort((a,b)=>a.dueDate.localeCompare(b.dueDate))[0];const corrections=follows.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
    const nv=NIVEIS[m.nivel]||{short:m.nivel||'',color:'#607d8b'};
    const periodsHTML=_scholarPeriodHTML(m)+periods.map(a=>_periodHTML(a.type,a.title,a.startDate,a.dueDate,can,a.id,a.progress)).join('');
    const listHTML=follows.slice(0,8).map(a=>_followItemHTML(a,can)).join('');
    return `<section class="track-person"><div class="track-person-head"><div class="track-person-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="track-person-title">${esc(m.nome)}</div><div class="track-person-sub">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openAct

--- occurrence 2 at char 573792 ---
er(a=>!TRACK_PERIOD_TYPES.has(a.type)&&!TRACK_LEGACY_TYPES.has(a.type)).sort((a,b)=>{const ad=_activityDate(a),bd=_activityDate(b);return (bd||'').localeCompare(ad||'')||_activityTime(b)-_activityTime(a)});
    const pending=follows.filter(a=>_isPending(a)||_isOverdue(a));const lastOrient=follows.filter(a=>a.type==='orientacao').sort((a,b)=>(_activityDate(b)||'').localeCompare(_activityDate(a)||''))[0];const nextDue=follows.filter(a=>!_activityDone(a)&&a.dueDate&&!dateInfo(a.dueDate)?.over).sort((a,b)=>a.dueDate.localeCompare(b.dueDate))[0];const corrections=follows.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
    const nv=NIVEIS[m.nivel]||{short:m.nivel||'',color:'#607d8b'};
    const periodsHTML=_scholarPeriodHTML(m)+periods.map(a=>_periodHTML(a.type,a.title,a.startDate,a.dueDate,can,a.id,a.progress)).join('');
    const listHTML=follows.slice(0,8).map(a=>_followItemHTML(a,can)).join('');
    return `<section class="track-person"><div class="track-person-head"><div class="track-person-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="track-person-title">${esc(m.nome)}</div><div class="track-person-sub">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openActivity(null,ownerPreset);setTimeout(()=>{if($('#activityType'))$('#activityType').value=type;i

--- occurrence 3 at char 577152 ---
)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openActivity(null,ownerPreset);setTimeout(()=>{if($('#activityType'))$('#activityType').value=type;if(type==='pendencia'){$('#activityStatus').value='afazer';$('#activityName').placeholder='Ex.: Enviar capítulo revisado';}if(type==='correcao'){$('#activityStatus').value='recebido';$('#activityName').placeholder='Ex.: Correção do capítulo 2';}},20)}
async function notifyAcompanhamentoOwner(ownerId,meta={}){if(!isAdmin||!ownerId||ownerId===myId)return;const me=currentActor(),f=FB();try{await f.addDoc(f.collection(window.db,'rede_notifications'),{recipientId:ownerId,senderId:me?.id||myId||'',senderUid:window.auth.currentUser?.uid||'',senderName:me?.name||'Coordenação',kind:meta.kind||'acompanhamento',title:meta.title||'Novo item de acompanhamento',message:(meta.message||'').slice(0,240),sourceType:'acompanhamento',sourceId:meta.sourceId||'',ownerId,read:false,ts:f.serverTimestamp()})}catch(e){console.warn('Não foi possível notificar o acompanhamento',e)}}
async function saveActivity(){
  if(activitySaving)return false;
  const ownerId=isAdmin?$('#activityOwnerSel').value:myId,type=$('#activityType').value;
  const title=$('#activityName').value.trim()||ACTIVITY_TYPES[type]||'Acompanhamento';
  if(!ownerId){toast('Selecione o Carbonauta.');return false}
  const startDate=$('#activityStart').value,dueDate=$('#activityDue').value;
  if(startDate&&dueDate&&startDate>dueDate){toast('A data final não pode ser anterior ao início.');return false}
  const m=memberById(ownerId),status=$('#activityStatus').value;
  if(!isAdmin&&['aprovado','reprovado'].includes(status)){toast('A aprovação fica com o orientador.');return false}
  const f=FB(),actorUid=window.auth.currentUser?.uid||'',wasNew=!editingActivityId;
  const ref=editingActivityId?f.doc(window.db,'rede_activities',editingActivityId):f.doc(f.collection(window.db,'rede_activities'));
  const data={ownerId,ownerName:m?.nome||'',type,status,title,description:$('#activityDesc').value.trim(),startDate,dueDate,
    progress:Math.max(0,Math.min(100,+$('#activityProgress').value||0)),updatedAt:f.serverTimestamp()};
  const button=$('#saveActivityBtn');activitySaving=true;button.disabled=true;button.textContent='Salvando…';
  try{
    await f.runTransaction(window.db,async tx=>{
      const snap=await tx.get(ref);
      if(!wasNew&&!snap.exists())throw new Error('Este registro foi excluído em outra tela. Reabra o acompanhamento.');
      if(snap.exists()){
        const previous=snap.data();
        if((previous.updatedAt?.toMillis?.()??null)!==activityLoadedRevision)throw new Error('Esse registro mudou em outra tela. Reabra antes de salvar para não perder nenhuma alteração.');
      }else{data.createdAt=f.serverTimestamp();data.ownerUid=(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?actorUid:'')}
      tx.set(ref,data,{merge:true});
    });
    activitySaving=false;closeOverlay('activityOverlay');toast('Acompanhamento salvo.');
    // Rewards/notifications must not turn a successful save into a false error.
    try{
      if(wasNew)await awardPoints(ownerId,'activity_created',ref.id,title);
      if(['concluido','aprovado','feito'].includes(status)||data.progress>=100)await awardPoints(ownerId,'activity_completed',ref.id,title);
      if(isAdmin&&ownerId!==myId)await notifyAcompanhamentoOwner(ownerId,{kind:type,title:'📌 Acompanhamento atualizado',message:`${title}${dueDate?' · prazo '+fmtDate(dueDate):''}`,sourceId:ref.id});
    }catch(e){

--- occurrence 4 at char 686336 ---
Btn').onclick=pullGoogleToCarbonautas;
if($('#medalPrev'))$('#medalPrev').onclick=()=>shiftMedalMonth(-1);
if($('#medalNext'))$('#medalNext').onclick=()=>shiftMedalMonth(1);
if($('#medalToday'))$('#medalToday').onclick=()=>{medalCursor=new Date(new Date().getFullYear(),new Date().getMonth(),1);medalLoadedKey='';renderMedals()};
if($('#reloadGameBtn'))$('#reloadGameBtn').onclick=()=>{const f=$('#carbonautaGameFrame');if(f)f.src='./game.html?v=P28&t='+Date.now()};
window.addEventListener('message',e=>{const frame=$('#carbonautaGameFrame');if(!frame||e.source!==frame.contentWindow)return;if(location.origin!=='null'&&e.origin!==location.origin)return;const d=e.data||{};if(d.type==='carbonautas-game-result'&&d.completed===true)reportGameReward(d.score)});

$('#chatInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(120,e.target.scrollHeight)+'px'});
$('#chatInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat()}});
$('#sendBtn').onclick=sendChat;
if($('#mOnlyMine')) $('#mOnlyMine').onchange=renderRecados;
if($('#muralRecadoBtn')) $('#muralRecadoBtn').onclick=openRecado;
const muralCommandEl=$('#muralCommand'), muralCommandToggle=$('#muralCommandToggle');
function syncMuralCommandToggle(){ if(!muralCommandEl||!muralCommandToggle) return; muralCommandToggle.textContent=muralCommandEl.classList.contains('collapsed') ? 'Mostrar atalhos' : 'Ocultar atalhos'; }
if(muralCommandToggle){ muralCommandToggle.onclick=()=>{ muralCommandEl.classList.toggle('collapsed'); syncMuralCommandToggle(); }; syncMuralCommandToggle(); }
if($('#goMuralBtn')) $('#goMuralBtn').onclick=()=>switchView('mural');
if($('#muralNewBtn')) $('#muralNewBtn').onclick=()=>openPostModal('atualizacao');
if($('#muralDirectTopBtn')) $('#muralDirectTopBtn').onclick=openRecado;
if($('#muralDirectBtn')) $('#muralDirectBtn').onclick=openRecado;

$('#dashCheckinBtn').onclick=()=>openCheckin();
$('#dashProductBtn').onclick=()=>openProduct();
$('#dashMedalsBtn').onclick=()=>switchView('medalhas');
$('#dashGameBtn').onclick=()=>switchView('jogo');
$('#dashDossierBtn').onclick=()=>openDossier();
$('#dashNewTaskBtn').onclick=()=>openActivityPreset('pendencia');
$('#dashProductsAllBtn').onclick=()=>switchView('track');
$('#dashBackupBtn').onclick=exportGeneralBackup;
$('#trackCheckinBtn').onclick=()=>openCheckin();
$('#trackProductBtn').onclick=()=>openProduct();
$('#trackDossierBtn').onclick=()=>openDossier();
$('#checkinOwner').onchange=loadCheckinForSelection; $('#checkinMonth').onchange=loadCheckinForSelection; $('#saveCheckinBtn').onclick=saveCheckin;
$('#saveProductBtn').onclick=saveProduct;
$('#dossierPerson').onchange=renderDossier; $('#dossierExcelBtn').onclick=exportDossierExcel; $('#dossierCsvBtn').onclick=exportDossierCsv; $('#dossierWordBtn').onclick=exportDossierWord; $('#dossierPrintBtn').onclick=printDossier;
$('#addActivityBtn').onclick=()=>openActivity(null);
$('#trackOwner').onchange=renderTrack; $('#trackStatus').onchange=renderTrack; $('#trackMine').onchange=renderTrack;
$('#activityProgress').oninput=e=>$('#activityProgressLabel').textContent=e.target.value+'%'; $('#activityType').onchange=syncActivityTypeUi;
$('#saveActivityBtn').onclick=saveActivity; $('#deleteActivityBtn').onclick=deleteActivity;
$('#saveProjectProfileBtn').onclick=saveProjectProfile;
if($('#newPostBtn')) $('#newPostBtn').onclick=()=>openPostModal('atualizacao'); if($('#muralPostBtn')) $('#muralPostBtn').onclick=()=>openPostModal('atualizacao'); $$('[data-mural-kind]').forEach(b=>b.onclick=()=>openMuralAction(b.dataset.muralKind)); $$('[data-mf]').forEach(b=>b.onclick=()=>{muralFilter=b.dataset.mf;$$('[data-mf]').forEach(x=>x.classList.toggle('on',x===b));renderMural();if($('#muralMsgs'))$('#muralMsgs').scrollTop=0;}); $('#postFilePick').onclick=()=>$('#postFile').click(); $('#postFile').onchange=e=>handlePostFile(e.target.files[0]); $('#savePostBtn').onclick=saveFeedPost;

$('#addPhotoBtn').onclick=openPhotoModal;
$('#gPickBtn').onclick=()=>$('#gFile').click();
$('#gFile').onchange=e=>{handleGalleryFiles(e.target.files);e.target.value=''};
$('#gProjeto').onchange=e=>{$('#gProjetoOutroRow').style.display=e.target.value==='__outro'?'block':'none'};
const gPasteZone=$('#gPasteZone');
if(gPasteZone){
  gPasteZone.addEventListener('paste',e=>{const item=[...(e.clipboardData?.items||[])].find(x=>x.type?.startsWith('image/'));if(item){e.preventDefault();handleGalleryFile(item.getAsFile(),true)}});
  gPasteZone.addEventListener('dragover',e=>{e.preventDefault();gPasteZone.classList.add('drag')});
  gPasteZone.addEventListener('dragleave',()=>gPasteZone.classList.remove('drag'));
  gPasteZone.addEventListener('drop',e=>{e.preventDefault();gPasteZone.classList.remove('drag');handleGalleryFiles([...(e.dataTransfer?.files||[])],true)});
}
$('#savePhotoBtn').onclick=savePhoto;
['fProjeto','fPessoa','fAno','fTipo'].forEach(id=>{const el=$('#'+id);if(el)el.onchange=renderFotos});
if($('#fBusca'))$('#fBusca').oninput=()=>{clearTimeout(window.__fBuscaT);window.__fBuscaT=setTimeout(renderFotos,180)};
if($('#fLimpar'))$('#fLimpar').onclick=()=>{['fProjeto','fPessoa','fAno','fTipo','fBusca'].forEach(id=>{const el=$('#'+id);if(el)el.value=''});renderFotos()};
if($('#fotosSelectBtn'))$('#fotosSelectBtn').onclick=toggleFotoSelecting;
if($('#fMais'))$('#fMais').onclick=()=>{const w=$('#fMais').closest('.mural-filters');w.classList.toggle('open');$('#fMais').textContent=w.classList.contains('open')?'Menos':'Filtros'};
if($('#muralSelAll'))$('#muralSelAll').onclick=()=>{fotosFiltradas().forEach(p=>fotoSelected.add(p.id));renderFotos()};
if($('#muralSelNone'))$('#muralSelNone').onclick=()=>{fotoS

--- occurrence 5 at char 1306810 ---
a-mid="${esc97(m.id)}">⚠️ Pendência</button><button type="button" data-p97-action="preset" data-kind="correcao" data-mid="${esc97(m.id)}">✍️ Correção</button></div>`:''}`};
}
function buildDeckCards(m){const can=memberCanEdit(m.id);return [overviewCard(m,can),academicCard(m,can),projectCard(m,can),timelineCard(m,can),historyCard(m,can),pendingCard(m,can)]}

function ensureDeck(){
  let ov=$('#p97Deck');if(ov)return ov;
  ov=document.createElement('div');ov.id='p97Deck';ov.hidden=true;ov.innerHTML=`
    <div class="p97-backdrop" data-p97-close></div>
    <section class="p97-table" role="dialog" aria-modal="true" aria-label="Acompanhamento em cartas">
      <header class="p97-deck-head"><button type="button" data-p97-close>← Pastas</button><div class="p97-deck-title"><span id="p97DeckKicker">ACOMPANHAMENTO</span><b id="p97DeckName">Carbonauta</b></div><button type="button" class="x" data-p97-close>×</button></header>
      <div class="p97-stage-wrap"><div class="p97-stage" id="p97Stage"></div></div>
      <footer class="p97-deck-nav"><button type="button" class="prev">← Anterior</button><div class="p97-deck-count"><small id="p97CardTitle">CARTA</small><b id="p97CardCount">1 de 6</b></div><button type="button" class="next">Próxima →</button></footer>
    </section>`;
  document.body.appendChild(ov);
  ov.addEventListener('click',e=>{
    if(e.target.closest('[data-p97-close]')){closeDeck();return}
    const a=e.target.closest('[data-p97-action]');if(!a)return;
    e.preventDefault();e.stopPropagation();
    const mid=a.dataset.mid||deckMemberId,action=a.dataset.p97Action;
    if(action==='activity')call('openActivity',null,mid);
    else if(action==='dossier')call('openDossier',mid);
    else if(action==='repo')call('openMemberRepository',mid);
    else if(action==='academic-edit')call('openAcademicCardEditor',mid);
    else if(action==='academic-download')call('downloadAcademicCard',mid);
    else if(action==='academic-share')call('shareAcademicCard',mid);
    else if(action==='project')call('openProjectProfile',mid);
    else if(action==='checkin')call('openCheckin',mid);
    else if(action==='product')call('openProduct',mid);
    else if(action==='preset')call('openActivityPreset',a.dataset.kind||'pendencia',mid);
    else if(action==='edit')call('editActivity',a.dataset.id);
  });
  $('.p97-deck-nav .prev',ov).addEventListener('click',()=>navigateDeck(-1));
  $('.p97-deck-nav .next',ov).addEventListener('click',()=>navigateDeck(1));
  const stage=$('#p97Stage',ov);
  stage.addEventListener('touchstart',e=>{if(e.touches.length!==1)return;deckTouch={x:e.touches[0].clientX,y:e.touches[0].clientY,t:Date.now()}},{passive:true});
  stage.addEventListener('touchend',e=>{if(!deckTouch)return;const p=e.changedTouches[0],dx=p.clientX-deckTouch.x,dy=p.clientY-deckTouch.y,dt=Date.now()-deckTouch.t;deckTouch=null;if(dt<750&&Math.abs(dx)>52&&Math.abs(dx)>Math.abs(dy)*1.2)navigateDeck(dx<0?1:-1)},{passive:true});
  return ov;
}
function openDeck(memberId){
  const s=appState(),m=(s?.members||[]).find(x=>x.id===memberId);if(!m)return;
  const ov=ensureDeck();deckMemberId=memberId;deckCardIndex=0;deckCards=buildDeckCards(m);
  const [soft,border,accent]=colorsFor(m.nome),table=$('.p97-table',ov);table.style.setProperty('--soft',soft);table.style.setProperty('--border',border);table.style.setProperty('--accent',accent);
  $('#p97DeckName').textContent=m.nome;ov.hidden=false;document.documentElement.style.overflow='hidden';renderDeckCard(0,true);
}
function closeDeck(){const ov=$('#p97Deck');if(!ov||ov.hidden)return;ov.hidden=true;$('#p97Stage').innerHTML='';deckMemberId='';deckCards=[];deckCardIndex=0;deckAnimating=false;document.documentElement.style.overflow=''}
function renderDeckCard(dir=0,first=false){
  if(!deckCards.length)return;const stage=$('#p97Stage'),old=$('.p97-playing-card',stage),card=deckCards[deckCardIndex];
  const put=()=>{stage.innerHTML='';const el=document.createElement('article');el.className='p97-playing-card';el.innerHTML=card.html;stage.appendChild(el);$('#p97DeckKicker').textContent=card.kicker||'ACOMPANHAMENTO';$('#p97CardTitle').textContent=card.title;$('#p97CardCount').textContent=`${deckCardIndex+1} de ${deckCards.length}`;requestAnimationFrame(()=>el.classList.add('drop'));el.scrollTop=0;deckAnimating=false};
  if(!old||first){put();return}deckAnimating=true;old.classList.remove('drop');old.classList.add(dir>=0?'out-left':'out-right');setTimeout(put,175);
}
function navigateDeck(delta){if(deckAnimating||!deckCards.length)return;deckCardIndex=(deckCardIndex+delta+deckCards.length)%deckCards.length;renderDeckCard(delta,false)}
function refreshOpenDeck(){
  if(!deckMemberId)return;const s=appState(),m=(s?.members||[]).find(x=>x.id===deckMemberId);if(!m){closeDeck();return}
  const oldTitle=deckCards[deckCardIndex]?.title;deckCards=buildDeckCards(m);const i=deckCards.findIndex(c=>c.title===oldTitle);if(i>=0)deckCardIndex=i;renderDeckCard(0,true);
}
window.p202RefreshAcademicDeck=refreshOpenDeck;

function renderAfterTrack(){renderFolders();refreshOpenDeck()}
function installTrackWrapper(){
  if(trackWrapped)return;
  let original=null;try{original=window.renderTrack||renderTrack}catch(_e){}
  if(typeof original!=='function'){setTimeout(installTrackWrapper,120);return}
  if(original.__p97Wrapped){trackWrapped=true;return}
  const wrapped=function(){const r=original.apply(this,arguments);requestAnimationFrame(renderAfterTrack);return r};
  wrapped.__p97Wrapped=true;wrapped.__p97Original=original;
  try{renderTrack=wrapped}catch(_e){}window.renderTrack=wrapped;trackWrapped=true;
  try{if(document.body.dataset.view==='track'||$('#viewTrack')?.classList.contains('on'))wrapped()}catch(_e


===== editActivity =====

--- occurrence 1 at char 564771 ---
'<option value="">Todos os Carbonautas</option>'+list.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join(''); el.value=cur;
}
function _trackDate(d){if(!d)return null;const [y,m,day]=String(d).split('-').map(Number);return y&&m&&day?new Date(y,m-1,day):null}
function _daysText(n){const a=Math.abs(Math.round(n)); if(a<31)return `${a} ${a===1?'dia':'dias'}`; const mo=Math.floor(a/30),d=a%30; return `${mo} ${mo===1?'mês':'meses'}${d?' e '+d+' dias':''}`}
function trackPeriodInfo(startDate,endDate){
  const start=_trackDate(startDate),end=_trackDate(endDate),now=new Date();now.setHours(0,0,0,0);
  if(!start||!end)return {pct:0,label:'Período incompleto',remain:'informe início e fim',days:null};
  start.setHours(0,0,0,0);end.setHours(0,0,0,0);const total=Math.max(1,(end-start)/86400000),elapsed=(now-start)/86400000,days=(end-now)/86400000;
  const pct=Math.max(0,Math.min(100,Math.round(elapsed/total*100)));
  if(now<start)return {pct,label:'Ainda não iniciado',remain:`${_daysText(elapsed)} para começar`,days};
  if(now>end)return {pct:100,label:'Período encerrado',remain:`encerrado há ${_daysText(days)}`,days};
  return {pct,label:'Em andamento',remain:`${_daysText(days)} restantes`,days};
}
function _activityTime(a){return a.updatedAt?.toMillis?a.updatedAt.toMillis():(a.createdAt?.toMillis?a.createdAt.toMillis():0)}
function _activityDate(a){return a.startDate||a.dueDate||''}
function _activityDone(a){return ['concluido','aprovado'].includes(a?.status)}
function _isPending(a){return !_activityDone(a) && ['correcao','pendencia','entrega'].includes(a.type)}
function _isOverdue(a){return !_activityDone(a)&&a.dueDate&&dateInfo(a.dueDate)?.over}
function _periodHTML(type,title,start,end,canEdit,id,manualProgress){
  const pi=trackPeriodInfo(start,end),label=ACTIVITY_TYPES[type]||type, pct=(start&&end)?pi.pct:Math.max(0,Math.min(100,+manualProgress||0));
  return `<div class="period-card"><div class="period-top"><span class="period-type">${TRACK_ICONS[type]||'•'} ${esc(label)}</span>${title&&title!==label?`<span class="period-title">${esc(title)}</span>`:''}<span class="period-remain">${esc(pi.remain)}</span>${canEdit&&id?`<button class="follow-edit" onclick="editActivity('${id}')" title="Editar">✎</button>`:''}</div><div class="period-bar"><span style="width:${pct}%"></span></div><div class="period-foot"><span>${start?'Início '+fmtDate(start):'Início não informado'}</span><b>${pct}% do tempo</b><span>${end?'Fim '+fmtDate(end):'Fim não informado'}</span></div></div>`;
}
function _scholarPeriodHTML(m){
  if(!m.bolsista)return '';
  const pi=trackPeriodInfo(m.bolsaInicio,m.bolsaFim),title=m.bolsaModalidade||'Bolsa';
  return `<div class="period-card"><div class="period-top"><span class="period-type">🎓 Bolsa</span><span class="period-title">${esc(title)}</span><span class="period-remain">${esc(pi.remain)}</span></div><div class="period-bar"><span style="width:${pi.pct}%"></span></div><div class="period-foot"><span>${m.bolsaInicio?'Início '+fmtDate(m.bolsaInicio):'Início não informado'}</span><b>${pi.pct}% do tempo</b><span>${m.bolsaFim?'Fim '+fmtDate(m.bolsaFim):'Fim não informado'}</span></div></div>`;
}
function _followItemHTML(a,can){
  const over=_isOverdue(a), d=a.dueDate||a.startDate||'', status=ACTIVITY_STATUS[a.status]||a.status||'';
  return `<div class="follow-item"><div class="follow-icon">${TRACK_ICONS[a.type]||'•'}</div><div class="follow-main"><div class="follow-name">${esc(ACTIVITY_TYPES[a.type]||a.type||'Acompanhamento')} · ${esc(a.title||status)}</div>${a.description?`<div class="follow-desc">${esc(a.description)}</div>`:''}<div style="margin-top:4px"><span class="track-alert ${_activityDone(a)?'track-ok':over?'':'track-warn'}">${_activityDone(a)?esc(status||'Concluído'):over?'Atrasado':esc(status)}</span></div></div><div style="display:flex;gap:4px;align-items:start"><span class="follow-date ${over?'over':''}">${d?fmtDate(d):''}</span>${can?`<button class="follow-edit" onclick="editActivity('${a.id}')" title="Editar">✎</button>`:''}</div></div>`;
}
let projectProfileMemberId='';
function projectIdentityHTML(m,can){
  const q=(m.perguntaCientifica||'').trim(),obj=(m.objetivoProjeto||'').trim(),aud=(m.grupoAtendido||'').trim(),vis=(m.ideiaVisibilidade||'').trim(),oth=(m.projetoOutros||'').trim();
  const empty='<span class="project-empty">Ainda não preenchido.</span>';
  return `<section class="project-identity"><div class="project-identity-head"><div class="project-identity-title">🧭 Identidade do projeto</div>${can?`<button type="button" class="project-identity-edit" onclick="openProjectProfile('${m.id}')">✎ Editar projeto</button>`:''}</div><div class="science-question"><div class="sq-label">★ Pergunta científica</div><div class="sq-text">${q?esc(q):empty}</div></div><div class="project-identity-grid"><div class="project-identity-item"><div class="pi-label">Objetivo central</div><div class="pi-text">${obj?esc(obj):empty}</div></div><div class="project-identity-item"><div class="pi-label">Grupo / público atendido</div><div class="pi-text">${aud?esc(aud):empty}</div></div><div class="project-identity-item full"><div class="pi-label">Ideia de visibilidade</div><div class="pi-text">${vis?esc(vis):empty}</div></div>${oth?`<div class="project-identity-item full"><div class="pi-label">Outros aspectos</div><div class="pi-text">${esc(oth)}</div></div>`:''}</div></section>`;
}
function openProjectProfile(id){
  const m=memberById(id);if(!m)return;if(!(isAdmin||isMe(id))){toast('Somente o próprio Carbonauta ou o coordenador pode editar este projeto.');return}
  projectProfileMemberId=id;$('#projectQuestion').value=m.perguntaCientifica||'';$('#projectObjective').value=m.objetivoProjeto||'

--- occurrence 2 at char 566539 ---
dInfo(start,end),label=ACTIVITY_TYPES[type]||type, pct=(start&&end)?pi.pct:Math.max(0,Math.min(100,+manualProgress||0));
  return `<div class="period-card"><div class="period-top"><span class="period-type">${TRACK_ICONS[type]||'•'} ${esc(label)}</span>${title&&title!==label?`<span class="period-title">${esc(title)}</span>`:''}<span class="period-remain">${esc(pi.remain)}</span>${canEdit&&id?`<button class="follow-edit" onclick="editActivity('${id}')" title="Editar">✎</button>`:''}</div><div class="period-bar"><span style="width:${pct}%"></span></div><div class="period-foot"><span>${start?'Início '+fmtDate(start):'Início não informado'}</span><b>${pct}% do tempo</b><span>${end?'Fim '+fmtDate(end):'Fim não informado'}</span></div></div>`;
}
function _scholarPeriodHTML(m){
  if(!m.bolsista)return '';
  const pi=trackPeriodInfo(m.bolsaInicio,m.bolsaFim),title=m.bolsaModalidade||'Bolsa';
  return `<div class="period-card"><div class="period-top"><span class="period-type">🎓 Bolsa</span><span class="period-title">${esc(title)}</span><span class="period-remain">${esc(pi.remain)}</span></div><div class="period-bar"><span style="width:${pi.pct}%"></span></div><div class="period-foot"><span>${m.bolsaInicio?'Início '+fmtDate(m.bolsaInicio):'Início não informado'}</span><b>${pi.pct}% do tempo</b><span>${m.bolsaFim?'Fim '+fmtDate(m.bolsaFim):'Fim não informado'}</span></div></div>`;
}
function _followItemHTML(a,can){
  const over=_isOverdue(a), d=a.dueDate||a.startDate||'', status=ACTIVITY_STATUS[a.status]||a.status||'';
  return `<div class="follow-item"><div class="follow-icon">${TRACK_ICONS[a.type]||'•'}</div><div class="follow-main"><div class="follow-name">${esc(ACTIVITY_TYPES[a.type]||a.type||'Acompanhamento')} · ${esc(a.title||status)}</div>${a.description?`<div class="follow-desc">${esc(a.description)}</div>`:''}<div style="margin-top:4px"><span class="track-alert ${_activityDone(a)?'track-ok':over?'':'track-warn'}">${_activityDone(a)?esc(status||'Concluído'):over?'Atrasado':esc(status)}</span></div></div><div style="display:flex;gap:4px;align-items:start"><span class="follow-date ${over?'over':''}">${d?fmtDate(d):''}</span>${can?`<button class="follow-edit" onclick="editActivity('${a.id}')" title="Editar">✎</button>`:''}</div></div>`;
}
let projectProfileMemberId='';
function projectIdentityHTML(m,can){
  const q=(m.perguntaCientifica||'').trim(),obj=(m.objetivoProjeto||'').trim(),aud=(m.grupoAtendido||'').trim(),vis=(m.ideiaVisibilidade||'').trim(),oth=(m.projetoOutros||'').trim();
  const empty='<span class="project-empty">Ainda não preenchido.</span>';
  return `<section class="project-identity"><div class="project-identity-head"><div class="project-identity-title">🧭 Identidade do projeto</div>${can?`<button type="button" class="project-identity-edit" onclick="openProjectProfile('${m.id}')">✎ Editar projeto</button>`:''}</div><div class="science-question"><div class="sq-label">★ Pergunta científica</div><div class="sq-text">${q?esc(q):empty}</div></div><div class="project-identity-grid"><div class="project-identity-item"><div class="pi-label">Objetivo central</div><div class="pi-text">${obj?esc(obj):empty}</div></div><div class="project-identity-item"><div class="pi-label">Grupo / público atendido</div><div class="pi-text">${aud?esc(aud):empty}</div></div><div class="project-identity-item full"><div class="pi-label">Ideia de visibilidade</div><div class="pi-text">${vis?esc(vis):empty}</div></div>${oth?`<div class="project-identity-item full"><div class="pi-label">Outros aspectos</div><div class="pi-text">${esc(oth)}</div></div>`:''}</div></section>`;
}
function openProjectProfile(id){
  const m=memberById(id);if(!m)return;if(!(isAdmin||isMe(id))){toast('Somente o próprio Carbonauta ou o coordenador pode editar este projeto.');return}
  projectProfileMemberId=id;$('#projectQuestion').value=m.perguntaCientifica||'';$('#projectObjective').value=m.objetivoProjeto||'';$('#projectAudience').value=m.grupoAtendido||'';$('#projectVisibility').value=m.ideiaVisibilidade||'';$('#projectOther').value=m.projetoOutros||'';openOverlay('projectProfileOverlay');
}
async function saveProjectProfile(){
  const id=projectProfileMemberId,m=memberById(id);if(!id||!m||!(isAdmin||isMe(id))){toast('Sem permissão para editar este projeto.');return}
  const data={perguntaCientifica:$('#projectQuestion').value.trim(),objetivoProjeto:$('#projectObjective').value.trim(),grupoAtendido:$('#projectAudience').value.trim(),ideiaVisibilidade:$('#projectVisibility').value.trim(),projetoOutros:$('#projectOther').value.trim(),projectProfileUpdatedAt:FB().serverTimestamp()};
  try{await fbSetMember(id,data);closeOverlay('projectProfileOverlay');toast('Identidade do projeto salva.');}catch(e){console.error(e);toast(e?.code==='permission-denied'?'O Firestore bloqueou a edição do projeto.':'Erro ao salvar a identidade do projeto.')}
}

function renderTrack(){
  refreshTrackOwners();
  let members=[...state.members].sort((a,b)=>(a.status==='egresso')-(b.status==='egresso')||a.nome.localeCompare(b.nome));
  const owner=$('#trackOwner').value,mode=$('#trackStatus').value;
  if(owner)members=members.filter(m=>m.id===owner);
  if($('#trackMine').checked){if(!myId){$('#trackMine').checked=false;toast('Seu perfil precisa estar vinculado ao login.')}else members=members.filter(m=>m.id===myId)}
  const now=new Date();now.setHours(0,0,0,0);
  const all=state.activities;
  const pendingCorrections=all.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
  const overdue=all.filter(_isOverdue).length;
  const next30=all.filter(a=>!_activityDone(a)&&a.dueDate).filter(a=>{const di=dateInfo(a.dueDate);return di&&di.diff>=0&&di.diff<=30}).length;
  c

--- occurrence 3 at char 577108 ---
const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprovar ou editar este registro.':
    (['aprovado','reprovado'].includes(activity?.status)?'Pode ajustar seu registro. Ao salvar, ele volta para revisão do orientador.':'O registro é seu: pode editar. A aprovação fica com o orientador.');
}
function editActivity(id){openActivity(id)}
function openActivityPreset(type,ownerPreset=''){openActivity(null,ownerPreset);setTimeout(()=>{if($('#activityType'))$('#activityType').value=type;if(type==='pendencia'){$('#activityStatus').value='afazer';$('#activityName').placeholder='Ex.: Enviar capítulo revisado';}if(type==='correcao'){$('#activityStatus').value='recebido';$('#activityName').placeholder='Ex.: Correção do capítulo 2';}},20)}
async function notifyAcompanhamentoOwner(ownerId,meta={}){if(!isAdmin||!ownerId||ownerId===myId)return;const me=currentActor(),f=FB();try{await f.addDoc(f.collection(window.db,'rede_notifications'),{recipientId:ownerId,senderId:me?.id||myId||'',senderUid:window.auth.currentUser?.uid||'',senderName:me?.name||'Coordenação',kind:meta.kind||'acompanhamento',title:meta.title||'Novo item de acompanhamento',message:(meta.message||'').slice(0,240),sourceType:'acompanhamento',sourceId:meta.sourceId||'',ownerId,read:false,ts:f.serverTimestamp()})}catch(e){console.warn('Não foi possível notificar o acompanhamento',e)}}
async function saveActivity(){
  if(activitySaving)return false;
  const ownerId=isAdmin?$('#activityOwnerSel').value:myId,type=$('#activityType').value;
  const title=$('#activityName').value.trim()||ACTIVITY_TYPES[type]||'Acompanhamento';
  if(!ownerId){toast('Selecione o Carbonauta.');return false}
  const startDate=$('#activityStart').value,dueDate=$('#activityDue').value;
  if(startDate&&dueDate&&startDate>dueDate){toast('A data final não pode ser anterior ao início.');return false}
  const m=memberById(ownerId),status=$('#activityStatus').value;
  if(!isAdmin&&['aprovado','reprovado'].includes(status)){toast('A aprovação fica com o orientador.');return false}
  const f=FB(),actorUid=window.auth.currentUser?.uid||'',wasNew=!editingActivityId;
  const ref=editingActivityId?f.doc(window.db,'rede_activities',editingActivityId):f.doc(f.collection(window.db,'rede_activities'));
  const data={ownerId,ownerName:m?.nome||'',type,status,title,description:$('#activityDesc').value.trim(),startDate,dueDate,
    progress:Math.max(0,Math.min(100,+$('#activityProgress').value||0)),updatedAt:f.serverTimestamp()};
  const button=$('#saveActivityBtn');activitySaving=true;button.disabled=true;button.textContent='Salvando…';
  try{
    await f.runTransaction(window.db,async tx=>{
      const snap=await tx.get(ref);
      if(!wasNew&&!snap.exists())throw new Error('Este registro foi excluído em outra tela. Reabra o acompanhamento.');
      if(snap.exists()){
        const previous=snap.data();
        if((previous.updatedAt?.toMillis?.()??null)!==activityLoadedRevision)throw new Error('Esse registro mudou em outra tela. Reabra antes de salvar para não perder nenhuma alteração.');
      }else{data.createdAt=f.serverTimestamp();data.ownerUid=(state.users||[]).find(u=>u.memberId===ownerId)?.uid||(ownerId===myId?actorUid:'')}
      tx.set(ref,data,{merge:true});
    });
    activitySaving=false;closeOverlay('activityOverlay');toast('Acompanhamento salvo.');
    // Rewards/notifications must not turn a successful save into a false error.
    try{
      if(wasNew)await awardPoints(ownerId,'activity_created',ref.id,title);
      if(['concluido','aprovado','feito'].includes(status)||data.progress>=100)await awardPoints(ownerId,'activity_completed',ref.id,title);
      if(isAdmin&&ownerId!==myId)await notifyAcompanhamentoOwner(ownerId,{kind:type,title:'📌 Acompanhamento atualizado',message:`${title}${dueDate?' · prazo '+fmtDate(due

--- occurrence 4 at char 696612 ---
rPrivatePeople;
if($('#privateBackBtn'))$('#privateBackBtn').onclick=e=>{e.preventDefault();e.stopPropagation();closePrivateThread()};
if($('#privateSendBtn'))$('#privateSendBtn').onclick=sendPrivateMessage;
if($('#privateInput')){$('#privateInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendPrivateMessage()}});$('#privateInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(130,e.target.scrollHeight)+'px'})}
if($('#privateEmojiToggle'))$('#privateEmojiToggle').onclick=()=>$('#privateEmojiBar')?.classList.toggle('show');
$$('#privateEmojiBar button').forEach(b=>b.onclick=()=>{const i=$('#privateInput');if(!i)return;i.value+=(i.value?' ':'')+b.textContent;i.focus()});
if($('#privateReplyCancel'))$('#privateReplyCancel').onclick=()=>{privateReplyTarget=null;renderPrivateReply()};
$$('[data-close]').forEach(b=>b.onclick=()=>closeOverlay(b.dataset.close));
$$('.overlay').forEach(o=>o.addEventListener('click',e=>{if(e.target===o)closeOverlay(o.id)}));
window.addEventListener('keydown',e=>{if(e.key==='Escape'){$$('.overlay.open').forEach(o=>closeOverlay(o.id));closeDetail();closeLightbox()}});
window.addEventListener('resize',()=>{if(simulation){const[W,H]=graphSize();simulation.force('center')&&simulation.force('center').x(W/2).y(H/2);simulation.alpha(.3).restart()}});

// expose for inline handlers
window.selectNode=selectNode; window.closeDetail=closeDetail; window.openMember=openMember;
/* P203H1: pontes públicas usadas pelos patches consolidados fora do módulo. */
window.switchView=switchView; window.renderSubNav=renderSubNav; window.renderMensagens=renderMensagens; window.renderPrivateThreads=renderPrivateThreads; window.openPrivateThread=openPrivateThread; window.openPrivateNew=openPrivateNew; window.closePrivateThread=closePrivateThread;
window.filterLinha=filterLinha; window.recadoTo=recadoTo;
window.toggleCronoItem=toggleCronoItem; window.deleteCronoItem=deleteCronoItem; window.unlinkAccess=unlinkAccess;
window.deleteChatMsg=deleteChatMsg; window.deletePublicacao=deletePublicacao; window.openMuralAction=openMuralAction; window.openProjectProfile=openProjectProfile; window.editActivity=openActivity; window.toggleReaction=toggleReaction; window.toggleComments=toggleComments; window.addSocialComment=addSocialComment; window.deleteFeedPost=deleteFeedPost;


function sparkleLogo(mode='wow'){
  const marks=$$('.logo-mark');
  marks.forEach(m=>{
    m.classList.remove('is-awakening','is-wow');
    void m.offsetWidth;
    m.classList.add(mode==='wake'?'is-awakening':'is-wow');
    setTimeout(()=>m.classList.remove('is-awakening','is-wow'),1700);
  });
}
setTimeout(()=>sparkleLogo('wake'),650);
setInterval(()=>{if(document.visibilityState==='visible') sparkleLogo('wow')},28000);
document.addEventListener('click',e=>{
  if(e.target.closest('.tab,.btn.primary,#sendBtn,#muralRecadoBtn,#addPubBtn,#addPhotoBtn,#newEventBtn,#myPrazoBtn,#privatePrazoBtn,#addActivityBtn')){
    sparkleLogo('wow');
  }
});
window.sparkleLogo=sparkleLogo;

let deferredInstallPrompt=null;
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredInstallPrompt=e;});
const installPwaBtn=$('#installPwaBtn');
if(installPwaBtn){
  const standalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true;
  if(standalone) installPwaBtn.style.display='none';
  installPwaBtn.onclick=async()=>{
    if(deferredInstallPrompt){deferredInstallPrompt.prompt();try{await deferredInstallPrompt.userChoice}catch(_e){}deferredInstallPrompt=null;return}
    if(/iphone|ipad|ipod/i.test(navigator.userAgent)) alert('No iPhone/iPad: toque em Compartilhar e escolha “Adicionar à Tela de Início”.');
    else alert('No celular ou PC: abra o menu do navegador e escolha “Instalar app” ou “Adicionar à tela inicial”.');
  };
  window.addEventListener('appinstalled',()=>{installPwaBtn.style.display='none'});
}
window.CARBONAUTAS_BUILD='P205-20260921';
if('serviceWorker' in navigator){
  window.addEventListener('load',async()=>{
    try{
      const reg=await navigator.serviceWorker.register('./sw.js?v=P205-20260921',{updateViaCache:'none'});
      await reg.update();
    }catch(e){console.warn('SW update',e)}
  });
}
refreshIdentityUI();
waitFirebase().then(startAuth);
</script>

<script id="carbonautas-dynamic-favicon">
(()=>{
  const link=document.getElementById('dynamicFavicon'); if(!link)return;
  const img=new Image(); img.src='./icon-192.png?v=P28';
  img.onload=()=>{
    const c=document.createElement('canvas'),ctx=c.getContext('2d'); c.width=c.height=64; let phase=0,last=0;
    const draw=(ts)=>{
      const cadence=document.hidden?1800:420; if(ts-last<cadence){requestAnimationFrame(draw);return} last=ts; phase=(phase+1)%240;
      const a=Math.sin(phase/18)*0.055,scale=0.88+0.05*(1+Math.sin(phase/14))/2;
      ctx.clearRect(0,0,64,64);ctx.save();ctx.translate(32,32);ctx.rotate(a);ctx.scale(scale,scale);ctx.shadowColor='rgba(40,225,255,.85)';ctx.shadowBlur=5+3*(1+Math.sin(phase/11))/2;ctx.drawImage(img,-32,-32,64,64);ctx.restore();
      try{link.href=c.toDataURL('image/png')}catch{} requestAnimationFrame(draw);
    }; requestAnimationFrame(draw);
  };
})();
</script>

<!-- ===== Carbonautas P128: camadas P44 a P129 consolidadas neste arquivo (antes injetadas pelo sw.js) ===== -->
<style data-src="agenda-soft-p108.css">
/* Carbonautas P108 · Agenda leve, clara e sem alterar a lógica */
#viewCrono .crono-scroll{
  background:
    radial-gradient(700px 320px at 12% 0%, rgba(135,214,198,.13), transparent 60%),
    radial-gradient(620px 300px at 92% 10%, rgba(160,183,242,.10), transparent 62%),
    #fbfdfc;
}
#viewCr

--- occurrence 5 at char 901064 ---
catch(_e){}

  // Bolsa perto do fim: aviso tratável, não uma falsa conclusão acadêmica.
  scope.forEach(m=>{
    if(m.bolsista&&m.bolsaFim){
      const pi=trackPeriodInfo(m.bolsaInicio,m.bolsaFim);
      if(pi.days!==null&&pi.days>=0&&pi.days<=45){
        const key=`bolsa:${m.id}:${m.bolsaFim}`;
        if(!p56IsAck(key))open.push({key,kind:'BOLSA · ATENÇÃO',who:m.nome,what:m.bolsaModalidade||'Bolsa em encerramento',detail:`Termina em ${_daysText(pi.days)} · ${fmtDate(m.bolsaFim)}.`,date:m.bolsaFim,sev:pi.days<=15?'critical':'warn',sort:2.5,action:'member',memberId:m.id,canAck:true});
      }
    }
    const acts=(state.activities||[]).filter(a=>a.ownerId===m.id);
    const check=acts.find(a=>a.type==='checkin'&&a.checkinMonth===mk);
    if(m.status!=='egresso'&&!check){
      open.push({key:`checkin:${m.id}:${mk}`,kind:'CHECK-IN · MÊS',who:m.nome,what:`Check-in de ${monthLabel(mk)}`,detail:'Registrar o que foi feito, dificuldades e próximo passo.',date:'',sev:'warn',sort:4,action:'checkin',memberId:m.id,canAck:false});
    } else if(check && (check.ownerId===myId || isAdmin)){
      const ms=p56Millis(check.updatedAt)||p56Millis(check.createdAt);
      if(p56MonthFromMillis(ms)===mk)done.push({key:`donecheckin:${check.id}`,kind:'✓ CHECK-IN',who:m.nome,what:`Check-in de ${monthLabel(mk)}`,doneAt:ms,backendDone:true});
    }
  });

  // Itens marcados manualmente como tratados continuam visíveis em Feitos.
  Object.values(acks).forEach(a=>{
    if(!a||!a.key)return;
    done.push({key:a.key,kind:`✓ ${a.kind||'TRATADO'}`,who:a.who||'—',what:a.what||'Item tratado',doneAt:+a.doneAt||0,localAck:true});
  });

  // evita duplicar o mesmo concluído quando houver cache local + conclusão real
  const seen=new Set();
  const cleanDone=done.sort((a,b)=>(b.doneAt||0)-(a.doneAt||0)).filter(x=>{const k=`${x.kind}|${x.who}|${x.what}|${x.sourceId||x.key}`;if(seen.has(k))return false;seen.add(k);return true}).slice(0,30);
  open.sort((a,b)=>a.sort-b.sort||(a.date||'9999').localeCompare(b.date||'9999'));
  return {open,done:cleanDone};
}

function p56OpenItem(key){
  const {open}=p56BuildItems(),it=open.find(x=>x.key===key);if(!it)return;
  try{
    if(it.action==='activity'){editActivity(it.sourceId);return}
    if(it.action==='review'){switchView('pubs');setTimeout(()=>openReviewConversation(it.sourceId),120);return}
    if(it.action==='checkin'){openCheckin(it.memberId);return}
    focusMemberTrack(it.memberId);
  }catch(e){console.warn('P56 abrir item',e)}
}
async function p56CompleteActivity(id){
  const a=(state.activities||[]).find(x=>x.id===id);if(!a)return toast('Item não encontrado.');
  if(_activityDone(a))return toast('Este item já está concluído.');
  if(!(isAdmin||isMe(a.ownerId)))return toast('Você não pode concluir este item.');
  if(!confirm(`Marcar como concluído?\n\n${p56ActivityLabel(a)}`))return;
  try{
    const f=FB();
    await f.updateDoc(f.doc(window.db,'rede_activities',id),{status:'concluido',updatedAt:f.serverTimestamp()});
    try{await awardPoints(a.ownerId,'activity_completed',id,p56ActivityLabel(a));if(a.dueDate)await awardPoints(a.ownerId,'deadline_done',id,p56ActivityLabel(a))}catch(_e){}
    toast('✓ Concluído. O item foi para Feitos.');
    setTimeout(()=>renderPainel(),120);
  }catch(e){console.error(e);toast('Não foi possível concluir este item.')}
}
function p56AckItem(key){
  const {open}=p56BuildItems(),it=open.find(x=>x.key===key);if(!it)return;
  p56SetAck(key,{kind:it.kind,who:it.who,what:it.what,memberId:it.memberId||'',sourceId:it.sourceId||''});
  renderPainel();toast('✓ Marcado como tratado.');
}
function p56UndoAck(key){p56UnsetAck(key);renderPainel();toast('Item voltou para a lista de atenção.')}

function p56ItemHTML(it){
  const tag=it.kind.split('·')[0].trim();
  return `<div class="p56-task ${it.sev==='critical'?'critical':'warn'}">
    <div class="p56-task-main">
      <div class="p56-task-top"><span class="p56-source">${esc(tag)}</span>${it.date?`<span class="p56-date">${esc(fmtDate(it.date))}</span>`:''}</div>
      <div class="p56-who">${esc(it.who)}</div>
      <div class="p56-what">${esc(it.what)}</div>
      <div class="p56-detail">${esc(it.detail||'')}</div>
    </div>
    <div class="p56-task-actions">
      <button type="button" class="btn" onclick="p56OpenItem('${esc(it.key)}')">Abrir</button>
      ${it.canComplete?`<button type="button" class="btn p56-done-btn" onclick="p56CompleteActivity('${esc(it.sourceId)}')">✓ Concluir</button>`:''}
      ${it.canAck?`<button type="button" class="btn p56-done-btn" onclick="p56AckItem('${esc(it.key)}')">✓ Já tratei</button>`:''}
    </div>
  </div>`;
}
function p56DoneHTML(it){
  return `<div class="p56-done-row"><span class="p56-check">✓</span><div class="p56-done-main"><b>${esc(it.who)}</b><span>${esc(it.what)}</span><small>${it.doneAt?p56DateTime(it.doneAt):'Concluído'}</small></div>${it.localAck?`<button type="button" class="p56-undo" onclick="p56UndoAck('${esc(it.key)}')">desfazer check</button>`:''}</div>`;
}
function p56RenderPanel(){
  if(!$('#dashKpis'))return;
  const {open,done}=p56BuildItems();
  const now=open.filter(x=>x.sev==='critical');
  const week=open.filter(x=>x.sev!=='critical');
  const overdue=open.filter(x=>x.action==='activity'&&x.date&&dateInfo(x.date)?.over).length;
  const redPeople=p56Scope().filter(m=>memberHealth(m).level==='red').length;
  $('#dashGreeting').textContent='Aqui ficam somente coisas que pedem uma ação. Abra o item certo e marque ✓ quando resolver.';
  $('#dashKpis').innerHTML=`<div class="dash-kpi ${open.length?'warn':'good'}"><div class="k-n">${open.length}</div><div class="k-l">precisam de ação</div></div><div class="dash-kpi ${overdue?'bad':'good'}"><div 

--- occurrence 6 at char 1000301 ---
teSafe(a.dueDate)}`,cls:'bad'};if(di&&di.diff===0)return{text:`Vence hoje · ${fmtDateSafe(a.dueDate)}`,cls:'bad'};if(di&&di.diff>0)return{text:`Vence em ${di.diff} dia(s) · ${fmtDateSafe(a.dueDate)}`,cls:di.diff<=2?'bad':'due'};}catch(_e){}
  return {text:`Prazo · ${fmtDateSafe(a.dueDate)}`,cls:'due'};
}
function contextType(a){if(a?.type==='correcao')return'correction';if(a?.type==='orientacao')return'orientation';return'deadline'}
function contextLabel(a){if(a?.type==='correcao')return'CORREÇÃO';if(a?.type==='orientacao')return'ORIENTAÇÃO';return'PRAZO'}

function openActivityDetail(id){
  injectUi();const a=byId(id);if(!a){try{toast('Este item não foi encontrado.')}catch(_e){}return}
  const owner=memberName(a.ownerId,a.ownerName),due=dueMeta(a),progress=Math.max(0,Math.min(100,Number(a.progress||0))),done=isDone(a),can=canManage(a),me=(typeof myId!=='undefined'?myId:'');
  document.getElementById('p66Title').textContent=done?'Item concluído':'O que precisa ser feito';
  document.getElementById('p66Body').innerHTML=`
    <div class="p66-person"><div class="p66-avatar">${esc(firstName(owner).slice(0,1).toUpperCase())}</div><div><b>${esc(owner)}</b><span>${esc(typeLabel(a))}</span></div></div>
    <div class="p66-tags"><span class="p66-tag">${esc(statusLabel(a))}</span>${a.startDate?`<span class="p66-tag">Início · ${esc(fmtDateSafe(a.startDate))}</span>`:''}<span class="p66-tag ${due.cls}">${esc(due.text)}</span></div>
    <div class="p66-problem"><div class="p66-problem-label">O problema / próximo passo</div><div class="p66-problem-title">${esc(a.title||typeLabel(a))}</div><div class="p66-problem-text ${a.description?'':'empty'}">${esc(a.description||'Este acompanhamento ainda não tem uma observação detalhada.')}</div></div>
    <div class="p66-progress"><div class="p66-progress-top"><span>Andamento informado</span><b>${progress}%</b></div><div class="p66-progress-bar"><span style="width:${progress}%"></span></div></div>`;
  const foot=document.getElementById('p66Foot');foot.innerHTML='';
  if(can){const edit=document.createElement('button');edit.type='button';edit.className='btn p66-edit';edit.textContent='✏️ Editar';edit.onclick=()=>{closeDetail();if(typeof editActivity==='function')editActivity(a.id)};foot.appendChild(edit)}
  if(!done&&can){const complete=document.createElement('button');complete.type='button';complete.className='btn p66-complete';complete.textContent='✓ Concluir';complete.onclick=()=>{closeDetail();if(typeof window.p56CompleteActivity==='function')window.p56CompleteActivity(a.id)};foot.appendChild(complete)}
  if(a.ownerId&&a.ownerId!==me&&typeof window.openPrivateChatWithContext==='function'){
    const chat=document.createElement('button');chat.type='button';chat.className='btn p66-primary';chat.textContent=`💬 Conversar com ${firstName(owner)}`;chat.onclick=()=>{closeDetail();window.openPrivateChatWithContext(a.ownerId,{type:contextType(a),label:contextLabel(a),title:a.title||typeLabel(a),eventLabel:a.dueDate?`Vence em ${fmtDateSafe(a.dueDate)}`:`Acompanhamento de ${owner}`,sourceId:`activity_${a.id}`})};foot.appendChild(chat)
  }
  const close=document.createElement('button');close.type='button';close.className='btn';close.textContent='Fechar';close.onclick=closeDetail;foot.appendChild(close);
  document.getElementById('p66ActionOverlay').classList.add('open');
}

function p66OpenItem(key){
  const k=String(key||'');
  if(k.startsWith('activity:')){openActivityDetail(k.slice('activity:'.length));return}
  if(typeof originalP56OpenItem==='function')return originalP56OpenItem(key);
}
function install(){
  injectUi();
  if(typeof window.p56OpenItem==='function'&&window.p56OpenItem!==p66OpenItem)originalP56OpenItem=window.p56OpenItem;
  window.p66OpenActivityDetail=openActivityDetail;
  window.p56OpenItem=p66OpenItem;
  console.info('Carbonautas',VERSION,BUILD,'painel abre direto no problema');
}
function boot(){install();setTimeout(install,900)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
</script>
<script data-src="dashboard-action-target-p67.js">
/* Carbonautas P67 · Painel: ir direto ao documento, correção, link ou conversa da demanda */
(function(){
'use strict';
const VERSION='P67';
const BUILD='20260917';

const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function norm(v=''){
  return String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase()
    .replace(/\.(docx?|xlsx?|pptx?|pdf|odt|rtf|csv)$/,'')
    .replace(/[^a-z0-9]+/g,' ').trim().replace(/\s+/g,' ');
}
function tokens(v=''){return new Set(norm(v).split(' ').filter(x=>x.length>2))}
function titleScore(a,b){
  const A=norm(a),B=norm(b);if(!A||!B)return 0;if(A===B)return 140;if(A.includes(B)||B.includes(A))return 100;
  const ta=tokens(A),tb=tokens(B);if(!ta.size||!tb.size)return 0;let hit=0;ta.forEach(x=>{if(tb.has(x))hit++});
  const coverage=hit/Math.max(ta.size,tb.size);return Math.round(coverage*90);
}
function activityById(id){try{return (state.activities||[]).find(a=>a.id===id)||null}catch(_e){return null}}
function closeDetail(){document.getElementById('p66ActionOverlay')?.classList.remove('open')}
function toastSafe(msg){try{if(typeof toast==='function')toast(msg);else console.log(msg)}catch(_e){console.log(msg)}}
function ownerMatches(a,p){return !!(p&&(p.memberId===a.ownerId||p.reviewReturnToId===a.ownerId||p.reviewReviewerId===a.ownerId||p.reviewNextRecipientId===a.ownerId))}
function pubTitle(p){return p?.reviewBaseTitle||p?.titulo||p?.fileName||'Arquivo'}
function pubScore(a,p){
  let s=Math.max(titleScore(a.title,p?.titulo),titleScore(a.tit

--- occurrence 7 at char 1000327 ---
ad'};if(di&&di.diff===0)return{text:`Vence hoje · ${fmtDateSafe(a.dueDate)}`,cls:'bad'};if(di&&di.diff>0)return{text:`Vence em ${di.diff} dia(s) · ${fmtDateSafe(a.dueDate)}`,cls:di.diff<=2?'bad':'due'};}catch(_e){}
  return {text:`Prazo · ${fmtDateSafe(a.dueDate)}`,cls:'due'};
}
function contextType(a){if(a?.type==='correcao')return'correction';if(a?.type==='orientacao')return'orientation';return'deadline'}
function contextLabel(a){if(a?.type==='correcao')return'CORREÇÃO';if(a?.type==='orientacao')return'ORIENTAÇÃO';return'PRAZO'}

function openActivityDetail(id){
  injectUi();const a=byId(id);if(!a){try{toast('Este item não foi encontrado.')}catch(_e){}return}
  const owner=memberName(a.ownerId,a.ownerName),due=dueMeta(a),progress=Math.max(0,Math.min(100,Number(a.progress||0))),done=isDone(a),can=canManage(a),me=(typeof myId!=='undefined'?myId:'');
  document.getElementById('p66Title').textContent=done?'Item concluído':'O que precisa ser feito';
  document.getElementById('p66Body').innerHTML=`
    <div class="p66-person"><div class="p66-avatar">${esc(firstName(owner).slice(0,1).toUpperCase())}</div><div><b>${esc(owner)}</b><span>${esc(typeLabel(a))}</span></div></div>
    <div class="p66-tags"><span class="p66-tag">${esc(statusLabel(a))}</span>${a.startDate?`<span class="p66-tag">Início · ${esc(fmtDateSafe(a.startDate))}</span>`:''}<span class="p66-tag ${due.cls}">${esc(due.text)}</span></div>
    <div class="p66-problem"><div class="p66-problem-label">O problema / próximo passo</div><div class="p66-problem-title">${esc(a.title||typeLabel(a))}</div><div class="p66-problem-text ${a.description?'':'empty'}">${esc(a.description||'Este acompanhamento ainda não tem uma observação detalhada.')}</div></div>
    <div class="p66-progress"><div class="p66-progress-top"><span>Andamento informado</span><b>${progress}%</b></div><div class="p66-progress-bar"><span style="width:${progress}%"></span></div></div>`;
  const foot=document.getElementById('p66Foot');foot.innerHTML='';
  if(can){const edit=document.createElement('button');edit.type='button';edit.className='btn p66-edit';edit.textContent='✏️ Editar';edit.onclick=()=>{closeDetail();if(typeof editActivity==='function')editActivity(a.id)};foot.appendChild(edit)}
  if(!done&&can){const complete=document.createElement('button');complete.type='button';complete.className='btn p66-complete';complete.textContent='✓ Concluir';complete.onclick=()=>{closeDetail();if(typeof window.p56CompleteActivity==='function')window.p56CompleteActivity(a.id)};foot.appendChild(complete)}
  if(a.ownerId&&a.ownerId!==me&&typeof window.openPrivateChatWithContext==='function'){
    const chat=document.createElement('button');chat.type='button';chat.className='btn p66-primary';chat.textContent=`💬 Conversar com ${firstName(owner)}`;chat.onclick=()=>{closeDetail();window.openPrivateChatWithContext(a.ownerId,{type:contextType(a),label:contextLabel(a),title:a.title||typeLabel(a),eventLabel:a.dueDate?`Vence em ${fmtDateSafe(a.dueDate)}`:`Acompanhamento de ${owner}`,sourceId:`activity_${a.id}`})};foot.appendChild(chat)
  }
  const close=document.createElement('button');close.type='button';close.className='btn';close.textContent='Fechar';close.onclick=closeDetail;foot.appendChild(close);
  document.getElementById('p66ActionOverlay').classList.add('open');
}

function p66OpenItem(key){
  const k=String(key||'');
  if(k.startsWith('activity:')){openActivityDetail(k.slice('activity:'.length));return}
  if(typeof originalP56OpenItem==='function')return originalP56OpenItem(key);
}
function install(){
  injectUi();
  if(typeof window.p56OpenItem==='function'&&window.p56OpenItem!==p66OpenItem)originalP56OpenItem=window.p56OpenItem;
  window.p66OpenActivityDetail=openActivityDetail;
  window.p56OpenItem=p66OpenItem;
  console.info('Carbonautas',VERSION,BUILD,'painel abre direto no problema');
}
function boot(){install();setTimeout(install,900)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
</script>
<script data-src="dashboard-action-target-p67.js">
/* Carbonautas P67 · Painel: ir direto ao documento, correção, link ou conversa da demanda */
(function(){
'use strict';
const VERSION='P67';
const BUILD='20260917';

const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function norm(v=''){
  return String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase()
    .replace(/\.(docx?|xlsx?|pptx?|pdf|odt|rtf|csv)$/,'')
    .replace(/[^a-z0-9]+/g,' ').trim().replace(/\s+/g,' ');
}
function tokens(v=''){return new Set(norm(v).split(' ').filter(x=>x.length>2))}
function titleScore(a,b){
  const A=norm(a),B=norm(b);if(!A||!B)return 0;if(A===B)return 140;if(A.includes(B)||B.includes(A))return 100;
  const ta=tokens(A),tb=tokens(B);if(!ta.size||!tb.size)return 0;let hit=0;ta.forEach(x=>{if(tb.has(x))hit++});
  const coverage=hit/Math.max(ta.size,tb.size);return Math.round(coverage*90);
}
function activityById(id){try{return (state.activities||[]).find(a=>a.id===id)||null}catch(_e){return null}}
function closeDetail(){document.getElementById('p66ActionOverlay')?.classList.remove('open')}
function toastSafe(msg){try{if(typeof toast==='function')toast(msg);else console.log(msg)}catch(_e){console.log(msg)}}
function ownerMatches(a,p){return !!(p&&(p.memberId===a.ownerId||p.reviewReturnToId===a.ownerId||p.reviewReviewerId===a.ownerId||p.reviewNextRecipientId===a.ownerId))}
function pubTitle(p){return p?.reviewBaseTitle||p?.titulo||p?.fileName||'Arquivo'}
function pubScore(a,p){
  let s=Math.max(titleScore(a.title,p?.titulo),titleScore(a.title,p?.fileName),titleScore

--- occurrence 8 at char 1090871 ---
ação do produto */
(function(){
'use strict';
const VERSION='P78', BUILD='20260917';
let scheduled=false, bolsistasOnly=false;
const qs=(s,r=document)=>r.querySelector(s), qsa=(s,r=document)=>[...r.querySelectorAll(s)];
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function S(){try{return state}catch(_e){return null}}
function myMember(){try{return myId||''}catch(_e){return''}}
function admin(){try{return !!isAdmin}catch(_e){return false}}
function esc(v=''){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]))}
function done(a){return ['concluido','aprovado','feito','finalizado'].includes(String(a?.status||'').toLowerCase())||Number(a?.progress||0)>=100}
function today0(){const d=new Date();d.setHours(0,0,0,0);return d}
function dueInfo(s){if(!/^\d{4}-\d{2}-\d{2}$/.test(String(s||'')))return null;const [y,m,d]=s.split('-').map(Number),x=new Date(y,m-1,d);x.setHours(0,0,0,0);const diff=Math.round((x-today0())/86400000);return {diff,date:x,label:x.toLocaleDateString('pt-BR',{day:'2-digit',month:'2-digit'})}}
function memberName(id){try{return S()?.members?.find(m=>m.id===id)?.nome||'Carbonauta'}catch(_e){return'Carbonauta'}}
function fmtNotif(n){try{const ms=n?.ts?.toMillis?n.ts.toMillis():+new Date(n?.ts||0);return ms?new Date(ms).toLocaleString('pt-BR',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'}):''}catch(_e){return''}}
function notifAudience(n){const src=String(n?.sourceType||'').toLowerCase(),title=String(n?.title||'').toLowerCase();if(src==='private_chat'||src==='acompanhamento'||src.startsWith('repositorio')||src==='review_message')return'PARA VOCÊ';if(src==='mural'||src==='recado'||/grupo|rede/.test(title))return'REDE';return'AVISO'}
function notifIcon(n){try{return typeof notificationIcon==='function'?notificationIcon(n.kind):'🔔'}catch(_e){return'🔔'}}
function openNotif(id){try{if(typeof openNotification==='function')return openNotification(id)}catch(_e){}qs('#notifyBtn')?.click()}
function openAct(id){try{if(typeof window.p69OpenActivity==='function')return window.p69OpenActivity(id);if(typeof window.p56OpenItem==='function')return window.p56OpenItem('activity:'+id);if(typeof editActivity==='function')return editActivity(id)}catch(_e){}}
function currentMonth(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`}
function briefingData(){
  const s=S(),id=myMember();if(!s||!id)return {unread:[],actions:[],next:''};
  const unread=(s.notifications||[]).filter(n=>!n.read).sort((a,b)=>{const A=a?.ts?.toMillis?a.ts.toMillis():+new Date(a?.ts||0),B=b?.ts?.toMillis?b.ts.toMillis():+new Date(b?.ts||0);return B-A}).slice(0,4);
  const actions=(s.activities||[]).filter(a=>a.ownerId===id&&!done(a)&&a.type!=='checkin').map(a=>({a,di:dueInfo(a.dueDate)})).filter(x=>x.di&&x.di.diff<=7).sort((x,y)=>x.di.diff-y.di.diff).slice(0,4);
  const chk=(s.activities||[]).find(a=>a.ownerId===id&&a.type==='checkin'&&a.checkinMonth===currentMonth());
  return {unread,actions,next:String(chk?.checkinNext||'').trim()};
}
function ensureCss(){if(qs('#p78Style'))return;const st=document.createElement('style');st.id='p78Style';st.textContent=`
.tab[data-view="lab"]{display:none!important}
#recadoBtn,#muralDirectTopBtn,#muralDirectBtn,[onclick^="recadoTo("]{display:none!important}
.p78-today{position:relative;overflow:hidden;border:1px solid #b8dfe3!important;background:linear-gradient(145deg,#f8ffff 0%,#eef9fb 62%,#f5f2ff 100%)!important;box-shadow:0 12px 28px rgba(10,70,76,.10)!important}
.p78-today:after{content:"";position:absolute;width:130px;height:130px;right:-45px;top:-55px;border-radius:50%;background:radial-gradient(circle,rgba(30,136,199,.15),transparent 70%);pointer-events:none}
.p78-head{display:flex;gap:9px;align-items:flex-start;margin-bottom:10px;position:relative;z-index:1}.p78-head-main{min-width:0;flex:1}.p78-kicker{font-size:9px;font-weight:900;letter-spacing:.08em;color:#0e6f78}.p78-title{font-size:17px;font-weight:900;color:#142f38;margin-top:2px}.p78-sub{font-size:10.5px;color:#66808a;margin-top:2px;line-height:1.35}.p78-count{min-width:28px;height:28px;border-radius:999px;background:#0e5c63;color:#fff;display:grid;place-items:center;font-size:11px;font-weight:900}
.p78-sec{margin-top:10px}.p78-sec-title{font-size:10px;font-weight:900;color:#405b65;margin-bottom:6px;text-transform:uppercase;letter-spacing:.05em}.p78-row{width:100%;border:1px solid #d6e7eb;background:#fff;border-radius:12px;padding:9px 10px;margin:0 0 7px;display:flex;gap:9px;align-items:flex-start;text-align:left;color:#19333d}.p78-row:hover{border-color:#a9cbd2}.p78-ico{font-size:17px;line-height:1.1}.p78-main{min-width:0;flex:1}.p78-main b{display:block;font-size:11.5px;line-height:1.3}.p78-main span{display:block;font-size:10px;line-height:1.35;color:#67808a;margin-top:2px}.p78-tag{font-size:8px;font-weight:950;border-radius:999px;padding:3px 6px;background:#edf5f7;color:#42616b;white-space:nowrap}.p78-tag.group{background:#eee9ff;color:#5e45b8}.p78-tag.urgent{background:#fff0dd;color:#9d5b00}.p78-empty{padding:10px;border-radius:12px;background:#effaf5;border:1px solid #c7ead8;color:#176b4a;font-size:11px;font-weight:800}.p78-next{padding:9px 10px;border-left:3px solid #6c5ce0;background:#f6f3ff;border-radius:10px;font-size:10.5px;line-height:1.4;color:#534c72}.p78-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:9px}.p78-mini{border:1px solid #c9d9de;background:#fff;border-radius:9px;padding:7px 9px;font-size:10px;font-weight:800;color:#274650}
#p78AboutOverlay{position:fixed;inset:0;z-index:2147483645;background:rgba(4,18,29,.72);display:none;align-items:center;justify-content:center;padding:18px}#p78Abo


===== openMemberRepository =====

--- occurrence 1 at char 301980 ---

  const modal=m.bolsaModalidade?esc(m.bolsaModalidade):'Bolsa';
  return `<div class="scholar-box"><div class="scholar-head"><b>🎓 ${modal}</b><span>${esc(si.label)}</span><span class="remain">${esc(si.remaining)}</span></div><div class="scholar-bar"><span style="width:${si.progress}%"></span></div><div class="scholar-foot"><span>${m.bolsaInicio?'Início '+fmtDate(m.bolsaInicio):'Início não informado'}</span><span>${si.progress}% do período</span><span>${m.bolsaFim?'Fim '+fmtDate(m.bolsaFim):'Fim não informado'}</span></div></div>`;
}
function memberRepositoryItems(memberId){
  const out=[];
  compactPublicationsForDisplay(state.publicacoes.filter(p=>p.memberId===memberId)).forEach(p=>out.push({source:'publicacao',id:p.id,title:p.titulo||p.fileName||'Documento',kind:(CATEGORIAS[repoCategoryKey(p.categoria)]?.label||p.categoriaOutro||p.categoria||'Publicação'),url:p.url||'',fileName:p.fileName||'',isFile:p.tipo==='arquivo',ts:p.ts,reviewFlow:!!p.reviewFlow,reviewVersion:p.reviewVersion||0,reviewStatus:p.reviewStatus||'',reviewReviewerId:p.reviewReviewerId||'',reviewReturnToId:p.reviewReturnToId||'',reviewNextRecipientId:p.reviewNextRecipientId||'',reviewNote:p.reviewNote||'',onlineEditVersion:p.onlineEditVersion||1,raw:p}));
  state.repositoryPackages.filter(p=>p.ownerMemberId===memberId).forEach(p=>out.push({source:'package',id:p.id,title:p.title||'Conjunto de arquivos',kind:'Dossiê · '+repoPurposeLabel(p),url:'',fileName:'',isFile:false,ts:p.ts,package:p}));
  state.activities.filter(a=>a.ownerId===memberId && (a.fileUrl||a.externalUrl)).forEach(a=>out.push({source:'atividade',id:a.id,title:a.title||'Registro de acompanhamento',kind:ACTIVITY_TYPES[a.type]||a.type||'Acompanhamento',url:a.fileUrl||a.externalUrl||'',fileName:a.fileName||'',isFile:!!a.fileUrl,ts:a.updatedAt||a.createdAt}));
  const m=memberById(memberId); (m?.resultados||[]).forEach((r,i)=>{if(r.url)out.push({source:'resultado',id:'r'+i,title:r.titulo||'Resultado / produto',kind:'Resultado / produto',url:r.url,fileName:'',isFile:false,ts:null})});
  return out.sort((a,b)=>{const ta=a.ts?.toMillis?a.ts.toMillis():0,tb=b.ts?.toMillis?b.ts.toMillis():0;return tb-ta});
}
let repoMemberId=null;
function openMemberRepository(memberId){repoMemberId=memberId;renderMemberRepository();openOverlay('repoOverlay')}
function renderMemberRepository(){
  const m=memberById(repoMemberId); if(!m||!$('#repoList'))return;
  $('#repoTitle').textContent='Repositório · '+m.nome;
  $('#repoScholar').innerHTML=scholarshipHTML(m);
  const list=memberRepositoryItems(m.id); $('#repoCount').textContent=list.length;
  const canAdd=isAdmin||isMe(m.id); $('#repoAddBtn').style.display=canAdd?'inline-flex':'none';
  $('#repoList').innerHTML=list.length?list.map(it=>{
    const icon=it.reviewFlow?'✍️':(it.isFile?'📄':'🔗');
    const view=it.source==='package'?`<button class="btn ghost" type="button" onclick="openRepositoryPackage('${it.id}')">📦 Abrir dossiê</button>`:it.raw?.labRun?`<button class="btn primary" type="button" onclick="openLabRun('${it.raw.labRunId}')">📈 Resultados</button>`:it.isFile?`<button class="btn ghost" type="button" onclick="openFilePreview(decodeURIComponent('${encodeInlineComponent(it.url)}'),decodeURIComponent('${encodeInlineComponent(it.fileName||it.title||'Arquivo')}'),{source:'publication',id:'${it.id}'})">Visualizar</button><button class="btn review-download" type="button" onclick="downloadRepoFile(decodeURIComponent('${encodeInlineComponent(it.url)}'),decodeURIComponent('${encodeInlineComponent(it.fileName||it.title||'Arquivo')}'),decodeURIComponent('${encodeInlineComponent(it.raw?.storagePath||'')}'))">Baixar</button>`:`<a class="btn ghost" href="${esc(it.url)}" target="_blank" rel="noopener">Abrir</a>`;
    const rev=it.reviewFlow?reviewBadgeHTML(it.raw):'';
    const online=it.source==='publicacao'?officeActionButtons(it.raw):'';
    const chat=it.source==='publicacao'&&it.isFile?repositoryCommunicationButtons(it.raw):'';
    const respond=it.reviewFlow&&canActOnReview(it.raw)?`<button class="btn review-action" type="button" onclick="openReviewResponse('${it.id}')">${reviewActionLabel(it.raw)}</button>`:'';
    const ver=!it.reviewFlow&&it.onlineEditVersion>1?`<span class="office-version">✏️ edição v${it.onlineEditVersion}</span>`:'';
    return `<div class="repo-item"><div class="repo-icon">${icon}</div><div class="repo-main"><div class="repo-title">${esc(it.title)}</div><div class="repo-meta">${esc(it.kind)}</div>${ver}${rev}${it.reviewNote?`<div class="review-note">💬 ${esc(it.reviewNote)}</div>`:''}${it.raw?scientificContextHTML(it.raw):''}</div><div class="repo-actions">${view}${it.raw?labRepoButton(it.raw):''}${online}${chat}${respond}</div></div>`
  }).join(''):'<div class="repo-empty">Ainda não há documentos ou links registrados para esta pessoa.</div>';
}
function fmtTime(ts){
  if(!ts) return '';
  const d=new Date(ts), now=new Date();
  const sameDay=d.toDateString()===now.toDateString();
  return sameDay ? d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})
                 : d.toLocaleDateString('pt-BR',{day:'2-digit',month:'2-digit'})+' '+d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'});
}
function dayLabel(ts){
  const d=new Date(ts), now=new Date();
  const y=new Date(now); y.setDate(now.getDate()-1);
  if(d.toDateString()===now.toDateString()) return 'Hoje';
  if(d.toDateString()===y.toDateString()) return 'Ontem';
  return d.toLocaleDateString('pt-BR',{day:'2-digit',month:'long',year:'numeric'});
}
function memberById(id){return state.members.find(m=>m.id===id)}
function memberForAuthor(id,name=''){const direct=memberById(id);const same=state.members.filter(m=>name&&m.nome===name);if(direc

--- occurrence 2 at char 368799 ---
ed('sel',false)}

function renderDetail(){
  const m=state.members.find(x=>x.id===selectedId); if(!m)return;
  const nv=NIVEIS[m.nivel];
  const inner=$('#detailInner');
  const others=state.members.filter(o=>o.id!==m.id);
  const includeAux = $('#tHubLinks') ? $('#tHubLinks').checked : true;
  const neigh=others.map(o=>({o,shared:pairReasons(m,o,includeAux).map(r=>r.label)})).filter(x=>x.shared.length)
    .sort((a,b)=>b.shared.length-a.shared.length);
  const avatar = m.foto
    ? `<img class="avatar" src="${esc(m.foto)}" alt="" onerror="this.replaceWith(Object.assign(document.createElement('div'),{className:'avatar',style:'background:${nv.color}',textContent:'${initials(m.nome)}'}))">`
    : `<div class="avatar" style="background:${nv.color}">${initials(m.nome)}</div>`;
  const prazos=(m.prazos||[]).slice().sort((a,b)=>(a.data||'').localeCompare(b.data||''));
  const editable=canEditMember(m.id);

  inner.innerHTML=`
    <div class="dh">
      <button class="btn ghost close" onclick="closeDetail()"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width:16px;height:16px"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button>
      <div class="dh-top">
        ${avatar}
        <div style="min-width:0">
          <h2>${esc(m.nome)}</h2>
          <div class="pill-row">
            <span class="pill" style="background:${nv.color}">${nv.short}</span>
            ${m.programa?`<span class="pill sub">${esc(m.programa)}</span>`:''}
            ${m.status==='egresso'?'<span class="pill sub">Egresso</span>':''}
            ${m.bolsista?'<span class="pill" style="background:#7437E8">Bolsista</span>':''}
            ${m.origem?`<span class="pill sub">${esc(m.origem)}</span>`:''}
          </div>
        </div>
      </div>
    </div>
    ${m.bolsista?`<div class="dsec"><div class="lab">Bolsa</div>${scholarshipHTML(m)}</div>`:''}
    <div class="dsec"><div class="lab">Repositório individual</div><div class="repo-summary"><span class="repo-count">${memberRepositoryItems(m.id).length}</span><span class="hint">documentos, produtos e links</span><button class="btn primary" style="margin-left:auto" onclick="openMemberRepository('${m.id}')">Abrir repositório</button></div></div>
    <div class="dsec"><div class="lab">Tema de pesquisa</div><p>${esc(m.tema)||'<span style="color:var(--ink-3)">sem descrição</span>'}</p></div>
    <div class="dsec"><div class="lab">Linhas de pesquisa</div><div class="chips">${m.linhas.map(l=>`<span class="chip linha" style="border-color:${linhaColor(l)}55" onclick="filterLinha('${esc(l)}')">${esc(l)}</span>`).join('')||'<span style="color:var(--ink-3)">nenhuma</span>'}</div></div>
    ${m.plataforma?`<div class="dsec"><div class="lab">Plataforma</div><a class="platlink" href="${esc(m.plataforma)}" target="_blank" rel="noopener"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 13a5 5 0 0 0 7.5.5l3-3a5 5 0 0 0-7-7l-1.5 1.5"/><path d="M14 11a5 5 0 0 0-7.5-.5l-3 3a5 5 0 0 0 7 7l1.5-1.5"/></svg>Abrir plataforma<span class="u">${esc(m.plataforma.replace('https://',''))}</span></a></div>`:''}
    ${m.driveLink?`<div class="dsec"><div class="lab">Dados no Drive</div><a class="platlink" href="${esc(m.driveLink)}" target="_blank" rel="noopener"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m8 3 4 7H4z"/><path d="m16 3-4 7h8z"/><path d="M4 10h16l-4 11H8z"/></svg>Abrir no Drive</a></div>`:''}
    ${(m.resultados&&m.resultados.length)?`<div class="dsec"><div class="lab">Resultados e produtos</div>${m.resultados.map(r=>`<a class="reslink" href="${esc(r.url)}" target="_blank" rel="noopener"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>${esc(r.titulo||r.url)}</a>`).join('')}</div>`:''}
    ${prazos.length?`<div class="dsec"><div class="lab">Prazos e entregas</div>${prazos.map(p=>{const di=dateInfo(p.data);const cls=p.feito?'done':di?(di.over?'over':di.soon?'soon':''):'';return `<div class="prazo ${cls}"><span>${esc(p.titulo)}</span><span class="d">${di?di.label:''}</span></div>`}).join('')}</div>`:''}
    ${m.notas?`<div class="dsec"><div class="lab">Notas</div><p>${esc(m.notas)}</p></div>`:''}
    <div class="dsec"><div class="lab">Trabalha com (${neigh.length})</div><div class="neighbors">${neigh.slice(0,10).map(x=>`<div class="nb" onclick="selectNode('${x.o.id}')"><span class="d" style="background:${NIVEIS[x.o.nivel].color}"></span>${esc(x.o.nome)}<span class="via">${esc(x.shared[0])}${x.shared.length>1?' +'+(x.shared.length-1):''}</span></div>`).join('')||'<span style="color:var(--ink-3);font-size:13px">Sem ligações pelas linhas atuais.</span>'}</div></div>
    <div class="dactions">
      ${editable?`<button class="btn primary" style="flex:1" onclick="openMember('${m.id}')">Editar</button>`:`<button class="btn" style="flex:1" disabled title="Só ${esc(m.nome)} ou o coordenador podem editar">Editar (bloqueado)</button>`}
      <button class="btn" onclick="recadoTo('${m.id}')">Recado</button>
    </div>`;
}
function filterLinha(l){
  hiddenLinhas=new Set(state.linhas.filter(x=>x!==l));
  renderAll(); toast('Filtrando por: '+l);
}

/* ============ MEMBER FORM ============ */
let formLinhas=new Set(), formResultados=[], formPrazos=[], formFoto='';
let formHub=false, formHubMotivo='', formVinculos=[];
function openMember(id){
  if(id && !canEditMember(id)){ toast('Só '+memberById(id).nome+' ou o coordenador podem editar este cadastro.'); return; }
  if(!id && !isAdmin){ toast('Só o coordenador pode cadastrar nov

--- occurrence 3 at char 573362 ---
o(a.startDate,a.dueDate);return pi.days!==null&&pi.days>=0&&pi.days<=90})}return true})
  }
  const grid=$('#trackGrid');$('#trackEmpty').style.display=members.length?'none':'block';
  grid.innerHTML=members.map(m=>{
    const acts=all.filter(a=>a.ownerId===m.id).sort((a,b)=>_activityTime(b)-_activityTime(a));const can=isAdmin||isMe(m.id);
    const periods=acts.filter(a=>TRACK_PERIOD_TYPES.has(a.type));const follows=acts.filter(a=>!TRACK_PERIOD_TYPES.has(a.type)&&!TRACK_LEGACY_TYPES.has(a.type)).sort((a,b)=>{const ad=_activityDate(a),bd=_activityDate(b);return (bd||'').localeCompare(ad||'')||_activityTime(b)-_activityTime(a)});
    const pending=follows.filter(a=>_isPending(a)||_isOverdue(a));const lastOrient=follows.filter(a=>a.type==='orientacao').sort((a,b)=>(_activityDate(b)||'').localeCompare(_activityDate(a)||''))[0];const nextDue=follows.filter(a=>!_activityDone(a)&&a.dueDate&&!dateInfo(a.dueDate)?.over).sort((a,b)=>a.dueDate.localeCompare(b.dueDate))[0];const corrections=follows.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
    const nv=NIVEIS[m.nivel]||{short:m.nivel||'',color:'#607d8b'};
    const periodsHTML=_scholarPeriodHTML(m)+periods.map(a=>_periodHTML(a.type,a.title,a.startDate,a.dueDate,can,a.id,a.progress)).join('');
    const listHTML=follows.slice(0,8).map(a=>_followItemHTML(a,can)).join('');
    return `<section class="track-person"><div class="track-person-head"><div class="track-person-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="track-person-title">${esc(m.nome)}</div><div class="track-person-sub">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.parentElement.appendChild(note)}
  note.textContent=isAdmin?'Você pode aprovar, reprov

--- occurrence 4 at char 802612 ---
);display:none;align-items:center;justify-content:center;padding:18px}
    #p44PeriodPicker .p44-card{width:min(520px,100%);background:#fff;border-radius:20px;box-shadow:0 22px 70px rgba(0,0,0,.35);overflow:hidden}
    #p44PeriodPicker .p44-head{display:flex;gap:12px;align-items:center;padding:18px 20px;border-bottom:1px solid #e3eaed;background:#f6fafb}
    #p44PeriodPicker .p44-head strong{font:800 20px/1.15 system-ui;color:#102631;flex:1}
    #p44PeriodPicker .p44-close{border:0;background:#e8eff2;border-radius:10px;width:40px;height:40px;font-size:24px;color:#1a323d}
    #p44PeriodPicker .p44-body{padding:18px 20px 22px}
    #p44PeriodPicker .p44-help{font:500 13px/1.45 system-ui;color:#60757d;margin:0 0 14px}
    #p44PeriodPicker .p44-options{display:grid;grid-template-columns:1fr 1fr;gap:10px}
    #p44PeriodPicker .p44-option{border:1px solid #d3e0e4;background:#fff;border-radius:14px;padding:14px;text-align:left;font:800 14px/1.25 system-ui;color:#17313d;min-height:62px}
    #p44PeriodPicker .p44-option:hover{border-color:#0E5C63;background:#f1faf9}
    #p44PeriodPicker .p44-option small{display:block;font:500 11px/1.35 system-ui;color:#74858c;margin-top:4px}
    #ooCloseBtn{opacity:1!important;color:#fff!important;background:#111923!important;border:2px solid #ff4d73!important;font-weight:900!important;position:relative!important;z-index:50!important;pointer-events:auto!important;visibility:visible!important}
    #ooCloseBtn:not(:disabled):hover{background:#3b1722!important;border-color:#ff315f!important;color:#fff!important}
    #ooCloseBtn .oo-full-label,#ooCloseBtn .oo-short-label{color:#fff!important;opacity:1!important}
    @media(max-width:700px){#p44PeriodPicker .p44-options{grid-template-columns:1fr}.p44-ruler-add{font-size:10px!important;padding:6px 8px!important}}
  `;
  document.head.appendChild(st);
}

function memberIdFromSection(section){
  if(!section)return'';
  for(const el of section.querySelectorAll('[onclick]')){
    const s=el.getAttribute('onclick')||'';
    let m=s.match(/openActivity\(null\s*,\s*['\"]([^'\"]+)['\"]\)/);
    if(m)return m[1];
    m=s.match(/openDossier\(['\"]([^'\"]+)['\"]\)/);
    if(m)return m[1];
    m=s.match(/openMemberRepository\(['\"]([^'\"]+)['\"]\)/);
    if(m)return m[1];
  }
  return'';
}

function ensurePicker(){
  let p=$('#p44PeriodPicker');
  if(p)return p;
  p=document.createElement('div');
  p.id='p44PeriodPicker';
  p.innerHTML=`<div class="p44-card" role="dialog" aria-modal="true" aria-labelledby="p44PickerTitle">
    <div class="p44-head"><strong id="p44PickerTitle">Régua de tempo</strong><button class="p44-close" type="button" aria-label="Fechar">×</button></div>
    <div class="p44-body"><p class="p44-help">Adicione ou edite um período acadêmico. Ele aparecerá na régua desta pessoa.</p>
      <div class="p44-options">
        <button class="p44-option" type="button" data-p44type="bolsa">🎓 Bolsa / IC<small>Modalidade, agência, início e fim</small></button>
        <button class="p44-option" type="button" data-p44type="mestrado">📘 Mestrado<small>Início, término e andamento</small></button>
        <button class="p44-option" type="button" data-p44type="doutorado">📚 Doutorado<small>Início, término e andamento</small></button>
        <button class="p44-option" type="button" data-p44type="tcc">📝 TCC<small>Início, término e andamento</small></button>
      </div>
    </div></div>`;
  document.body.appendChild(p);
  const close=()=>{p.style.display='none';p.dataset.memberId='';};
  p.querySelector('.p44-close').onclick=close;
  p.addEventListener('click',e=>{if(e.target===p)close()});
  p.querySelectorAll('[data-p44type]').forEach(b=>b.addEventListener('click',()=>{
    const id=p.dataset.memberId||'',type=b.dataset.p44type||'';close();if(!id)return;
    if(type==='bolsa'){
      if(typeof window.openMember==='function')window.openMember(id);
      setTimeout(()=>{
        const cb=$('#mBolsista'),target=cb?.closest('.fg')||$('#mBolsaModalidade')?.closest('.fg');
        if(target){target.scrollIntoView({behavior:'smooth',block:'center'});target.style.outline='3px solid #FFC857';target.style.borderRadius='10px';setTimeout(()=>target.style.outline='',2200)}
        cb?.focus();
      },180);
      return;
    }
    const section=[...document.querySelectorAll('#trackGrid .track-person')].find(x=>memberIdFromSection(x)===id);
    const reg=[...(section?.querySelectorAll('button[onclick]')||[])].find(x=>(x.getAttribute('onclick')||'').includes('openActivity(null'));
    if(reg)reg.click();
    setTimeout(()=>{
      const sel=$('#activityType');if(sel){sel.value=type;sel.dispatchEvent(new Event('change',{bubbles:true}))}
      const name=$('#activityName');if(name&&!name.value)name.value=type==='mestrado'?'Mestrado':type==='doutorado'?'Doutorado':'TCC';
      $('#activityStart')?.focus();
    },90);
  }));
  return p;
}

function openPicker(id,name){
  const p=ensurePicker();p.dataset.memberId=id||'';
  const t=$('#p44PickerTitle');if(t)t.textContent=`Régua de tempo${name?' · '+name:''}`;
  p.style.display='flex';
}

function ensureRulerButtons(){
  const grid=$('#trackGrid');if(!grid)return;
  grid.querySelectorAll('.track-person').forEach(section=>{
    const id=memberIdFromSection(section);if(!id)return;
    const title=[...section.querySelectorAll('.follow-title')].find(x=>(x.textContent||'').trim().toLowerCase()==='régua de tempo');
    if(!title||title.querySelector('.p44-ruler-add'))return;
    const name=section.querySelector('.track-person-title')?.textContent?.trim()||'';
    title.classList.add('p44-ruler-title');
    const b=document.createElement('button');b.type='button';b.className='p44-ruler-add';b.textContent='＋ Adicio

--- occurrence 5 at char 811985 ---
='undefined'){
      ACTIVITY_TYPES.ic=TYPES.ic.label;
      ACTIVITY_TYPES.ie=TYPES.ie.label;
      ACTIVITY_TYPES.it=TYPES.it.label;
      ACTIVITY_TYPES.projeto_periodo=TYPES.projeto_periodo.label;
    }
    if(typeof TRACK_ICONS!=='undefined'){
      TRACK_ICONS.ic=TYPES.ic.icon;TRACK_ICONS.ie=TYPES.ie.icon;TRACK_ICONS.it=TYPES.it.icon;TRACK_ICONS.projeto_periodo=TYPES.projeto_periodo.icon;
    }
    if(typeof TRACK_PERIOD_TYPES!=='undefined'){
      ['ic','ie','it','projeto_periodo'].forEach(t=>TRACK_PERIOD_TYPES.add(t));
    }
  }catch(e){console.warn('P45 register types',e)}
}

function addSelectOptions(){
  const sel=$('#activityType');if(!sel)return;
  const anchor=sel.querySelector('option[value="orientacao"]');
  Object.entries(TYPES).forEach(([value,meta])=>{
    if(sel.querySelector(`option[value="${value}"]`))return;
    const o=document.createElement('option');o.value=value;o.textContent=meta.label;
    if(anchor)sel.insertBefore(o,anchor);else sel.appendChild(o);
  });
  if(!sel.dataset.p45labels){
    sel.addEventListener('change',updatePeriodLabels);
    sel.dataset.p45labels='1';
  }
  updatePeriodLabels();
}

function updatePeriodLabels(){
  const sel=$('#activityType'),type=sel?.value||'';
  const start=$('#activityStart')?.closest('.fg')?.querySelector('label');
  const due=$('#activityDue')?.closest('.fg')?.querySelector('label');
  const isCustom=!!TYPES[type];
  if(start)start.textContent=isCustom?(type==='projeto_periodo'?'Início do projeto':'Início do período'):'Início / data de orientação';
  if(due)due.textContent=isCustom?(type==='projeto_periodo'?'Fim previsto do projeto':'Fim do período'):'Fim / prazo';
  if(isCustom){
    const name=$('#activityName');
    if(name&&!name.value)name.placeholder=type==='projeto_periodo'?'Ex.: Projeto de monitoramento microclimático':`Ex.: ${TYPES[type].title}`;
  }
}

function memberIdFromSection(section){
  if(!section)return'';
  for(const el of section.querySelectorAll('[onclick]')){
    const s=el.getAttribute('onclick')||'';
    let m=s.match(/openActivity\(null\s*,\s*['\"]([^'\"]+)['\"]\)/);if(m)return m[1];
    m=s.match(/openDossier\(['\"]([^'\"]+)['\"]\)/);if(m)return m[1];
    m=s.match(/openMemberRepository\(['\"]([^'\"]+)['\"]\)/);if(m)return m[1];
  }
  return'';
}

function openCustomPeriod(memberId,type){
  const section=[...document.querySelectorAll('#trackGrid .track-person')].find(x=>memberIdFromSection(x)===memberId);
  const reg=[...(section?.querySelectorAll('button[onclick]')||[])].find(x=>(x.getAttribute('onclick')||'').includes('openActivity(null'));
  if(!reg)return;
  reg.click();
  setTimeout(()=>{
    addSelectOptions();
    const sel=$('#activityType');if(sel){sel.value=type;sel.dispatchEvent(new Event('change',{bubbles:true}))}
    const name=$('#activityName');if(name&&!name.value)name.value=TYPES[type]?.title||'';
    const status=$('#activityStatus');if(status&&!status.value)status.value='andamento';
    $('#activityStart')?.focus();
  },80);
}

function enhancePicker(){
  const p=$('#p44PeriodPicker'),box=p?.querySelector('.p44-options');if(!p||!box)return;
  const defs=[
    ['ic','🔬 IC','Iniciação Científica · início e fim'],
    ['ie','🤝 IE','Iniciação à Extensão · início e fim'],
    ['it','💡 IT','Iniciação Tecnológica · início e fim'],
    ['projeto_periodo','📁 Projeto','Datas do projeto de pesquisa']
  ];
  defs.forEach(([type,label,help])=>{
    if(box.querySelector(`[data-p45type="${type}"]`))return;
    const b=document.createElement('button');b.type='button';b.className='p44-option';b.dataset.p45type=type;b.innerHTML=`${label}<small>${help}</small>`;
    b.addEventListener('click',()=>{
      const id=p.dataset.memberId||'';p.style.display='none';p.dataset.memberId='';if(id)openCustomPeriod(id,type);
    });
    box.appendChild(b);
  });
  const help=p.querySelector('.p44-help');if(help)help.textContent='Adicione períodos acadêmicos e as datas dos projetos. O próprio aluno pode registrar os períodos dele; a coordenação pode registrar para qualquer pessoa.';
}

function boot(){
  registerTypes();addSelectOptions();enhancePicker();
  setTimeout(()=>{registerTypes();addSelectOptions();enhancePicker();try{if(typeof renderTrack==='function')renderTrack()}catch(e){}},400);
  setInterval(()=>{registerTypes();addSelectOptions();enhancePicker()},1800);
  console.info('Carbonautas períodos acadêmicos',VERSION,'carregado');
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();

</script>
<script data-src="chat-whatsapp-p46.js">
/* Carbonautas P46 · chat estilo WhatsApp: Fofoca geral + Panelinhas + respostas */
(function(){
'use strict';
const VERSION='P46';
const $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=s=>String(s||'').trim();
const PRESET_TOPICS=['Geral','Campo','Laboratório','Dados','Artigos','Eventos','Prazos'];
let mode='general';
let activeTopic='Todos';
let composerTopic='Geral';
let activeGroupId='';
let replyTarget=null;
let chatDocs=[];
let members=[];
let me=null;
let myMemberId='';
let unsubChat=null,unsubMembers=null;
let mounted=false;

function f(){return window.fbFns}
function toast(msg){if(typeof window.toast==='function')window.toast(msg);else console.log('[Carbonautas]',msg)}
function millis(ts){return ts?.toMillis?ts.toMillis():(ts?.seconds?ts.seconds*1000:+new Date(ts||0))}
function timeLabel(ts){const d=new Date(millis(ts)||Date.now());return d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}
function dayKey(ts){const d=new Date(millis(ts)||Date.now());return d.toLocaleDateString('

--- occurrence 6 at char 1306354 ---
le="height:10px"></div>${pending.length?pending.slice(0,8).map(a=>followHtml(a,can)).join(''):'<div class="p97-empty">Nada urgente registrado nesta pasta.</div>'}${can?`<div class="p97-card-actions"><button type="button" data-p97-action="checkin" data-mid="${esc97(m.id)}">✓ Check-in</button><button type="button" data-p97-action="product" data-mid="${esc97(m.id)}">🎯 Produto</button><button type="button" data-p97-action="preset" data-kind="pendencia" data-mid="${esc97(m.id)}">⚠️ Pendência</button><button type="button" data-p97-action="preset" data-kind="correcao" data-mid="${esc97(m.id)}">✍️ Correção</button></div>`:''}`};
}
function buildDeckCards(m){const can=memberCanEdit(m.id);return [overviewCard(m,can),academicCard(m,can),projectCard(m,can),timelineCard(m,can),historyCard(m,can),pendingCard(m,can)]}

function ensureDeck(){
  let ov=$('#p97Deck');if(ov)return ov;
  ov=document.createElement('div');ov.id='p97Deck';ov.hidden=true;ov.innerHTML=`
    <div class="p97-backdrop" data-p97-close></div>
    <section class="p97-table" role="dialog" aria-modal="true" aria-label="Acompanhamento em cartas">
      <header class="p97-deck-head"><button type="button" data-p97-close>← Pastas</button><div class="p97-deck-title"><span id="p97DeckKicker">ACOMPANHAMENTO</span><b id="p97DeckName">Carbonauta</b></div><button type="button" class="x" data-p97-close>×</button></header>
      <div class="p97-stage-wrap"><div class="p97-stage" id="p97Stage"></div></div>
      <footer class="p97-deck-nav"><button type="button" class="prev">← Anterior</button><div class="p97-deck-count"><small id="p97CardTitle">CARTA</small><b id="p97CardCount">1 de 6</b></div><button type="button" class="next">Próxima →</button></footer>
    </section>`;
  document.body.appendChild(ov);
  ov.addEventListener('click',e=>{
    if(e.target.closest('[data-p97-close]')){closeDeck();return}
    const a=e.target.closest('[data-p97-action]');if(!a)return;
    e.preventDefault();e.stopPropagation();
    const mid=a.dataset.mid||deckMemberId,action=a.dataset.p97Action;
    if(action==='activity')call('openActivity',null,mid);
    else if(action==='dossier')call('openDossier',mid);
    else if(action==='repo')call('openMemberRepository',mid);
    else if(action==='academic-edit')call('openAcademicCardEditor',mid);
    else if(action==='academic-download')call('downloadAcademicCard',mid);
    else if(action==='academic-share')call('shareAcademicCard',mid);
    else if(action==='project')call('openProjectProfile',mid);
    else if(action==='checkin')call('openCheckin',mid);
    else if(action==='product')call('openProduct',mid);
    else if(action==='preset')call('openActivityPreset',a.dataset.kind||'pendencia',mid);
    else if(action==='edit')call('editActivity',a.dataset.id);
  });
  $('.p97-deck-nav .prev',ov).addEventListener('click',()=>navigateDeck(-1));
  $('.p97-deck-nav .next',ov).addEventListener('click',()=>navigateDeck(1));
  const stage=$('#p97Stage',ov);
  stage.addEventListener('touchstart',e=>{if(e.touches.length!==1)return;deckTouch={x:e.touches[0].clientX,y:e.touches[0].clientY,t:Date.now()}},{passive:true});
  stage.addEventListener('touchend',e=>{if(!deckTouch)return;const p=e.changedTouches[0],dx=p.clientX-deckTouch.x,dy=p.clientY-deckTouch.y,dt=Date.now()-deckTouch.t;deckTouch=null;if(dt<750&&Math.abs(dx)>52&&Math.abs(dx)>Math.abs(dy)*1.2)navigateDeck(dx<0?1:-1)},{passive:true});
  return ov;
}
function openDeck(memberId){
  const s=appState(),m=(s?.members||[]).find(x=>x.id===memberId);if(!m)return;
  const ov=ensureDeck();deckMemberId=memberId;deckCardIndex=0;deckCards=buildDeckCards(m);
  const [soft,border,accent]=colorsFor(m.nome),table=$('.p97-table',ov);table.style.setProperty('--soft',soft);table.style.setProperty('--border',border);table.style.setProperty('--accent',accent);
  $('#p97DeckName').textContent=m.nome;ov.hidden=false;document.documentElement.style.overflow='hidden';renderDeckCard(0,true);
}
function closeDeck(){const ov=$('#p97Deck');if(!ov||ov.hidden)return;ov.hidden=true;$('#p97Stage').innerHTML='';deckMemberId='';deckCards=[];deckCardIndex=0;deckAnimating=false;document.documentElement.style.overflow=''}
function renderDeckCard(dir=0,first=false){
  if(!deckCards.length)return;const stage=$('#p97Stage'),old=$('.p97-playing-card',stage),card=deckCards[deckCardIndex];
  const put=()=>{stage.innerHTML='';const el=document.createElement('article');el.className='p97-playing-card';el.innerHTML=card.html;stage.appendChild(el);$('#p97DeckKicker').textContent=card.kicker||'ACOMPANHAMENTO';$('#p97CardTitle').textContent=card.title;$('#p97CardCount').textContent=`${deckCardIndex+1} de ${deckCards.length}`;requestAnimationFrame(()=>el.classList.add('drop'));el.scrollTop=0;deckAnimating=false};
  if(!old||first){put();return}deckAnimating=true;old.classList.remove('drop');old.classList.add(dir>=0?'out-left':'out-right');setTimeout(put,175);
}
function navigateDeck(delta){if(deckAnimating||!deckCards.length)return;deckCardIndex=(deckCardIndex+delta+deckCards.length)%deckCards.length;renderDeckCard(delta,false)}
function refreshOpenDeck(){
  if(!deckMemberId)return;const s=appState(),m=(s?.members||[]).find(x=>x.id===deckMemberId);if(!m){closeDeck();return}
  const oldTitle=deckCards[deckCardIndex]?.title;deckCards=buildDeckCards(m);const i=deckCards.findIndex(c=>c.title===oldTitle);if(i>=0)deckCardIndex=i;renderDeckCard(0,true);
}
window.p202RefreshAcademicDeck=refreshOpenDeck;

function renderAfterTrack(){renderFolders();refreshOpenDeck()}
function installTrackWrapper(){
  if(trackWrapped)return;
  let original=null;try{original=window.renderTrack||renderTrack}catch(_e){}
  if(typeof original!=='function'){setTimeou


===== openDossier =====

--- occurrence 1 at char 573280 ---
m.bolsaInicio,dueDate:m.bolsaFim});return periods.some(a=>{const pi=trackPeriodInfo(a.startDate,a.dueDate);return pi.days!==null&&pi.days>=0&&pi.days<=90})}return true})
  }
  const grid=$('#trackGrid');$('#trackEmpty').style.display=members.length?'none':'block';
  grid.innerHTML=members.map(m=>{
    const acts=all.filter(a=>a.ownerId===m.id).sort((a,b)=>_activityTime(b)-_activityTime(a));const can=isAdmin||isMe(m.id);
    const periods=acts.filter(a=>TRACK_PERIOD_TYPES.has(a.type));const follows=acts.filter(a=>!TRACK_PERIOD_TYPES.has(a.type)&&!TRACK_LEGACY_TYPES.has(a.type)).sort((a,b)=>{const ad=_activityDate(a),bd=_activityDate(b);return (bd||'').localeCompare(ad||'')||_activityTime(b)-_activityTime(a)});
    const pending=follows.filter(a=>_isPending(a)||_isOverdue(a));const lastOrient=follows.filter(a=>a.type==='orientacao').sort((a,b)=>(_activityDate(b)||'').localeCompare(_activityDate(a)||''))[0];const nextDue=follows.filter(a=>!_activityDone(a)&&a.dueDate&&!dateInfo(a.dueDate)?.over).sort((a,b)=>a.dueDate.localeCompare(b.dueDate))[0];const corrections=follows.filter(a=>a.type==='correcao'&&!_activityDone(a)).length;
    const nv=NIVEIS[m.nivel]||{short:m.nivel||'',color:'#607d8b'};
    const periodsHTML=_scholarPeriodHTML(m)+periods.map(a=>_periodHTML(a.type,a.title,a.startDate,a.dueDate,can,a.id,a.progress)).join('');
    const listHTML=follows.slice(0,8).map(a=>_followItemHTML(a,can)).join('');
    return `<section class="track-person"><div class="track-person-head"><div class="track-person-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="track-person-title">${esc(m.nome)}</div><div class="track-person-sub">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · Bolsista':''}</div></div><div class="track-person-actions"><span class="track-health ${memberHealth(m).level}">● ${memberHealth(m).level==='green'?'Em dia':memberHealth(m).level==='yellow'?'Atenção':'Intervir'}</span>${can?`<button class="btn primary" type="button" onclick="openActivity(null,'${m.id}')">＋ Registrar</button>`:''}<button class="btn" type="button" onclick="openDossier('${m.id}')">Dossiê</button><button class="btn" type="button" onclick="openMemberRepository('${m.id}')">Repositório</button></div></div><div class="track-person-body">${projectIdentityHTML(m,can)}${can?`<div class="track-quick-actions"><button class="btn" onclick="openCheckin('${m.id}')">✓ Check-in</button><button class="btn" onclick="openProduct('${m.id}')">🎯 Produto</button><button class="btn" onclick="openActivityPreset('pendencia','${m.id}')">⚠️ Pendência</button><button class="btn" onclick="openActivityPreset('correcao','${m.id}')">✍️ Correção</button></div>`:''}<div class="track-dashboard"><div><div class="follow-title">Régua de tempo</div><div class="track-periods">${periodsHTML||'<div class="track-empty-person">Nenhum período de bolsa, mestrado, doutorado ou TCC registrado.</div>'}</div></div><aside class="track-sidebox"><h4>Situação do acompanhamento</h4><div class="track-mini-stat"><span>Correções pendentes</span><b>${corrections}</b></div><div class="track-mini-stat"><span>Última orientação</span><b>${lastOrient?fmtDate(lastOrient.startDate||lastOrient.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Próxima entrega</span><b>${nextDue?fmtDate(nextDue.dueDate):'—'}</b></div><div class="track-mini-stat"><span>Pendências / alertas</span><b>${pending.length}</b></div></aside></div><div class="follow-list"><div class="follow-title">Histórico e próximos passos</div>${listHTML||'<div class="track-empty-person">Sem orientações, correções ou pendências registradas.</div>'}</div></div></section>`;
  }).join('');
}
function syncActivityTypeUi(){const type=$('#activityType')?.value;const hint=$('#correctionCycleHint');if(hint)hint.style.display=type==='correcao'?'block':'none'}
function openActivity(id=null,ownerPreset=''){
  if(!myId&&!isAdmin){toast('Seu perfil precisa estar vinculado ao login.');return}
  editingActivityId=id;activityOwnerPreset=ownerPreset||'';const a=id?state.activities.find(x=>x.id===id):null;
  if(a&&!(isAdmin||isMe(a.ownerId))){toast('Sem permissão para editar este acompanhamento.');return}
  activityLoadedRevision=a?.updatedAt?.toMillis?.()??null;
  const sel=$('#activityOwnerSel'),ordered=[...state.members].sort((x,y)=>x.nome.localeCompare(y.nome));sel.innerHTML=ordered.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');
  $('#activityOwnerRow').style.display=isAdmin?'block':'none';sel.value=a?.ownerId||activityOwnerPreset||(myId||ordered[0]?.id||'');
  $('#activityTitle').textContent=a?'Editar acompanhamento':'Registrar acompanhamento';$('#activityType').value=a?.type&&ACTIVITY_TYPES[a.type]?a.type:'orientacao';$('#activityStatus').value=a?.status||'andamento';$('#activityName').value=a?.title||'';$('#activityDesc').value=a?.description||'';$('#activityStart').value=a?.startDate||'';$('#activityDue').value=a?.dueDate||'';$('#activityProgress').value=a?.progress||0;$('#activityProgressLabel').textContent=(a?.progress||0)+'%';$('#deleteActivityBtn').style.display=a?'inline-flex':'none';syncActivityTypeUi();syncActivityDecisionUi(a);openOverlay('activityOverlay');
}
function syncActivityDecisionUi(activity){
  const select=$('#activityStatus');
  [...select.options].forEach(o=>{o.disabled=!isAdmin&&['aprovado','reprovado'].includes(o.value)});
  if(!isAdmin&&['aprovado','reprovado'].includes(select.value))select.value='revisando';
  let note=$('#activityDecisionHint');
  if(!note){note=document.createElement('p');note.id='activityDecisionHint';note.className='muted';select.pare

--- occurrence 2 at char 596855 ---
VITY_TYPES[a.type]||a.type)}</td><td><b>${esc(a.title||'')}</b>${a.description?'<br>'+esc(a.description):''}</td><td>${esc(ACTIVITY_STATUS[a.status]||a.status||'')}</td><td>${esc(a.dueDate?fmtDate(a.dueDate):a.startDate?fmtDate(a.startDate):'')}</td></tr>`).join('')}</tbody></table>`:'<div class="dash-empty">Sem registros.</div>';return `<article class="dossier-sheet"><div class="dossier-head"><div class="dossier-avatar" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="dossier-name">${esc(m.nome)}</div><div class="dossier-meta">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.bolsista?' · Bolsista':''} · Saúde: ${h.level==='green'?'em dia':h.level==='yellow'?'atenção':'intervenção'}</div></div></div><section class="dossier-section"><h3>★ Pergunta científica</h3><div class="dossier-question">${esc(m.perguntaCientifica||'Ainda não preenchida.')}</div></section><section class="dossier-section"><h3>Identidade do projeto</h3><table class="dossier-table"><tr><th>Objetivo</th><td>${esc(m.objetivoProjeto||'—')}</td></tr><tr><th>Público atendido</th><td>${esc(m.grupoAtendido||'—')}</td></tr><tr><th>Visibilidade</th><td>${esc(m.ideiaVisibilidade||'—')}</td></tr><tr><th>Outros</th><td>${esc(m.projetoOutros||'—')}</td></tr></table></section><section class="dossier-section"><h3>Acompanhamento (${acts.length})</h3>${table}</section><section class="dossier-section"><h3>Produtos (${products.length}) · Check-ins (${checkins.length}) · Repositório (${repo.length}) · Prazos (${cron.length})</h3><div class="dash-sub">O Excel do dossiê contém as tabelas completas de cada bloco.</div></section></article>`}
function refreshDossierPerson(){const sel=$('#dossierPerson'),cur=sel.value;const list=(isAdmin?state.members:state.members.filter(m=>m.id===myId)).sort((a,b)=>a.nome.localeCompare(b.nome));sel.innerHTML=list.map(m=>`<option value="${m.id}">${esc(m.nome)}</option>`).join('');sel.value=list.some(m=>m.id===cur)?cur:(myId&&list.some(m=>m.id===myId)?myId:list[0]?.id||'')}
function renderDossier(){const m=memberById($('#dossierPerson')?.value);if(!m)return;$('#dossierContent').innerHTML=dossierHTML(m)}
function openDossier(memberId=''){refreshDossierPerson();if(memberId&&[...$('#dossierPerson').options].some(o=>o.value===memberId))$('#dossierPerson').value=memberId;renderDossier();openOverlay('dossierOverlay')}
function dossierWorkbook(memberId){const m=memberById(memberId),{acts,repo,cron,checkins,products}=dossierRowsFor(m);const wb=XLSX.utils.book_new();const profile=[{Nome:m.nome,Nivel:NIVEIS[m.nivel]?.label||m.nivel,Programa:m.programa||'',Status:m.status||'',Bolsista:m.bolsista?'Sim':'Não','Pergunta científica':m.perguntaCientifica||'','Objetivo central':m.objetivoProjeto||'','Grupo atendido':m.grupoAtendido||'','Ideia de visibilidade':m.ideiaVisibilidade||'',Outros:m.projetoOutros||''}];XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(profile),'Perfil');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(acts.map(a=>({Tipo:ACTIVITY_TYPES[a.type]||a.type,Titulo:a.title||'',Status:ACTIVITY_STATUS[a.status]||a.status,Inicio:a.startDate||'',Prazo:a.dueDate||'',Descricao:a.description||''}))), 'Acompanhamento');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(products.map(a=>({Produto:a.title||'',Tipo:PRODUCT_TYPES[a.productType]||a.productType,Status:ACTIVITY_STATUS[a.status]||a.status,Prazo:a.dueDate||'',Observacoes:a.description||''}))), 'Produtos');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(checkins.map(a=>({Mes:a.checkinMonth||'',Fez:a.checkinDid||'',Entregou:a.checkinDelivered||'',Dificuldade:a.checkinDifficulty||'',Proximo:a.checkinNext||''}))), 'Checkins');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(repo.map(r=>({Titulo:r.title||'',Tipo:r.kind||'',Arquivo:r.fileName||'',URL:r.url||'',Correcao:r.reviewFlow?'Sim':'Não',Versao:r.reviewVersion||'',Status:r.reviewStatus||''}))), 'Repositorio');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(cron.map(c=>({Titulo:c.titulo||'',Data:c.data||'',Concluido:c.feito?'Sim':'Não'}))), 'Prazos');return wb}
function exportDossierExcel(){const id=$('#dossierPerson').value,m=memberById(id);if(!m)return;if(!window.XLSX){ensureLib('xlsx').then(exportDossierExcel).catch(e=>toast(e.message));return}XLSX.writeFile(dossierWorkbook(id),`Dossie_${slugify(m.nome)}.xlsx`)}
function exportDossierCsv(){const id=$('#dossierPerson').value,m=memberById(id);if(!m)return;const {acts}=dossierRowsFor(m),rows=[['Nome','Tipo','Título','Status','Início','Prazo','Descrição'],...acts.map(a=>[m.nome,ACTIVITY_TYPES[a.type]||a.type,a.title||'',ACTIVITY_STATUS[a.status]||a.status,a.startDate||'',a.dueDate||'',a.description||''])];const csv='\ufeff'+rows.map(r=>r.map(v=>'"'+csvTextCell(v).replace(/"/g,'""')+'"').join(';')).join('\n');const b=new Blob([csv],{type:'text/csv;charset=utf-8'}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=`Dossie_${slugify(m.nome)}.csv`;a.click();URL.revokeObjectURL(u)}
function exportDossierWord(){const m=memberById($('#dossierPerson').value);if(!m)return;const html=`<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:w="urn:schemas-microsoft-com:office:word" xmlns="http://www.w3.org/TR/REC-html40"><head><meta charset="utf-8"><title>Dossiê · ${esc(m.nome)}</title><style>body{font-family:Arial,sans-serif;margin:32px;color:#152b38}h1,h2,h3{font-family:Georgia,serif}table{width:100%;border-collapse:collapse;font-size:11pt}td,th{border:1px solid #ddd;padding:7px;text-align:left}.dossier-question{padding:14px;background:#f2efff;border:1px solid #d7cdfa}</style></head><body><h1>Dossiê do Carbonauta</h1>${dossierHT

--- occurrence 3 at char 686288 ---
eToGoogle;
if($('#pullGoogleBtn'))$('#pullGoogleBtn').onclick=pullGoogleToCarbonautas;
if($('#medalPrev'))$('#medalPrev').onclick=()=>shiftMedalMonth(-1);
if($('#medalNext'))$('#medalNext').onclick=()=>shiftMedalMonth(1);
if($('#medalToday'))$('#medalToday').onclick=()=>{medalCursor=new Date(new Date().getFullYear(),new Date().getMonth(),1);medalLoadedKey='';renderMedals()};
if($('#reloadGameBtn'))$('#reloadGameBtn').onclick=()=>{const f=$('#carbonautaGameFrame');if(f)f.src='./game.html?v=P28&t='+Date.now()};
window.addEventListener('message',e=>{const frame=$('#carbonautaGameFrame');if(!frame||e.source!==frame.contentWindow)return;if(location.origin!=='null'&&e.origin!==location.origin)return;const d=e.data||{};if(d.type==='carbonautas-game-result'&&d.completed===true)reportGameReward(d.score)});

$('#chatInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(120,e.target.scrollHeight)+'px'});
$('#chatInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat()}});
$('#sendBtn').onclick=sendChat;
if($('#mOnlyMine')) $('#mOnlyMine').onchange=renderRecados;
if($('#muralRecadoBtn')) $('#muralRecadoBtn').onclick=openRecado;
const muralCommandEl=$('#muralCommand'), muralCommandToggle=$('#muralCommandToggle');
function syncMuralCommandToggle(){ if(!muralCommandEl||!muralCommandToggle) return; muralCommandToggle.textContent=muralCommandEl.classList.contains('collapsed') ? 'Mostrar atalhos' : 'Ocultar atalhos'; }
if(muralCommandToggle){ muralCommandToggle.onclick=()=>{ muralCommandEl.classList.toggle('collapsed'); syncMuralCommandToggle(); }; syncMuralCommandToggle(); }
if($('#goMuralBtn')) $('#goMuralBtn').onclick=()=>switchView('mural');
if($('#muralNewBtn')) $('#muralNewBtn').onclick=()=>openPostModal('atualizacao');
if($('#muralDirectTopBtn')) $('#muralDirectTopBtn').onclick=openRecado;
if($('#muralDirectBtn')) $('#muralDirectBtn').onclick=openRecado;

$('#dashCheckinBtn').onclick=()=>openCheckin();
$('#dashProductBtn').onclick=()=>openProduct();
$('#dashMedalsBtn').onclick=()=>switchView('medalhas');
$('#dashGameBtn').onclick=()=>switchView('jogo');
$('#dashDossierBtn').onclick=()=>openDossier();
$('#dashNewTaskBtn').onclick=()=>openActivityPreset('pendencia');
$('#dashProductsAllBtn').onclick=()=>switchView('track');
$('#dashBackupBtn').onclick=exportGeneralBackup;
$('#trackCheckinBtn').onclick=()=>openCheckin();
$('#trackProductBtn').onclick=()=>openProduct();
$('#trackDossierBtn').onclick=()=>openDossier();
$('#checkinOwner').onchange=loadCheckinForSelection; $('#checkinMonth').onchange=loadCheckinForSelection; $('#saveCheckinBtn').onclick=saveCheckin;
$('#saveProductBtn').onclick=saveProduct;
$('#dossierPerson').onchange=renderDossier; $('#dossierExcelBtn').onclick=exportDossierExcel; $('#dossierCsvBtn').onclick=exportDossierCsv; $('#dossierWordBtn').onclick=exportDossierWord; $('#dossierPrintBtn').onclick=printDossier;
$('#addActivityBtn').onclick=()=>openActivity(null);
$('#trackOwner').onchange=renderTrack; $('#trackStatus').onchange=renderTrack; $('#trackMine').onchange=renderTrack;
$('#activityProgress').oninput=e=>$('#activityProgressLabel').textContent=e.target.value+'%'; $('#activityType').onchange=syncActivityTypeUi;
$('#saveActivityBtn').onclick=saveActivity; $('#deleteActivityBtn').onclick=deleteActivity;
$('#saveProjectProfileBtn').onclick=saveProjectProfile;
if($('#newPostBtn')) $('#newPostBtn').onclick=()=>openPostModal('atualizacao'); if($('#muralPostBtn')) $('#muralPostBtn').onclick=()=>openPostModal('atualizacao'); $$('[data-mural-kind]').forEach(b=>b.onclick=()=>openMuralAction(b.dataset.muralKind)); $$('[data-mf]').forEach(b=>b.onclick=()=>{muralFilter=b.dataset.mf;$$('[data-mf]').forEach(x=>x.classList.toggle('on',x===b));renderMural();if($('#muralMsgs'))$('#muralMsgs').scrollTop=0;}); $('#postFilePick').onclick=()=>$('#postFile').click(); $('#postFile').onchange=e=>handlePostFile(e.target.files[0]); $('#savePostBtn').onclick=saveFeedPost;

$('#addPhotoBtn').onclick=openPhotoModal;
$('#gPickBtn').onclick=()=>$('#gFile').click();
$('#gFile').onchange=e=>{handleGalleryFiles(e.target.files);e.target.value=''};
$('#gProjeto').onchange=e=>{$('#gProjetoOutroRow').style.display=e.target.value==='__outro'?'block':'none'};
const gPasteZone=$('#gPasteZone');
if(gPasteZone){
  gPasteZone.addEventListener('paste',e=>{const item=[...(e.clipboardData?.items||[])].find(x=>x.type?.startsWith('image/'));if(item){e.preventDefault();handleGalleryFile(item.getAsFile(),true)}});
  gPasteZone.addEventListener('dragover',e=>{e.preventDefault();gPasteZone.classList.add('drag')});
  gPasteZone.addEventListener('dragleave',()=>gPasteZone.classList.remove('drag'));
  gPasteZone.addEventListener('drop',e=>{e.preventDefault();gPasteZone.classList.remove('drag');handleGalleryFiles([...(e.dataTransfer?.files||[])],true)});
}
$('#savePhotoBtn').onclick=savePhoto;
['fProjeto','fPessoa','fAno','fTipo'].forEach(id=>{const el=$('#'+id);if(el)el.onchange=renderFotos});
if($('#fBusca'))$('#fBusca').oninput=()=>{clearTimeout(window.__fBuscaT);window.__fBuscaT=setTimeout(renderFotos,180)};
if($('#fLimpar'))$('#fLimpar').onclick=()=>{['fProjeto','fPessoa','fAno','fTipo','fBusca'].forEach(id=>{const el=$('#'+id);if(el)el.value=''});renderFotos()};
if($('#fotosSelectBtn'))$('#fotosSelectBtn').onclick=toggleFotoSelecting;
if($('#fMais'))$('#fMais').onclick=()=>{const w=$('#fMais').closest('.mural-filters');w.classList.toggle('open');$('#fMais').textContent=w.classList.contains('open')?'Menos':'Filtros'};
if($('#muralSelAll'))$('#muralSelAll').onclick=()=>{fotosFiltradas().forEach(p=>fotoSelected.add(p.id));renderFotos()};
if($('#mura

--- occurrence 4 at char 686608 ---
w Date().getMonth(),1);medalLoadedKey='';renderMedals()};
if($('#reloadGameBtn'))$('#reloadGameBtn').onclick=()=>{const f=$('#carbonautaGameFrame');if(f)f.src='./game.html?v=P28&t='+Date.now()};
window.addEventListener('message',e=>{const frame=$('#carbonautaGameFrame');if(!frame||e.source!==frame.contentWindow)return;if(location.origin!=='null'&&e.origin!==location.origin)return;const d=e.data||{};if(d.type==='carbonautas-game-result'&&d.completed===true)reportGameReward(d.score)});

$('#chatInput').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(120,e.target.scrollHeight)+'px'});
$('#chatInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat()}});
$('#sendBtn').onclick=sendChat;
if($('#mOnlyMine')) $('#mOnlyMine').onchange=renderRecados;
if($('#muralRecadoBtn')) $('#muralRecadoBtn').onclick=openRecado;
const muralCommandEl=$('#muralCommand'), muralCommandToggle=$('#muralCommandToggle');
function syncMuralCommandToggle(){ if(!muralCommandEl||!muralCommandToggle) return; muralCommandToggle.textContent=muralCommandEl.classList.contains('collapsed') ? 'Mostrar atalhos' : 'Ocultar atalhos'; }
if(muralCommandToggle){ muralCommandToggle.onclick=()=>{ muralCommandEl.classList.toggle('collapsed'); syncMuralCommandToggle(); }; syncMuralCommandToggle(); }
if($('#goMuralBtn')) $('#goMuralBtn').onclick=()=>switchView('mural');
if($('#muralNewBtn')) $('#muralNewBtn').onclick=()=>openPostModal('atualizacao');
if($('#muralDirectTopBtn')) $('#muralDirectTopBtn').onclick=openRecado;
if($('#muralDirectBtn')) $('#muralDirectBtn').onclick=openRecado;

$('#dashCheckinBtn').onclick=()=>openCheckin();
$('#dashProductBtn').onclick=()=>openProduct();
$('#dashMedalsBtn').onclick=()=>switchView('medalhas');
$('#dashGameBtn').onclick=()=>switchView('jogo');
$('#dashDossierBtn').onclick=()=>openDossier();
$('#dashNewTaskBtn').onclick=()=>openActivityPreset('pendencia');
$('#dashProductsAllBtn').onclick=()=>switchView('track');
$('#dashBackupBtn').onclick=exportGeneralBackup;
$('#trackCheckinBtn').onclick=()=>openCheckin();
$('#trackProductBtn').onclick=()=>openProduct();
$('#trackDossierBtn').onclick=()=>openDossier();
$('#checkinOwner').onchange=loadCheckinForSelection; $('#checkinMonth').onchange=loadCheckinForSelection; $('#saveCheckinBtn').onclick=saveCheckin;
$('#saveProductBtn').onclick=saveProduct;
$('#dossierPerson').onchange=renderDossier; $('#dossierExcelBtn').onclick=exportDossierExcel; $('#dossierCsvBtn').onclick=exportDossierCsv; $('#dossierWordBtn').onclick=exportDossierWord; $('#dossierPrintBtn').onclick=printDossier;
$('#addActivityBtn').onclick=()=>openActivity(null);
$('#trackOwner').onchange=renderTrack; $('#trackStatus').onchange=renderTrack; $('#trackMine').onchange=renderTrack;
$('#activityProgress').oninput=e=>$('#activityProgressLabel').textContent=e.target.value+'%'; $('#activityType').onchange=syncActivityTypeUi;
$('#saveActivityBtn').onclick=saveActivity; $('#deleteActivityBtn').onclick=deleteActivity;
$('#saveProjectProfileBtn').onclick=saveProjectProfile;
if($('#newPostBtn')) $('#newPostBtn').onclick=()=>openPostModal('atualizacao'); if($('#muralPostBtn')) $('#muralPostBtn').onclick=()=>openPostModal('atualizacao'); $$('[data-mural-kind]').forEach(b=>b.onclick=()=>openMuralAction(b.dataset.muralKind)); $$('[data-mf]').forEach(b=>b.onclick=()=>{muralFilter=b.dataset.mf;$$('[data-mf]').forEach(x=>x.classList.toggle('on',x===b));renderMural();if($('#muralMsgs'))$('#muralMsgs').scrollTop=0;}); $('#postFilePick').onclick=()=>$('#postFile').click(); $('#postFile').onchange=e=>handlePostFile(e.target.files[0]); $('#savePostBtn').onclick=saveFeedPost;

$('#addPhotoBtn').onclick=openPhotoModal;
$('#gPickBtn').onclick=()=>$('#gFile').click();
$('#gFile').onchange=e=>{handleGalleryFiles(e.target.files);e.target.value=''};
$('#gProjeto').onchange=e=>{$('#gProjetoOutroRow').style.display=e.target.value==='__outro'?'block':'none'};
const gPasteZone=$('#gPasteZone');
if(gPasteZone){
  gPasteZone.addEventListener('paste',e=>{const item=[...(e.clipboardData?.items||[])].find(x=>x.type?.startsWith('image/'));if(item){e.preventDefault();handleGalleryFile(item.getAsFile(),true)}});
  gPasteZone.addEventListener('dragover',e=>{e.preventDefault();gPasteZone.classList.add('drag')});
  gPasteZone.addEventListener('dragleave',()=>gPasteZone.classList.remove('drag'));
  gPasteZone.addEventListener('drop',e=>{e.preventDefault();gPasteZone.classList.remove('drag');handleGalleryFiles([...(e.dataTransfer?.files||[])],true)});
}
$('#savePhotoBtn').onclick=savePhoto;
['fProjeto','fPessoa','fAno','fTipo'].forEach(id=>{const el=$('#'+id);if(el)el.onchange=renderFotos});
if($('#fBusca'))$('#fBusca').oninput=()=>{clearTimeout(window.__fBuscaT);window.__fBuscaT=setTimeout(renderFotos,180)};
if($('#fLimpar'))$('#fLimpar').onclick=()=>{['fProjeto','fPessoa','fAno','fTipo','fBusca'].forEach(id=>{const el=$('#'+id);if(el)el.value=''});renderFotos()};
if($('#fotosSelectBtn'))$('#fotosSelectBtn').onclick=toggleFotoSelecting;
if($('#fMais'))$('#fMais').onclick=()=>{const w=$('#fMais').closest('.mural-filters');w.classList.toggle('open');$('#fMais').textContent=w.classList.contains('open')?'Menos':'Filtros'};
if($('#muralSelAll'))$('#muralSelAll').onclick=()=>{fotosFiltradas().forEach(p=>fotoSelected.add(p.id));renderFotos()};
if($('#muralSelNone'))$('#muralSelNone').onclick=()=>{fotoSelected.clear();renderFotos()};
if($('#muralZipBtn'))$('#muralZipBtn').onclick=downloadSelectedPhotos;
$('#lbClose').onclick=closeLightbox;
$('#lbDel').onclick=deleteLightboxPhoto;
$('#lightbox').addEventListener('click',e=>{if(e.target.id==='lightbox')closeLightbox()});


--- occurrence 5 at char 802537 ---
icker{position:fixed;inset:0;z-index:2147483645;background:rgba(5,18,28,.68);display:none;align-items:center;justify-content:center;padding:18px}
    #p44PeriodPicker .p44-card{width:min(520px,100%);background:#fff;border-radius:20px;box-shadow:0 22px 70px rgba(0,0,0,.35);overflow:hidden}
    #p44PeriodPicker .p44-head{display:flex;gap:12px;align-items:center;padding:18px 20px;border-bottom:1px solid #e3eaed;background:#f6fafb}
    #p44PeriodPicker .p44-head strong{font:800 20px/1.15 system-ui;color:#102631;flex:1}
    #p44PeriodPicker .p44-close{border:0;background:#e8eff2;border-radius:10px;width:40px;height:40px;font-size:24px;color:#1a323d}
    #p44PeriodPicker .p44-body{padding:18px 20px 22px}
    #p44PeriodPicker .p44-help{font:500 13px/1.45 system-ui;color:#60757d;margin:0 0 14px}
    #p44PeriodPicker .p44-options{display:grid;grid-template-columns:1fr 1fr;gap:10px}
    #p44PeriodPicker .p44-option{border:1px solid #d3e0e4;background:#fff;border-radius:14px;padding:14px;text-align:left;font:800 14px/1.25 system-ui;color:#17313d;min-height:62px}
    #p44PeriodPicker .p44-option:hover{border-color:#0E5C63;background:#f1faf9}
    #p44PeriodPicker .p44-option small{display:block;font:500 11px/1.35 system-ui;color:#74858c;margin-top:4px}
    #ooCloseBtn{opacity:1!important;color:#fff!important;background:#111923!important;border:2px solid #ff4d73!important;font-weight:900!important;position:relative!important;z-index:50!important;pointer-events:auto!important;visibility:visible!important}
    #ooCloseBtn:not(:disabled):hover{background:#3b1722!important;border-color:#ff315f!important;color:#fff!important}
    #ooCloseBtn .oo-full-label,#ooCloseBtn .oo-short-label{color:#fff!important;opacity:1!important}
    @media(max-width:700px){#p44PeriodPicker .p44-options{grid-template-columns:1fr}.p44-ruler-add{font-size:10px!important;padding:6px 8px!important}}
  `;
  document.head.appendChild(st);
}

function memberIdFromSection(section){
  if(!section)return'';
  for(const el of section.querySelectorAll('[onclick]')){
    const s=el.getAttribute('onclick')||'';
    let m=s.match(/openActivity\(null\s*,\s*['\"]([^'\"]+)['\"]\)/);
    if(m)return m[1];
    m=s.match(/openDossier\(['\"]([^'\"]+)['\"]\)/);
    if(m)return m[1];
    m=s.match(/openMemberRepository\(['\"]([^'\"]+)['\"]\)/);
    if(m)return m[1];
  }
  return'';
}

function ensurePicker(){
  let p=$('#p44PeriodPicker');
  if(p)return p;
  p=document.createElement('div');
  p.id='p44PeriodPicker';
  p.innerHTML=`<div class="p44-card" role="dialog" aria-modal="true" aria-labelledby="p44PickerTitle">
    <div class="p44-head"><strong id="p44PickerTitle">Régua de tempo</strong><button class="p44-close" type="button" aria-label="Fechar">×</button></div>
    <div class="p44-body"><p class="p44-help">Adicione ou edite um período acadêmico. Ele aparecerá na régua desta pessoa.</p>
      <div class="p44-options">
        <button class="p44-option" type="button" data-p44type="bolsa">🎓 Bolsa / IC<small>Modalidade, agência, início e fim</small></button>
        <button class="p44-option" type="button" data-p44type="mestrado">📘 Mestrado<small>Início, término e andamento</small></button>
        <button class="p44-option" type="button" data-p44type="doutorado">📚 Doutorado<small>Início, término e andamento</small></button>
        <button class="p44-option" type="button" data-p44type="tcc">📝 TCC<small>Início, término e andamento</small></button>
      </div>
    </div></div>`;
  document.body.appendChild(p);
  const close=()=>{p.style.display='none';p.dataset.memberId='';};
  p.querySelector('.p44-close').onclick=close;
  p.addEventListener('click',e=>{if(e.target===p)close()});
  p.querySelectorAll('[data-p44type]').forEach(b=>b.addEventListener('click',()=>{
    const id=p.dataset.memberId||'',type=b.dataset.p44type||'';close();if(!id)return;
    if(type==='bolsa'){
      if(typeof window.openMember==='function')window.openMember(id);
      setTimeout(()=>{
        const cb=$('#mBolsista'),target=cb?.closest('.fg')||$('#mBolsaModalidade')?.closest('.fg');
        if(target){target.scrollIntoView({behavior:'smooth',block:'center'});target.style.outline='3px solid #FFC857';target.style.borderRadius='10px';setTimeout(()=>target.style.outline='',2200)}
        cb?.focus();
      },180);
      return;
    }
    const section=[...document.querySelectorAll('#trackGrid .track-person')].find(x=>memberIdFromSection(x)===id);
    const reg=[...(section?.querySelectorAll('button[onclick]')||[])].find(x=>(x.getAttribute('onclick')||'').includes('openActivity(null'));
    if(reg)reg.click();
    setTimeout(()=>{
      const sel=$('#activityType');if(sel){sel.value=type;sel.dispatchEvent(new Event('change',{bubbles:true}))}
      const name=$('#activityName');if(name&&!name.value)name.value=type==='mestrado'?'Mestrado':type==='doutorado'?'Doutorado':'TCC';
      $('#activityStart')?.focus();
    },90);
  }));
  return p;
}

function openPicker(id,name){
  const p=ensurePicker();p.dataset.memberId=id||'';
  const t=$('#p44PickerTitle');if(t)t.textContent=`Régua de tempo${name?' · '+name:''}`;
  p.style.display='flex';
}

function ensureRulerButtons(){
  const grid=$('#trackGrid');if(!grid)return;
  grid.querySelectorAll('.track-person').forEach(section=>{
    const id=memberIdFromSection(section);if(!id)return;
    const title=[...section.querySelectorAll('.follow-title')].find(x=>(x.textContent||'').trim().toLowerCase()==='régua de tempo');
    if(!title||title.querySelector('.p44-ruler-add'))return;
    const name=section.querySelector('.track-person-title')?.textContent?.trim()||'';
    title.classList.add('p44-ruler-title');
    const b=document.createElement('b

--- occurrence 6 at char 811915 ---
'}
};

function registerTypes(){
  try{
    if(typeof ACTIVITY_TYPES!=='undefined'){
      ACTIVITY_TYPES.ic=TYPES.ic.label;
      ACTIVITY_TYPES.ie=TYPES.ie.label;
      ACTIVITY_TYPES.it=TYPES.it.label;
      ACTIVITY_TYPES.projeto_periodo=TYPES.projeto_periodo.label;
    }
    if(typeof TRACK_ICONS!=='undefined'){
      TRACK_ICONS.ic=TYPES.ic.icon;TRACK_ICONS.ie=TYPES.ie.icon;TRACK_ICONS.it=TYPES.it.icon;TRACK_ICONS.projeto_periodo=TYPES.projeto_periodo.icon;
    }
    if(typeof TRACK_PERIOD_TYPES!=='undefined'){
      ['ic','ie','it','projeto_periodo'].forEach(t=>TRACK_PERIOD_TYPES.add(t));
    }
  }catch(e){console.warn('P45 register types',e)}
}

function addSelectOptions(){
  const sel=$('#activityType');if(!sel)return;
  const anchor=sel.querySelector('option[value="orientacao"]');
  Object.entries(TYPES).forEach(([value,meta])=>{
    if(sel.querySelector(`option[value="${value}"]`))return;
    const o=document.createElement('option');o.value=value;o.textContent=meta.label;
    if(anchor)sel.insertBefore(o,anchor);else sel.appendChild(o);
  });
  if(!sel.dataset.p45labels){
    sel.addEventListener('change',updatePeriodLabels);
    sel.dataset.p45labels='1';
  }
  updatePeriodLabels();
}

function updatePeriodLabels(){
  const sel=$('#activityType'),type=sel?.value||'';
  const start=$('#activityStart')?.closest('.fg')?.querySelector('label');
  const due=$('#activityDue')?.closest('.fg')?.querySelector('label');
  const isCustom=!!TYPES[type];
  if(start)start.textContent=isCustom?(type==='projeto_periodo'?'Início do projeto':'Início do período'):'Início / data de orientação';
  if(due)due.textContent=isCustom?(type==='projeto_periodo'?'Fim previsto do projeto':'Fim do período'):'Fim / prazo';
  if(isCustom){
    const name=$('#activityName');
    if(name&&!name.value)name.placeholder=type==='projeto_periodo'?'Ex.: Projeto de monitoramento microclimático':`Ex.: ${TYPES[type].title}`;
  }
}

function memberIdFromSection(section){
  if(!section)return'';
  for(const el of section.querySelectorAll('[onclick]')){
    const s=el.getAttribute('onclick')||'';
    let m=s.match(/openActivity\(null\s*,\s*['\"]([^'\"]+)['\"]\)/);if(m)return m[1];
    m=s.match(/openDossier\(['\"]([^'\"]+)['\"]\)/);if(m)return m[1];
    m=s.match(/openMemberRepository\(['\"]([^'\"]+)['\"]\)/);if(m)return m[1];
  }
  return'';
}

function openCustomPeriod(memberId,type){
  const section=[...document.querySelectorAll('#trackGrid .track-person')].find(x=>memberIdFromSection(x)===memberId);
  const reg=[...(section?.querySelectorAll('button[onclick]')||[])].find(x=>(x.getAttribute('onclick')||'').includes('openActivity(null'));
  if(!reg)return;
  reg.click();
  setTimeout(()=>{
    addSelectOptions();
    const sel=$('#activityType');if(sel){sel.value=type;sel.dispatchEvent(new Event('change',{bubbles:true}))}
    const name=$('#activityName');if(name&&!name.value)name.value=TYPES[type]?.title||'';
    const status=$('#activityStatus');if(status&&!status.value)status.value='andamento';
    $('#activityStart')?.focus();
  },80);
}

function enhancePicker(){
  const p=$('#p44PeriodPicker'),box=p?.querySelector('.p44-options');if(!p||!box)return;
  const defs=[
    ['ic','🔬 IC','Iniciação Científica · início e fim'],
    ['ie','🤝 IE','Iniciação à Extensão · início e fim'],
    ['it','💡 IT','Iniciação Tecnológica · início e fim'],
    ['projeto_periodo','📁 Projeto','Datas do projeto de pesquisa']
  ];
  defs.forEach(([type,label,help])=>{
    if(box.querySelector(`[data-p45type="${type}"]`))return;
    const b=document.createElement('button');b.type='button';b.className='p44-option';b.dataset.p45type=type;b.innerHTML=`${label}<small>${help}</small>`;
    b.addEventListener('click',()=>{
      const id=p.dataset.memberId||'';p.style.display='none';p.dataset.memberId='';if(id)openCustomPeriod(id,type);
    });
    box.appendChild(b);
  });
  const help=p.querySelector('.p44-help');if(help)help.textContent='Adicione períodos acadêmicos e as datas dos projetos. O próprio aluno pode registrar os períodos dele; a coordenação pode registrar para qualquer pessoa.';
}

function boot(){
  registerTypes();addSelectOptions();enhancePicker();
  setTimeout(()=>{registerTypes();addSelectOptions();enhancePicker();try{if(typeof renderTrack==='function')renderTrack()}catch(e){}},400);
  setInterval(()=>{registerTypes();addSelectOptions();enhancePicker()},1800);
  console.info('Carbonautas períodos acadêmicos',VERSION,'carregado');
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();

</script>
<script data-src="chat-whatsapp-p46.js">
/* Carbonautas P46 · chat estilo WhatsApp: Fofoca geral + Panelinhas + respostas */
(function(){
'use strict';
const VERSION='P46';
const $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=s=>String(s||'').trim();
const PRESET_TOPICS=['Geral','Campo','Laboratório','Dados','Artigos','Eventos','Prazos'];
let mode='general';
let activeTopic='Todos';
let composerTopic='Geral';
let activeGroupId='';
let replyTarget=null;
let chatDocs=[];
let members=[];
let me=null;
let myMemberId='';
let unsubChat=null,unsubMembers=null;
let mounted=false;

function f(){return window.fbFns}
function toast(msg){if(typeof window.toast==='function')window.toast(msg);else console.log('[Carbonautas]',msg)}
function millis(ts){return ts?.toMillis?ts.toMillis():(ts?.seconds?ts.seconds*1000:+new Date(ts||0))}
function timeLabel(ts){const d=new Date(millis(ts)||Date.now());return d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}
function dayKey(ts){

--- occurrence 7 at char 1306301 ---
strong><span>itens abertos</span></div></div><div style="height:10px"></div>${pending.length?pending.slice(0,8).map(a=>followHtml(a,can)).join(''):'<div class="p97-empty">Nada urgente registrado nesta pasta.</div>'}${can?`<div class="p97-card-actions"><button type="button" data-p97-action="checkin" data-mid="${esc97(m.id)}">✓ Check-in</button><button type="button" data-p97-action="product" data-mid="${esc97(m.id)}">🎯 Produto</button><button type="button" data-p97-action="preset" data-kind="pendencia" data-mid="${esc97(m.id)}">⚠️ Pendência</button><button type="button" data-p97-action="preset" data-kind="correcao" data-mid="${esc97(m.id)}">✍️ Correção</button></div>`:''}`};
}
function buildDeckCards(m){const can=memberCanEdit(m.id);return [overviewCard(m,can),academicCard(m,can),projectCard(m,can),timelineCard(m,can),historyCard(m,can),pendingCard(m,can)]}

function ensureDeck(){
  let ov=$('#p97Deck');if(ov)return ov;
  ov=document.createElement('div');ov.id='p97Deck';ov.hidden=true;ov.innerHTML=`
    <div class="p97-backdrop" data-p97-close></div>
    <section class="p97-table" role="dialog" aria-modal="true" aria-label="Acompanhamento em cartas">
      <header class="p97-deck-head"><button type="button" data-p97-close>← Pastas</button><div class="p97-deck-title"><span id="p97DeckKicker">ACOMPANHAMENTO</span><b id="p97DeckName">Carbonauta</b></div><button type="button" class="x" data-p97-close>×</button></header>
      <div class="p97-stage-wrap"><div class="p97-stage" id="p97Stage"></div></div>
      <footer class="p97-deck-nav"><button type="button" class="prev">← Anterior</button><div class="p97-deck-count"><small id="p97CardTitle">CARTA</small><b id="p97CardCount">1 de 6</b></div><button type="button" class="next">Próxima →</button></footer>
    </section>`;
  document.body.appendChild(ov);
  ov.addEventListener('click',e=>{
    if(e.target.closest('[data-p97-close]')){closeDeck();return}
    const a=e.target.closest('[data-p97-action]');if(!a)return;
    e.preventDefault();e.stopPropagation();
    const mid=a.dataset.mid||deckMemberId,action=a.dataset.p97Action;
    if(action==='activity')call('openActivity',null,mid);
    else if(action==='dossier')call('openDossier',mid);
    else if(action==='repo')call('openMemberRepository',mid);
    else if(action==='academic-edit')call('openAcademicCardEditor',mid);
    else if(action==='academic-download')call('downloadAcademicCard',mid);
    else if(action==='academic-share')call('shareAcademicCard',mid);
    else if(action==='project')call('openProjectProfile',mid);
    else if(action==='checkin')call('openCheckin',mid);
    else if(action==='product')call('openProduct',mid);
    else if(action==='preset')call('openActivityPreset',a.dataset.kind||'pendencia',mid);
    else if(action==='edit')call('editActivity',a.dataset.id);
  });
  $('.p97-deck-nav .prev',ov).addEventListener('click',()=>navigateDeck(-1));
  $('.p97-deck-nav .next',ov).addEventListener('click',()=>navigateDeck(1));
  const stage=$('#p97Stage',ov);
  stage.addEventListener('touchstart',e=>{if(e.touches.length!==1)return;deckTouch={x:e.touches[0].clientX,y:e.touches[0].clientY,t:Date.now()}},{passive:true});
  stage.addEventListener('touchend',e=>{if(!deckTouch)return;const p=e.changedTouches[0],dx=p.clientX-deckTouch.x,dy=p.clientY-deckTouch.y,dt=Date.now()-deckTouch.t;deckTouch=null;if(dt<750&&Math.abs(dx)>52&&Math.abs(dx)>Math.abs(dy)*1.2)navigateDeck(dx<0?1:-1)},{passive:true});
  return ov;
}
function openDeck(memberId){
  const s=appState(),m=(s?.members||[]).find(x=>x.id===memberId);if(!m)return;
  const ov=ensureDeck();deckMemberId=memberId;deckCardIndex=0;deckCards=buildDeckCards(m);
  const [soft,border,accent]=colorsFor(m.nome),table=$('.p97-table',ov);table.style.setProperty('--soft',soft);table.style.setProperty('--border',border);table.style.setProperty('--accent',accent);
  $('#p97DeckName').textContent=m.nome;ov.hidden=false;document.documentElement.style.overflow='hidden';renderDeckCard(0,true);
}
function closeDeck(){const ov=$('#p97Deck');if(!ov||ov.hidden)return;ov.hidden=true;$('#p97Stage').innerHTML='';deckMemberId='';deckCards=[];deckCardIndex=0;deckAnimating=false;document.documentElement.style.overflow=''}
function renderDeckCard(dir=0,first=false){
  if(!deckCards.length)return;const stage=$('#p97Stage'),old=$('.p97-playing-card',stage),card=deckCards[deckCardIndex];
  const put=()=>{stage.innerHTML='';const el=document.createElement('article');el.className='p97-playing-card';el.innerHTML=card.html;stage.appendChild(el);$('#p97DeckKicker').textContent=card.kicker||'ACOMPANHAMENTO';$('#p97CardTitle').textContent=card.title;$('#p97CardCount').textContent=`${deckCardIndex+1} de ${deckCards.length}`;requestAnimationFrame(()=>el.classList.add('drop'));el.scrollTop=0;deckAnimating=false};
  if(!old||first){put();return}deckAnimating=true;old.classList.remove('drop');old.classList.add(dir>=0?'out-left':'out-right');setTimeout(put,175);
}
function navigateDeck(delta){if(deckAnimating||!deckCards.length)return;deckCardIndex=(deckCardIndex+delta+deckCards.length)%deckCards.length;renderDeckCard(delta,false)}
function refreshOpenDeck(){
  if(!deckMemberId)return;const s=appState(),m=(s?.members||[]).find(x=>x.id===deckMemberId);if(!m){closeDeck();return}
  const oldTitle=deckCards[deckCardIndex]?.title;deckCards=buildDeckCards(m);const i=deckCards.findIndex(c=>c.title===oldTitle);if(i>=0)deckCardIndex=i;renderDeckCard(0,true);
}
window.p202RefreshAcademicDeck=refreshOpenDeck;

function renderAfterTrack(){renderFolders();refreshOpenDeck()}
function installTrackWrapper(){
  if(trackWrapped)return;
  let original=null;try{original=window.renderTrack||renderTrack}cat


===== p97Deck =====

--- occurrence 1 at char 1277491 ---
-left:auto;align-self:center;color:#73888e;font-size:11px;font-weight:800;white-space:nowrap;padding:8px 4px}
  .p97-folder-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px 14px}
  .p97-folder{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;min-height:155px;border:1px solid var(--border);border-radius:0 24px 24px 24px;background:linear-gradient(145deg,var(--soft),#fff 65%);box-shadow:0 16px 38px rgba(21,55,66,.08);padding:23px 18px 16px;cursor:pointer;transition:transform .18s ease,box-shadow .18s ease;overflow:visible;text-align:left}
  .p97-folder:before{content:"";position:absolute;left:-1px;top:-20px;width:44%;height:28px;border:1px solid var(--border);border-bottom:0;border-radius:15px 15px 0 0;background:var(--soft)}
  .p97-folder:hover{transform:translateY(-3px);box-shadow:0 22px 48px rgba(21,55,66,.12)}.p97-folder:active{transform:translateY(0) scale(.99)}
  .p97-folder-head{display:flex;align-items:center;gap:12px}.p97-folder-av{width:50px;height:50px;border-radius:16px;display:grid;place-items:center;background:var(--accent);color:#fff;font-weight:950;font-size:16px;overflow:hidden;flex:0 0 auto}.p97-folder-av img{width:100%;height:100%;object-fit:cover}
  .p97-folder-main{min-width:0;flex:1}.p97-folder-main b{display:block;font-size:17px;color:#173640;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.p97-folder-main small{display:block;margin-top:3px;color:#75898f;font-weight:650}
  .p97-health{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:6px 9px;font-size:10px;font-weight:900}.p97-health.good{background:#e8f7ef;color:#21704e}.p97-health.warn{background:#fff5d9;color:#956800}.p97-health.bad{background:#fee9ed;color:#b13e55}
  .p97-folder-meta{display:flex;gap:7px;flex-wrap:wrap;margin-top:16px}.p97-mini{background:rgba(255,255,255,.72);border:1px solid rgba(80,120,125,.12);border-radius:10px;padding:6px 8px;color:#526b72;font-size:10px;font-weight:800}.p97-open{margin-left:auto;color:var(--accent);font-weight:950;font-size:11px;padding:7px 3px}
  .p97-empty{padding:36px 18px;border:1px dashed #cadada;border-radius:22px;text-align:center;color:#73878d;background:#fff}
  #p97Deck[hidden]{display:none!important}#p97Deck{position:fixed;inset:0;z-index:200500;display:grid;place-items:center;padding:20px;isolation:isolate}
  #p97Deck .p97-backdrop{position:absolute;inset:0;background:rgba(10,33,43,.58);backdrop-filter:blur(13px);animation:p97Fade .2s ease both}
  #p97Deck .p97-table{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;width:min(820px,94vw);height:min(780px,90dvh);display:flex;flex-direction:column;background:linear-gradient(160deg,#f9fcfb,#eef6f4);border:1px solid rgba(255,255,255,.82);border-radius:30px;box-shadow:0 34px 100px rgba(0,25,35,.34);overflow:visible;animation:p97Table .24s ease-out both}
  #p97Deck .p97-table:before,#p97Deck .p97-table:after{content:"";position:absolute;inset:18px 34px -18px;border-radius:28px;background:color-mix(in srgb,var(--soft) 74%,#d8e9e5);z-index:-2;transform:rotate(-1.5deg);box-shadow:0 22px 60px rgba(0,25,35,.13)}#p97Deck .p97-table:after{inset:10px 18px -10px;z-index:-1;transform:rotate(.8deg);background:color-mix(in srgb,var(--soft) 45%,#fff)}
  .p97-deck-head{flex:0 0 auto;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:12px;padding:13px 14px;border-bottom:1px solid #dce8e6;background:rgba(255,255,255,.94);border-radius:30px 30px 0 0}.p97-deck-head button{height:44px;border:1px solid #d1dfe0;background:#fff;color:#23434c;border-radius:14px;padding:0 13px;font-weight:900}.p97-deck-head .x{width:44px;padding:0;font-size:24px}.p97-deck-title{min-width:0}.p97-deck-title span{display:block;font-size:9px;letter-spacing:.14em;color:#748990;font-weight:950}.p97-deck-title b{display:block;margin-top:2px;color:#173640;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .p97-stage-wrap{position:relative;flex:1 1 auto;min-height:0;overflow:hidden;padding:22px 24px 14px}.p97-stage{height:100%;position:relative;display:grid}
  .p97-playing-card{grid-area:1/1;position:relative;width:100%;height:100%;overflow:auto;background:linear-gradient(160deg,var(--soft),#fff 35%);border:1px solid var(--border);border-top:6px solid var(--accent);border-radius:25px;box-shadow:0 24px 58px rgba(16,48,58,.16);padding:20px;will-change:transform,opacity;overscroll-behavior:contain}
  .p97-playing-card:before{content:"";position:absolute;inset:0 0 auto;height:120px;background:linear-gradient(120deg,var(--soft),transparent);pointer-events:none}.p97-playing-card>*{position:relative;z-index:1}
  .p97-playing-card.drop{animation:p97Drop .42s cubic-bezier(.16,.88,.3,1.18) both}.p97-playing-card.out-left{animation:p97OutLeft .19s ease-in both}.p97-playing-card.out-right{animation:p97OutRight .19s ease-in both}
  .p97-card-kicker{font-size:9px;font-weight:950;letter-spacing:.13em;color:var(--accent);text-transform:uppercase}.p97-card-title{font-family:'Fraunces',serif;font-size:27px;line-height:1.04;color:#173640;margin:4px 0 14px}
  .p97-personhero{display:flex;gap:14px;align-items:center}.p97-personhero .av{width:66px;height:66px;border-radius:21px;background:var(--accent);color:#fff;display:grid;place-items:center;font-size:20px;font-weight:950;overflow:hidden;flex:0 0 auto}.p97-personhero .av img{width:100%;height:100%;object-fit:cover}.p97-personhero h3{margin:0;color:#173640;font-size:22px}.p97-personhero p{margin:3px 0 0;color:#72868d}.p97-summarychips{display:flex;gap:7px;flex-wrap:wrap;margin:15px 0}.p97-summarychips span{border:1px solid rgba(64,108,115,.14);background:rgba(255,255,255,.72);border-radius:11px;

--- occurrence 2 at char 1277531 ---
8e;font-size:11px;font-weight:800;white-space:nowrap;padding:8px 4px}
  .p97-folder-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px 14px}
  .p97-folder{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;min-height:155px;border:1px solid var(--border);border-radius:0 24px 24px 24px;background:linear-gradient(145deg,var(--soft),#fff 65%);box-shadow:0 16px 38px rgba(21,55,66,.08);padding:23px 18px 16px;cursor:pointer;transition:transform .18s ease,box-shadow .18s ease;overflow:visible;text-align:left}
  .p97-folder:before{content:"";position:absolute;left:-1px;top:-20px;width:44%;height:28px;border:1px solid var(--border);border-bottom:0;border-radius:15px 15px 0 0;background:var(--soft)}
  .p97-folder:hover{transform:translateY(-3px);box-shadow:0 22px 48px rgba(21,55,66,.12)}.p97-folder:active{transform:translateY(0) scale(.99)}
  .p97-folder-head{display:flex;align-items:center;gap:12px}.p97-folder-av{width:50px;height:50px;border-radius:16px;display:grid;place-items:center;background:var(--accent);color:#fff;font-weight:950;font-size:16px;overflow:hidden;flex:0 0 auto}.p97-folder-av img{width:100%;height:100%;object-fit:cover}
  .p97-folder-main{min-width:0;flex:1}.p97-folder-main b{display:block;font-size:17px;color:#173640;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.p97-folder-main small{display:block;margin-top:3px;color:#75898f;font-weight:650}
  .p97-health{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:6px 9px;font-size:10px;font-weight:900}.p97-health.good{background:#e8f7ef;color:#21704e}.p97-health.warn{background:#fff5d9;color:#956800}.p97-health.bad{background:#fee9ed;color:#b13e55}
  .p97-folder-meta{display:flex;gap:7px;flex-wrap:wrap;margin-top:16px}.p97-mini{background:rgba(255,255,255,.72);border:1px solid rgba(80,120,125,.12);border-radius:10px;padding:6px 8px;color:#526b72;font-size:10px;font-weight:800}.p97-open{margin-left:auto;color:var(--accent);font-weight:950;font-size:11px;padding:7px 3px}
  .p97-empty{padding:36px 18px;border:1px dashed #cadada;border-radius:22px;text-align:center;color:#73878d;background:#fff}
  #p97Deck[hidden]{display:none!important}#p97Deck{position:fixed;inset:0;z-index:200500;display:grid;place-items:center;padding:20px;isolation:isolate}
  #p97Deck .p97-backdrop{position:absolute;inset:0;background:rgba(10,33,43,.58);backdrop-filter:blur(13px);animation:p97Fade .2s ease both}
  #p97Deck .p97-table{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;width:min(820px,94vw);height:min(780px,90dvh);display:flex;flex-direction:column;background:linear-gradient(160deg,#f9fcfb,#eef6f4);border:1px solid rgba(255,255,255,.82);border-radius:30px;box-shadow:0 34px 100px rgba(0,25,35,.34);overflow:visible;animation:p97Table .24s ease-out both}
  #p97Deck .p97-table:before,#p97Deck .p97-table:after{content:"";position:absolute;inset:18px 34px -18px;border-radius:28px;background:color-mix(in srgb,var(--soft) 74%,#d8e9e5);z-index:-2;transform:rotate(-1.5deg);box-shadow:0 22px 60px rgba(0,25,35,.13)}#p97Deck .p97-table:after{inset:10px 18px -10px;z-index:-1;transform:rotate(.8deg);background:color-mix(in srgb,var(--soft) 45%,#fff)}
  .p97-deck-head{flex:0 0 auto;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:12px;padding:13px 14px;border-bottom:1px solid #dce8e6;background:rgba(255,255,255,.94);border-radius:30px 30px 0 0}.p97-deck-head button{height:44px;border:1px solid #d1dfe0;background:#fff;color:#23434c;border-radius:14px;padding:0 13px;font-weight:900}.p97-deck-head .x{width:44px;padding:0;font-size:24px}.p97-deck-title{min-width:0}.p97-deck-title span{display:block;font-size:9px;letter-spacing:.14em;color:#748990;font-weight:950}.p97-deck-title b{display:block;margin-top:2px;color:#173640;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .p97-stage-wrap{position:relative;flex:1 1 auto;min-height:0;overflow:hidden;padding:22px 24px 14px}.p97-stage{height:100%;position:relative;display:grid}
  .p97-playing-card{grid-area:1/1;position:relative;width:100%;height:100%;overflow:auto;background:linear-gradient(160deg,var(--soft),#fff 35%);border:1px solid var(--border);border-top:6px solid var(--accent);border-radius:25px;box-shadow:0 24px 58px rgba(16,48,58,.16);padding:20px;will-change:transform,opacity;overscroll-behavior:contain}
  .p97-playing-card:before{content:"";position:absolute;inset:0 0 auto;height:120px;background:linear-gradient(120deg,var(--soft),transparent);pointer-events:none}.p97-playing-card>*{position:relative;z-index:1}
  .p97-playing-card.drop{animation:p97Drop .42s cubic-bezier(.16,.88,.3,1.18) both}.p97-playing-card.out-left{animation:p97OutLeft .19s ease-in both}.p97-playing-card.out-right{animation:p97OutRight .19s ease-in both}
  .p97-card-kicker{font-size:9px;font-weight:950;letter-spacing:.13em;color:var(--accent);text-transform:uppercase}.p97-card-title{font-family:'Fraunces',serif;font-size:27px;line-height:1.04;color:#173640;margin:4px 0 14px}
  .p97-personhero{display:flex;gap:14px;align-items:center}.p97-personhero .av{width:66px;height:66px;border-radius:21px;background:var(--accent);color:#fff;display:grid;place-items:center;font-size:20px;font-weight:950;overflow:hidden;flex:0 0 auto}.p97-personhero .av img{width:100%;height:100%;object-fit:cover}.p97-personhero h3{margin:0;color:#173640;font-size:22px}.p97-personhero p{margin:3px 0 0;color:#72868d}.p97-summarychips{display:flex;gap:7px;flex-wrap:wrap;margin:15px 0}.p97-summarychips span{border:1px solid rgba(64,108,115,.14);background:rgba(255,255,255,.72);border-radius:11px;padding:7px 9px;color:#536c73;font-size:

--- occurrence 3 at char 1277644 ---
te-columns:repeat(2,minmax(0,1fr));gap:16px 14px}
  .p97-folder{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;min-height:155px;border:1px solid var(--border);border-radius:0 24px 24px 24px;background:linear-gradient(145deg,var(--soft),#fff 65%);box-shadow:0 16px 38px rgba(21,55,66,.08);padding:23px 18px 16px;cursor:pointer;transition:transform .18s ease,box-shadow .18s ease;overflow:visible;text-align:left}
  .p97-folder:before{content:"";position:absolute;left:-1px;top:-20px;width:44%;height:28px;border:1px solid var(--border);border-bottom:0;border-radius:15px 15px 0 0;background:var(--soft)}
  .p97-folder:hover{transform:translateY(-3px);box-shadow:0 22px 48px rgba(21,55,66,.12)}.p97-folder:active{transform:translateY(0) scale(.99)}
  .p97-folder-head{display:flex;align-items:center;gap:12px}.p97-folder-av{width:50px;height:50px;border-radius:16px;display:grid;place-items:center;background:var(--accent);color:#fff;font-weight:950;font-size:16px;overflow:hidden;flex:0 0 auto}.p97-folder-av img{width:100%;height:100%;object-fit:cover}
  .p97-folder-main{min-width:0;flex:1}.p97-folder-main b{display:block;font-size:17px;color:#173640;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.p97-folder-main small{display:block;margin-top:3px;color:#75898f;font-weight:650}
  .p97-health{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:6px 9px;font-size:10px;font-weight:900}.p97-health.good{background:#e8f7ef;color:#21704e}.p97-health.warn{background:#fff5d9;color:#956800}.p97-health.bad{background:#fee9ed;color:#b13e55}
  .p97-folder-meta{display:flex;gap:7px;flex-wrap:wrap;margin-top:16px}.p97-mini{background:rgba(255,255,255,.72);border:1px solid rgba(80,120,125,.12);border-radius:10px;padding:6px 8px;color:#526b72;font-size:10px;font-weight:800}.p97-open{margin-left:auto;color:var(--accent);font-weight:950;font-size:11px;padding:7px 3px}
  .p97-empty{padding:36px 18px;border:1px dashed #cadada;border-radius:22px;text-align:center;color:#73878d;background:#fff}
  #p97Deck[hidden]{display:none!important}#p97Deck{position:fixed;inset:0;z-index:200500;display:grid;place-items:center;padding:20px;isolation:isolate}
  #p97Deck .p97-backdrop{position:absolute;inset:0;background:rgba(10,33,43,.58);backdrop-filter:blur(13px);animation:p97Fade .2s ease both}
  #p97Deck .p97-table{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;width:min(820px,94vw);height:min(780px,90dvh);display:flex;flex-direction:column;background:linear-gradient(160deg,#f9fcfb,#eef6f4);border:1px solid rgba(255,255,255,.82);border-radius:30px;box-shadow:0 34px 100px rgba(0,25,35,.34);overflow:visible;animation:p97Table .24s ease-out both}
  #p97Deck .p97-table:before,#p97Deck .p97-table:after{content:"";position:absolute;inset:18px 34px -18px;border-radius:28px;background:color-mix(in srgb,var(--soft) 74%,#d8e9e5);z-index:-2;transform:rotate(-1.5deg);box-shadow:0 22px 60px rgba(0,25,35,.13)}#p97Deck .p97-table:after{inset:10px 18px -10px;z-index:-1;transform:rotate(.8deg);background:color-mix(in srgb,var(--soft) 45%,#fff)}
  .p97-deck-head{flex:0 0 auto;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:12px;padding:13px 14px;border-bottom:1px solid #dce8e6;background:rgba(255,255,255,.94);border-radius:30px 30px 0 0}.p97-deck-head button{height:44px;border:1px solid #d1dfe0;background:#fff;color:#23434c;border-radius:14px;padding:0 13px;font-weight:900}.p97-deck-head .x{width:44px;padding:0;font-size:24px}.p97-deck-title{min-width:0}.p97-deck-title span{display:block;font-size:9px;letter-spacing:.14em;color:#748990;font-weight:950}.p97-deck-title b{display:block;margin-top:2px;color:#173640;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .p97-stage-wrap{position:relative;flex:1 1 auto;min-height:0;overflow:hidden;padding:22px 24px 14px}.p97-stage{height:100%;position:relative;display:grid}
  .p97-playing-card{grid-area:1/1;position:relative;width:100%;height:100%;overflow:auto;background:linear-gradient(160deg,var(--soft),#fff 35%);border:1px solid var(--border);border-top:6px solid var(--accent);border-radius:25px;box-shadow:0 24px 58px rgba(16,48,58,.16);padding:20px;will-change:transform,opacity;overscroll-behavior:contain}
  .p97-playing-card:before{content:"";position:absolute;inset:0 0 auto;height:120px;background:linear-gradient(120deg,var(--soft),transparent);pointer-events:none}.p97-playing-card>*{position:relative;z-index:1}
  .p97-playing-card.drop{animation:p97Drop .42s cubic-bezier(.16,.88,.3,1.18) both}.p97-playing-card.out-left{animation:p97OutLeft .19s ease-in both}.p97-playing-card.out-right{animation:p97OutRight .19s ease-in both}
  .p97-card-kicker{font-size:9px;font-weight:950;letter-spacing:.13em;color:var(--accent);text-transform:uppercase}.p97-card-title{font-family:'Fraunces',serif;font-size:27px;line-height:1.04;color:#173640;margin:4px 0 14px}
  .p97-personhero{display:flex;gap:14px;align-items:center}.p97-personhero .av{width:66px;height:66px;border-radius:21px;background:var(--accent);color:#fff;display:grid;place-items:center;font-size:20px;font-weight:950;overflow:hidden;flex:0 0 auto}.p97-personhero .av img{width:100%;height:100%;object-fit:cover}.p97-personhero h3{margin:0;color:#173640;font-size:22px}.p97-personhero p{margin:3px 0 0;color:#72868d}.p97-summarychips{display:flex;gap:7px;flex-wrap:wrap;margin:15px 0}.p97-summarychips span{border:1px solid rgba(64,108,115,.14);background:rgba(255,255,255,.72);border-radius:11px;padding:7px 9px;color:#536c73;font-size:10px;font-weight:850}
  .p97-card-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:16px}.p97-card-actions b

--- occurrence 4 at char 1277785 ---
:155px;border:1px solid var(--border);border-radius:0 24px 24px 24px;background:linear-gradient(145deg,var(--soft),#fff 65%);box-shadow:0 16px 38px rgba(21,55,66,.08);padding:23px 18px 16px;cursor:pointer;transition:transform .18s ease,box-shadow .18s ease;overflow:visible;text-align:left}
  .p97-folder:before{content:"";position:absolute;left:-1px;top:-20px;width:44%;height:28px;border:1px solid var(--border);border-bottom:0;border-radius:15px 15px 0 0;background:var(--soft)}
  .p97-folder:hover{transform:translateY(-3px);box-shadow:0 22px 48px rgba(21,55,66,.12)}.p97-folder:active{transform:translateY(0) scale(.99)}
  .p97-folder-head{display:flex;align-items:center;gap:12px}.p97-folder-av{width:50px;height:50px;border-radius:16px;display:grid;place-items:center;background:var(--accent);color:#fff;font-weight:950;font-size:16px;overflow:hidden;flex:0 0 auto}.p97-folder-av img{width:100%;height:100%;object-fit:cover}
  .p97-folder-main{min-width:0;flex:1}.p97-folder-main b{display:block;font-size:17px;color:#173640;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.p97-folder-main small{display:block;margin-top:3px;color:#75898f;font-weight:650}
  .p97-health{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:6px 9px;font-size:10px;font-weight:900}.p97-health.good{background:#e8f7ef;color:#21704e}.p97-health.warn{background:#fff5d9;color:#956800}.p97-health.bad{background:#fee9ed;color:#b13e55}
  .p97-folder-meta{display:flex;gap:7px;flex-wrap:wrap;margin-top:16px}.p97-mini{background:rgba(255,255,255,.72);border:1px solid rgba(80,120,125,.12);border-radius:10px;padding:6px 8px;color:#526b72;font-size:10px;font-weight:800}.p97-open{margin-left:auto;color:var(--accent);font-weight:950;font-size:11px;padding:7px 3px}
  .p97-empty{padding:36px 18px;border:1px dashed #cadada;border-radius:22px;text-align:center;color:#73878d;background:#fff}
  #p97Deck[hidden]{display:none!important}#p97Deck{position:fixed;inset:0;z-index:200500;display:grid;place-items:center;padding:20px;isolation:isolate}
  #p97Deck .p97-backdrop{position:absolute;inset:0;background:rgba(10,33,43,.58);backdrop-filter:blur(13px);animation:p97Fade .2s ease both}
  #p97Deck .p97-table{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;width:min(820px,94vw);height:min(780px,90dvh);display:flex;flex-direction:column;background:linear-gradient(160deg,#f9fcfb,#eef6f4);border:1px solid rgba(255,255,255,.82);border-radius:30px;box-shadow:0 34px 100px rgba(0,25,35,.34);overflow:visible;animation:p97Table .24s ease-out both}
  #p97Deck .p97-table:before,#p97Deck .p97-table:after{content:"";position:absolute;inset:18px 34px -18px;border-radius:28px;background:color-mix(in srgb,var(--soft) 74%,#d8e9e5);z-index:-2;transform:rotate(-1.5deg);box-shadow:0 22px 60px rgba(0,25,35,.13)}#p97Deck .p97-table:after{inset:10px 18px -10px;z-index:-1;transform:rotate(.8deg);background:color-mix(in srgb,var(--soft) 45%,#fff)}
  .p97-deck-head{flex:0 0 auto;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:12px;padding:13px 14px;border-bottom:1px solid #dce8e6;background:rgba(255,255,255,.94);border-radius:30px 30px 0 0}.p97-deck-head button{height:44px;border:1px solid #d1dfe0;background:#fff;color:#23434c;border-radius:14px;padding:0 13px;font-weight:900}.p97-deck-head .x{width:44px;padding:0;font-size:24px}.p97-deck-title{min-width:0}.p97-deck-title span{display:block;font-size:9px;letter-spacing:.14em;color:#748990;font-weight:950}.p97-deck-title b{display:block;margin-top:2px;color:#173640;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .p97-stage-wrap{position:relative;flex:1 1 auto;min-height:0;overflow:hidden;padding:22px 24px 14px}.p97-stage{height:100%;position:relative;display:grid}
  .p97-playing-card{grid-area:1/1;position:relative;width:100%;height:100%;overflow:auto;background:linear-gradient(160deg,var(--soft),#fff 35%);border:1px solid var(--border);border-top:6px solid var(--accent);border-radius:25px;box-shadow:0 24px 58px rgba(16,48,58,.16);padding:20px;will-change:transform,opacity;overscroll-behavior:contain}
  .p97-playing-card:before{content:"";position:absolute;inset:0 0 auto;height:120px;background:linear-gradient(120deg,var(--soft),transparent);pointer-events:none}.p97-playing-card>*{position:relative;z-index:1}
  .p97-playing-card.drop{animation:p97Drop .42s cubic-bezier(.16,.88,.3,1.18) both}.p97-playing-card.out-left{animation:p97OutLeft .19s ease-in both}.p97-playing-card.out-right{animation:p97OutRight .19s ease-in both}
  .p97-card-kicker{font-size:9px;font-weight:950;letter-spacing:.13em;color:var(--accent);text-transform:uppercase}.p97-card-title{font-family:'Fraunces',serif;font-size:27px;line-height:1.04;color:#173640;margin:4px 0 14px}
  .p97-personhero{display:flex;gap:14px;align-items:center}.p97-personhero .av{width:66px;height:66px;border-radius:21px;background:var(--accent);color:#fff;display:grid;place-items:center;font-size:20px;font-weight:950;overflow:hidden;flex:0 0 auto}.p97-personhero .av img{width:100%;height:100%;object-fit:cover}.p97-personhero h3{margin:0;color:#173640;font-size:22px}.p97-personhero p{margin:3px 0 0;color:#72868d}.p97-summarychips{display:flex;gap:7px;flex-wrap:wrap;margin:15px 0}.p97-summarychips span{border:1px solid rgba(64,108,115,.14);background:rgba(255,255,255,.72);border-radius:11px;padding:7px 9px;color:#536c73;font-size:10px;font-weight:850}
  .p97-card-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:16px}.p97-card-actions button{min-height:44px;border:1px solid #d1dfe0;background:#fff;color:#21424b;border-radius:14px;padding:10px 13px;font-weight:900}.p97-card-a

--- occurrence 5 at char 1278162 ---
:28px;border:1px solid var(--border);border-bottom:0;border-radius:15px 15px 0 0;background:var(--soft)}
  .p97-folder:hover{transform:translateY(-3px);box-shadow:0 22px 48px rgba(21,55,66,.12)}.p97-folder:active{transform:translateY(0) scale(.99)}
  .p97-folder-head{display:flex;align-items:center;gap:12px}.p97-folder-av{width:50px;height:50px;border-radius:16px;display:grid;place-items:center;background:var(--accent);color:#fff;font-weight:950;font-size:16px;overflow:hidden;flex:0 0 auto}.p97-folder-av img{width:100%;height:100%;object-fit:cover}
  .p97-folder-main{min-width:0;flex:1}.p97-folder-main b{display:block;font-size:17px;color:#173640;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.p97-folder-main small{display:block;margin-top:3px;color:#75898f;font-weight:650}
  .p97-health{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:6px 9px;font-size:10px;font-weight:900}.p97-health.good{background:#e8f7ef;color:#21704e}.p97-health.warn{background:#fff5d9;color:#956800}.p97-health.bad{background:#fee9ed;color:#b13e55}
  .p97-folder-meta{display:flex;gap:7px;flex-wrap:wrap;margin-top:16px}.p97-mini{background:rgba(255,255,255,.72);border:1px solid rgba(80,120,125,.12);border-radius:10px;padding:6px 8px;color:#526b72;font-size:10px;font-weight:800}.p97-open{margin-left:auto;color:var(--accent);font-weight:950;font-size:11px;padding:7px 3px}
  .p97-empty{padding:36px 18px;border:1px dashed #cadada;border-radius:22px;text-align:center;color:#73878d;background:#fff}
  #p97Deck[hidden]{display:none!important}#p97Deck{position:fixed;inset:0;z-index:200500;display:grid;place-items:center;padding:20px;isolation:isolate}
  #p97Deck .p97-backdrop{position:absolute;inset:0;background:rgba(10,33,43,.58);backdrop-filter:blur(13px);animation:p97Fade .2s ease both}
  #p97Deck .p97-table{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;width:min(820px,94vw);height:min(780px,90dvh);display:flex;flex-direction:column;background:linear-gradient(160deg,#f9fcfb,#eef6f4);border:1px solid rgba(255,255,255,.82);border-radius:30px;box-shadow:0 34px 100px rgba(0,25,35,.34);overflow:visible;animation:p97Table .24s ease-out both}
  #p97Deck .p97-table:before,#p97Deck .p97-table:after{content:"";position:absolute;inset:18px 34px -18px;border-radius:28px;background:color-mix(in srgb,var(--soft) 74%,#d8e9e5);z-index:-2;transform:rotate(-1.5deg);box-shadow:0 22px 60px rgba(0,25,35,.13)}#p97Deck .p97-table:after{inset:10px 18px -10px;z-index:-1;transform:rotate(.8deg);background:color-mix(in srgb,var(--soft) 45%,#fff)}
  .p97-deck-head{flex:0 0 auto;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:12px;padding:13px 14px;border-bottom:1px solid #dce8e6;background:rgba(255,255,255,.94);border-radius:30px 30px 0 0}.p97-deck-head button{height:44px;border:1px solid #d1dfe0;background:#fff;color:#23434c;border-radius:14px;padding:0 13px;font-weight:900}.p97-deck-head .x{width:44px;padding:0;font-size:24px}.p97-deck-title{min-width:0}.p97-deck-title span{display:block;font-size:9px;letter-spacing:.14em;color:#748990;font-weight:950}.p97-deck-title b{display:block;margin-top:2px;color:#173640;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .p97-stage-wrap{position:relative;flex:1 1 auto;min-height:0;overflow:hidden;padding:22px 24px 14px}.p97-stage{height:100%;position:relative;display:grid}
  .p97-playing-card{grid-area:1/1;position:relative;width:100%;height:100%;overflow:auto;background:linear-gradient(160deg,var(--soft),#fff 35%);border:1px solid var(--border);border-top:6px solid var(--accent);border-radius:25px;box-shadow:0 24px 58px rgba(16,48,58,.16);padding:20px;will-change:transform,opacity;overscroll-behavior:contain}
  .p97-playing-card:before{content:"";position:absolute;inset:0 0 auto;height:120px;background:linear-gradient(120deg,var(--soft),transparent);pointer-events:none}.p97-playing-card>*{position:relative;z-index:1}
  .p97-playing-card.drop{animation:p97Drop .42s cubic-bezier(.16,.88,.3,1.18) both}.p97-playing-card.out-left{animation:p97OutLeft .19s ease-in both}.p97-playing-card.out-right{animation:p97OutRight .19s ease-in both}
  .p97-card-kicker{font-size:9px;font-weight:950;letter-spacing:.13em;color:var(--accent);text-transform:uppercase}.p97-card-title{font-family:'Fraunces',serif;font-size:27px;line-height:1.04;color:#173640;margin:4px 0 14px}
  .p97-personhero{display:flex;gap:14px;align-items:center}.p97-personhero .av{width:66px;height:66px;border-radius:21px;background:var(--accent);color:#fff;display:grid;place-items:center;font-size:20px;font-weight:950;overflow:hidden;flex:0 0 auto}.p97-personhero .av img{width:100%;height:100%;object-fit:cover}.p97-personhero h3{margin:0;color:#173640;font-size:22px}.p97-personhero p{margin:3px 0 0;color:#72868d}.p97-summarychips{display:flex;gap:7px;flex-wrap:wrap;margin:15px 0}.p97-summarychips span{border:1px solid rgba(64,108,115,.14);background:rgba(255,255,255,.72);border-radius:11px;padding:7px 9px;color:#536c73;font-size:10px;font-weight:850}
  .p97-card-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:16px}.p97-card-actions button{min-height:44px;border:1px solid #d1dfe0;background:#fff;color:#21424b;border-radius:14px;padding:10px 13px;font-weight:900}.p97-card-actions .primary{background:#168f94;border-color:#168f94;color:#fff}
  .p97-info-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.p97-info{border:1px solid rgba(75,113,118,.14);background:rgba(255,255,255,.76);border-radius:16px;padding:13px}.p97-info.full{grid-column:1/-1}.p97-info label{display:block;font-size:9px;text-transform:uppercase;letter-spacing:.08em;color

--- occurrence 6 at char 1278189 ---
--border);border-bottom:0;border-radius:15px 15px 0 0;background:var(--soft)}
  .p97-folder:hover{transform:translateY(-3px);box-shadow:0 22px 48px rgba(21,55,66,.12)}.p97-folder:active{transform:translateY(0) scale(.99)}
  .p97-folder-head{display:flex;align-items:center;gap:12px}.p97-folder-av{width:50px;height:50px;border-radius:16px;display:grid;place-items:center;background:var(--accent);color:#fff;font-weight:950;font-size:16px;overflow:hidden;flex:0 0 auto}.p97-folder-av img{width:100%;height:100%;object-fit:cover}
  .p97-folder-main{min-width:0;flex:1}.p97-folder-main b{display:block;font-size:17px;color:#173640;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.p97-folder-main small{display:block;margin-top:3px;color:#75898f;font-weight:650}
  .p97-health{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:6px 9px;font-size:10px;font-weight:900}.p97-health.good{background:#e8f7ef;color:#21704e}.p97-health.warn{background:#fff5d9;color:#956800}.p97-health.bad{background:#fee9ed;color:#b13e55}
  .p97-folder-meta{display:flex;gap:7px;flex-wrap:wrap;margin-top:16px}.p97-mini{background:rgba(255,255,255,.72);border:1px solid rgba(80,120,125,.12);border-radius:10px;padding:6px 8px;color:#526b72;font-size:10px;font-weight:800}.p97-open{margin-left:auto;color:var(--accent);font-weight:950;font-size:11px;padding:7px 3px}
  .p97-empty{padding:36px 18px;border:1px dashed #cadada;border-radius:22px;text-align:center;color:#73878d;background:#fff}
  #p97Deck[hidden]{display:none!important}#p97Deck{position:fixed;inset:0;z-index:200500;display:grid;place-items:center;padding:20px;isolation:isolate}
  #p97Deck .p97-backdrop{position:absolute;inset:0;background:rgba(10,33,43,.58);backdrop-filter:blur(13px);animation:p97Fade .2s ease both}
  #p97Deck .p97-table{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;width:min(820px,94vw);height:min(780px,90dvh);display:flex;flex-direction:column;background:linear-gradient(160deg,#f9fcfb,#eef6f4);border:1px solid rgba(255,255,255,.82);border-radius:30px;box-shadow:0 34px 100px rgba(0,25,35,.34);overflow:visible;animation:p97Table .24s ease-out both}
  #p97Deck .p97-table:before,#p97Deck .p97-table:after{content:"";position:absolute;inset:18px 34px -18px;border-radius:28px;background:color-mix(in srgb,var(--soft) 74%,#d8e9e5);z-index:-2;transform:rotate(-1.5deg);box-shadow:0 22px 60px rgba(0,25,35,.13)}#p97Deck .p97-table:after{inset:10px 18px -10px;z-index:-1;transform:rotate(.8deg);background:color-mix(in srgb,var(--soft) 45%,#fff)}
  .p97-deck-head{flex:0 0 auto;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:12px;padding:13px 14px;border-bottom:1px solid #dce8e6;background:rgba(255,255,255,.94);border-radius:30px 30px 0 0}.p97-deck-head button{height:44px;border:1px solid #d1dfe0;background:#fff;color:#23434c;border-radius:14px;padding:0 13px;font-weight:900}.p97-deck-head .x{width:44px;padding:0;font-size:24px}.p97-deck-title{min-width:0}.p97-deck-title span{display:block;font-size:9px;letter-spacing:.14em;color:#748990;font-weight:950}.p97-deck-title b{display:block;margin-top:2px;color:#173640;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .p97-stage-wrap{position:relative;flex:1 1 auto;min-height:0;overflow:hidden;padding:22px 24px 14px}.p97-stage{height:100%;position:relative;display:grid}
  .p97-playing-card{grid-area:1/1;position:relative;width:100%;height:100%;overflow:auto;background:linear-gradient(160deg,var(--soft),#fff 35%);border:1px solid var(--border);border-top:6px solid var(--accent);border-radius:25px;box-shadow:0 24px 58px rgba(16,48,58,.16);padding:20px;will-change:transform,opacity;overscroll-behavior:contain}
  .p97-playing-card:before{content:"";position:absolute;inset:0 0 auto;height:120px;background:linear-gradient(120deg,var(--soft),transparent);pointer-events:none}.p97-playing-card>*{position:relative;z-index:1}
  .p97-playing-card.drop{animation:p97Drop .42s cubic-bezier(.16,.88,.3,1.18) both}.p97-playing-card.out-left{animation:p97OutLeft .19s ease-in both}.p97-playing-card.out-right{animation:p97OutRight .19s ease-in both}
  .p97-card-kicker{font-size:9px;font-weight:950;letter-spacing:.13em;color:var(--accent);text-transform:uppercase}.p97-card-title{font-family:'Fraunces',serif;font-size:27px;line-height:1.04;color:#173640;margin:4px 0 14px}
  .p97-personhero{display:flex;gap:14px;align-items:center}.p97-personhero .av{width:66px;height:66px;border-radius:21px;background:var(--accent);color:#fff;display:grid;place-items:center;font-size:20px;font-weight:950;overflow:hidden;flex:0 0 auto}.p97-personhero .av img{width:100%;height:100%;object-fit:cover}.p97-personhero h3{margin:0;color:#173640;font-size:22px}.p97-personhero p{margin:3px 0 0;color:#72868d}.p97-summarychips{display:flex;gap:7px;flex-wrap:wrap;margin:15px 0}.p97-summarychips span{border:1px solid rgba(64,108,115,.14);background:rgba(255,255,255,.72);border-radius:11px;padding:7px 9px;color:#536c73;font-size:10px;font-weight:850}
  .p97-card-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:16px}.p97-card-actions button{min-height:44px;border:1px solid #d1dfe0;background:#fff;color:#21424b;border-radius:14px;padding:10px 13px;font-weight:900}.p97-card-actions .primary{background:#168f94;border-color:#168f94;color:#fff}
  .p97-info-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.p97-info{border:1px solid rgba(75,113,118,.14);background:rgba(255,255,255,.76);border-radius:16px;padding:13px}.p97-info.full{grid-column:1/-1}.p97-info label{display:block;font-size:9px;text-transform:uppercase;letter-spacing:.08em;color:#788b91;font-weight:900;ma

--- occurrence 7 at char 1278417 ---
-folder-head{display:flex;align-items:center;gap:12px}.p97-folder-av{width:50px;height:50px;border-radius:16px;display:grid;place-items:center;background:var(--accent);color:#fff;font-weight:950;font-size:16px;overflow:hidden;flex:0 0 auto}.p97-folder-av img{width:100%;height:100%;object-fit:cover}
  .p97-folder-main{min-width:0;flex:1}.p97-folder-main b{display:block;font-size:17px;color:#173640;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.p97-folder-main small{display:block;margin-top:3px;color:#75898f;font-weight:650}
  .p97-health{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:6px 9px;font-size:10px;font-weight:900}.p97-health.good{background:#e8f7ef;color:#21704e}.p97-health.warn{background:#fff5d9;color:#956800}.p97-health.bad{background:#fee9ed;color:#b13e55}
  .p97-folder-meta{display:flex;gap:7px;flex-wrap:wrap;margin-top:16px}.p97-mini{background:rgba(255,255,255,.72);border:1px solid rgba(80,120,125,.12);border-radius:10px;padding:6px 8px;color:#526b72;font-size:10px;font-weight:800}.p97-open{margin-left:auto;color:var(--accent);font-weight:950;font-size:11px;padding:7px 3px}
  .p97-empty{padding:36px 18px;border:1px dashed #cadada;border-radius:22px;text-align:center;color:#73878d;background:#fff}
  #p97Deck[hidden]{display:none!important}#p97Deck{position:fixed;inset:0;z-index:200500;display:grid;place-items:center;padding:20px;isolation:isolate}
  #p97Deck .p97-backdrop{position:absolute;inset:0;background:rgba(10,33,43,.58);backdrop-filter:blur(13px);animation:p97Fade .2s ease both}
  #p97Deck .p97-table{--soft:#eff8f6;--border:#c7dfd8;--accent:#2f7d5a;position:relative;width:min(820px,94vw);height:min(780px,90dvh);display:flex;flex-direction:column;background:linear-gradient(160deg,#f9fcfb,#eef6f4);border:1px solid rgba(255,255,255,.82);border-radius:30px;box-shadow:0 34px 100px rgba(0,25,35,.34);overflow:visible;animation:p97Table .24s ease-out both}
  #p97Deck .p97-table:before,#p97Deck .p97-table:after{content:"";position:absolute;inset:18px 34px -18px;border-radius:28px;background:color-mix(in srgb,var(--soft) 74%,#d8e9e5);z-index:-2;transform:rotate(-1.5deg);box-shadow:0 22px 60px rgba(0,25,35,.13)}#p97Deck .p97-table:after{inset:10px 18px -10px;z-index:-1;transform:rotate(.8deg);background:color-mix(in srgb,var(--soft) 45%,#fff)}
  .p97-deck-head{flex:0 0 auto;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:12px;padding:13px 14px;border-bottom:1px solid #dce8e6;background:rgba(255,255,255,.94);border-radius:30px 30px 0 0}.p97-deck-head button{height:44px;border:1px solid #d1dfe0;background:#fff;color:#23434c;border-radius:14px;padding:0 13px;font-weight:900}.p97-deck-head .x{width:44px;padding:0;font-size:24px}.p97-deck-title{min-width:0}.p97-deck-title span{display:block;font-size:9px;letter-spacing:.14em;color:#748990;font-weight:950}.p97-deck-title b{display:block;margin-top:2px;color:#173640;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .p97-stage-wrap{position:relative;flex:1 1 auto;min-height:0;overflow:hidden;padding:22px 24px 14px}.p97-stage{height:100%;position:relative;display:grid}
  .p97-playing-card{grid-area:1/1;position:relative;width:100%;height:100%;overflow:auto;background:linear-gradient(160deg,var(--soft),#fff 35%);border:1px solid var(--border);border-top:6px solid var(--accent);border-radius:25px;box-shadow:0 24px 58px rgba(16,48,58,.16);padding:20px;will-change:transform,opacity;overscroll-behavior:contain}
  .p97-playing-card:before{content:"";position:absolute;inset:0 0 auto;height:120px;background:linear-gradient(120deg,var(--soft),transparent);pointer-events:none}.p97-playing-card>*{position:relative;z-index:1}
  .p97-playing-card.drop{animation:p97Drop .42s cubic-bezier(.16,.88,.3,1.18) both}.p97-playing-card.out-left{animation:p97OutLeft .19s ease-in both}.p97-playing-card.out-right{animation:p97OutRight .19s ease-in both}
  .p97-card-kicker{font-size:9px;font-weight:950;letter-spacing:.13em;color:var(--accent);text-transform:uppercase}.p97-card-title{font-family:'Fraunces',serif;font-size:27px;line-height:1.04;color:#173640;margin:4px 0 14px}
  .p97-personhero{display:flex;gap:14px;align-items:center}.p97-personhero .av{width:66px;height:66px;border-radius:21px;background:var(--accent);color:#fff;display:grid;place-items:center;font-size:20px;font-weight:950;overflow:hidden;flex:0 0 auto}.p97-personhero .av img{width:100%;height:100%;object-fit:cover}.p97-personhero h3{margin:0;color:#173640;font-size:22px}.p97-personhero p{margin:3px 0 0;color:#72868d}.p97-summarychips{display:flex;gap:7px;flex-wrap:wrap;margin:15px 0}.p97-summarychips span{border:1px solid rgba(64,108,115,.14);background:rgba(255,255,255,.72);border-radius:11px;padding:7px 9px;color:#536c73;font-size:10px;font-weight:850}
  .p97-card-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:16px}.p97-card-actions button{min-height:44px;border:1px solid #d1dfe0;background:#fff;color:#21424b;border-radius:14px;padding:10px 13px;font-weight:900}.p97-card-actions .primary{background:#168f94;border-color:#168f94;color:#fff}
  .p97-info-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.p97-info{border:1px solid rgba(75,113,118,.14);background:rgba(255,255,255,.76);border-radius:16px;padding:13px}.p97-info.full{grid-column:1/-1}.p97-info label{display:block;font-size:9px;text-transform:uppercase;letter-spacing:.08em;color:#788b91;font-weight:900;margin-bottom:6px}.p97-info div{color:#26454e;font-size:13px;line-height:1.45}.p97-question{font-family:'Fraunces',serif!important;font-size:20px!important;color:#1d3841!important}
  .p97-period{border:1px solid rgba(75,113,118,.1

--- occurrence 8 at char 1285324 ---
d;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.p97-metric{border:1px solid rgba(75,113,118,.14);background:rgba(255,255,255,.78);border-radius:18px;padding:15px}.p97-metric strong{display:block;font-size:26px;color:#183844}.p97-metric span{color:#73888e;font-size:10px;font-weight:800}
  .p97-deck-nav{flex:0 0 auto;display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:9px;padding:11px 14px 14px;border-top:1px solid #dce8e6;background:rgba(255,255,255,.94);border-radius:0 0 30px 30px}.p97-deck-nav button{height:46px;border:1px solid #ceddde;background:#fff;color:#21424b;border-radius:15px;font-weight:900}.p97-deck-nav .next{background:#168f94;color:#fff;border-color:#168f94}.p97-deck-count{text-align:center;min-width:92px}.p97-deck-count small{display:block;color:#7a8d93;font-size:8px;font-weight:950;letter-spacing:.1em}.p97-deck-count b{display:block;margin-top:3px;color:#36535b;font-size:12px}
  @keyframes p97Fade{from{opacity:0}to{opacity:1}}@keyframes p97Table{from{opacity:0;transform:translateY(15px) scale(.985)}to{opacity:1;transform:none}}@keyframes p97Drop{0%{opacity:0;transform:translateY(-82px) rotate(-3deg) scale(.94)}56%{opacity:1;transform:translateY(8px) rotate(.7deg) scale(1.006)}78%{transform:translateY(-3px) rotate(-.25deg)}100%{opacity:1;transform:none}}@keyframes p97OutLeft{to{opacity:0;transform:translateX(-70px) rotate(-3deg) scale(.96)}}@keyframes p97OutRight{to{opacity:0;transform:translateX(70px) rotate(3deg) scale(.96)}}
  @media(max-width:760px){
    #viewTrack .track-wrap{padding:8px 10px 110px!important}.p97-hero{display:block;margin-top:2px}.p97-hero h2{font-size:31px}.p97-actionline{justify-content:flex-start;margin-top:10px;display:grid;grid-template-columns:repeat(2,minmax(0,1fr))}.p97-actionline button{width:100%;font-size:11px;padding:9px}
    .p97-toolbar{grid-template-columns:1fr 1fr}.p97-toolbar .person{grid-column:1/-1}.p97-field{min-height:44px;border-radius:14px}.p97-folder-grid{grid-template-columns:1fr;gap:16px}.p97-folder{min-height:140px;border-radius:0 22px 22px 22px;padding:21px 15px 14px}.p97-folder:before{top:-18px;height:26px}.p97-folder-av{width:46px;height:46px;border-radius:15px}
    #p97Deck{padding:6px 6px calc(6px + env(safe-area-inset-bottom));place-items:stretch;align-items:stretch;overflow:hidden}#p97Deck .p97-table{width:calc(100vw - 12px);height:calc(100dvh - 12px - env(safe-area-inset-bottom));max-height:none;border-radius:24px;margin:0}.p97-deck-head{padding:9px;border-radius:24px 24px 0 0;gap:7px}.p97-deck-head button{height:42px;padding:0 10px}.p97-deck-head .x{width:42px;padding:0}.p97-deck-title b{font-size:13px}
    .p97-stage-wrap{padding:13px 8px 10px}.p97-playing-card{padding:15px 13px;border-radius:20px}.p97-card-title{font-size:23px}.p97-info-grid{grid-template-columns:1fr}.p97-info.full{grid-column:auto}.p97-question{font-size:18px!important}.p97-pending-grid{grid-template-columns:1fr 1fr}.p97-deck-nav{padding:8px 8px calc(8px + env(safe-area-inset-bottom));border-radius:0 0 24px 24px;gap:6px}.p97-deck-nav button{height:44px;font-size:11px}.p97-deck-count{min-width:64px}.p97-deck-count small{font-size:7px}.p97-deck-count b{font-size:11px}
  }
  @media(prefers-reduced-motion:reduce){#p97Deck .p97-backdrop,#p97Deck .p97-table,.p97-playing-card.drop,.p97-playing-card.out-left,.p97-playing-card.out-right{animation:none!important}}
  `;
  document.head.appendChild(st);
}

function folderMembers(){
  const s=appState();let list=[...(s?.members||[])];
  const q=norm(folderState.q),type=folderState.type,status=folderState.status;
  if(q)list=list.filter(m=>norm([m.nome,m.programa,m.tema,m.origem].filter(Boolean).join(' ')).includes(q));
  if(type)list=list.filter(m=>String(m.nivel||'')===type);
  if(status)list=list.filter(m=>healthFor(m).level===status);
  if(folderState.sort==='pending')list.sort((a,b)=>{const A=statusCounts(a),B=statusCounts(b);return (B.late*10+B.open)-(A.late*10+A.open)||a.nome.localeCompare(b.nome,'pt-BR')});
  else if(folderState.sort==='type')list.sort((a,b)=>memberLevel(a).localeCompare(memberLevel(b),'pt-BR')||a.nome.localeCompare(b.nome,'pt-BR'));
  else list.sort((a,b)=>a.nome.localeCompare(b.nome,'pt-BR'));
  return list;
}
function fillTypeOptions(){
  const sel=$('#p97Type');if(!sel)return;
  const s=appState();const vals=[...new Set((s?.members||[]).map(m=>String(m.nivel||'')).filter(Boolean))];
  const sig=vals.sort().join('|');if(sel.dataset.sig===sig)return;const keep=folderState.type;
  sel.dataset.sig=sig;sel.innerHTML='<option value="">Todos os tipos</option>'+vals.map(v=>{const m=(s?.members||[]).find(x=>String(x.nivel||'')===v);return `<option value="${esc97(v)}">${esc97(memberLevel(m||{nivel:v}))}</option>`}).join('');
  if(vals.includes(keep))sel.value=keep;
}
function folderHtml(m){
  const [soft,border,accent]=colorsFor(m.nome),h=healthFor(m),details=p135HealthDetails(m),c=statusCounts(m);
  const av=m.foto?`<img src="${esc97(m.foto)}" alt="">`:esc97(initials97(m.nome));
  return `<button type="button" class="p97-folder" data-p97-member="${esc97(m.id)}" style="--soft:${soft};--border:${border};--accent:${accent}">
    <div class="p97-folder-head"><div class="p97-folder-av">${av}</div><div class="p97-folder-main"><b>${esc97(m.nome)}</b><small>${esc97(memberLevel(m))}${m.programa?' · '+esc97(m.programa):''}</small></div><span class="p97-health ${healthClass(h)}">● ${healthLabel(h)}</span></div>
    <div class="p97-folder-meta"><span class="p135-folder-reason">${esc97(details[0]?`${details[0].title} — ${details[0].when}`:(h.reasons?.[0]||'Em dia'))}</span><span class="p97-open">abrir →</span></div>
  </button>`;
}
function ensureFolderUi(){
  const wrap=$('#viewT


===== Situação agora =====

--- occurrence 1 at char 1298926 ---
push({kind:'profile',activityId:'',title:'Pergunta científica',dueDate:'',when:'não preenchida'});
  return rows;
}
function p135HealthDetailsHTML(details,fallback){
  if(!details.length)return `<div>${esc97((fallback||['em dia']).slice(0,4).join(' · '))}</div>`;
  return `<div class="p135-health-reasons">${details.slice(0,5).map(d=>`<div class="p135-health-reason ${esc97(d.kind||'')}"><b>${esc97(d.title)}</b><span>${d.dueDate?esc97(fmtDate(d.dueDate))+' · ':''}${esc97(d.when)}</span></div>`).join('')}</div>`;
}
function p135SnapshotHTML(m){
  const acts=actsFor(m.id),open=acts.filter(a=>!activityDone(a));
  const due=open.filter(a=>a.dueDate).map(a=>({a,d:dueDiff(a)})).filter(x=>x.d!==null&&x.d>=0).sort((a,b)=>a.d-b.d)[0];
  const late=open.filter(overdue).sort((a,b)=>String(a.dueDate||'').localeCompare(String(b.dueDate||'')))[0];
  const last=[...acts].sort((a,b)=>(millis(b.updatedAt)||millis(b.createdAt))-(millis(a.updatedAt)||millis(a.createdAt)))[0];
  const action=late||due?.a||open.find(a=>a.type==='correcao')||open.find(a=>a.type==='pendencia')||open[0];
  const lastMs=last?(millis(last.updatedAt)||millis(last.createdAt)):0,lastDate=lastMs?new Date(lastMs).toLocaleDateString('pt-BR'):'';
  const dueTxt=due?(due.d===0?'hoje':due.d===1?'amanhã':`em ${due.d} dias`):'—';
  return `<div class="p135-snapshot"><div><span>Próximo prazo</span><b>${due?esc97(due.a.title||typeLabel(due.a)):'—'}</b><small>${due?esc97(fmtDate(due.a.dueDate))+' · '+dueTxt:'Nenhum prazo futuro aberto'}</small></div><div><span>Última atividade</span><b>${last?esc97(last.title||typeLabel(last)):'—'}</b><small>${lastDate||'Sem registro'}</small></div><div><span>Próxima ação</span><b>${action?esc97(action.title||typeLabel(action)):'Tudo em dia'}</b><small>${late?'Está atrasado':due?`Prazo ${dueTxt}`:action?'Item ainda aberto':'Nenhuma ação imediata'}</small></div></div>`;
}
function overviewCard(m,can){
  const h=healthFor(m),details=p135HealthDetails(m),c=statusCounts(m),av=m.foto?`<img src="${esc97(m.foto)}" alt="">`:esc97(initials97(m.nome));
  return {title:'Visão geral',kicker:'ACOMPANHAMENTO',html:`<div class="p97-card-kicker">PASTA DE ACOMPANHAMENTO</div><div class="p97-card-title">Situação agora</div><div class="p97-personhero"><div class="av">${av}</div><div><h3>${esc97(m.nome)}</h3><p>${esc97(memberLevel(m))}${m.programa?' · '+esc97(m.programa):''}${m.bolsista?' · Bolsista':''}</p></div><span class="p97-health ${healthClass(h)}">● ${healthLabel(h)}</span></div><div class="p97-summarychips"><span>${c.open} itens abertos</span><span>${c.late} atrasados</span><span>${c.corr} correções</span><span>${c.soon} prazo(s) em 30 dias</span></div><div class="p97-info full"><label>Por que está assim?</label>${p135HealthDetailsHTML(details,h.reasons)}</div>${p135SnapshotHTML(m)}<div class="p97-card-actions">${can?`<button class="primary" type="button" data-p97-action="activity" data-mid="${esc97(m.id)}">＋ Registrar</button>`:''}<button type="button" data-p97-action="dossier" data-mid="${esc97(m.id)}">Dossiê</button><button type="button" data-p97-action="repo" data-mid="${esc97(m.id)}">As coisa tudo</button></div>`};
}

function p202AcademicUrl(v){
  const raw=String(v||'').trim();if(!raw)return'';
  try{const u=new URL(raw);return /^https?:$/.test(u.protocol)?u.href:''}catch(_e){return''}
}
function p202OrcidId(v){return (String(v||'').match(/\d{4}-\d{4}-\d{4}-[\dX]{4}/i)||[])[0]||''}
function academicCard(m,can){
  const bio=String(m.minibio||'').trim(),lattes=p202AcademicUrl(m.lattesUrl),orcid=p202AcademicUrl(m.orcidUrl),av=m.foto?`<img src="${esc97(m.foto)}" alt="">`:esc97(initials97(m.nome)),missing=[!bio?'minibio':'',!lattes?'Lattes':'',!orcid?'ORCID':''].filter(Boolean);
  return {title:'Cartão acadêmico',kicker:'PERFIL',html:`<div class="p202-academic"><div class="p202-academic-head"><span class="p202-academic-badge">✦ perfil acadêmico</span><span class="p202-academic-note">pronto para evento</span></div><div class="p202-academic-person"><div class="p202-academic-av">${av}</div><div><h3>${esc97(m.nome)}</h3><p>${esc97(memberLevel(m))}${m.programa?' · '+esc97(m.programa):''}</p></div></div><div class="p202-academic-bio ${bio?'':'empty'}">${bio?esc97(bio):'Adicione uma minibio curta para apresentar esta pessoa em eventos, mesas e colaborações.'}</div><div class="p202-academic-links"><a class="p202-academic-link ${lattes?'':'off'}" ${lattes?`href="${esc97(lattes)}" target="_blank" rel="noopener"`:''}>📚 Currículo Lattes</a><a class="p202-academic-link ${orcid?'':'off'}" ${orcid?`href="${esc97(orcid)}" target="_blank" rel="noopener"`:''}>🟢 ORCID${orcid?` · ${esc97(p202OrcidId(orcid))}`:''}</a></div>${missing.length&&can?`<div class="p202-academic-missing">Complete ${esc97(missing.join(', '))} para deixar o cartão pronto para compartilhar.</div>`:''}<div class="p97-card-actions">${can?`<button class="primary" type="button" data-p97-action="academic-edit" data-mid="${esc97(m.id)}">✎ Editar cartão</button>`:''}<button type="button" data-p97-action="academic-download" data-mid="${esc97(m.id)}">↓ Baixar PNG</button><button type="button" data-p97-action="academic-share" data-mid="${esc97(m.id)}">↗ Compartilhar</button></div></div>`};
}

function timelineCard(m,can){
  const acts=actsFor(m.id),periods=acts.filter(a=>['mestrado','doutorado','tcc'].includes(a.type)),bits=[];
  if(m.bolsista)bits.push(periodHtml('🎓 Bolsa',m.bolsaModalidade||'Bolsa',m.bolsaInicio,m.bolsaFim,0));
  periods.forEach(a=>bits.push(periodHtml(`${iconFor(a.type)} ${typeLabel(a)}`,a.title,a.startDate,a.dueDate,a.progress)));
  return {title:'Régua de tempo',kicker:'TEMPO',html:`<div class="p97-card-kicker">⏳ régua de tempo</div><div class="p97-card-title">On


===== Editar cadastro =====

--- occurrence 1 at char 1035701 ---
 0 auto}.p69-copy{min-width:0}.p69-copy b{display:block;font-size:13px;color:#17313c}.p69-copy span{display:block;font-size:11px;line-height:1.45;color:#60757d;margin-top:3px}.p69-main{background:#0e5c63!important;border-color:#0e5c63!important;color:#fff!important;font-weight:850!important}.p69-wait{background:#b96b00!important;border-color:#b96b00!important;color:#fff!important;font-weight:850!important}.p69-secondary{background:#fff!important;color:#405962!important}.p69-hidden{display:none!important}.p69-panel-note{font-size:11px;font-weight:750;line-height:1.4;margin-top:6px;color:#425d66}.p69-panel-note.wait{color:#8c5700}.p69-panel-note.go{color:#176f4d}.p69-badge{display:inline-flex;border-radius:999px;padding:4px 7px;margin-right:5px;font-size:9px;font-weight:950;letter-spacing:.02em}.p69-badge.wait{background:#fff0cf;color:#8c5700}.p69-badge.go{background:#e5f7ef;color:#176f4d}
  `;document.head.appendChild(s)
}
function removeOldState(){document.querySelector('#p66Body .p69-state')?.remove()}
function stateBox(kind,title,text,ico){
  removeOldState();const body=document.getElementById('p66Body');if(!body)return;const el=document.createElement('div');el.className=`p69-state ${kind}`;el.innerHTML=`<div class="p69-ico">${ico}</div><div class="p69-copy"><b>${esc(title)}</b><span>${esc(text)}</span></div>`;const progress=body.querySelector('.p66-progress');if(progress)body.insertBefore(el,progress);else body.appendChild(el)
}
function clearP67NoTarget(){const x=document.querySelector('#p66Body .p67-linked');if(x&&/Nenhum documento vinculado/i.test(x.textContent||''))x.remove()}
function makePrimaryChat(a,owner,wording){
  const foot=document.getElementById('p66Foot');if(!foot)return;let b=[...foot.querySelectorAll('button')].find(x=>/Conversar com|Pedir arquivo/i.test(x.textContent||''));
  if(!b){b=document.createElement('button');b.type='button';foot.insertBefore(b,foot.lastElementChild||null)}
  b.className='btn p69-wait';b.textContent=`💬 ${wording||`Pedir arquivo a ${first(owner)}`}`;b.onclick=()=>chat(a.ownerId,a,'ARQUIVO PARA CORREÇÃO');
}
function demoteRegistry(){const edit=document.querySelector('#p66Foot .p66-edit');if(edit){edit.textContent='⚙️ Editar cadastro';edit.classList.add('p69-secondary');edit.title='Alterar apenas o registro de acompanhamento'}}
function renameComplete(text){const b=document.querySelector('#p66Foot .p66-complete');if(b){b.textContent=text||'✓ Marcar resolvido';b.classList.add('p69-secondary')}}
function insertMainButton(label,fn){
  const foot=document.getElementById('p66Foot');if(!foot)return;foot.querySelector('.p69-direct')?.remove();const b=document.createElement('button');b.type='button';b.className='btn p69-main p69-direct';b.textContent=label;b.onclick=fn;const complete=foot.querySelector('.p66-complete');if(complete)foot.insertBefore(b,complete);else foot.insertBefore(b,foot.lastElementChild||null)
}
function tuneModal(id){
  const a=activity(id),ov=document.getElementById('p66ActionOverlay');if(!a||!ov?.classList.contains('open'))return;ensureCss();demoteRegistry();
  const owner=memberName(a.ownerId),review=actionableReview(a),pub=visiblePublication(a),p67Target=document.querySelector('#p66Foot .p67-target');
  if(review){clearP67NoTarget();stateBox('go','Arquivo recebido — dá para agir agora',`${owner} enviou uma versão pelo fluxo de correção. Abra o documento e devolva a correção por ali.`,'✍️');if(p67Target)p67Target.remove();insertMainButton('✍️ Corrigir agora',()=>openReview(review));renameComplete('✓ Marcar resolvido');return}
  if(p67Target){stateBox('go','Material encontrado — abra para resolver',`Há um documento relacionado a “${a.title||'este acompanhamento'}”. Use o botão principal para ir direto a ele.`,'📄');p67Target.classList.add('p69-main');p67Target.textContent=a.type==='correcao'?'📄 Abrir documento':'📄 Abrir material';renameComplete('✓ Marcar resolvido');return}
  if(pub){clearP67NoTarget();stateBox('go','Documento encontrado no Repositório',`Encontrei “${pub.fileName||pub.titulo||'Arquivo'}”. Abra o documento para continuar.`,'📄');insertMainButton(a.type==='correcao'?'📄 Abrir para corrigir':'📄 Abrir documento',()=>openFile(pub));renameComplete('✓ Marcar resolvido');return}
  clearP67NoTarget();
  if(a.type==='correcao'){
    stateBox('wait','Ainda não há arquivo para você corrigir',`Existe um registro de correção para ${owner}, mas nenhum arquivo foi enviado a você pelo fluxo de correção. A próxima ação útil é pedir o arquivo ou orientar por conversa.`,'⏳');makePrimaryChat(a,owner,`Pedir arquivo a ${first(owner)}`);renameComplete('✓ Encerrar acompanhamento');return
  }
  if(['produto','entrega'].includes(String(a.type||'').toLowerCase())){
    stateBox('wait','Entrega ainda sem documento vinculado',`O prazo está registrado para ${owner}, mas não há um arquivo ligado a este acompanhamento. Você pode pedir a entrega diretamente.`,'⏳');makePrimaryChat(a,owner,`Pedir entrega a ${first(owner)}`);renameComplete('✓ Encerrar acompanhamento');return
  }
  stateBox('info','Este item é acompanhamento, não um documento',`Use a conversa com ${owner} para tratar o próximo passo. “Editar cadastro” serve apenas para corrigir datas, status ou descrição.`,'💬');renameComplete('✓ Marcar resolvido')
}

function cardKey(card){const b=card.querySelector('button[onclick*="p56OpenItem"]');const m=String(b?.getAttribute('onclick')||'').match(/p56OpenItem\('([^']+)'\)/);return m?.[1]||card.dataset.p69Key||''}
function tuneActivityCard(card,a){
  if(!a)return;const actions=card.querySelector('.p56-task-actions'),detail=card.querySelector('.p56-detail'),owner=memberName(a.ownerId),review=actionableReview(a),pub=visiblePublication(a);card.dataset.p69Key

--- occurrence 2 at char 1038633 ---
st a=activity(id),ov=document.getElementById('p66ActionOverlay');if(!a||!ov?.classList.contains('open'))return;ensureCss();demoteRegistry();
  const owner=memberName(a.ownerId),review=actionableReview(a),pub=visiblePublication(a),p67Target=document.querySelector('#p66Foot .p67-target');
  if(review){clearP67NoTarget();stateBox('go','Arquivo recebido — dá para agir agora',`${owner} enviou uma versão pelo fluxo de correção. Abra o documento e devolva a correção por ali.`,'✍️');if(p67Target)p67Target.remove();insertMainButton('✍️ Corrigir agora',()=>openReview(review));renameComplete('✓ Marcar resolvido');return}
  if(p67Target){stateBox('go','Material encontrado — abra para resolver',`Há um documento relacionado a “${a.title||'este acompanhamento'}”. Use o botão principal para ir direto a ele.`,'📄');p67Target.classList.add('p69-main');p67Target.textContent=a.type==='correcao'?'📄 Abrir documento':'📄 Abrir material';renameComplete('✓ Marcar resolvido');return}
  if(pub){clearP67NoTarget();stateBox('go','Documento encontrado no Repositório',`Encontrei “${pub.fileName||pub.titulo||'Arquivo'}”. Abra o documento para continuar.`,'📄');insertMainButton(a.type==='correcao'?'📄 Abrir para corrigir':'📄 Abrir documento',()=>openFile(pub));renameComplete('✓ Marcar resolvido');return}
  clearP67NoTarget();
  if(a.type==='correcao'){
    stateBox('wait','Ainda não há arquivo para você corrigir',`Existe um registro de correção para ${owner}, mas nenhum arquivo foi enviado a você pelo fluxo de correção. A próxima ação útil é pedir o arquivo ou orientar por conversa.`,'⏳');makePrimaryChat(a,owner,`Pedir arquivo a ${first(owner)}`);renameComplete('✓ Encerrar acompanhamento');return
  }
  if(['produto','entrega'].includes(String(a.type||'').toLowerCase())){
    stateBox('wait','Entrega ainda sem documento vinculado',`O prazo está registrado para ${owner}, mas não há um arquivo ligado a este acompanhamento. Você pode pedir a entrega diretamente.`,'⏳');makePrimaryChat(a,owner,`Pedir entrega a ${first(owner)}`);renameComplete('✓ Encerrar acompanhamento');return
  }
  stateBox('info','Este item é acompanhamento, não um documento',`Use a conversa com ${owner} para tratar o próximo passo. “Editar cadastro” serve apenas para corrigir datas, status ou descrição.`,'💬');renameComplete('✓ Marcar resolvido')
}

function cardKey(card){const b=card.querySelector('button[onclick*="p56OpenItem"]');const m=String(b?.getAttribute('onclick')||'').match(/p56OpenItem\('([^']+)'\)/);return m?.[1]||card.dataset.p69Key||''}
function tuneActivityCard(card,a){
  if(!a)return;const actions=card.querySelector('.p56-task-actions'),detail=card.querySelector('.p56-detail'),owner=memberName(a.ownerId),review=actionableReview(a),pub=visiblePublication(a);card.dataset.p69Key=`activity:${a.id}`;
  if(review){if(detail)detail.innerHTML=`<span class="p69-badge go">🔥 SUA AÇÃO</span><span class="p69-badge go">📄 ARQUIVO RECEBIDO</span><div class="p69-panel-note go">${esc(owner)} enviou material para correção. Próxima ação: corrigir e devolver.</div>`;if(actions){actions.innerHTML='';const b=document.createElement('button');b.className='btn p69-main';b.textContent='✍️ Corrigir agora';b.onclick=()=>openReview(review);actions.appendChild(b)}return}
  if(a.type==='correcao'&&!pub){if(detail)detail.innerHTML=`<span class="p69-badge wait">⏳ AGUARDANDO ARQUIVO</span><div class="p69-panel-note wait">Não há arquivo enviado para você corrigir. Este registro, sozinho, não é uma demanda de correção.</div>`;if(actions){actions.innerHTML='';if(a.ownerId!==me()&&typeof window.openPrivateChatWithContext==='function'){const b=document.createElement('button');b.className='btn p69-wait';b.textContent=`💬 Pedir arquivo`;b.onclick=()=>chat(a.ownerId,a,'ARQUIVO PARA CORREÇÃO');actions.appendChild(b)}const v=document.createElement('button');v.className='btn';v.textContent='Ver contexto';v.onclick=()=>p69OpenActivity(a.id);actions.appendChild(v)}return}
  if(pub&&['correcao','produto','entrega'].includes(String(a.type||'').toLowerCase())){if(detail)detail.innerHTML=`<span class="p69-badge go">📄 ARQUIVO ENCONTRADO</span><div class="p69-panel-note go">${esc(pub.fileName||pub.titulo||'Arquivo')} · abra o material para continuar.</div>`;if(actions){const open=actions.querySelector('button')||document.createElement('button');actions.innerHTML='';open.className='btn p69-main';open.textContent=a.type==='correcao'?'📄 Abrir para corrigir':'📄 Abrir documento';open.onclick=()=>openFile(pub);actions.appendChild(open)}return}
}
function tuneCards(){
  ensureCss();document.querySelectorAll('#dashAttention .p56-task').forEach(card=>{const key=cardKey(card);if(!key.startsWith('activity:'))return;const a=activity(key.slice(9));tuneActivityCard(card,a)})
}
function scheduleCards(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;tuneCards()})}
function p69OpenActivity(id){
  const fn=baseOpenActivity||window.p67OpenActivity||window.p66OpenActivityDetail;if(typeof fn!=='function')return;
  fn(id);setTimeout(()=>tuneModal(id),80);setTimeout(()=>tuneModal(id),350);setTimeout(()=>tuneModal(id),1000)
}
function p69OpenItem(key){const k=String(key||'');if(k.startsWith('activity:'))return p69OpenActivity(k.slice(9));if(typeof baseOpenItem==='function')return baseOpenItem(key)}
function install(){
  ensureCss();if(!baseOpenActivity&&typeof window.p67OpenActivity==='function'&&window.p67OpenActivity!==p69OpenActivity)baseOpenActivity=window.p67OpenActivity;if(!baseOpenItem&&typeof window.p56OpenItem==='function'&&window.p56OpenItem!==p69OpenItem)baseOpenItem=window.p56OpenItem;
  window.p69OpenActivity=p69OpenActivity;window.p67OpenActivity=p69OpenActivity;window.p56OpenItem=p6


===== Desligar acesso =====

--- occurrence 1 at char 240670 ---
"hint">(as ligações do grafo são geradas por linhas compartilhadas)</span></label>
        <div class="linhas-pick" id="linhasPick"></div>
      </div>
      <div class="fg full">
        <label>Vínculos organizacionais <span class="hint">(ligações no grafo que não vêm de linha compartilhada)</span></label>
        <div class="hub-row">
          <label class="toggle" style="padding:0;gap:8px"><input type="checkbox" id="mHub"> Conecta-se com o grupo inteiro (hub)</label>
        </div>
        <input id="mHubMotivo" placeholder="Por quê? Ex.: dá visibilidade a todos os programas" style="margin-top:8px;border:1px solid var(--line);border-radius:9px;padding:8px 10px;font-size:13px;width:100%;display:none">
        <input id="vincSearch" placeholder="Buscar pessoa para conectar…" style="margin-top:10px;border:1px solid var(--line);border-radius:9px;padding:8px 10px;font-size:13px;width:100%">
        <div class="linhas-pick" id="vincPick" style="margin-top:8px"></div>
        <div id="vincMotivos" style="margin-top:8px"></div>
      </div>
      <div class="fg full"><label>Plataforma</label><input id="mPlataforma" placeholder="https://..."></div>
      <div class="fg full">
        <label>Link do Drive com os dados <span class="hint">(precisa estar como "qualquer pessoa com o link")</span></label>
        <input id="mDrive" placeholder="https://drive.google.com/...">
        <span class="hint">Esta página não tem login. Qualquer pessoa com a URL do site vê este link e pode abri-lo. Não coloque aqui nada que não possa ser público.</span>
      </div>
      <div class="fg full">
        <label>Resultados e produtos</label>
        <div id="repList"></div>
        <span class="add-mini" id="addRep">+ adicionar resultado</span>
      </div>
      <div class="fg full">
        <label>Prazos e entregas</label>
        <div id="prazoList"></div>
        <span class="add-mini" id="addPrazo">+ adicionar prazo</span>
      </div>
      <div class="fg full"><label>Notas</label><textarea id="mNotas" placeholder="Observações internas"></textarea></div>
    </div>
    <div class="modal-f">
      <button class="btn danger" id="deleteMember" style="margin-right:auto;display:none">Desligar acesso</button>
      <button class="btn danger" id="purgeMember" style="display:none">Excluir definitivamente</button>
      <button class="btn" data-close="memberOverlay">Cancelar</button>
      <button class="btn primary" id="saveMember">Salvar</button>
    </div>
  </div>
</div>

<!-- MEMBER REPOSITORY MODAL -->
<div class="overlay" id="repoOverlay">
  <div class="modal" style="max-width:820px">
    <div class="modal-h"><h2 id="repoTitle">Repositório</h2><div class="spacer"></div><button class="btn primary" id="repoAddBtn" style="display:none">+ Adicionar documento</button><button class="btn ghost" data-close="repoOverlay">Fechar</button></div>
    <div class="modal-b">
      <div id="repoScholar"></div>
      <div class="repo-summary" style="margin:4px 0 16px"><span class="repo-count" id="repoCount">0</span><span class="hint">itens vinculados a esta pessoa</span></div>
      <div class="repo-list" id="repoList"></div>
    </div>
  </div>
</div>

<!-- ACCOUNT MODAL -->
<div class="overlay" id="idOverlay">
  <div class="modal" style="max-width:440px">
    <div class="modal-h"><h2>Minha conta</h2><div class="spacer"></div><button class="btn ghost" data-close="idOverlay">Fechar</button></div>
    <div class="modal-b">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:14px">
        <span class="av" id="accountAv" style="width:44px;height:44px;border-radius:12px;display:grid;place-items:center;color:#fff;font-weight:700;background:var(--petrol)">?</span>
        <div style="min-width:0"><div id="accountName" style="font-weight:700"></div><div id="accountEmail" class="hint"></div><div id="accountLink" class="hint" style="margin-top:3px;font-size:11px"></div></div>
        <span class="role-pill" id="accountRole" style="margin-left:auto"></span>
      </div>
      <div class="access-note">Sua identidade vem do Firebase Authentication. Você só pode editar o que pertence ao seu perfil; o coordenador administra a plataforma.</div>
    </div>
    <div class="modal-f">
      <button class="btn" id="accountRepairBtn">Corrigir vínculo</button>
      <button class="btn" id="accountResetBtn">Redefinir senha</button>
      <button class="btn danger" id="accountLogoutBtn">Sair</button>
    </div>
  </div>
</div>

<!-- LINHAS MODAL -->
<div class="overlay" id="linhasOverlay">
  <div class="modal" style="max-width:480px">
    <div class="modal-h"><h2>Linhas de pesquisa</h2><div class="spacer"></div><button class="btn ghost" data-close="linhasOverlay">Fechar</button></div>
    <div class="modal-b">
      <p class="hint" style="margin:0 0 14px">Renomear atualiza automaticamente todos os membros que usam essa linha. Excluir remove a linha de quem a tiver marcada — não apaga o membro.</p>
      <div class="id-list" id="linhasMgmtList" style="max-height:360px"></div>
      <div class="vinc-row" style="margin-top:10px">
        <input id="newLinha" placeholder="Nova linha de pesquisa" style="flex:1">
        <button class="btn primary" id="addLinhaBtn" style="flex:0 0 auto">Adicionar</button>
      </div>
    </div>
  </div>
</div>

<!-- ACCESS MANAGEMENT MODAL -->
<div class="overlay" id="pinOverlay">
  <div class="modal" style="max-width:650px">
    <div class="modal-h"><h2>Gerenciar acessos</h2><div class="spacer"></div><button class="btn ghost" data-close="pinOverlay">Fechar</button></div>
    <div class="modal-b">
      <div class="access-note" style="margin-bottom:16px"><strong>Autocadastro ativo.</strong> Cada Carbonau

--- occurrence 2 at char 384676 ---
do e referências históricas poderão ficar sem o perfil de origem. Continuar?`))return;
  const typed=prompt(`Confirmação final: digite exatamente o nome abaixo para excluir:

${m.nome}`);
  if((typed||'').trim()!==(m.nome||'').trim()){
    toast('Exclusão cancelada: o nome digitado não confere.');
    return;
  }
  try{
    const f=FB(), linked=(state.users||[]).filter(u=>u.memberId===id), batch=f.writeBatch(window.db);
    batch.delete(f.doc(window.db,'rede_members',id));
    batch.delete(f.doc(window.db,'rede_member_claims',id));
    await batch.commit();
    closeOverlay('memberOverlay');
    if(selectedId===id)closeDetail();
    toast(linked.length
      ?'Perfil excluído. A conta de autenticação permanece bloqueada até a limpeza administrativa final.'
      :'Perfil excluído definitivamente.');
    renderPeopleManager();
  }catch(e){
    console.error(e);
    toast(e?.code==='permission-denied'?'Permissão negada pelas regras do Firestore.':'Erro na exclusão definitiva.');
  }
}

/* ============ GERENCIAR PESSOAS ============ */
function renderPeopleManager(){
  if(!isAdmin || !$('#peopleList')) return;
  const q=($('#peopleSearch')?.value||'').trim().toLowerCase();
  const items=[...state.members].filter(m=>!q || [m.nome,m.programa,m.status,m.origem].some(v=>(v||'').toLowerCase().includes(q))).sort((a,b)=>(a.nome||'').localeCompare(b.nome||''));
  $('#peopleList').innerHTML=items.map(m=>{const nv=NIVEIS[m.nivel]||{color:'#607d8b',short:m.nivel||''};return `<div class="people-row"><div class="p-av" style="background:${nv.color}">${m.foto?`<img src="${esc(m.foto)}" alt="">`:initials(m.nome)}</div><div><div class="p-name">${esc(m.nome)}</div><div class="p-meta">${esc(nv.short)}${m.programa?' · '+esc(m.programa):''}${m.status==='egresso'?' · Egresso':''}${m.bolsista?' · 🎓 Bolsista':''}</div></div><div class="p-actions"><button type="button" class="btn" onclick="editPersonFromManager('${m.id}')">Editar / vínculos</button>${!m.protegido?(m.status==='egresso'?`<button type="button" class="btn danger" onclick="permanentlyDeleteMember('${m.id}')">Excluir definitivamente</button>`:`<button type="button" class="btn danger" onclick="removePersonFromManager('${m.id}')">Desligar acesso</button>`):''}</div></div>`}).join('') || '<div class="crono-empty">Nenhuma pessoa encontrada.</div>';
}
function openPeopleManager(){if(!isAdmin)return;renderPeopleManager();openOverlay('peopleOverlay')}
function editPersonFromManager(id){closeOverlay('peopleOverlay');openMember(id)}
async function removePersonFromManager(id){
  if(!isAdmin)return; const m=memberById(id); if(!m||m.protegido||m.status==='egresso')return;
  if(!confirm(`Desligar o acesso de ${m.nome}? O cadastro e todo o histórico serão preservados como egresso.`))return;
  try{
    await fbSetMember(id,{status:'egresso'});
    toast('Acesso desligado. O histórico foi preservado.'); renderPeopleManager();
  }catch(e){console.error(e);toast(e?.code==='permission-denied'?'Permissão negada pelas regras do Firestore.':'Erro ao desligar o acesso.')}
}

/* ============ VISUALIZADOR DE ARQUIVOS ============ */
let previewObjectUrl='', previewWorkbook=null;
function inferFileExt(url,name=''){
  const n=(name||'').split('?')[0].toLowerCase(); const m=n.match(/\.([a-z0-9]{2,6})$/); if(m)return m[1];
  try{const d=decodeURIComponent(new URL(url).pathname).toLowerCase();const x=d.match(/\.([a-z0-9]{2,6})$/);return x?x[1]:''}catch(e){return ''}
}
function sanitizePreviewHtml(html){
  const doc=new DOMParser().parseFromString('<div>'+html+'</div>','text/html');
  doc.querySelectorAll('script,style,iframe,object,embed,form').forEach(n=>n.remove());
  doc.querySelectorAll('*').forEach(el=>{
    [...el.attributes].forEach(a=>{const n=a.name.toLowerCase(),v=(a.value||'').trim().toLowerCase();if(n.startsWith('on')||(n==='href'&&v.startsWith('javascript:')))el.removeAttribute(a.name)});
  });
  return doc.body.firstElementChild?.innerHTML||'';
}
async function previewFetch(url){
  const r=await fetch(url,{credentials:'omit',cache:'no-store'});
  if(!r.ok) throw new Error('Não foi possível carregar o arquivo ('+r.status+').');
  return r.arrayBuffer();
}
function onlineViewerHtml(url,name='',service='google'){
  const safeUrl=encodeURIComponent(url), safeName=esc(name||'Arquivo');
  const src=service==='office'
    ? `https://view.officeapps.live.com/op/embed.aspx?src=${safeUrl}`
    : `https://docs.google.com/gview?embedded=1&url=${safeUrl}`;
  return `<div class="online-preview-wrap"><div class="online-preview-note"><span>Visualização online de <b>${safeName}</b></span><div><button class="btn" type="button" onclick="switchOnlineViewer('google')">Google</button><button class="btn" type="button" onclick="switchOnlineViewer('office')">Microsoft</button></div></div><iframe class="file-preview-frame" src="${src}" title="${safeName}" referrerpolicy="no-referrer"></iframe></div>`;
}
let currentPreviewUrl='', currentPreviewName='';
function switchOnlineViewer(service='google'){
  const body=$('#filePreviewBody'); if(!body||!currentPreviewUrl)return;
  body.innerHTML=onlineViewerHtml(currentPreviewUrl,currentPreviewName,service);
}
function showWorkbookSheet(i){
  if(!previewWorkbook||!window.XLSX)return;
  const names=previewWorkbook.SheetNames||[]; i=Math.max(0,Math.min(names.length-1,+i||0));
  document.querySelectorAll('#excelTabs button').forEach((b,j)=>b.classList.toggle('on',j===i));
  const ws=previewWorkbook.Sheets[names[i]], html=window.XLSX.utils.sheet_to_html(ws,{id:'excelTable'});
  const el=$('#excelSheet'); if(el) el.innerHTML=sanitizePreviewHtml(html);
}
function externalViewerUrl(url,ext=''){
  const e=String(ext||'').toLowerCase(),safe=encodeURIComponent(url);
  if